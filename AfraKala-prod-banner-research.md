# بریف تحقیق کوتاه — منطق نوار هشدار «production»

> **مأموریت فقط‌خواندنی.** هیچ کد/migration/نوشتن DB و هیچ تغییری روی کانتینر. فقط پیدا کردن منطق و گزارش.
> هدف: مشخص‌کردن اینکه نوار قرمز «هشدار ایمنی: محیط production روی آدرس تست/محلی اجرا شده است» **بر اساس چه شرطی** نمایش داده می‌شود — تا بفهمیم چرا با `.env.lan` که به `192.168.170.8` اشاره می‌کند، false-positive می‌دهد.
>
> **نحوهٔ استفاده:**
> ```powershell
> cd D:\AfraKalaTest\app
> claude
> ```
> ```
> Read AfraKala-prod-banner-research.md completely and execute it. Read-only — no code changes, no builds, no DB writes. Report inline (no file needed).
> ```

---

## قواعد
- فقط خواندن: `rg`، خواندن فایل، و در صورت لزوم `docker inspect`/`docker compose config` (فقط بازرسی، بدون build/up/down).
- هیچ کلید/رمز چاپ نشود — فقط نام متغیر و مقدار غیرحساس (host/port/flag).
- هیچ فایلی را ویرایش نکن. گزارش را همان‌جا در چت بده (به فایل نیازی نیست).

---

## گام ۱ — متن بنر را در کد پیدا کن
```powershell
rg -n "production روی آدرس|محیط production|هشدار ایمنی|قبل از ورود اطلاعات واقعی" src
rg -n "production|isProduction|PROD|prod" src --type tsx | rg -i "warn|banner|alert|env|mode"
```
- فایل و کامپوننتی که این بنر را رندر می‌کند پیدا کن. مسیر فایل + خط.

## گام ۲ — شرط دقیق نمایش را استخراج کن
- کد شرطی که تصمیم می‌گیرد بنر نشان داده شود را عیناً نقل کن. دنبال این‌ها بگرد:
  - آیا بر اساس `import.meta.env.PROD` / `MODE` / `NODE_ENV` است؟ (یعنی هر build با `mode=production` — که rebuild ما هم همین بود — بنر را روشن می‌کند، صرف‌نظر از URL.)
  - آیا بر اساس مقایسهٔ `VITE_SUPABASE_URL` با یک الگوی «تولید» است؟
  - آیا بر اساس host مرورگر (`window.location.hostname`) است؟ مثلاً «اگر build تولیدی است ولی hostname یک IP محلی/LAN است».
  - آیا ترکیبی از این‌هاست؟
- **این مهم‌ترین خروجی است:** دقیقاً بگو بنر با چه ترکیبی از شرط‌ها true می‌شود.

## گام ۳ — چرا الان false-positive می‌دهد
- مقادیر فعلی مربوطه را بررسی کن (فقط غیرحساس):
```powershell
docker inspect afrakala-lan-web --format "{{range .Config.Env}}{{println .}}{{end}}" | Select-String "VITE_|MODE|NODE_ENV|PROD|APP_ENV" | Select-String -NotMatch "KEY|SECRET|PASSWORD|TOKEN|ROLE"
```
- بر اساس شرط گام ۲ و این مقادیر، توضیح بده **چرا بنر روشن است** با اینکه `.env.lan` به `.8` (توسعه) اشاره می‌کند. محتمل‌ترین حدس: build با `mode=production` انجام شده (چون یک deploy واقعی است) و منطق بنر «production build + hostname غیرتولیدی» را خطر تلقی می‌کند — در حالی که این دقیقاً حالت درست محیط تست ماست.

## گام ۴ — گزینه‌های اصلاح (فقط توصیف، بدون اجرا)
بر اساس منطقی که پیدا کردی، ۲ تا ۳ گزینهٔ اصلاح ممکن را فهرست کن، مثلاً:
- افزودن یک متغیر صریح مثل `VITE_APP_ENV=lan-test` که بنر بر اساس آن تصمیم بگیرد (نه بر اساس `PROD`)، تا محیط LAN-تست رسماً «غیرتولید» شناخته شود.
- تغییر شرط بنر تا `192.168.170.8` (و شبکهٔ LAN) را به‌عنوان محیط مجاز غیرتولید بشناسد.
- هر گزینهٔ دیگری که از خود کد برمی‌آید.
برای هر گزینه: چه فایلی عوض می‌شود، و آیا rebuild لازم دارد یا فقط تغییر env.

## خروجی گزارش (در چت)
1. فایل:خط بنر + کد شرط دقیق.
2. حکم: بنر بر اساس چه چیزی روشن می‌شود.
3. چرا الان false-positive است (با مقادیر فعلی).
4. گزینه‌های اصلاح + اینکه کدام rebuild لازم دارد.
5. تأیید: هیچ فایلی تغییر نکرد، هیچ build/DB انجام نشد.