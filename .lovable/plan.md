## هدف
Redeploy تابع `migrate-helper` با کد فعلی (stub خالی) بدون تغییر سورس.

## مراحل
1. فراخوانی `supabase--deploy_edge_functions` با `["migrate-helper"]`.
2. گزارش URL عمومی: `https://kwwkppkcihrbeurwudjh.supabase.co/functions/v1/migrate-helper`

## ریسک
بسیار کم — فقط redeploy کد موجود.
