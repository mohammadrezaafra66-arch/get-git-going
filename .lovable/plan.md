مشکل از قیمت خرید نیست. برای این محصول قیمت خرید معتبر وجود دارد، اما ۴ قانون قیمت‌گذاری فعال همه `settlement_type_id` دارند و تابع «محاسبه و انتشار قیمت‌ها» فقط `sale_price_type_id` را می‌فرستد؛ پس هیچ قانونی match نمی‌شود و برای هر ۴ نوع قیمت خطا می‌دهد.

برنامه اصلاح حداقلی:

1. در `src/lib/pricing/engine.ts` منطق انتخاب قانون را اصلاح می‌کنم تا وقتی `settlement_type_id` از ورودی نیامده، قانون بتواند از روی `sale_price_type_id` خودش match شود و فقط به خاطر settlement رد نشود.
2. مقدار `settlement_type_id` واقعی قانون انتخاب‌شده را در خروجی محاسبه، snapshot و جزئیات قیمت ثبت می‌کنم، نه مقدار خالی ورودی.
3. رفتار فرم‌ها، داده‌ها و قوانین موجود تغییر نمی‌کند؛ فقط مسیر «محاسبه و انتشار قیمت‌ها» با قوانین فعلی سازگار می‌شود.
4. بعد از تغییر، همین محصول باید به‌جای ۴ خطا، برای ۴ نوع قیمت فعال محاسبه موفق داشته باشد.

Self-Host Acceptance Check:
- بدون migration دیتابیس.
- بدون secret جدید.
- بدون وابستگی خارجی/CDN.
- بدون تغییر RBAC/RLS.
- سازگار با Docker/Linux/self-host؛ فقط اصلاح منطق frontend/shared pricing است.