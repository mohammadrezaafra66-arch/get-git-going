\n# Plan — AFRA-20260517-CUSTOMERS-U01-S23B-PLAN (Plan only, no code)\n\n## 1. Existing implementation found\n- `src/shared/components/CustomerImportForm.tsx` (354 lines) — Excel-only (`.xlsx`/`.xls`) parser using `xlsx`, 3-step UI (file → mapping/preview → result), Persian RTL.\n- Route: `src/routes/_app.sales.customers_.import.tsx`, guarded by `requireAnyRole([\