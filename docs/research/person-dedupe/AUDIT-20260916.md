# Person dedupe audit — test DB (2026-09-16)

Read-only probes against LAN test DB `afrakala` on `afrakala-lan-db`.

## Counts

| Metric | Value |
|--------|------:|
| Active persons | 92 |
| Active with neither mobile nor Asan code | 42 |
| `person_merge_candidates` pending | 0 |
| Exact same `normalize_fa_text(display_name)` groups | 0 |
| Asan code collisions (distinct persons, non-revoked) | 0 |
| Mobile collisions (distinct persons, non-revoked) | 0 |
| Substring «پیرایش» active hits | 0 (on this test DB today) |

## Reading

- Identifier uniqueness is holding on test: no shared mobile/Asan across persons.
- The merge queue is empty because seed migration 234 was one-shot; nothing re-fills it.
- Duplicate *pain* on receive is substring name search (`search_visible_persons`), not exact-name twins. Test currently has no «پیرایش» rows; production/operator reports remain the UX driver.
- 42 incomplete active people cannot be matched by Asan re-import and are the main factory for future duplicates — complete or delete via `/admin/persons-cleanup`.

## Sample incomplete display names (first 15)

E2E / audit fixtures dominate (e.g. `شخص آزمایشی N`, `E2E_AUDIT_…`). Real incomplete Asan leftovers may coexist; cleanup UI lists all.

## Next steps (implemented in this change set)

1. Hard gate on `person_create_full`: active person needs mobile or Asan; identifier hit aborts with Persian reuse message.
2. Continuous `person_detect_merge_candidates` + triggers + nav badge.
3. Receive wizard: ambiguous name → operator picker.
4. Run detector on test; cleanup incompletes remains a human step on `/admin/persons-cleanup` and `/persons/merge`.
