# Production run · 2026-09-14 · migration 542

**Target:** `192.168.170.10` · DB `postgres` · container `afrakala-lan-db` · repo `C:\afrakala` (on `main @ 9bc8d554`, never switched)
**Migration:** `supabase/migrations/20260914130000_542_close_definer_views_shop_secret_inactive_profiles.sql`
on `origin/feature/security-fix-542 @ b79587a1` (not merged), taken with `git archive` into `C:\r542\src\`
**Rehearsal report:** `docs/research/security/security-fix-542-2026-09-14.md` on the same branch
**Executor:** Claude Code at the production keyboard. Owner pre-authorised every phase except one stop before apply; owner replied **GO**.

**Outcome: PASSED.** 542 is applied and recorded (ledger 696 → 697). All three findings closed as measured
below. Control personas (active admin, the real accountant) unchanged. PostgREST restarted; app serves.
No image built, no deploy, no data row written except the one ledger row.

Scripts and raw probe output used in this run: `docs/research/r542-run-20260914/` (ASCII; no identifier,
no setting value).

---

## Close-out

| | before | after |
|---|---|---|
| ledger rows | **696** | **697** |
| ledger max | `20260913111000` | **`20260914130000`** |
| row `20260914130000` | 0 | **1** |
| `shop_settings_read_authed` / `shop_settings_read_non_secret` | 1 / 0 | **0 / 1** |
| `tg_profiles_guard_status_change()` / trigger `profiles_guard_status_change` | absent / 0 | **present / 1** |
| `has_role` / `has_any_role` overloads | 2 / 2 | 2 / 2 |
| profiles not active | 12 | 12 (5 of them hold a role, per the 542 NOTICE) |

## Probes — phase 2 (before) vs phase 7 (after)

Method: per persona one `BEGIN ISOLATION LEVEL REPEATABLE READ READ ONLY … ROLLBACK`;
owner (`supabase_admin`) baseline counts taken **in the same snapshot**, then
`request.jwt.claims = {"sub": <id>, "role": "authenticated"}` and `SET LOCAL ROLE authenticated`.
`uid_resolved=true`, `uid_match=true`, `current_user=authenticated` for every persona in both runs.
Personas picked deterministically (`ORDER BY id`); `pick_md5=8fda3cf7352d449844f7f8cb9e87cd39` in both runs,
so before and after measured the same five users. `active_accountant_count=1` both runs.

Owner baseline in the same snapshot: bank_accounts 2 → 2, customer capital 1223 → 1223, salesperson
capital 228 → 228, shop_settings 28 → 28, Didar key non-empty true → true,
**audit_logs 117620 → 118442** (live staff activity between the runs, not 542).

| persona | roles | vw_account_balances | v_dyn_customer_cap | v_dyn_salesperson_cap | publish_recipients_view | audit_logs | shop_settings | Didar key readable | has_role admin |
|---|---|---|---|---|---|---|---|---|---|
| inactive, no role | — | **2 → 0** | **1223 → 0** | **228 → 0** | **24 → 0** | 0 = 0 | **28 → 0** | **true → false** | f = f |
| sales | sales | **2 → 0** | **1223 → 0** | **228 → 0** | 24 = 24 | 0 = 0 | **28 → 27** | **true → false** | f = f |
| inactive admin | admin | **2 → 0** | **1223 → 0** | **228 → 0** | **24 → 0** | **117620 → 0** | **28 → 0** | **true → false** | **t → f** |
| active admin (control) | admin+sales | 2 = 2 | 1223 = 1223 | 228 = 228 | 24 = 24 | 117620 → 118442 (= owner both times) | 28 = 28 | true = true | t = t |
| real accountant (control) | accountant+admin+sales | 2 = 2 | 1223 = 1223 | 228 = 228 | 24 = 24 | 117620 → 118442 (= owner both times) | 28 = 28 | true = true | t = t |

`vw_supplier_payables` = `ERR 42501` for every persona before and after: on production `authenticated`
holds no grant on it (the rehearsal report recorded the same). 542 changed its predicate anyway.

**Against the owner's expectations:**
- inactive-no-role and sales drop to 0 on the views — **yes** for bank balances and both capital views.
  **Sales keeps `publish_recipients_view` = 24**, by design: 542 lists `sales` for that view because
  `/pricing/sale-lists/$listId/publish` is a sales page. The rehearsal recorded the same `24 = 24`.
  This was stated before GO.
- sales can no longer read `didar_api_key` — **yes** (true → false; the value was never selected into output).
- inactive admin reads 0 from `audit_logs` — **yes** (117620 → 0).
- active admin and real accountant unchanged — **yes**: every object equals the owner's count in the same
  snapshot, before and after. The `audit_logs` number moved for the owner too.

---

## Phases — actual

### 0 · preflight (read-only)
```
r542_preflight|db=postgres|replica=false|ledger_rows=696|ledger_max=20260913111000|row_542=0|row_526=1|server=15.6
r542_shape|views_present=5|policy_read_authed=1|policy_read_non_secret=0|policy_write_admin=1|guard_fn=false|guard_trigger=0|has_role_overloads=2|has_any_role_overloads=2|profiles_not_active=12
exit=0
```
Persona candidates: inactive_norole 2, inactive_admin 3, active_sales_only 5, active_admin_not_accountant 17,
active_accountant 1 (`accountant+admin+sales`). Profiles: active 24, inactive 5, rejected 7.

### 1 · delivery (`docker cp`)
Migration 22736 bytes, 0 non-ASCII bytes, no BOM, no CR.
```
cp_exit=0
host=1a1514cfb54591bedd681b3393f5c83f
cont=1a1514cfb54591bedd681b3393f5c83f
match=True
```
Every helper script was delivered the same way with md5 matched on both sides.

### 2 · before probes — table above; raw output `r542-run-20260914/phase2-before.txt`
`exit=0`. Note: this one run's output was passed through `Tee-Object`; phase 7 was redirected to a file
instead, with no pipe.

### 3 · schema-only backup
```
pg_dump: warning: there are circular foreign-key constraints on this table:   (normal, 526 saw it too)
dump_exit=0
b5ca2939de60420e28d6556704fe2b41  /tmp/r542_pre_schema.dump
29          (catalogue entries matching the 542 objects)
cpout_exit=0
host=b5ca2939de60420e28d6556704fe2b41   match=True   3364333 bytes
```
Kept at **`C:\r542\r542_pre_schema.dump`** on the production laptop, and at `/tmp/r542_pre_schema.dump`
inside `afrakala-lan-db` until the container is recreated.

### 4 · STOP → owner replied **GO**
Immediately before apply: container md5 re-checked (match), preflight re-run (identical to phase 0).

### 5 · apply — verbatim
`PGOPTIONS=-clock_timeout=10s … psql -v ON_ERROR_STOP=1 --single-transaction -f /tmp/r542_mig542.sql 2>&1`
```
SET
psql:/tmp/r542_mig542.sql:153: NOTICE:  542/R1: public.vw_account_balances now requires one of {admin,manager,accountant}
psql:/tmp/r542_mig542.sql:153: NOTICE:  542/R1: public.vw_supplier_payables now requires one of {admin,manager,accountant}
psql:/tmp/r542_mig542.sql:153: NOTICE:  542/R1: public.v_dynamic_customer_capital_balances now requires one of {admin,manager,accountant}
psql:/tmp/r542_mig542.sql:153: NOTICE:  542/R1: public.v_dynamic_salesperson_capital_balances now requires one of {admin,manager,accountant}
psql:/tmp/r542_mig542.sql:153: NOTICE:  542/R1: public.publish_recipients_view now requires one of {admin,manager,accountant,sales,purchase_specialist}
psql:/tmp/r542_mig542.sql:153: NOTICE:  542/R1: views changed 5, already in place 0, total 5
DO
psql:/tmp/r542_mig542.sql:239: NOTICE:  542/R2: policy statements executed 2, keys now visible to admins only: 1 (didar_api_key)
DO
psql:/tmp/r542_mig542.sql:432: NOTICE:  542/R3: public.has_role(uuid,text) now requires profiles.status = active
psql:/tmp/r542_mig542.sql:432: NOTICE:  542/R3: public.has_any_role(uuid,text[]) now requires profiles.status = active
psql:/tmp/r542_mig542.sql:432: NOTICE:  542/R3: public.is_board_manager(uuid) now requires profiles.status = active
psql:/tmp/r542_mig542.sql:432: NOTICE:  542/R3: public.is_hr_manager(uuid) now requires profiles.status = active
psql:/tmp/r542_mig542.sql:432: NOTICE:  542/R3: has_dynamic_permission(uuid,text,text) now requires profiles.status = active
psql:/tmp/r542_mig542.sql:432: NOTICE:  542/R3: statements executed 8, already in place 0; profiles not active: 12, of which hold a role: 5
DO
exit=0
```
Same counts as the rehearsal's first run (views 5, policy statements 2, R3 statements 8). No `ERROR`, no lock timeout.

### 6 · ledger row
**First attempt failed harmlessly — operator error, recorded so nobody repeats it.** The INSERT was passed
with `psql -c "…"` inside a single-quoted `sh -c '…'`; PowerShell 5.1 stripped the inner double quotes and
psql received only `INSERT`:
```
ERROR:  syntax error at end of input
LINE 1: INSERT
insert_exit=1
```
Nothing was written: an immediate preflight showed `ledger_rows=696|row_542=0`. This is exactly the
"no double quotes inside single-quoted sh -c" rule; the fix was the same file + `docker cp` + md5 route as
every other step (`r542-run-20260914/r542-ledger.sql`, md5 `b732bb0b0ef79f8348cc8df623efed22`, matched):
```
BEGIN
r542_ledger_before|rows=696|row_542=0
INSERT 0 1
r542_ledger_after|rows=697|row_542=1|max=20260914130000
COMMIT
exit=0
```
Independent re-check: `r542_preflight|db=postgres|replica=false|ledger_rows=697|ledger_max=20260914130000|row_542=1|row_526=1`.

### 7 · after probes — table above; raw output `r542-run-20260914/phase7-after.txt`
Same two scripts (container md5 re-checked equal to phase 2), `exit=0`.

### 8 · `docker restart afrakala-lan-rest`
`restart_exit=0`; `running`, RestartCount 0. Logs: `Successfully connected to PostgreSQL 15.6`,
`Schema cache loaded 256 Relations, 261 Relationships, 361 Functions`.

### 9 · app
```
/login       status=200 type=text/html; charset=utf-8
/api/healthz status=200 type=application/json
```
All `afrakala-lan-*` Up; `afrakala-lan-db-role-fix` `Exited (0)`. `afrakala-lan-web` not touched (Up 19 hours).

### Cleanup
Helper scripts and the migration copy removed from the container `/tmp`; only `r542_pre_schema.dump` remains.
`C:\r542\` left on the laptop deliberately (zip, extracted migration, scripts, dump).

---

## Not verified / remaining manual steps

1. **Rotate the Didar key** (owner). Closing the read path does not invalidate copies anyone already took;
   it was readable by every signed-in non-viewer until today.
2. **Ban the non-active accounts in GoTrue** (owner). 542 stops their JWT from carrying roles; they can still
   sign in, and policies with `qual=true` or `role()='authenticated'` still answer them (rehearsal report §4).
3. **Real HTTP path not exercised.** Probes used JWT-claims simulation in psql, not PostgREST requests.
   A browser check with a sales login on `/pricing/sale-lists/new` (settings still load) and with an admin on
   `/operations/didar` (key still shown), and an accountant on `/accounting/treasury` and
   `/accounting/dynamic-capital`, would close that.
4. **Performance** (~1.4–1.65× on heavy unpaginated RLS reads, accepted by the owner) not measured on production.
5. **Test computer / `afrakala`:** 542 is not applied there by this run; `og81-migration-ledger-matches-disk`
   there will disagree until it is applied and recorded, and 542 is not yet on `main`.
6. Still open by design (rehearsal report §4): `v_promotion_suggestions`, `product_computed_prices_public`,
   `v_customer_credit_exposure`, `role_permissions`, `delete_bot_api_key_secure`.
7. `PROGRESS.md` not edited in this run (instruction scope: run record only, no PR).

## Reversal, if ever needed

Not improvised. A new numbered forward migration written from `C:\r542\r542_pre_schema.dump`
(md5 `b5ca2939de60420e28d6556704fe2b41`), rehearsed on a restore, approved separately.
Ledger row `20260914130000` is never deleted.

## What this run did not do

No branch switch or merge in `C:\afrakala`. No image build/load, no deploy, no web restart. No data row written
other than the ledger row. No secret printed; the Didar key value was never selected into any output.
No key rotation, no account ban. No push to `main`/`staging`, no PR. `192.168.170.8` not touched.
