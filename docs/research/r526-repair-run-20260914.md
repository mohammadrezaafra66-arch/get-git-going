# Production repair run · 2026-09-14 · 526-repair-20260914

**Target:** `192.168.170.10` · DB `postgres` · container `afrakala-lan-db` · repo `C:\afrakala`
**Runbook:** `docs/runbooks/526-repair-20260914.md` on `origin/feature/526-repair @ b22131c5` (not merged; scripts taken by `git archive`, Block 0b)
**Evidence for the runbook:** `docs/research/526-repair-measurement-20260914.md` on the same branch
**Executor:** Claude Code at the production keyboard. Owner pre-authorised Blocks 0–4 and 6–8; one stop before Block 5 (owner replied GO); two Expect mismatches stopped the run and were overridden by the owner, recorded below.

**Outcome: PASSED.** Migration 526's SQL is now applied on production. All nine repairs present. Ledger untouched (696 → 696). No data rows written by 526. No container restarted, no image built, no deploy.

> **For the owner:** the bank-deposit export for the checked range now returns **5 payment rows that it did not return before** (6 receipts before and after; 6 → 11 rows in total), and those 5 are exactly the 5 approved bank payments it should contain.

---

## Close-out

| | before (Block 1/2/3) | after (Block 6/7) |
|---|---|---|
| ledger rows | **696** | **696** |
| ledger max | `20260913111000` | (unchanged — no ledger write in this runbook) |
| ledger row `20260913090000` (526) | 1 — present but **untrue** | 1 — **now true** |
| 526 effects needed | **9 of 9** | **0 of 9** |
| `asan_list_bank_deposit_export` out columns | 10 | **11** (`direction` restored) |
| export `anon` EXECUTE | false | **false** |
| export `authenticated` EXECUTE | — | true |
| `expire_stale_credit_holds` overloads | 2 | **1** |
| checkout `C:\afrakala` | `feature/prod-env-gap @ 5f03e15c` | **`main @ 9bc8d554`** (= `origin/main`) |

### Bank-deposit export, Block 3 vs Block 7

Same script, same accountant/admin JWT, `READ ONLY` transaction ending in `ROLLBACK`.

| field | Block 3 (before) | Block 7 (after) |
|---|---:|---:|
| `uid_resolved` | true | true |
| `out_columns` | 10 | **11** |
| `total_rows` | 6 | **11** |
| `receipt_rows` | 0 | **6** |
| `payment_rows` | 0 | **5** |
| `rows_without_direction` | 6 | **0** |
| `voucher_ids_in_output` | 0 | **5** |
| `expected_payment_rows` | 5 | 5 |
| `payments_match` | false | **true** |
| `blocked_rows` | 0 | 0 |

Before: the 6 rows had no `direction` column, so the script classes them as `receipt_rows=0`
`rows_without_direction=6`. After: those same 6 are receipts (`receipt_rows` 6 = Block 3 `total_rows`
6), and 5 payment rows were added, equal to the 5 approved payments that should be there.
**+5 payment rows. No approval happened between the two blocks** (`expected_payment_rows` 5 → 5).

---

## Owner overrides (two)

### Override 1 — Block 0, branch Expect

| Expect | Actual |
|---|---|
| `main` | `feature/prod-env-gap` |

Production's checkout was on `feature/prod-env-gap @ 5f03e15c`, not `main`. The run stopped; the
owner overrode it knowingly. Owner's reasoning, verbatim in substance: the Expect line exists to
prove the tree is at a known state, and it is — `feature/prod-env-gap` was branched from
`main @ 9bc8d554` and carries exactly one docs commit; the migration file's md5 matches the rehearsed
file byte-for-byte; and the check scripts came from `git archive` on `FETCH_HEAD b22131c5`, not the
working tree, so the branch name touches nothing this run depends on.

Measured before continuing, so the reasoning rests on facts:

```
git merge-base main HEAD      -> 9bc8d55405356e2475bf7d0dc47b1803e921a909
git log --oneline main..HEAD  -> 5f03e15c docs(release-line): the .10 runtime-env gap, measured but not applied
git diff --stat main HEAD     -> docs/research/release-line/prod-env-gap-20260914.md | 478 +++  (1 file)
```

**Runbook correction required:** Block 0's Expect should read
*"`main` OR a branch whose code files are identical to `main`"* (checkable with
`git diff --stat main HEAD` touching only `docs/`). A runbook that stops on a cosmetic difference
gets its Expect lines ignored, which is how a real one gets ignored too.

The checkout was switched back to `main` after Block 8 (see "After Block 8").

### Override 2 — Block 5, line order of the apply output

The transaction had already committed (`exit=0` under `--single-transaction`) when the mismatch was
seen. The run stopped before Block 6; the owner overrode it.

| line | Expect | Actual |
|---|---|---|
| 1 | `SET` | `SET` |
| 2 | `DO` | `psql:/tmp/r526_mig526.sql:82: NOTICE:  526: precondition OK -- all nine target objects exist` |
| 3 | `psql:/tmp/r526_mig526.sql:82: NOTICE:  526: precondition OK -- all nine target objects exist` | `DO` |
| 4–28 | as in the runbook | identical, line for line |

Owner's reason: stdout/stderr interleaving, not database behaviour. `NOTICE` goes to stderr and the
`DO` command tag to stdout; `2>&1` merges them, and their relative order depends on buffering. The
Expect block itself prints NOTICE-before-DO at lines 1168 and 1306 — same file, same run, opposite
order to its own line 82. What matters is what was measured: 28 lines, every line's text identical,
all nine effect lines present, zero `-- no-op`, zero `ERROR`, `exit=0`. Blocks 6 and 7 are the proof.

**Runbook correction required:** Block 5's Expect must state that NOTICE and DO lines may interleave
in either order because of the `2>&1` merge, and that the criterion is the **set** of lines, the
absence of `-- no-op` and `ERROR`, and `exit=0` — not their sequence. Whoever runs this next must not
stop on the same non-difference.

---

## Blocks — actual next to Expect

### Block 0 — files and hashes (0b, `git archive` from `FETCH_HEAD`)

`git archive` exit 0; `FETCH_HEAD` = `b22131c5`.

| Expect | Actual | |
|---|---|---|
| `FA1BB671A6BB1373122AB7B9590AA361` 526 migration | `FA1BB671A6BB1373122AB7B9590AA361` | match |
| `B25C852CE7BEF03F4B085DCF19127C8B` r526-preflight.sql | `B25C852CE7BEF03F4B085DCF19127C8B` | match |
| `B104B9F97C486195C308E586ED59C53B` r526-precheck.sql | `B104B9F97C486195C308E586ED59C53B` | match |
| `8653026B56BD2500A1134C809EE0A3B8` r526-export-check.sql | `8653026B56BD2500A1134C809EE0A3B8` | match |
| `1DE575CE8778AEBA1DA091222D54CDFE` r526-postcheck.sql | `1DE575CE8778AEBA1DA091222D54CDFE` | match |
| `main` | `feature/prod-env-gap` | **MISMATCH → override 1** |

### Block 0.5 — scripts into the container (`docker cp`)

| Expect | Actual | |
|---|---|---|
| no `Error response from daemon`, `exit=0` | none, `exit=0` | match |
| `b25c852ce7bef03f4b085dcf19127c8b  /tmp/r526_preflight.sql` | `b25c852ce7bef03f4b085dcf19127c8b  /tmp/r526_preflight.sql` | match |
| `b104b9f97c486195c308e586ed59c53b  /tmp/r526_precheck.sql` | `b104b9f97c486195c308e586ed59c53b  /tmp/r526_precheck.sql` | match |
| `8653026b56bd2500a1134c809ee0a3b8  /tmp/r526_export_check.sql` | `8653026b56bd2500a1134c809ee0a3b8  /tmp/r526_export_check.sql` | match |
| `1de575ce8778aeba1da091222d54cdfe  /tmp/r526_postcheck.sql` | `1de575ce8778aeba1da091222d54cdfe  /tmp/r526_postcheck.sql` | match |

### Block 1 — preflight

```
Expect: r526_preflight|db=postgres|replica=false|ledger_rows=696|ledger_max=20260913111000|row_526=1|row_539=1
Actual: r526_preflight|db=postgres|replica=false|ledger_rows=696|ledger_max=20260913111000|row_526=1|row_539=1
Expect: exit=0
Actual: exit=0
```
Match.

### Block 2 — pre-check

```
Expect: need_386a=true need_386b=true need_386c=true need_394=true need_396a=true need_396b=true need_396c=true need_404=true need_409=true
Actual: need_386a=true need_386b=true need_386c=true need_394=true need_396a=true need_396b=true need_396c=true need_404=true need_409=true
Expect: r526_context|export_out_columns=10|export_anon_exec=false|expire_overloads=2|callers_of_expire=0
Actual: r526_context|export_out_columns=10|export_anon_exec=false|expire_overloads=2|callers_of_expire=0
Expect: r526_precheck|targets_missing=0|needs=9|of=9|VERDICT=APPLY-NEEDED
Actual: r526_precheck|targets_missing=0|needs=9|of=9|VERDICT=APPLY-NEEDED
Expect: exit=0
Actual: exit=0
```
Match.

### Block 3 — export BEFORE

```
Expect: starts r526_export|uid_resolved=true|out_columns=10|  with payment_rows=0, receipt_rows=0, voucher_ids_in_output=0
Actual: r526_export|uid_resolved=true|out_columns=10|total_rows=6|receipt_rows=0|payment_rows=0|rows_without_direction=6|voucher_ids_in_output=0|expected_payment_rows=5|payments_match=false|blocked_rows=0
Expect: exit=0
Actual: exit=0
```
Match. Recorded `total_rows=6`, `expected_payment_rows=5`.

### Block 4 — schema-only backup

| Expect | Actual | |
|---|---|---|
| `exit=0` after `pg_dump` (circular-FK warning normal) | `exit=0`; `pg_dump: warning: there are circular foreign-key constraints on this table:` + its detail/hint lines | match |
| md5 line for `/tmp/r526_pre_schema.dump` | `413e1ebf544358c6b56294c3b7c7148a  /tmp/r526_pre_schema.dump` | match |
| object count ≥ 20 (rehearsal 23) | **26** | match |
| `exit=0` after `docker cp` | `exit=0` | match |
| host md5 = container md5 | `413e1ebf544358c6b56294c3b7c7148a` | match |

Pre-526 schema kept at **`C:\r526\r526_pre_schema.dump`** (md5 `413e1ebf544358c6b56294c3b7c7148a`) on
the production laptop, and still at `/tmp/r526_pre_schema.dump` inside `afrakala-lan-db` until the
container is recreated.

### STOP before Block 5 → owner replied **GO**.

### Block 5 — apply

First half:

| Expect | Actual | |
|---|---|---|
| `exit=0` | `exit=0` | match |
| `host=fa1bb671a6bb1373122ab7b9590aa361` | `host=fa1bb671a6bb1373122ab7b9590aa361` | match |
| `cont=fa1bb671a6bb1373122ab7b9590aa361` | `cont=fa1bb671a6bb1373122ab7b9590aa361` | match |
| `match=True` | `match=True` | match |

Second half, actual output verbatim:

```
SET
psql:/tmp/r526_mig526.sql:82: NOTICE:  526: precondition OK -- all nine target objects exist
DO
ALTER VIEW
526: [386a] set security_invoker=true on product_computed_prices_public (was unset)
CREATE VIEW
526: [386b] redefined v_promotion_suggestions -- restored security_invoker=true and the uid() IS NOT NULL guard
CREATE VIEW
526: [386c] redefined vw_account_balances -- restored the uid() IS NOT NULL guard
CREATE FUNCTION
526: [394] redefined create_purchase -- restored the tehran_today() future-date comparison
CREATE FUNCTION
526: [396a] redefined get_payables_list -- restored the tehran_today() due-date comparison
CREATE FUNCTION
526: [396b] redefined upsert_staff_daily_performance_metric -- restored the tehran_today() comparisons
ALTER POLICY
ALTER POLICY
526: [396c] restored tehran_today() on sdpm_insert_privileged / sdpm_update_privileged
DROP FUNCTION
CREATE FUNCTION
526: [404] dropped and redefined asan_list_bank_deposit_export -- restored the payment-vouchers branch and the direction column
psql:/tmp/r526_mig526.sql:1168: NOTICE:  526: [409 precheck] no other function in public source-references expire_stale_credit_holds(...) -- safe to drop the (integer) overload
DO
DROP FUNCTION
526: [409] dropped the stale expire_stale_credit_holds(integer) overload
psql:/tmp/r526_mig526.sql:1306: NOTICE:  526 OK: all five migrations' asserted end states (386/394/396/404/409) are present in the catalogue. Ledger rows for 386/394/396/404/409 are untouched. This migration does NOT write supabase_migrations.schema_migrations -- the operator's ledger step does, and expects INSERT 0 1.
DO
exit=0
```

Against the Expect block: 28 lines each, same text on every line; lines 2 and 3 in swapped order
(**override 2**). No `-- no-op`, no `ERROR`, no lock timeout. As the runbook instructs, the final
NOTICE's "expects INSERT 0 1" was ignored — there is deliberately no ledger step.

### Block 6 — post-check

```
Expect: need_386a=false need_386b=false need_386c=false need_394=false need_396a=false need_396b=false need_396c=false need_404=false need_409=false
Actual: need_386a=false need_386b=false need_386c=false need_394=false need_396a=false need_396b=false need_396c=false need_404=false need_409=false
Expect: r526_postcheck|needs=0|of=9|export_anon_exec=false|export_authenticated_exec=true|expire_overloads=1|ledger_rows=696|row_526=1
Actual: r526_postcheck|needs=0|of=9|export_anon_exec=false|export_authenticated_exec=true|expire_overloads=1|ledger_rows=696|row_526=1
Expect: r526_postcheck|VERDICT=ALL-NINE-PRESENT
Actual: r526_postcheck|VERDICT=ALL-NINE-PRESENT
Expect: exit=0
Actual: exit=0
```
Match. `ledger_rows` 696 = Block 1's 696.

### Block 7 — export AFTER

```
Expect: starts r526_export|uid_resolved=true|out_columns=11|  with rows_without_direction=0 and payments_match=true
Actual: r526_export|uid_resolved=true|out_columns=11|total_rows=11|receipt_rows=6|payment_rows=5|rows_without_direction=0|voucher_ids_in_output=5|expected_payment_rows=5|payments_match=true|blocked_rows=0
```

| Expect | Actual | |
|---|---|---|
| `payment_rows` = `expected_payment_rows` | 5 = 5 | match |
| `receipt_rows + payment_rows = total_rows` | 6 + 5 = 11 | match |
| `receipt_rows` = Block 3 `total_rows` | 6 = 6 | match |
| `expected_payment_rows` = Block 3 value | 5 = 5 | match |
| `exit=0` | `exit=0` | match |

### Block 8 — container cleanup

```
Expect: 1
Actual: 1
```
Match. Only `/tmp/r526_pre_schema.dump` remains in the container.

### Block 9 — NOT RUN (the accountant's, by decision)

Not attempted: the app was not opened and nothing was exported. Still outstanding — see below.

---

## After Block 8 — checkout back to `main` (decision OG-I)

Pre-switch checks: `git status --porcelain` had no tracked changes, only three untracked docs
(`docs/research/asan-code-samples-20260913.md`, `env-parity-20260913.md`, `owner-prep-20260913.md`),
none of which exists on `main`, so they carried over untouched; `git stash list` empty; local `main`
= `origin/main` = `9bc8d554`. Nothing could be lost.

```
BEFORE branch=feature/prod-env-gap head=5f03e15c
git switch main  -> Switched to branch 'main' / Your branch is up to date with 'origin/main'.  switch_exit=0
AFTER  branch=main head=9bc8d554
```

The 526 migration file on `main` still hashes `FA1BB671A6BB1373122AB7B9590AA361`. `5f03e15c` remains
on `feature/prod-env-gap`. No `git pull` was run (none was needed: `main` = `origin/main`).

This run record was committed from a separate `git worktree` in a temp directory, so the
`C:\afrakala` checkout stays on `main`.

---

## What differed from the rehearsal

| | rehearsal (`rehearsal_526_20260914`) | production |
|---|---|---|
| Block 0 branch | — (test computer) | `feature/prod-env-gap`, not `main` — override 1 |
| Block 0.5 `docker cp` | succeeded on the test computer on 2026-09-14 | **succeeded** — first measurement on the production laptop; the OG-68 mount-layer error did not occur, including for the copy **out** in Block 4 |
| Block 1 `db=` | `rehearsal_526_20260914` | `postgres` (by design) |
| Block 3 counts | `total_rows=6`, `expected_payment_rows=5` | **identical.** The runbook's prose predicted production would be larger ("it has more days of data"); it was not. No Expect line depended on it. Not investigated. |
| Block 4 object count | 23 | **26** (≥ 20 passes). Not investigated. |
| Block 4 dump md5 | — | `413e1ebf544358c6b56294c3b7c7148a` (a different database; no comparison meaningful) |
| Block 5 line 2/3 order | `DO` then NOTICE | NOTICE then `DO` — override 2 |
| Blocks 1, 2, 6, 7 | as recorded | identical apart from `db=` |

---

## Not verified / remaining manual steps

1. **Block 9 — accountant, in the app.** Export bank deposits for a range containing a known approved
   bank payment; confirm the payment appears with a **negative** `Mablagh`, receipts positive, 15
   columns; then confirm Asan accepts a negative amount as an outgoing movement. **Nobody has ever
   verified the Asan side.** If Asan rejects it: do not import, report — that is the case for a
   forward migration written from the Block 4 dump.
2. **The running app image was not re-checked in this run.** The runbook assumes production runs
   `3bc526c4` (the 2026-09-13 release recorded `APP_GIT_SHA=3bc526c4`), whose code reads `direction`.
   Block 7 proves the database function; the downloaded file is only proved by Block 9.
3. **Runbook corrections** (Blocks 0 and 5, above) are not yet made on `feature/526-repair`.
4. `C:\r526\` (zip, extracted scripts, `r526_pre_schema.dump`) is left on the laptop deliberately —
   the dump is the only basis for a future reversal. Do not delete it.

## Reversal, if ever needed

Not improvised. A reversal of any 526 repair is a **new numbered forward migration**, written from
the pre-526 definitions in `C:\r526\r526_pre_schema.dump`, rehearsed on a restore, and approved
separately. 526 is never re-run wholesale, and the ledger row `20260913090000` is never deleted.

## What this run did not do

No `mig_apply`. No INSERT/DELETE on `supabase_migrations.schema_migrations`. No container restart,
image build/load/tag, or deploy. No merge, no force. The test computer was not touched.
