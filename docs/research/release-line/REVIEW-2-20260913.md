# بازبینیِ مستقلِ دوم — شاخهٔ `fix/release-line-runtime-config`

## ۱. حکم

**REJECT** — `BROKEN_PROBES=2` · `OVER_REJECTIONS=0` · از شش موردِ واگذارشده، **۲.۱ و ۲.۲ BLOCKER**‌اند · در فاز ۳ شش false-PASS تأیید شد.

شاخه: `fix/release-line-runtime-config` @ `88997bd6` · پایه: `origin/main` @ `9bc8d554` · باکس: `D:\AfraKalaTest\app` (تست، `192.168.170.8`) · تاریخ: ۲۰۲۶-۰۹-۱۴
بازبینیِ اول: `REVIEW-20260913.md` (`88887140`، REJECT). دورِ اصلاح: `beece1ff` (B1)، `5d4df9e1` (B2)، `dac904e7` (B3)، `608c3f4d` (B4)، `88997bd6` (پیوست).
پیوستِ پیاده‌ساز **بعد از** تشکیلِ حکم خوانده شد.

سطوحِ شاهد: `measured` (دستور + خروجی + exit code جدا از هر pipe) · `quoted` · `cited` · `compared` · `assertion` (استدلال، برچسب‌خورده).

### چرا REJECT، در یک پاراگراف

دورِ اصلاح سه چیز را **واقعاً** درست کرده و من با imageها و ورودی‌های خودم اثباتشان کردم: D6(i) حالا `http://kong:8000`، `*.supabase.co`، literalِ دوبارهٔ Ollama، image ناموجود و bundle خالی را می‌گیرد؛ D8 نثرِ ستون-۰، PASSِ داخلِ fence و سرتیترِ بدشکل را رد می‌کند؛ D2 حالا image **کانتینرِ در حال اجرا** را تگ می‌کند و باگِ B3 (تگ‌کردنِ `:lan`) را می‌گیرد. هر gate روی همهٔ ۲۲ مسیرِ خروجش خطِ `GATE` چاپ می‌کند، و خروجی و exit code در Windows PowerShell 5.1 و pwsh 7.6 یکی است — هم با `-File` هم با paste.
ولی سه چیز همان کلاسِ حادثه را باز می‌گذارد: **(الف)** D9 و D6(iii) فقط محیطِ **ساختگیِ خودِ probe** را می‌سنجند، نه محیطِ استقرار؛ و `.env.lan`ِ ثبت‌شدهٔ production اصلاً `APP_SUPABASE_PUBLIC_URL` ندارد، پس مرورگرها بعد از deploy مقدارِ `SUPABASE_URL` (طول ۱۶، هم‌طولِ `http://kong:8000`) را می‌گیرند و **همهٔ gateها سبز می‌مانند**. **(ب)** سرِ سندِ emit‌شده می‌گوید `apply-release.ps1` هر بلوک را با `Expect:`اش می‌سنجد؛ من آن را روی سندی اجرا کردم که gateِ Block 0اش FAIL و imageِ probeاش ناموجود بود و `VERDICT: PASSED`، `exit 0` گرفتم. **(ج)** `exit 1` در پنجرهٔ تعاملیِ console host، پنجره و دلیلِ `GATE … FAIL` را با هم نابود می‌کند (اندازه‌گیری شد).

---

## ۲. جدولِ probeها — هر دو جهت، شاهدِ خودم

### فاز ۰ — وضعیتِ اولیه (`measured`)

```
origin/main                 9bc8d55405356e2475bf7d0dc47b1803e921a909
HEAD                        88997bd6c0659ebbe56b8303cae9b72ad12306bd  (merge-base = origin/main)
git log origin/main..HEAD   20 commits (88997bd6 … 549ec858)
git diff --stat             16 files, 3274 insertions, 51 deletions
git status --porcelain      (empty)
afrakala-app:lan            sha256:296eb4b4899f…
afrakala-lan-web            Image=sha256:0c3106602cc9…  StartedAt=2026-09-12T17:46:06.169711504Z  Restarts=0
afrakala-app:review-ae47de4a  "No such image", exit 1   ← ادعای پیاده‌ساز درست است
```

### روش

- imageهای خودم از `git archive 88997bd6` در scratchpad با `docker build -t review2-afrakala:<name>` (هرگز `docker compose build`): `clean`=`9e91fa2e3e20`، `kong`=`d0bdeb37cb1d`، `supa`=`9898a7a4dab5`، `ollama2`=`d953f1a22a11`، `evasion`=`5b916cfbcded` — همه `BUILD_EXIT=0`. کاشت‌ها در **source** (`src/routes/__root.tsx`) انجام شد تا از Vite/minifier عبور کنند. imageهای مشتق (`FROM review2-afrakala:clean`) فقط برای حالت‌های سرور/فایل.
- هر gate از سندی که **emitterِ همین commit** (`git show 88997bd6:release/emit-blocks.ps1`، sha256 prefix `6a457ca7b374`، برابر با فایلِ درخت) تولید کرد، عیناً استخراج شد؛ تنها تغییر: de-indent و جایگزینیِ نامِ image/کانتینر/تگ به فضای‌نامِ خصوصی (هر جایگزینی با `diff` چاپ شد).
- هیچ اجرایی `SUPABASE_URL` به `192.168.170.10` نداشت: `-D6TargetHost 10.99.99.99`. پیش از اجرای image حادثه، env آن بررسی شد: هیچ `SUPABASE_URL`ی پخته نیست.
- هر gate با `powershell.exe -File` **و** `pwsh.exe -File` اجرا شد؛ `out=$(…); rc=$?` — exit code مستقیم، بدونِ pipe.

### D6 — باید FAIL شود

| # | ورودی | خروجیِ من (5.1 و 7.6 یکسان) | exit |
|---|---|---|---|
| 1.1 | image واقعیِ حادثه `afrakala-app:3bc526c4` = `296eb4b4899f` | `http://192.168.170.8:9000  (explicit port, IP literal)` · `GATE D6 FAIL host literal(s) in the client bundle: http://192.168.170.8:9000` | `1` / `1` |
| 1.2 | `kong` (کاشتِ source، حضور مستقلاً شمرده شد: `kong:8000 hits: 1`) | `http://kong:8000  (explicit port, bare name with no dot)` | `1` / `1` |
| 1.3 | `supa` (`https://kwwkppkcihrbeurwudjh.supabase.co`) | `(Supabase host)` + `kwwkppkcihrbeurwudjh.supabase.co  (Supabase project host)` | `1` / `1` |
| 1.4 | `review2-afrakala:does-not-exist` | `FAIL D6(0): … does not exist on this machine. Nothing was measured.` | `1` / `1` |
| 1.5 | `ollama2` (شمارشِ مستقل: clean=`1`، ollama2=`2`) | `http://192.168.170.8:11434  (allowed 1x as illustrative text, found 2x)` | `1` / `1` |
| 1.6 | `nologin` (`index.mjs` حذف) | `OK D6(i)` · `OK D6(ii)` · `FAIL D6(iii): the image served no supabaseUrl at all.` | `1` / `1` |
| 1.6b | `fakeserver-empty` (`/login` = 200 بدونِ config) | همان `FAIL D6(iii)` | `1` / `1` |

### D6 — باید PASS شود

| # | ورودی | خروجی | exit |
|---|---|---|---|
| 1.7 | `clean` با help textِ مشروعِ ai-providers (۱×) | `OK D6(i) (475 js files read, 176 URL literal(s) classified)` · `OK D6(ii)` · `OK D6(iii): served config = "supabaseUrl":"http://10.99.99.99:8000"` · `GATE D6 PASS` · `GATE D9 PASS` | `0` |
| 1.8 | همان، زیر **هر دو** shell | `compared`: خروجیِ کامل پس از حذفِ `\r` — sha256 prefix `81d94f7d5cd2` در هر دو؛ exit `0`/`0`. همین مقایسه برای `incident` (`00de86508304`، `1`/`1`)، `evasion` (`844f617a6eb8`) و `d9-kong` (`947f3ace41f0`). تنها اختلافِ بایتی: پایانِ خط. **و** بلوکِ کاملِ D6/D9 به‌صورت **paste** در پنجرهٔ تعاملیِ 5.1 و 7.6: همان شش خط، `GATE D9 PASS`. | `0` / `0` |

**هیچ اختلافِ رفتاری بین دو shell پیدا نشد** — نه با `-File`، نه با paste.

### D8 — باید REJECT شود (هر مورد در 5.1 و 7.6؛ «فایل نوشته شد؟» با `ls release/out`)

| # | ورودی (از `rehearsal-e4b.md`) | خروجی | exit | نوشته شد؟ |
|---|---|---|---|---|
| 1.9 | e4b + `## VERDICT: FAIL (replay stopped early)` در انتها | `final : :2049  ## VERDICT: FAIL …` + فهرستِ `:878` / `:2047` / `:2049` | `1` | خیر |
| 1.10 | e4b + `## VERDICT: FAIL` + نثرِ ستون-۰ `VERDICT: PASS is the outcome we were hoping for, but it did not happen.` | `final : :2049  ## VERDICT: FAIL` | `1` | خیر |
| 1.11 | خطِ `:2047` جایش `` ``` `` + `## VERDICT: PASS` + `` ``` `` (و نسخهٔ `~~~`) | `final : :878  ## VERDICT: FAIL (replay stopped early)` | `1` | خیر |
| 1.12 | `## verdict: pass` · `##VERDICT: PASS` · `### VERDICT: PASS` · `## VERDICT: Pass` · `## VERDICT:PASS` | `FATAL: … verdict-like heading(s) the rehearsal engine never writes: :2047 …` (هر پنج) | `1` | خیر |

### D8 — باید ACCEPT شود

| # | ورودی | خروجی | exit |
|---|---|---|---|
| 1.13 | `rehearsal-e4b.md` بدونِ تغییر | `Written: …RELEASE-r2-t1_13.md` | `0` |

**چرا پذیرفته می‌شود:** `measured` (awk با شمارشِ fence): فقط دو سرتیترِ حکم در فایل هست، هر دو بیرونِ fence: `:878 ## VERDICT: FAIL (replay stopped early)` و `:2047 ## VERDICT: PASS`. فایل ۲۰۴۷ خط دارد، پس **آخرین سرتیترِ واقعی `:2047` است و PASS است** — و آن دقیقاً شکلی است که `release/lib/rehearse-engine.sh:1245` می‌نویسد (`echo "## VERDICT: PASS" | tee -a "$REPORT"`).

### D3

| ورودی (فضای‌نامِ `review2-d3`) | خروجی | exit |
|---|---|---|
| دو نام روی `alpine:latest` (`28bd5fe8b56d`) — باید FAIL | `GATE D3 FAIL :lan and :lan-rollback are the same image (28bd5fe8b56d)` | `1` / `1` |
| `:lan`=`28bd5fe8b56d`، rollback=`cd17e2ac9824` — باید PASS | `OK D3: lan=28bd5fe8b56d rollback=cd17e2ac9824` · `GATE D3 PASS` | `0` / `0` |

### D1a / D1b (در یک `git clone --shared` خصوصی روی `88997bd6`؛ درختِ repo لمس نشد)

| ورودی | خروجی | exit (5.1 / 7.6) |
|---|---|---|
| D1a، build sha = `deadbeef` — FAIL | `GATE D1a FAIL HEAD 88997bd6 is not the build sha deadbeef` | `1` / `1` |
| D1a، فایلِ untracked — FAIL | `?? review2-dirty.txt` · `GATE D1a FAIL working tree is not clean (1 path(s))` | `1` / `1` |
| D1a، فایلِ tracked تغییرکرده — FAIL | ` M supabase/migrations/…533_pg_cron_http_scheduler.sql` · `GATE D1a FAIL …` | `1` / `1` |
| D1a، کنترلِ تمیز — PASS | `OK D1a: HEAD = 88997bd6, tree clean` · `GATE D1a PASS` | `0` / `0` |
| D1b، ۵۳۳ غایب — FAIL و **نام** | `MISSING 20260913101000_533_pg_cron_http_scheduler.sql` · `GATE D1b FAIL 1 migration file(s) missing: 20260913101000_533_pg_cron_http_scheduler.sql` | `1` / `1` |
| D1b، هر ۱۵ حاضر — PASS | `OK D1b: all 15 migration files present` · `GATE D1b PASS` | `0` / `0` |

### D2

| ورودی | خروجی | ORACLE مستقل (`docker image inspect` تگ ↔ `docker inspect` کانتینر) | exit |
|---|---|---|---|
| کانتینرِ یک‌بارمصرفِ `review2-d2-web` (`alpine:latest`، بدون env برنامه) — PASS | `OK D2: review2-d2:lan-rollback = running image sha256:28bd5fe8…` · `GATE D2 PASS` | rollback = running = `sha256:28bd5fe8b56d…` ✔ | `0` / `0` |
| mutant **M2** — `docker tag review2-d2:lan $rollbackTag` (خودِ باگِ B3) — FAIL | `GATE D2 FAIL rollback tag is 'sha256:cd17e2ac…' but the running image is sha256:28bd5fe8…` | rollback = `cd17…` ≠ running | `1` / `1` |
| mutant **M1** — `$runningId` از `docker image inspect review2-d2:lan` به‌جای کانتینر — **باید FAIL** | **`OK D2: … = running image sha256:cd17e2ac…` · `GATE D2 PASS`** | rollback = `cd17…` ≠ running = `28bd…` ✘ | **`0` / `0`** |
| stackِ زنده، عیناً (`afrakala-lan-web`) | `Error response from daemon: No such image: sha256:0c3106602cc9…` · `GATE D2 FAIL docker tag of the running image failed, no rollback point taken` | — (هیچ تگی ساخته نشد؛ `docker images afrakala-app` پس از اجرا: فقط `:lan` و `rollback-9c113aac`) | `1` / `1` |

### D9 (از راهِ خودِ خطِ docker در بلوک، با `$target`)

| ورودی | خروجی | exit |
|---|---|---|
| `$target='kong'` — FAIL | `OK D6(iii): … http://kong:8000` · `GATE D6 PASS` · `GATE D9 FAIL served host 'kong' is a bare name with no dot` | `1` / `1` |
| `$target='localhost'` — FAIL | `GATE D9 FAIL served host 'localhost' is loopback` | `1` / `1` |
| `$target='10.99.99.99'` — PASS | `GATE D9 PASS` | `0` / `0` |
| **محیطِ استقرار = `SUPABASE_URL=http://kong:8000`, `APP_SUPABASE_PUBLIC_URL=` (شکلِ compose)** — **باید FAIL** | خودِ image سرو می‌کند `"supabaseUrl":"http://kong:8000"`؛ همان بلوکِ emit‌شده روی **همان image** → **`GATE D9 PASS`** | **`0`** |

### `BROKEN_PROBES=2`

۱. **D2 — mutant M1.** `cited` `release/emit-blocks.ps1:624,639-640`: هم «حقیقت» (`$runningId`) و هم «موضوع» (`$rbId`) از یک خواندن می‌آیند؛ پس اگر آن یک خط به `:lan` برگردد — همان کاری که D2ِ نسخهٔ `ae47de4a` می‌کرد — gate می‌گوید `OK D2: … = running image …` در حالی که oracleِ من نشان می‌دهد نیست. M2 (خودِ باگِ B3) گرفته می‌شود. اگر مالک M1 را خارج از مدل بداند، این عدد ۱ است و حکم تغییر نمی‌کند.
   **حداقل اصلاح:** پس از `docker tag`، `.Image`ِ کانتینر را **دوباره و مستقل** بخوان و با `.Id`ِ تگ مقایسه کن.
۲. **D9 — نمی‌تواند محیطِ استقرار را ببیند.** `cited` `release/emit-blocks.ps1:820`: تنها ورودیِ D6(iii) و D9 این است: `docker run --rm --entrypoint sh -e SUPABASE_URL="http://${target}:8000" $img …`؛ و `:842` میزبان را از همان `$served` می‌خواند. پس D9 روی production **همیشه** `192.168.170.10` را می‌بیند. `compared` روی imageِ `clean`:

   ```
   DEPLOY-SHAPED A  -e SUPABASE_URL=http://kong:8000 -e APP_SUPABASE_PUBLIC_URL=           -> "supabaseUrl":"http://kong:8000"
   DEPLOY-SHAPED B  -e SUPABASE_URL=http://kong:8000 -e APP_SUPABASE_PUBLIC_URL=http://192.168.170.8:9000 -> "supabaseUrl":"http://192.168.170.8:9000"
   emitted D6/D9 block, same image, target 10.99.99.99                                        -> GATE D6 PASS, GATE D9 PASS, exit 0
   ```

   حالتِ A دقیقاً حالتی است که متنِ خودِ بلوک (`:836-840`) برای توجیهِ وجودِ D9 می‌آورد. حالتِ B **نتیجهٔ حادثه** است (مرورگر → Kongِ تست) از راهِ runtime env؛ و هیچ gateی مقدارِ سرو‌شدهٔ کانتینرِ **مستقرشده** را نمی‌سنجد — بلوکِ verify (`:931-942`) فقط کدِ HTTP می‌گیرد. برای اینکه این فرضی نیست، بخش ۵ را ببینید: `.env.lan`ِ production کلیدِ `APP_SUPABASE_PUBLIC_URL` را **ندارد**.
   **حداقل اصلاح:** در بلوکِ verify (پس از deploy) یک gate: `/login`ِ **زنده** را بگیر، `supabaseUrl` را استخراج کن، origin را با آدرسِ عمومیِ اعلام‌شده **برابرِ دقیق** بسنج (نه substring)، و D9 را روی همین مقدار اجرا کن؛ `GATE … FAIL` + توقف.

### `OVER_REJECTIONS=0`

هیچ ورودیِ مشروعی رد نشد. تنها FAILِ «ناخواسته» — D2 روی stackِ زنده — درست است: imageِ در حال اجرا در store نیست (بخش ۵).

---

## ۳. شش موردِ واگذارشده

### ۲.۱ جملهٔ سرِ سند — **BLOCKER**

`quoted` `release/emit-blocks.ps1:222-224` (در هر سندِ emit‌شده، خطوطِ ۶-۸):
> Every block below has a literal 'Expect:' line. release/validate-blocks.ps1 checks this document mechanically before anyone runs it. release/apply-release.ps1 stops at the FIRST block whose live output disagrees with its Expect: line -- exactly BLOCKS.md's own rule.

و `:3-4`: «so … release/apply-release.ps1 can execute it mechanically».

`quoted` کدِ متناقض: `release/apply-release.ps1:4-5` — «executes the Preflight + Phase 4 (migration) blocks … and STOPS before Phase 5 (image/deploy)»؛ `release/lib/apply-release-engine.sh:131-134` — `if [[ "$line" == "# Phase 5"* ]]; then STOPPED_BEFORE_DEPLOY=1 … break`؛ `:136,:145` فقط `mig_apply` و `ledger_insert_only` را match می‌کند؛ `:86-87` فقط exit codeِ `mig_apply` را برمی‌گرداند — خطوطِ `Expect: <NOTICE>` هیچ‌جا مقایسه نمی‌شوند؛ پیش‌پرواز فقط `pg_is_in_recovery()` است (`:60-65`)، نه `Expect: ledger_rows|…`ِ سند.

`measured` — سندی با **شکلِ همین release** (صفر migration، از e4b با FINALِ خالی) و build sha `deadbeef`، روی Postgresِ یک‌بارمصرفِ `review2-pg` (دیتابیس `review2_apply_probe`):

```
$ powershell -File release/apply-release.ps1 -ReleaseMd …RELEASE-r2-zero-wrong.md -TargetDb review2_apply_probe -Container review2-pg -DbUser postgres
pg_is_in_recovery() = f (Expect: f)
document declares: 0 mig_apply block(s), 0 ledger-row-only block(s) before Phase 5
--- reached Phase 5 (image transfer). Stopping cleanly -- deploy is a human step. ---
=== VERDICT: PASSED (all preflight + migration blocks matched their Expect: lines;
PASSED. Log: …
APPLY_EXIT=0
```

همان سند، gateِ D1aِ Block 0 به‌دستِ انسان: `GATE D1a FAIL HEAD 88997bd6 is not the build sha deadbeef`، `D1A_EXIT=1`. و D6 آن سند `$img = 'afrakala-app:deadbeef'` است که وجود ندارد.

**پیامد روی production:** این release صفر migration دارد (`git diff --stat origin/main...HEAD -- supabase/` خالی). پس تنها چیزی که `apply-release.ps1` برای مالک چاپ می‌کند یک `PASSED` است که **هیچ gateی پشتش نیست**، زیرِ سندی که سرش می‌گوید هر بلوک مکانیکی سنجیده می‌شود. این همان کلاسِ ۲۰۲۶-۰۹-۱۳ است: بررسیِ سبز، بی‌اندازه‌گیری.
**حداقل اصلاح:** جملهٔ سرِ سند را با دامنهٔ واقعی جایگزین کن («apply-release.ps1 فقط `pg_is_in_recovery` و exit codeِ خطوطِ `mig_apply`/`ledger_insert_only` پیش از `# Phase 5` را اجرا می‌کند؛ Block 0 و هر بلوکِ `GATE` را **انسان** اجرا می‌کند و هیچ اسکریپتی آن‌ها را نمی‌سنجد»)، و خطِ `VERDICT: PASSED`ِ engine را طوری بنویس که صریحاً بگوید Block 0 و gateها اجرا **نشدند**.

### ۲.۲ `exit 1` در پنجرهٔ تعاملی — **BLOCKER**

`measured`. روش: ارسالِ کلیدی با `SendKeys` امن نبود (قفلِ foreground؛ اسکریپت پیش از ارسال بررسی کرد، پنجرهٔ فعال Chrome بود، و **هیچ کلیدی نفرستاد**). جایش همان کاری که paste در console می‌کند: `AttachConsole(pid)` + `WriteConsoleInputW` با key eventها در input bufferِ **همان** پنجرهٔ تست — هیچ کلیدی نمی‌تواند به پنجرهٔ دیگری برود. پنجره با `-NoExit` و `Start-Transcript`؛ بلوک = D1a با sha اشتباه (از سندِ emit‌شده).

| میزبان | shell | بلوک | پروسهٔ shell پس از paste | پنجره | transcript |
|---|---|---|---|---|---|
| `conhost.exe` | 5.1 | D1a FAIL | `alive=False` | **`window_still_exists=False`** | `GATE D1a FAIL HEAD 88997bd6 is not the build sha deadbeef` چاپ شد، بعد پنجره رفت |
| `conhost.exe` | 7.6 | D1a FAIL | `False` | **`False`** | همان |
| `conhost.exe` | 5.1 | D1a PASS (کنترل) | `True` | `True` | `GATE D1a PASS` |
| `conhost.exe` | 5.1 | **D6/D9 کامل، image حادثه** | `False` | **`False`** | `GATE D6 FAIL host literal(s) in the client bundle: http://192.168.170.8:9000` |
| `conhost.exe` | 5.1 / 7.6 | D6/D9 کامل، `clean` | `True` | `True` | `GATE D6 PASS` · `GATE D9 PASS` |
| Windows Terminal | 5.1 | D1a FAIL | `False` | `True` | دلیل روی صفحه ماند (تصویرِ `PrintWindow`): `GATE D1a FAIL …` سپس `[process exited with code 1 (0x00000001)]` و `You can now close this terminal with Ctrl+D, or press Enter to restart.` |
| Windows Terminal | 7.6 | D1a FAIL | `False` | `True` | همان |

**پیامد روی production:** در console host، پنجره و دلیل با هم ناپدید می‌شوند — مالک فقط می‌بیند پنجره بسته شد. در Windows Terminal دلیل می‌ماند ولی session مرده است و **Enter** (طبیعی‌ترین کلیدِ بعد از paste) یک shellِ **تازه** بالا می‌آورد، بی‌متغیر و در cwdِ پیش‌فرض، آمادهٔ paste بلوکِ بعدی (deploy). B4 خودش گفت gateها «by design» دستی‌اند؛ پس خطِ `GATE … FAIL` **تنها** خروجیِ gate است، و روشِ اجرای مستندِ ۰۹-۱۲ (`production-migration-run-20260912.md:10-11` — «Every production command was typed by the owner») دقیقاً همین paste است. نوعِ ترمینالِ production ثبت نشده (بخش ۵).
**حداقل اصلاح:** gateها را به‌صورت فایل اجرا کن (`powershell -NoProfile -File release\gates\<id>.ps1 …` و بعد `if ($LASTEXITCODE) { … }`)، یا هر بلوکِ paste‌شدنی را در `& { … }` بپیچ و `exit 1` را با `return` + یک پرچمِ ماندگار جایگزین کن — تا پنجره و دلیل بمانند.

### ۲.۳ D1b روی مجموعهٔ تهی — **NOT A BLOCKER** (با یافته)

`measured`: سندِ emit‌شده از rehearsalی که FINAL‌اش خالی است → `$expected = @()` → `OK D1b: all 0 migration files present` · `GATE D1b PASS`، exit `0` (5.1 و 7.6).
**برای این release:** صفر migration، پس هیچ فایلی نیست که غایب باشد؛ D1b برای این release درست ولی **بی‌اطلاعات** است — «PASS» یعنی «چیزی برای سنجیدن نبود». خطرِ واقعی جای دیگر است، `measured`: emitter همان FINALِ خالی را پذیرفت در حالی که بلوکِ pre-replayِ همان سند ۲۰ ردیفِ `APPLY` داشت و بخشِ Sequenced expectations برای ۱۱ migration پر بود — `Parsed rehearsal: 0 APPLY …`، `EMIT_EXIT=0`. یعنی «مجموعهٔ تهی» از «parse شکست‌خورده» قابل تشخیص نیست. این gate را به‌تنها بازدارنده نمی‌دانم چون برای این release حقیقتِ ادعاشده («صفر migration») از `git diff` مستقل تأیید می‌شود.
**بهبودِ پیشنهادی:** `GATE D1b PASS (0 files — nothing to check)` و رد در emitter وقتی FINAL تهی و pre-replay ناتهی است.

### ۲.۴ شمارهٔ تکراریِ Block 30 — **NOT A BLOCKER**

`measured` روی سندِ emit‌شده از e4b: `### Block 28 - image transfer` (`:465`)، **Block 29 وجود ندارد**، `### Block 30 - artifact probe (D6)` (`:532`) و `### Block 30 - rollback tag` (`:692`). روی سندِ صفر-migration: `Block 8` دو بار، `Block 7` غایب. `cited` علت: `release/emit-blocks.ps1:706` و `:710` دو `$blockN++` پشتِ سرِ هم، و بعد از بلوکِ D6 (`:866`) هیچ `++`. `release/validate-blocks.ps1` روی همان سند: `blocks found : 35` · `PASSED — no problems found.` · `VALIDATE_EXIT=0`.
پیامد: ابهام در ارجاعِ انسانی («در Block 30 ایستادم» دو معنا دارد). ارجاعِ درونِ سند به rollback (`Block 31`/`Block 32`) درست محاسبه می‌شود و engine از شماره استفاده نمی‌کند.
**حداقل اصلاح:** `++` اضافهٔ `:710` را به بعد از بلوکِ D6 منتقل کن؛ یک checkِ تکراری‌نبودن در validator.

### ۲.۵ پیش‌فرضِ `D6TargetHost=192.168.170.10` — **NOT A BLOCKER**

`cited` `release/emit-blocks.ps1:31` و `:820`. روی production مقدارِ درست است. خطرش روی باکسِ تست است: اجرای بلوکِ emit‌شدهٔ پیش‌فرض اینجا کانتینری با `SUPABASE_URL` به سمتِ production بالا می‌آورد — همان کاری که این مأموریت ممنوع کرده؛ من آن را اجرا نکردم. ترکیبش با یافتهٔ substring (فاز ۳، حملهٔ ۴) بدتر است: `.10` زیررشتهٔ `.100`…`.109` است.
**بهبود:** پارامتر را اجباری کن (بدون پیش‌فرض).

### ۲.۶ D9 با aliasِ نقطه‌دار؛ D6 و `"kong:8000"`ِ بی‌scheme — **NOT A BLOCKER به‌تنها** (بخشِ D9 زیرِ BROKEN #2 رفته)

- D9: `measured` `$target='kong.internal'` → `OK D9: served host 'kong.internal' is browser-reachable` · `GATE D9 PASS`، exit `0`. ولی چون D9 اصلاً محیطِ استقرار را نمی‌بیند (BROKEN #2)، این ضعف فرعی است؛ اصلاحِ BROKEN #2 (برابریِ دقیق با آدرسِ اعلام‌شده) این را هم می‌بندد.
- D6: `measured` روی imageِ `evasion` (کاشتِ source، از Vite گذشته). bundleِ واقعی: `const Bz="192.168.170.8:9000"` و `e.__r2e="http://"+Bz` و `e.__r2a="HTTP://192.168.170.8:9000"` و `"//kong:8000"` و `"kong:8000"` — minifier concat را fold **نکرد**. نتیجه: `OK D6(i) (… 177 URL literal(s) classified)` · `GATE D6 PASS` · `GATE D9 PASS`، exit `0` در هر دو shell. یعنی خودِ `host:port`ِ حادثه، به شکلی که مستقیم به URL کارا تبدیل می‌شود، از probe می‌گذرد؛ و ادعای B1 («IP literals, explicit ports … fail unconditionally»، `:728-729`) فقط برای literalهای با schemeِ کوچک درست است.
  چرا بازدارنده نیست: `measured` روی `clean` — صفر `IPv4:port`ِ بی‌scheme، صفر `kong|localhost|db|auth|rest|storage:port`، صفر `//host:port`، صفر schemeِ بزرگ. آرتیفکتِ امروز تمیز است و شکلِ دقیقِ حادثه (`http://IP:port`) گرفته می‌شود.
  **بهبود:** متنِ ادعا را محدود کن، و الگوی `[0-9]{1,3}(\.[0-9]{1,3}){3}:[0-9]+` و `-i` برای scheme اضافه شود.

---

## ۴. فاز ۳ — حمله به کدِ دورِ اصلاح

| # | حمله | حکم | شاهد |
|---|---|---|---|
| ۱ | **D8: fenceِ بسته‌نشده، حکمِ FAILِ واقعیِ بعدی را پنهان می‌کند** | **confirmed** | `measured`: e4b + `` ``` `` + `truncated stage output` + `## VERDICT: FAIL (replay stopped early)` → `Written: RELEASE-r2-t3_unclosed.md`، exit `0` (5.1 و 7.6). `cited` `release/emit-blocks.ps1:67-68`: هر خطِ fence وضعیت را toggle می‌کند و پایانِ فایل با fenceِ باز هیچ خطایی نمی‌دهد. مسیرِ واقعی (`assertion` + `cited`): `rehearse-engine.sh:1169-1171` fence را با سه `tee -a` جدا باز و بسته می‌کند و `03-gates.md` با `-a` بین اجراها انباشته می‌شود (`:303-313`)؛ قطعِ یک اجرا بینِ `:1169` و `:1171` و اجرای دوباره‌ای که FAIL می‌دهد همین شکل را می‌سازد. آن را با قطعِ واقعیِ rehearsal بازتولید نکردم. |
| ۲ | **D8: `~~~` داخلِ fenceِ `` ``` `` پاریته را برمی‌گرداند** | **confirmed** | `measured`: e4b + `` ``` `` / `~~~` / `` ``` `` / `## VERDICT: FAIL …` → exit `0`، سند نوشته شد. در CommonMark این FAIL بیرونِ fence است. |
| ۳ | **D6: آیا allowlist با میزبانی واقعاً خطرناک راضی می‌شود؟** | **confirmed** | `cited` `:793`: `$allowHosts` **سقفِ شمارش ندارد**. `measured` imageِ `allow-dangerous`: `window.location.href="https://get-git-going.lovable.app/login"` + دو بار `https://myafrakala.ir` → `OK D6(i) (… 179 URL literal(s) classified)` · `GATE D6 PASS`، exit `0`. `measured` در `clean`: `get-git-going.lovable.app` امروز فقط یک بار و فقط به‌صورت `…/sitemap.xml` هست. `assertion`: `get-git-going.lovable.app` نسخهٔ ابریِ Lovableِ همین برنامه است (از نامِ repo و `cloudUrl`ِ حذف‌شده در `vite.config.ts`)؛ فرستادنِ کارمند به آن = ورود به دیتابیسی دیگر — همان کلاسِ حادثه. این را اندازه نگرفتم. |
| ۴ | **D6(iii) مقایسهٔ substring است** | **confirmed** | `cited` `:827`: `$served -notmatch [regex]::Escape($target)`. `measured` imageِ `fakeserver-substr` (سروری که env را نادیده می‌گیرد و `http://10.99.99.99:8000` سرو می‌کند) با `$target='10.99.99.9'` → `OK D6(iii)` · `GATE D6 PASS` · `GATE D9 PASS`، exit `0`. روی production: `192.168.170.10` زیررشتهٔ `192.168.170.100:*` و هر پورتی است. کنترل: `fake-mismatch` (`192.168.170.8:9000`) → `GATE D6 FAIL served config does not name the target 10.99.99.99`، exit `1`. |
| ۵ | **سقفِ شمارش با scheme/اسلش/escape دور زده می‌شود؟** | **refuted** | `measured`: `http://192.168.170.8:11434/api/chat` → `(allowed 1x …, found 2x)`، `1`؛ `https://192.168.170.8:11434` → `(explicit port, IP literal)`، `1`؛ `http:\/\/192.168.170.8:11434` داخلِ JSON → `found 2x`، `1`. کلاسِ کاراکتر `/` را ندارد، پس مسیر و اسلش به همان literal نرمال می‌شوند. |
| ۶ | **D2 وقتی `afrakala-lan-web` وجود ندارد** | **refuted (درست رفتار می‌کند)** | `measured`: `error: no such object: review2-nonexistent-web` · `FAIL D2: could not read the image … is running (got '')` · `GATE D2 FAIL cannot read the image …`، exit `1` (5.1 و 7.6). |
| ۷ | **خطِ GATE روی همهٔ مسیرهای خروج؟** | **confirmed (همه دارند) + یک زیرِ-check خالی‌گذر** | `measured` شمارشِ ایستا روی سندِ emit‌شده: **۱۶** `exit 1`، هر ۱۶ بلافاصله پس از `GATE <id> FAIL`؛ **۶** `GATE <id> PASS`؛ هیچ `exit` دیگری. هر ۱۶ مسیر را **اجرا** هم کردم: D1a×2، D1b×1، D2×3، D3×1، D6(0)، D6(i)-nothing (`nojs` → `the scan measured nothing (docker exit 0, 0 js file(s) read)`)، D6(i)-literal، D6(ii) (`nogrep`)، D6(iii)-empty، D6(iii)-mismatch، D9-parse (`fake-space` → `GATE D9 FAIL no host could be parsed`)، D9-loopback، D9-bare. یافته: با grepِ شکسته، `OK D6(i) (475 js files read, 0 URL literal(s) classified)` چاپ می‌شود — (i) بی‌اندازه‌گیری OK می‌دهد و فقط (ii) نجاتش می‌دهد. |
| ۸ | **ارتقای وابستگی با یک URLِ ثالثِ تازه** | **refuted (بلند و با نام شکست می‌خورد)** | `measured` imageِ `newdep`: `https://collect.newvendor-review2.io  (host not on the reviewed list)` · `GATE D6 FAIL …`، exit `1`. نکته، `quoted` `Dockerfile:14-20`: `bun install` **بدونِ** lockfile («We intentionally do NOT copy bun.lock*») — پس rebuildِ روزِ release می‌تواند نسخهٔ تازه‌تری بکشد و probe را بلند قرمز کند (over-rejection، نه false pass). |
| ۹ | **D3 بدونِ تگِ rollback** | **confirmed** | `measured`: `:lan` حاضر، `lan-rollback` غایب → `OK D3: lan=28bd5fe8b56d rollback=` · `GATE D3 PASS`، exit `0` (5.1 و 7.6). `cited` `:668-670` فقط برابری را می‌سنجد. با ۲.۲ ترکیب می‌شود: D2 شکست می‌خورد و پنجره می‌بندد، مالک پنجرهٔ تازه باز می‌کند و بلوکِ load + D3 را paste می‌کند → `GATE D3 PASS` بی نقطهٔ بازگشت. |

---

## ۵. فاز ۴ — دو اندازه‌گیریِ باز

### ۴.۱ D2 روی production

**روی این باکس، مستقل تأیید شد (`measured`):**

```
docker inspect afrakala-lan-web --format '{{.Image}}'   -> sha256:0c3106602cc90f364f9cb00c39fee9411d3033c0708f7dfb94f2a5c26aeb7450
docker image inspect sha256:0c3106602cc9…               -> Error response from daemon: No such image   image_inspect_exit=1
docker images --no-trunc -q | grep -c 0c3106602cc9     -> 0   (exit 1)
docker images -a --no-trunc | grep 0c3106602cc9        -> (nothing, exit 1)
docker info  -> driver=overlayfs, [[driver-type io.containerd.snapshotter.v1]]   server=29.6.2
```

**روی production (`8b04c479e022`): تأییدناپذیر.**
- `8b04c479` در هیچ فایلی از repo، `release/runs`، `release/out`، `D:\AfraKalaTest\dumps` و memoryها نیست؛ تنها ذکرش پیوستِ پیاده‌ساز `:178` است («روی این باکس نیست»).
- `quoted` `D:\AfraKalaTest\dumps\research\owner-prep-20260913.md:237` (گردآوری‌شده روی `.10`، `:3`): `Docker engine: Docker Desktop 4.71.0, server 29.4.1, linux/x86_64, overlayfs`. «overlayfs» چیزی را روشن نمی‌کند: همین باکس هم `driver=overlayfs` گزارش می‌دهد در حالی که containerd snapshotter است، و روی همین باکس imageِ در حال اجرا در store نیست.
- آخرین تصویرِ ثبت‌شده از image store production مالِ ۰۹-۱۲ است (`production-migration-run-20260912.md:1818-1819`: `afrakala-app:lan dc926c1a7224` / `afrakala-app:lan-rollback dc926c1a7224`) — قبل از حادثه و قبل از `8b04c479e022`.

**چه چیزی آن را روشن می‌کند** (read-only، به دستِ مالک روی `.10`):
```
docker inspect afrakala-lan-web --format '{{.Image}}'
docker image inspect <همان id> --format '{{.Id}}'; $LASTEXITCODE
docker info --format '{{.DriverStatus}}'
```

**پیامد:** اگر آن image در store نباشد، D2 روی production دقیقاً مثلِ اینجا `GATE D2 FAIL docker tag of the running image failed` می‌دهد و **release آن‌طور که نوشته شده نمی‌تواند جلو برود** — تا اندازه‌گیری نشده، این یک **بازدارندهٔ release** است (مستقل از حکمِ کدِ PR). D2 در این حالت درست رفتار می‌کند (fail-closed)؛ مشکل این است که برای آن هیچ مسیرِ جایگزینی در runbook نیست.

**و آنچه این پرسش را سنگین‌تر می‌کند** — `quoted` `D:\AfraKalaTest\dumps\research\env-parity-20260913.md` (`:3` «Gathered on `192.168.170.10`»؛ `A` = `C:\afrakala\deploy\lan\.env.lan`):
```
:58  | SUPABASE_URL      | set (len=16) | set (len=16) | same |
:62  | VITE_SUPABASE_URL | set (len=26) | set (len=26) | same |
:15  A keys : 40
:17  union  : 40
```
و `APP_SUPABASE_PUBLIC_URL` در این جدولِ ۴۰-کلیدی **صفر بار** آمده (`grep -c` → `0`). `cited` `deploy/lan/docker-compose.yml:45-67`: سرویسِ `web` نه `env_file` دارد نه `VITE_SUPABASE_URL` را در زمانِ اجرا پاس می‌دهد؛ `:67` `APP_SUPABASE_PUBLIC_URL: ${APP_SUPABASE_PUBLIC_URL:-}`. `cited` `src/lib/runtime-config.ts:87-91`: ترتیب `APP_SUPABASE_PUBLIC_URL || VITE_SUPABASE_URL || SUPABASE_URL`. پس با فایلِ ثبت‌شده، مرورگرهای production مقدارِ `SUPABASE_URL` را می‌گیرند — طولِ ۱۶، که `measured` برابرِ طولِ `http://kong:8000` است (`http://192.168.170.10:8000` = ۲۶ = طولِ `VITE_SUPABASE_URL`). برابریِ مقدار `assertion` است (فقط طول ثبت شده). و BROKEN #2 نشان داد هیچ gateی این را نمی‌بیند.

### ۴.۲ نسخهٔ PowerShellِ production

**تأییدناپذیر.** هیچ `$PSVersionTable` یا ذکرِ نسخه برای `.10` ثبت نشده. شواهدِ غیرمستقیم (`assertion`، نه اندازه‌گیری):
- `quoted` `production-migration-run-20260912.md:1931`: «PowerShell strips quotes when passing a JSON body to `curl.exe`» — رفتارِ legacyِ ارسالِ آرگومان که مشخصهٔ Windows PowerShell 5.1 (و pwsh پیش از 7.3) است.
- `quoted` `CLAUDE.md:174-175`: «…because Windows PowerShell 5.1 treats a native command's stderr as an…» — shellِ مرجعِ پروژه.
- نوعِ پنجره (console host یا Windows Terminal) و نسخهٔ Windowsِ `.10` هم ثبت نشده — که برای ۲.۲ تعیین‌کننده است.

آنچه **من** اثبات کردم: هر gate در 5.1 (`5.1.26100.9278`) و pwsh `7.6.4` خروجی و exit یکسان داد، با `-File` و با paste. pwsh 7.0–7.2 آزموده نشد. **روشن‌کننده:** `$PSVersionTable.PSVersion; $Host.Name` در **همان** پنجره‌ای که release در آن اجرا می‌شود.

---

## ۶. typecheck

```
$ npm run typecheck > typecheck.log 2>&1; echo "NPM_EXIT=$?"      (یک بار؛ بدونِ pipe)
NPM_EXIT=2
error TS count : 70
distinct files : 6   (products.index 18 · sales-reminders 15 · invoices/functions 13 · accounting/functions 13 · audit/index 6 · admin.automation 5)
errors in the 5 changed .ts/.tsx files of this branch : 0 each
```

**`2` درست است.** `tsc --noEmit` وقتی diagnostic دارد غیرصفر برمی‌گرداند و `npm run` همان را منتقل می‌کند. من `$?` را بلافاصله پس از redirect خواندم، نه پس از `|`. `0`ِ بازبینیِ اول قابل بازتولید نیست؛ اینکه چطور خوانده شده (احتمالاً exitِ آخرین فرمانِ یک pipe) `assertion` است.

---

## ۷. اختلاف با پیوستِ پیاده‌ساز (`runtime-config-completion-20260913.md:464-663`)

**ادعاهایی که بازتولید نکردم / با شاهدِ من نمی‌خواند:**

۱. **`BROKEN_PROBES=0`، `OVER_REJECT=0`** (`:617`). مالِ من: `BROKEN_PROBES=2` (D2-M1، D9 روی محیطِ استقرار) به‌علاوهٔ شش false-PASSِ تأییدشده در فاز ۳.
۲. **mutantِ D2** (`:579`) فقط «بازگرداندنِ منبعِ `afrakala-app:lan`» (M2ِ من) است. mutantی که **خواندن** را به `:lan` برمی‌گرداند (M1) آزموده نشده و PASS می‌دهد.
۳. **(الف) در `:503-504`: «هر literalی که شکلِ backend دارد — IP، پورتِ صریح … — همیشه FAIL است».** `measured` روی `evasion`: `"192.168.170.8:9000"` + `"http://"+Bz` و `HTTP://192.168.170.8:9000` → `GATE D6 PASS`. `:657-658` (تأییدنشده ۴) فقط `"kong:8000"` را نام می‌برد و حادثه‌ای‌ترین شکل — خودِ `host:port`ِ حادثه در ترکیبِ واقعیِ خروجیِ Vite — را کم‌برآورد می‌کند.
۴. **B2، `:538`: «بیرون از code fence … fail-closed».** fenceِ بسته‌نشده و `~~~`ِ داخلِ `` ``` `` حکمِ FAILِ واقعی را پنهان و سند را منتشر می‌کنند (فاز ۳، ۱ و ۲).
۵. **B4، `:595`: «نقصِ واقعی این است که شکستِ دستی می‌توانست دیده نشود»**، و `:650-652` پیش‌بینی می‌کند پنجره بسته می‌شود. اندازه‌گیریِ من: در console host بله؛ در Windows Terminal دلیل می‌ماند ولی session می‌میرد و Enter یک shellِ تازه می‌سازد. یعنی اصلاحِ B4 در حالتِ اجرای مستند یا دلیل را پاک می‌کند یا session را.
۶. **`:515`: «منطقِ D9 دست نخورد»** — درست است، و مشکل همین است: D9 هیچ‌گاه ورودیِ مستقلی از محیطِ استقرار نداشت (BROKEN #2)؛ پیوست این را ذکر نمی‌کند.
۷. **`:600`: `validate-blocks.ps1` روی سندِ emit‌شده `PASSED`** — درست است (`VALIDATE_EXIT=0`)، ولی روی سندی با `Block 30` دوبار؛ یعنی validator تکرار را نمی‌بیند.
۸. **`:473` و جدول‌های `:477-483`، `:519-527` (وضعِ پیش از اصلاح و باگِ quotingِ 5.1)** — emitterِ قدیمی را اجرا نکردم؛ نه تأیید نه رد.
۹. **`:565-567` (برابریِ `.Image` و `.Id` روی چهار کانتینرِ دیگر)** — تکرار نکردم؛ oracleِ من روی کانتینرِ یک‌بارمصرف با همان قالب سازگار بود.

**آنچه یافتم و پیوست ذکر نمی‌کند:**

۱۰. **`.env.lan`ِ production `APP_SUPABASE_PUBLIC_URL` ندارد** (`env-parity-20260913.md:15,17,58,62`)، پس با این شاخه مرورگرهای production `SUPABASE_URL` (طولِ ۱۶) را می‌گیرند؛ و هیچ gateی — نه D6(iii)، نه D9، نه بلوکِ verify — مقدارِ سرو‌شدهٔ کانتینرِ مستقرشده را نمی‌سنجد.
۱۱. **`apply-release.ps1` روی سندی با gateهای FAIL، `PASSED` و `exit 0` داد** (۲.۱، `measured`)؛ پیوست تناقضِ جمله را «دست‌نخورده» می‌گذارد (`:662-663`) ولی آن را نسنجیده.
۱۲. **D6(iii) مقایسهٔ substring است** (`:827`) — `10.99.99.9` از `10.99.99.99` پاس شد؛ `.10` زیررشتهٔ `.100-.109`.
۱۳. **میزبان‌های allowlist سقفِ شمارش ندارند** — redirect به `get-git-going.lovable.app` پاس شد.
۱۴. **D3 بدونِ تگِ rollback `GATE D3 PASS` می‌دهد.**
۱۵. **D6(i) با grepِ شکسته `OK` با `0 URL literal(s) classified` می‌دهد** (فقط D6(ii) نجاتش می‌دهد).
۱۶. **emitter FINALِ خالی را در حضورِ ۲۰ ردیفِ pre-replay بی‌صدا به «۰ APPLY» تبدیل می‌کند.**
۱۷. **`bun install` بدونِ lockfile** (`Dockerfile:14-20`) — allowlistِ «ساخته‌شده از موجودیِ اندازه‌گیری‌شده» (`:509`) در rebuildِ بعدی تضمینی ندارد (بلند شکست می‌خورد، نه خاموش).
۱۸. **ردپای پیاده‌ساز روی باکس:** `afrakala-app:rc-realpath` (`404ecc35ed29`)، `rc-browser` (`ae5722e8bfc6`)، `rc-banner` (`4da37c3a0591`)، `rc-key` (`b1d5d1b6adc0`)، `rc-pilot` (`d0a6f7c7da4c`)، همه ۲۰۲۶-۰۹-۱۳ — پاک‌سازیِ `:633-635` آن‌ها را نام نمی‌برد. حذفشان تصمیمِ مالک است؛ من لمسشان نکردم.

**آنچه پیوست می‌گوید و تأیید کردم:** ۷ ردیفِ جدولِ B1 (با imageهای خودم)؛ ۵ ردیفِ REJECT/ACCEPTِ B2 که آزمودم؛ ردیف‌های PASS، M2، زنده و کانتینرِ ناموجودِ B3؛ هر خطِ GATE در B4؛ `70/6` و `exit 2`؛ حذفِ `afrakala-app:review-ae47de4a`؛ `:lan` و `StartedAt` بی‌تغییر.

---

## ۸. آنچه نتوانستم تأیید کنم

۱. **هیچ‌چیز روی `192.168.170.10`.** نه حضورِ `8b04c479e022` در store، نه نسخهٔ PowerShell، نه نوعِ ترمینال، نه نسخهٔ Windows، نه **مقدارِ** `SUPABASE_URL` (فقط طولِ ۱۶ ثبت شده). پیش‌بینیِ «مرورگرهای production پس از deploy `http://kong:8000` می‌گیرند» استنتاج از طول + compose + `runtime-config.ts` است، نه اندازه‌گیری.
۲. **paste با Ctrl+V واقعی** — قفلِ foreground ارسالِ کلید را امن نمی‌کرد؛ من key eventها را مستقیم در input bufferِ console نوشتم (همان چیزی که paste در console host تولید می‌کند). مسیرِ pasteِ خودِ Windows Terminal (bracketed paste) و رفتارِ متنِ باقی‌ماندهٔ یک pasteِ چندبلوکی پس از `exit` اندازه‌گیری نشد.
۳. **واقع‌گرایی حملهٔ fenceِ D8** — probe را با سند اندازه گرفتم؛ تولیدِ چنین سندی با قطعِ واقعیِ `rehearse-engine.sh` بازتولید نشد.
۴. **رفتارِ مرورگرِ واقعی** با `supabaseUrl` سرو‌شدهٔ `kong` — فقط HTML سرو‌شده را سنجیدم.
۵. **pwsh 7.0–7.2** آزموده نشد.
۶. **طولِ `git rev-parse --short HEAD` روی cloneِ production** — اگر از ۸ بیشتر شود D1a به‌دروغ FAIL می‌دهد (over-rejection). اینجا clone و repo هر دو `88997bd6` دادند.
۷. **اینکه `get-git-going.lovable.app` به دیتابیسِ دیگری وصل است** — استنتاج از نام.
۸. **`release/build.ps1` end-to-end** (D4/D7) در این بازبینی دوباره اجرا نشد.
۹. **`apply-release-engine.sh:47-48`** نامِ دیتابیسِ `afrakala` را رد می‌کند (`REFUSED: 'afrakala' is a real database`)؛ اینکه نامِ دیتابیسِ هدف روی production چیست و این guard آنجا چه می‌کند را نسنجیدم (این فایل در diffِ این شاخه نیست).
۱۰. **ادعاهای «پیش از اصلاح» پیوست** (`:477-483`، باگِ quotingِ 5.1) — emitterِ قدیمی را اجرا نکردم.

---

## پیوست — پاک‌سازی و سلامتِ stack (`measured`، پایانِ کار)

```
docker rmi review2-afrakala:{clean,kong,supa,ollama2,evasion,nologin,fakeserver-empty,fakeserver-substr,
  nojs,nogrep,fake-mismatch,fake-space,cap-slash,cap-https,cap-escaped,allow-dangerous,newdep}   -> no failure
review2 images left: 0 · review2 containers left: 0 · review2-d2 / review2-d3 tags: 0 · review2-pg: removed
docker image inspect 9e91fa2e3e20 d0bdeb37cb1d 9898a7a4dab5 d953f1a22a11 5b916cfbcded           -> none present
afrakala-app:lan   sha256:296eb4b4899f           (unchanged)
afrakala-lan-web   Image=sha256:0c3106602cc9…  StartedAt=2026-09-12T17:46:06.169711504Z  Restarts=0  Health=healthy
afrakala-app:lan-rollback                        (does not exist — D2 on the live stack failed before tagging)
git status --porcelain                           (empty)   HEAD=88997bd6
test console windows REVIEW2-*                   all closed
```

هیچ خواندن یا نوشتنی روی `192.168.170.10`؛ هیچ deploy، restart یا `compose build`؛ هیچ `docker commit`/`pause`؛ هیچ کانتینری با `SUPABASE_URL` به سمتِ `.10`؛ `:lan` جابه‌جا نشد؛ هیچ ردیفی در `afrakala` لمس نشد؛ هیچ `npm run build` بیرون از Docker؛ هیچ اصلاحی در کد.

## آنچه جلوی تأیید را می‌گیرد — حداقلِ اصلاح برای هر مورد

| # | بازدارنده | حداقلِ اصلاح |
|---|---|---|
| 1 | **D9/D6(iii) محیطِ استقرار را نمی‌بینند**؛ production `APP_SUPABASE_PUBLIC_URL` ندارد | gateِ پس از deploy روی `/login`ِ **زنده**: `supabaseUrl` استخراج، برابریِ **دقیقِ** origin با آدرسِ عمومیِ اعلام‌شده، D9 روی همان مقدار؛ و شرطِ پیشین در runbook: وجودِ `APP_SUPABASE_PUBLIC_URL` در `.env.lan`ِ هدف |
| 2 | **D2 خود-ارجاع (M1)** | پس از `docker tag`، `.Image`ِ کانتینر را مستقل دوباره بخوان و با `.Id`ِ تگ مقایسه کن |
| 3 | **۲.۱ جملهٔ سرِ سند + `PASSED`ِ engine** | متن را با دامنهٔ واقعی جایگزین کن؛ `VERDICT: PASSED` صریحاً بگوید Block 0 و gateها اجرا نشدند |
| 4 | **۲.۲ `exit 1` در پنجرهٔ paste** | gateها به‌صورتِ `-File`، یا `& { … }` با `return` و پرچمِ ماندگار |
| 5 | **D8 fence (فاز ۳، ۱–۲)** | fenceِ باز در پایانِ فایل → FATAL؛ fence فقط با همان کاراکتر بسته شود |

و پیش از **اجرای** release (نه کدِ PR): اندازه‌گیریِ ۴.۱ روی `.10`.
