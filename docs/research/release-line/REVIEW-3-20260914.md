# REJECT — بازبینیِ هدفمندِ سوم، `fix/release-line-runtime-config` @ `1d2253d5` · `BROKEN_PROBES=1` · `OVER_REJECTIONS=0`

شاخه: `fix/release-line-runtime-config` @ `1d2253d5` (پیوست) · commitهای اصلاح: `90a754c1` (C1)، `47054bce` (C2)، `a9b58b71` (C3)، `27659650` (C4)، `271b044f` (C5)، `d3a5ea65` (C6) · پایه: `origin/main` @ `9bc8d554` · باکس: `D:\AfraKalaTest\app` (تست، `192.168.170.8`) · تاریخ: ۲۰۲۶-۰۹-۱۴
بازبینی‌های قبلی: `REVIEW-20260913.md` (`88887140`) و `REVIEW-2-20260913.md` (`f46c0e20`)، هر دو REJECT.
پیوستِ پیاده‌ساز (`runtime-config-completion-20260913.md:664-983`) **پس از** تشکیلِ حکم خوانده شد.

سطوحِ شاهد: `measured` (دستور + خروجی + exit code که جدا از هر pipe خوانده شد) · `quoted` · `cited` · `compared` · `assertion` (استدلال، برچسب‌خورده).

## ۱. حکم در یک پاراگراف

هر شش اصلاح **با ورودی‌های خودم، در هر دو جهت، زیرِ 5.1 و pwsh 7** همان کاری را می‌کنند که ادعا شده. regression سالم است، typecheck `70/6` است، و هر FAIL لازم FAIL شد و هر PASS لازم PASS. ولی یک probe هنوز بی‌آنکه درست باشد PASS می‌دهد، و این همان کلاسِ حادثه است: imageی که `const $w="192.168.170.8"` و `"http://"+$w+":9000"` را در bundle دارد — یعنی **IPِ حادثه بدونِ پورت**، که در زمانِ اجرا به `http://192.168.170.8:9000` تبدیل می‌شود — از D6(i) و D9 با `GATE D6 PASS` · `GATE D9 PASS`، exit `0` در هر دو shell عبور کرد. خودِ متنِ بلوکِ D6 می‌گوید «an IP literal … ALWAYS fails» و «does it contain ANY host». این یک قدم با imageِ evasionِ بازبینیِ دوم فاصله دارد (همان الحاق، فقط پورت هم جدا شده)، و D10 آن را نمی‌بیند چون D10 فقط config ِ سرو‌شده را می‌خواند، نه آدرسی را که کدِ پخته‌شده استفاده می‌کند. پس `BROKEN_PROBES=1` و طبقِ شرطِ تکمیل، **REJECT**. اصلاحِ حداقلی کوچک است (بخشِ ۹).

---

## ۲. فاز ۰ — وضعیتِ اولیه (`measured`)

```
branch                       fix/release-line-runtime-config   HEAD=1d2253d5
git status --porcelain       ?? docs/research/_last-report.md
                             ?? docs/research/_last-report.prev-20260914T1133-asan541.md
                             ?? docs/research/release-line/_last-report.md      (مالِ من نیستند؛ لمس نشدند)
merge-base --is-ancestor 9bc8d554 HEAD   exit 0
commits  90a754c1 C1 · 47054bce C2 · a9b58b71 C3 · 27659650 C4 · 271b044f C5 · d3a5ea65 C6 · 1d2253d5 appendix
afrakala-app:lan             sha256:296eb4b4899ff75d…   (= afrakala-app:3bc526c4)
afrakala-lan-web             Image=sha256:0c3106602cc9…  StartedAt=2026-09-12T17:46:06.169711504Z  Restarts=0  healthy
docker                       29.6.2, [[driver-type io.containerd.snapshotter.v1]]
imageهای c6fix-* / rc-*       هیچ‌کدام حاضر نبود (پیوست :942 می‌گوید پیاده‌ساز afrakala-app:rc-* را حذف کرده)
shells                       powershell.exe 5.1.26100.9278 · pwsh 7.6.4 · wt.exe حاضر
```

### روش

- imageها از `git archive 1d2253d5` در scratchpad با `docker build` (هرگز compose): `review3-afrakala:clean` = `53c784739c59`؛ `evasion` = `1c8929926687` (کاشت در **source**، `src/routes/__root.tsx`، مطابقِ توصیفِ REVIEW-2 §2.6)؛ `attack` = `f2a05555652b` (کاشتِ من، بخشِ ۶). همه `BUILD_EXIT=0`. `measured`: کاشت‌ها پس از Vite/minifier عیناً در bundle ماندند:
  `const $w="192.168.170.8:9000";…e.__r3e="http://"+$w,e.__r3a="HTTP://192.168.170.8:9000",e.__r3k="//kong:8000",e.__r3b="kong:8000"` (evasion) و `const $w="192.168.170.8";…e.__r3t=\`${t}kong:8000\`` (attack).
- imageهای مشتق (FROM clean): `served99` (`ENV APP_SUPABASE_PUBLIC_URL=http://10.99.99.99:8000`) و `persian` (فایلِ JS با IPv4:port چسبیده به حرفِ فارسی).
- سندها را **emitterِ همین commit** در یک `git clone --shared` خصوصی روی `1d2253d5` تولید کرد (فایلِ emitter جز پایانِ خط با درخت یکسان، `compared`)، زیرِ 5.1: `RELEASE-r3full.md` (از e4b، `-D6TargetHost 10.99.99.99 -LiveSiteUrl http://127.0.0.1:38110`، manifest با sha `1d2253d5`) و `RELEASE-r3zero.md` (e4b با FINALِ خالی، sha `deadbeef`) — `EMIT_FULL_EXIT=0`، `EMIT_ZERO_EXIT=0`.
- هر gate عیناً از سند استخراج شد (de-indent)؛ تنها تغییر جایگزینیِ نامِ image/کانتینر/تگ/آدرس به فضای‌نامِ `review3-*`، هر جایگزینی با `diff` چاپ شد. اجرا با `powershell.exe -File` و `pwsh.exe -File`، خروجی به فایل، `rc=$?` بلافاصله — بدونِ pipe.
- هیچ کانتینری `SUPABASE_URL` یا `APP_SUPABASE_PUBLIC_URL` به `.10` نداشت؛ آدرسِ هدفِ آزمایشی `10.99.99.99`.

---

## ۳. شش اصلاح — جدولِ شاهد (همهٔ ردیف‌ها `measured`؛ خروجیِ 5.1 و 7 یکسان مگر ذکر شود)

### C1 — D10، سایتِ زنده پس از deploy

«استقرار» = کانتینرِ `clean` یا nginxِ یک‌بارمصرف روی `127.0.0.1:381xx`؛ `$expected='http://10.99.99.99:8000'` مگر ذکر شود. oracleِ مستقل پیش از gate: `curl … /login | grep -oE '"supabaseUrl":"[^"]*"'`.

| # | استقرار | باید | خروجی | exit 5.1 / 7 |
|---|---|---|---|---|
| 1 | clean، `SUPABASE_URL=http://kong:8000`، `APP_SUPABASE_PUBLIC_URL=` (شکلِ production امروز) | FAIL | `GATE D10 FAIL live supabaseUrl is 'http://kong:8000', not exactly 'http://10.99.99.99:8000'` | `1` / `1` |
| 2 | clean، `APP_SUPABASE_PUBLIC_URL=http://192.168.170.8:9000` (میزبانِ حادثه) | FAIL | `… is 'http://192.168.170.8:9000', not exactly …` | `1` / `1` |
| 3 | clean، `…=http://10.99.99.99:8000.nip.io` (superstring) | FAIL | `… is 'http://10.99.99.99:8000.nip.io', not exactly …` | `1` / `1` |
| 4 | clean، `…=http://10.99.99.99:8000/` | FAIL | `… is 'http://10.99.99.99:8000/', not exactly …` | `1` / `1` |
| 5 | clean، `.99` سرو؛ `$expected='http://10.99.99.9:8000'` (.9 داخلِ .99) | FAIL | `… is 'http://10.99.99.99:8000', not exactly 'http://10.99.99.9:8000'` | `1` / `1` |
| 6 | nginx، دو `<script>` با دو انتسابِ config | FAIL | `GATE D10 FAIL the live /login carries 2 runtime config assignment(s), parseable=True` | `1` / `1` |
| 7 | پورتی که هیچ‌چیز روی آن گوش نمی‌دهد (`:38119`) | FAIL | `GATE D10 FAIL could not read http://127.0.0.1:38119/login (curl exit 7)` | `1` / `1` |
| 8 | nginx، `/login` = 200 با بدنهٔ خالی | FAIL | `… could not read … (curl exit 0)` | `1` / `1` |
| 9 | nginx، `/login` بدونِ config | FAIL | `… carries 0 runtime config assignment(s), parseable=False` | `1` / `1` |
| 10 | clean، `APP_SUPABASE_PUBLIC_URL=http://10.99.99.99:8000` | **PASS** | `OK D10: http://127.0.0.1:38110/login serves supabaseUrl 'http://10.99.99.99:8000' -- exactly the expected public address` · `GATE D10 PASS` | `0` / `0` |

ردیفِ ۴ over-rejection نیست: `cited` `src/lib/storage/upload-with-progress.ts:17` آدرس را با `{SUPABASE_URL}/storage/v1/…` می‌چسباند و `src/routes/api.admin.automation.torob.enqueue.ts:34` با `` `${supabaseUrl}/auth/v1/user` ``؛ اسلشِ پایانی `//storage/v1` می‌سازد. (`measured`: supabase-js 2.116.0 خودش `new URL("rest/v1", base)` می‌کند، پس فقط آن مسیر بی‌حساسیت است.)

**C1: verified، هر دو جهت، هر دو shell.**

### C2 — D2 پس از tag کانتینر را دوباره می‌خواند

کانتینرِ `review3-d2-web` (entrypoint `sleep`، از `review3-afrakala:clean` — imageِ **manifest-list با attestation** که buildx در containerd store می‌سازد، یعنی شکلِ imageِ production)؛ فریب `review3-d2:lan` = `evasion`. oracle پس از هر اجرا: `docker image inspect <tag>` ↔ `docker inspect <container>`. نام‌گذاری طبقِ مأموریت (REVIEW-2 و پیوست نام‌ها را برعکس دارند؛ هر دو mutant آزموده شد).

| ورودی | diff نسبت به gateِ emit‌شده | باید | خروجی | oracle | exit 5.1 / 7 |
|---|---|---|---|---|---|
| gateِ درست | فقط نام‌ها | PASS | `OK D2: review3-d2:lan-rollback = running image sha256:53c78473…` · `GATE D2 PASS` | `53c784739c59` = `53c784739c59` | `0` / `0` |
| **M1** — tag از `:lan` | `docker tag review3-d2:lan $rollbackTag` | FAIL | `GATE D2 FAIL rollback tag is 'sha256:1c892992…' but the running image is sha256:53c78473…` | `1c8929926687` ≠ `53c784739c59` | `1` / `1` |
| **M2** — `:lan` به‌عنوانِ «در حال اجرا» (در REVIEW-2 PASS می‌داد) | `$runningId = [string](docker image inspect review3-d2:lan --format "{{.Id}}")` | FAIL | `FAIL D2: … but review3-d2-web, re-read after tagging, runs sha256:53c78473…` · `GATE D2 FAIL rollback tag is 'sha256:1c892992…' but review3-d2-web is running sha256:53c78473…` | `1c8929926687` ≠ `53c784739c59` | `1` / `1` |

`measured` پیش از اجرا: `.Image`ِ کانتینر = `.Id`ِ تگ = `sha256:53c784739c59…` برای imageِ manifest-list — یعنی مقایسهٔ `.Id` ↔ `.Image` روی شکلِ buildِ این پروژه در containerd store معنادار است.
**C2: verified، هر دو جهت، هر دو shell.** (نکته: mutantها پس از FAIL تگِ غلط را باقی می‌گذارند؛ gate آن را برنمی‌گرداند. guardِ C4 retag و deploy را متوقف می‌کند.)

### C3 — `apply-release.ps1` دیگر PASSED ِ بی‌قید نمی‌گوید

سناریوی بازبینیِ دوم، عیناً: سندِ صفر-migration (`RELEASE-r3zero.md`)، Postgresِ یک‌بارمصرف `review3-pg` / `review3_apply_probe`.

```
$ powershell.exe -File clone\release\apply-release.ps1 -ReleaseMd …RELEASE-r3zero.md -TargetDb review3_apply_probe -Container review3-pg -DbUser postgres
pg_is_in_recovery() = f (Expect: f)
document declares: 0 mig_apply block(s), 0 ledger-row-only block(s) before Phase 5
--- reached Phase 5 (image transfer). Stopping cleanly -- deploy is a human step. ---
executed: 0 of 0 mig_apply block(s), 0 of 0 ledger-row-only block(s)
=== VERDICT: MIGRATION PHASE PASSED -- RELEASE NOT VERIFIED: BLOCK 0 AND ALL GATES NOT RUN ===
    … It did NOT run Block 0 (D1a checkout = build sha, D1b migration
    files on disk) and it did NOT run ANY GATE block. NOT EXECUTED, NOT MEASURED: D1a D1b D2 D3 D6 D9 D10
MIGRATION PHASE PASSED -- release NOT verified: Block 0 and every GATE block were NOT run by this script. Log: …
APPLY_EXIT=0 (5.1)  APPLY_EXIT=0 (7)       خطوطِ شاملِ PASSED بدونِ «MIGRATION PHASE PASSED»: 0
```

همان سند، Block 0 به دستِ «انسان»: `GATE D1a FAIL HEAD 1d2253d5 is not the build sha deadbeef` (`1`/`1`)؛ D6 همان سند: `FAIL D6(0): image afrakala-app:deadbeef does not exist on this machine. Nothing was measured.` · `GATE D6 FAIL …` (`1`/`1`).
**C3: verified.** حکم نامِ هر gateِ اجرانشده را از خودِ سند می‌آورد. (جهتِ دیگر — migrationِ شکست‌خورده → STOP — را پیوست `:775` گزارش کرده؛ من دوباره نسنجیدم چون کدِ آن مسیر در این commit تغییر نکرد، `cited` diffِ `a9b58b71` فقط خطوطِ پس از بررسیِ شمارش را عوض کرده.)

### C4 — `throw` داخلِ `& { }` و guard در paste

روش: پنجرهٔ تازه برای هر اجرا (`conhost.exe <shell>` یا `wt.exe -w new <shell>`، `-NoExit`، `Start-Transcript`)؛ متنِ paste = ناحیهٔ **خامِ سند با تورفتگیِ ۴ فاصله** (نه de-indent)؛ تحویل = `AttachConsole(pid)` + `WriteConsoleInputW` در input bufferِ **همان** shell؛ خواندن = `ReadConsoleOutputCharacterW` روی screen buffer به‌علاوهٔ transcript. بدنهٔ ناحیهٔ deploy با `Write-Host "DEPLOY BODY RAN (review3 stub …)"` جایگزین شد و guardِ آن عیناً ماند (`diff` چاپ شد؛ فایلِ نسخهٔ واقعی پیش از هر paste حذف شد). بستنِ پنجره با تایپِ `exit` — هیچ فرایندِ ترمینالی کشته نشد.
کلاسِ پنجره `measured`: conhost = `ConsoleWindowClass`، WT = `PseudoConsoleWindow`.

| میزبان | shell | paste | shell زنده پس از هر دو | دلیل روی صفحه | ناحیهٔ deploy |
|---|---|---|---|---|---|
| conhost | 5.1 | D1a-FAIL، سپس deploy | True | `GATE D1a FAIL HEAD 1d2253d5 is not the build sha deadbeef` + `STOPPED at gate D1a: … this shell is still open` | `NOT RUN: gate(s) FAILED earlier in this shell and have not printed PASS since: GATE D1a FAIL HEAD 1d2253d5 …` — بدنه اجرا نشد |
| conhost | 7.6.4 | همان | True | True | NOT RUN |
| Windows Terminal | 5.1 | همان | True | خطِ اولِ `GATE D1a FAIL` از بافرِ ConPTY بیرون scroll شد؛ دلیل در خطِ `STOPPED at gate D1a: HEAD 1d2253d5 …` و در `NOT RUN: … GATE D1a FAIL HEAD …` روی صفحه ماند؛ transcript `:43` خطِ کامل را دارد | NOT RUN |
| Windows Terminal | 7.6.4 | همان | True | True | NOT RUN |
| conhost | 5.1 | **یک paste** شاملِ D1a-FAIL + deploy | True | True | NOT RUN |
| conhost | 5.1 | ناحیهٔ کاملِ D6+D9 (۱۸۱ خط) روی `evasion`، سپس deploy | True | `GATE D6 FAIL host literal(s) in the client bundle: //kong:8000, 192.168.170.8:9000, kong:8000, HTTP://192.168.170.8:9000` | NOT RUN |
| Windows Terminal | 5.1 | D1a-PASS، سپس deploy (جهتِ دیگر) | True | — | `GATE D1a PASS` › `DEPLOY BODY RAN` |

`-File`: هر gateِ FAIL در بخش‌های ۳ و ۵ exit `1` داد و هر PASS exit `0` — یعنی `throw` کدِ خروجِ اسکریپت را حفظ می‌کند.
**C4: verified زیرِ 5.1 و 7، در conhost و WT.** قیدِ روش: Ctrl+V/کلیکِ راستِ واقعی نیست — تلاشِ من برای pasteِ واقعیِ conhost (`WM_COMMAND 0xFFF1` پس از گذاشتنِ متن در clipboard) هیچ متنی تحویل نداد، پس کنار گذاشته شد و clipboardِ کاربر برگردانده شد (`CLIP_RESTORED`).

### C5 — fence در D8

| ورودی (از `rehearsal-e4b.md`، LF) | باید | خروجی | exit 5.1 / 7 | سند نوشته شد؟ |
|---|---|---|---|---|
| e4b + `` ``` `` / `~~~` / `` ``` `` / `## VERDICT: FAIL …` (fenceِ بسته‌شده با کاراکترِ دیگر) | REJECT | `FATAL: the FINAL verdict … is not PASS.` · `final : :2051  ## VERDICT: FAIL (replay stopped early)` | `1` / `1` | خیر |
| e4b + `` ``` `` + متن + `## VERDICT: FAIL …` (باز تا پایانِ فایل) | REJECT | `FATAL: rehearsal report ends inside a code fence opened at :2048 ('```') that is never closed.` | `1` / `1` | خیر |
| e4b + `~~~` / `` ``` `` / `## VERDICT: FAIL …` (من) | REJECT | `… opened at :2048 ('~~~') that is never closed.` | `1` / `1` | خیر |
| e4b + ` ```` ` / `` ``` `` / `## VERDICT: FAIL …` (من) | REJECT | `… opened at :2048 ('````') …` | `1` / `1` | خیر |
| **`rehearsal-e4b.md` بدونِ تغییر** | ACCEPT | `Written: …` | `0` / `0` | بله |

**C5: verified.** (دو ورودیِ ساختگی که FAIL را پنهان می‌کنند در بخشِ ۶.)

### C6 — D6: scheme بی‌حساسیت به حروف، host:port ِ بی‌scheme، مقایسهٔ دقیق

| image / ورودی | باید | خروجی | exit 5.1 / 7 |
|---|---|---|---|
| `evasion` (بازسازیِ مستقلِ من از کاشت‌های REVIEW-2) | FAIL | `//kong:8000 (scheme-less host:port, explicit port, bare name with no dot)` · `192.168.170.8:9000 (… IP literal)` · `kong:8000 (…)` · `HTTP://192.168.170.8:9000 (explicit port, IP literal)` · `GATE D6 FAIL host literal(s) in the client bundle: //kong:8000, 192.168.170.8:9000, kong:8000, HTTP://192.168.170.8:9000` | `1` / `1` |
| `served99` با `$target='10.99.99.9'` (.9 داخلِ .99) | FAIL | `GATE D6 FAIL served supabaseUrl 'http://10.99.99.99:8000' is not exactly http://10.99.99.9:8000` | `1` / `1` |
| `served99` با `$target='10.99.99.99'` (کنترل) | PASS | `GATE D6 PASS` · `GATE D9 PASS` | `0` / `0` |
| `persian` (`"آدرس 10.1.1.1:9000،"` و `"آدرس۲10.2.2.2:9000"`) | FAIL | `10.1.1.1:9000 (… IP literal)` · `10.2.2.2:9000 (… IP literal)` — چسبیدنِ بایت‌های UTF-8 به IP آن را پنهان نکرد | `1` / `1` |
| **bundleِ مشروعِ فعلی** (`clean`، شاملِ help textِ ai-providers) | PASS | `OK D6(i): … (475 js files read, 176 URL literal(s) and 0 scheme-less host:port literal(s) classified)` · `OK D6(ii)` · `OK D6(iii): served config = "supabaseUrl":"http://10.99.99.99:8000"` · `GATE D6 PASS` · `GATE D9 PASS` | `0` / `0` |

**C6: آنچه ادعا شده verified است** — ولی بخشِ ۶، حملهٔ ۱: همین probe یک هم‌خانوادهٔ نزدیکِ evasion را رد نمی‌کند.

---

## ۴. regression — یک مورد در هر جهت (`measured`، هر دو shell یکسان)

| probe | ورودی | خروجی | exit 5.1 / 7 |
|---|---|---|---|
| D1a | sha اشتباه (`deadbeef`) | `GATE D1a FAIL HEAD 1d2253d5 is not the build sha deadbeef` | `1` / `1` |
| D1a | cloneِ تمیز، sha درست | `OK D1a: HEAD = 1d2253d5, tree clean` · `GATE D1a PASS` | `0` / `0` |
| D1b | `…533_pg_cron_http_scheduler.sql` حذف شد | `MISSING 20260913101000_533_pg_cron_http_scheduler.sql` · `GATE D1b FAIL 1 migration file(s) missing: 20260913101000_533_pg_cron_http_scheduler.sql` | `1` / `1` |
| D1b | هر ۱۵ حاضر | `OK D1b: all 15 migration files present` · `GATE D1b PASS` | `0` / `0` |
| D3 | دو تگ روی `alpine` | `GATE D3 FAIL :lan and :lan-rollback are the same image (28bd5fe8b56d)` | `1` / `1` |
| D3 | `lan`=alpine، `rollback`=`postgres:15-alpine` | `OK D3: lan=28bd5fe8b56d rollback=cd17e2ac9824` · `GATE D3 PASS` | `0` / `0` |
| D8 | `rehearsal-e4b.md` | `Written: …` | `0` / `0` |
| D6 | bundleِ فعلی | `GATE D6 PASS` · `GATE D9 PASS` | `0` / `0` |

یک اجرای D3 نامعتبر بود و تکرار شد: sed ِ اولِ من `afrakala-app:lan ` (با فاصله) را جایگزین نکرده بود و gate `:lan` ِ واقعی را **فقط خواند**؛ هیچ تگی عوض نشد. همچنین در clone یک بار `release/runs` (tracked) را اشتباهی پاک کردم؛ `git checkout -- release/runs` برگرداند و D1a ِ تمیز پس از آن اجرا شد (`git status --porcelain` = خالی). **regression: pass.**

---

## ۵. `BROKEN_PROBES=1` · `OVER_REJECTIONS=0`

### BROKEN #1 — D6(i): IPv4 بدونِ پورت، الحاق در زمانِ اجرا

`measured` — imageِ `attack` (کاشت در source، از Vite گذشت): bundle شاملِ `const $w="192.168.170.8"` و `e.__r3u="http://"+$w+":9000"` است — یعنی کدِ اجرایی `http://192.168.170.8:9000` می‌سازد.

```
OK D6(i): no baked host literal in the client bundle (475 js files read, 177 URL literal(s) and 0 scheme-less host:port literal(s) classified)
OK D6(ii): runtime config mechanism present
OK D6(iii): served config = "supabaseUrl":"http://10.99.99.99:8000"
GATE D6 PASS
OK D9: served host '10.99.99.99' is browser-reachable
GATE D9 PASS
EXIT 5.1 = 0     EXIT 7 = 0
```

`cited` چرا: `release/emit-blocks.ps1:879` — grepِ scheme-less فقط `…:[0-9]{1,5}` می‌خواند (پورت اجباری)؛ `:898` IP را فقط در همان نامزدها طبقه‌بندی می‌کند. `quoted` ادعایی که نقض می‌شود، `:841` «does it contain ANY host»، و `:847-848` «an IP literal, an explicit port, … ALWAYS fails, whatever list a host is on».
چرا BROKEN و نه «محدودیتِ مستند»: پیوست `:975` می‌گوید «IPv4:port هر جا دیده می‌شود» و از IPv4 ِ **بی‌پورت** چیزی نمی‌گوید؛ این شکل با evasionِ REVIEW-2 (`Bz="host:port"` + `"http://"+Bz`) فقط در یک توکن فرق دارد؛ نتیجهٔ کارکردیِ آن مرورگرِ کارمند → Kongِ تست است (خودِ حادثه)؛ و **هیچ gateِ دیگری آن را نمی‌بیند** — D10 مقدارِ `window.__APP_RUNTIME_CONFIG__` را می‌خواند که درست است، نه آدرسی را که کدِ پخته‌شده به آن fetch می‌کند (`assertion` از ساختار؛ fetchِ مرورگر را نسنجیدم).
`measured` روی `clean`: literalهای IPv4ِ بی‌پورت در bundleِ مشروع فقط `"0.0.0.0"` ×1 و `"127.0.0.1"` ×3‌اند (به‌علاوهٔ رشته‌های نسخه‌مانندِ غیرِ quote مثل `-1.5.75.75`) — پس default-deny روی IPv4ِ quote‌شده بدونِ over-rejection شدنی است.

### OVER_REJECTIONS=0

هیچ ورودیِ مشروعی رد نشد: bundleِ فعلی PASS، e4b ACCEPT، D2 روی imageِ production‌شکل PASS، D10 روی آدرسِ دقیق PASS. تنها FAILهای «روی چیزِ واقعی» هر دو درست‌اند: D2 روی stackِ زندهٔ این باکس (بخشِ ۷، شکاف ۵) و D10 با اسلشِ پایانی (بخشِ ۳، C1 ردیفِ ۴).

---

## ۶. فاز ۳ — حمله به آنچه اصلاح‌ها آوردند

| # | حمله | حکم | شاهد |
|---|---|---|---|
| 1 | **D6(i): IPv4 بدونِ پورت + `"http://"+h+":9000"`** | **confirmed — BLOCKER** | بخشِ ۵. |
| 2 | **D6(i): `name:port` پس از جایگزینیِ template** — `` `${t}kong:8000` `` با `t = location.protocol+"//"` (شکافِ ۲ ِ پیاده‌ساز) | confirmed — NOT BLOCKER | همان imageِ `attack`، `0 scheme-less host:port literal(s)`، `GATE D6 PASS`، `0`/`0`. `cited` `:904`: نامِ بی‌نقطه فقط بینِ دو quote شمرده می‌شود و `}` quote نیست. چرا بازدارنده نیست (`assertion`): نامِ بی‌نقطه (`kong`) از مرورگر resolve نمی‌شود، پس نتیجه‌اش خرابیِ **پرصدا** است، نه دیتابیسِ اشتباهِ خاموش؛ و گرفتنِ هر `word:NN` در متن over-reject می‌کند (پیوست `:847-851` این را اندازه گرفته). |
| 3 | **D10: صفحه‌ای که پس از configِ درست آن را بازنویسی می‌کند** — `window.__APP_RUNTIME_CONFIG__.supabaseUrl="http://192.168.170.8:9000"` در `<script>` دوم؛ و `window["__APP_RUNTIME_CONFIG__"]={…192.168.170.8…}` | confirmed — NOT BLOCKER | هر دو: `OK D10: … serves supabaseUrl 'http://10.99.99.99:8000'` · `GATE D10 PASS`، `0`/`0`. `cited` `:1159`: شمارشِ انتساب فقط شکلِ `window.__APP_RUNTIME_CONFIG__\s*=` را می‌بیند. چرا بازدارنده نیست: D10 برای خطای **env** است و HTML را `RootShell` با یک انتسابِ واحد می‌سازد (`src/routes/__root.tsx`)؛ اسکریپتِ دوم یعنی تغییرِ کد، نه تنظیمات. |
| 4 | **D10: کلید با حروفِ متفاوت** — `{"SUPABASEURL":"http://10.99.99.99:8000"}` | confirmed — NOT BLOCKER | `GATE D10 PASS`، `0`/`0`: دسترسیِ property در PowerShell به حروف حساس نیست (`:1168`). مرورگر `supabaseUrl` را `undefined` می‌بیند و `client.ts` خطای صریح می‌دهد — پرصدا. |
| 5 | **D10: کلیدِ تکراری** — `{"supabaseUrl":"<درست>","supabaseUrl":"<حادثه>"}` | **refuted** | 5.1 و 7 هر دو مقدارِ **آخر** را خواندند (مثلِ مرورگر): `GATE D10 FAIL live supabaseUrl is 'http://192.168.170.8:9000' …`، `1`/`1`. |
| 6 | **D8: fenceِ غیرِ CommonMark که FAIL را پنهان می‌کند** — (الف) خطِ `\t```` + `## VERDICT: FAIL …` + `\t````؛ (ب) `` ```a`b `` + `## VERDICT: FAIL …` + `` ``` `` | confirmed — NOT BLOCKER | هر دو: `Written: …`، `0`/`0`. `cited` `:96,:101`: `^\s{0,3}` یک tab را یک فاصله می‌شمارد (CommonMark: ستونِ ۴ = indented code، نه fence) و info string با backtick را fence می‌گیرد. پس ادعای «CommonMark» در `:85-86` دقیق نیست. چرا بازدارنده نیست: هر دو ورودیِ **ساختگی** دو خطِ ناهنجار **بیرون** از fence می‌خواهند؛ خروجیِ engine بیرون از fence فقط سرتیتر و متنِ خودش است. و `assertion` که اندازه گرفتم: یک خطِ fence‌مانندِ **تنها** داخلِ خروجیِ stage وضعیت را وارونه می‌کند و وارونگی تا پایانِ فایل می‌ماند، پس قاعدهٔ «fenceِ باز در EOF» آن را FATAL می‌کند (همان ردیف‌های C5). |
| 7 | **guardِ C4 در shellِ تازه / gateِ اجرانشده** (شکافِ ۱ ِ پیاده‌ساز) | confirmed — NOT BLOCKER | conhost 5.1، پنجرهٔ تازه، فقط ناحیهٔ deploy paste شد → `DEPLOY BODY RAN (review3 stub …)`. guard فقط «شکستِ ثبت‌شده» را می‌بیند، نه «PASSِ غایب»؛ gateی که skip یا با Ctrl+C قطع شود هیچ ثبت نمی‌کند. بازدارنده نیست چون پیش از اصلاح هم shellِ تازه بی‌حافظه بود (پسرفت نیست) و سرِ سند صریحاً می‌گوید «in a new window, re-run the gates first»؛ ولی بهبودِ روشن این است که guard فهرستِ **PASS**ِ همهٔ gateهای لازم را طلب کند. |
| 8 | **D3 بدونِ تگِ rollback** (یافتهٔ REVIEW-2، ترکیب با #7) | confirmed (پیش‌موجود) — NOT BLOCKER | `lan`=alpine، rollback غایب → `OK D3: lan=28bd5fe8b56d rollback=` · `GATE D3 PASS`، `0`/`0`. با #7: D2 در پنجرهٔ A شکست بخورد، پنجرهٔ B → retag + D3 PASS + deploy بی نقطهٔ بازگشت. |

---

## ۷. پنج شکافِ «تأیید نشده» ِ پیاده‌ساز — حکمِ هرکدام

| # | شکاف (پیوست `:971-978`) | آنچه آزمودم | حکم |
|---|---|---|---|
| 1 | رکوردِ شکست فقط در همان shell | حملهٔ ۷: shellِ تازه deploy را اجرا کرد | **NOT BLOCKER** (پسرفت نیست، مستند است؛ بهبود: allowlistِ PASS) |
| 2 | `name:port` در template literal یا رشتهٔ بلندتر | حملهٔ ۲: `` `${t}kong:8000` `` → PASS. **همسایهٔ خطرناکش، IPv4 ِ بی‌پورت، BROKEN #1 است** | شکافِ نام‌برده: **NOT BLOCKER** · IPv4 ِ بی‌پورت: **BLOCKER** |
| 3 | imageِ evasion بازسازی شده، نه اصل | من هم مستقل از توصیفِ REVIEW-2 ساختم، از Vite گذشت، کاشت‌ها عیناً در bundle → `GATE D6 FAIL` با هر چهار literal | **NOT BLOCKER** |
| 4 | pwsh 7.0–7.2 آزموده نشد | untestable (فقط 7.6.4 نصب است؛ دانلود نکردم) | **NOT BLOCKER** — production 5.1 است و هر اجرای من زیرِ 5.1 رفت |
| 5 | D2 روی stackِ زنده دست‌نخورده | **این باکس** (`measured`، تگ فقط به نامِ `review3-live:lan-rollback`): `Error response from daemon: No such image: sha256:0c3106602cc9…` · `GATE D2 FAIL docker tag of the running image failed, no rollback point taken`، `1`/`1`؛ هیچ تگی ساخته نشد — fail-closed درست. **شکلِ production** (`measured`): imageِ manifest-list در containerd store → `.Image` = `.Id` و `GATE D2 PASS`. روی `.10`: untestable | **NOT BLOCKER** |

---

## ۸. آمادگیِ production — D10 و env ِ `.10`

**حکم: FAIL ِ D10 روی production ِ امروز رفتارِ درست است، نه gateی که release ِ خوب را مسدود کند.**
`measured` روی شکلِ دقیقِ env ِ production (`SUPABASE_URL=http://kong:8000`، `APP_SUPABASE_PUBLIC_URL` خالی): imageِ همین شاخه به مرورگرها `"supabaseUrl":"http://kong:8000"` می‌دهد (C1 ردیفِ ۱). `cited` `src/lib/runtime-config.ts:87-91`: ترتیب `APP_SUPABASE_PUBLIC_URL || VITE_SUPABASE_URL || SUPABASE_URL`؛ `cited` `deploy/lan/docker-compose.yml:45-67`: `VITE_SUPABASE_URL` در `environment:` سرویسِ `web` پاس داده نمی‌شود و `:67` `APP_SUPABASE_PUBLIC_URL: ${APP_SUPABASE_PUBLIC_URL:-}`. پس release با env ِ امروز **واقعاً** برنامه را برای هر مرورگر خراب می‌کند؛ gateی که آن را PASS کند همان probe ِ دروغ است.

**آنچه باید پیش از هر release روی `.10` عوض شود:**
1. در `C:\afrakala\deploy\lan\.env.lan` یک خط: `APP_SUPABASE_PUBLIC_URL=http://192.168.170.10:8000` — یا دقیقاً هر originی که مرورگرِ کارمند Kong ِ production را با آن می‌بیند. این مقدار **استنتاج** است (پیش‌فرضِ emitter و طولِ ۲۶ ِ `VITE_SUPABASE_URL` در `env-parity-20260913.md`)؛ مالک باید آن را روی `.10` تأیید کند.
2. سندِ release با `-ExpectedSupabaseUrl` **برابرِ همان رشته** emit شود (بدونِ اسلشِ پایانی)، و `-LiveSiteUrl` آدرسی که staff باز می‌کنند.
3. **یک خطر که D10 به‌تنها نمی‌بندد:** D10 **پس از** deploy می‌دود. بدونِ بندِ ۱، اجرای runbook روی `.10` یعنی deploy → مرورگرها به `kong:8000` → D10 FAIL → بلوکِ rollback؛ یعنی یک پنجرهٔ قطعی. runbook امروز هیچ بررسیِ **پیش از deploy** برای این کلید ندارد (`cited` `release/build.ps1:67-80`: D7 فقط وجودِ فایلِ `.env.lan` را می‌سنجد). پیشنهاد: یک gateِ پیش از deploy روی خودِ `.env.lan` هدف (`APP_SUPABASE_PUBLIC_URL` حاضر و برابرِ `-ExpectedSupabaseUrl`).
4. D2 روی `.10`: مأموریت می‌گوید `8b04c479e022` در store هست و قابلِ tag؛ آزمونِ شکلِ production ِ من (بخشِ ۳، C2) نشان می‌دهد مقایسهٔ `.Image`↔`.Id` در containerd store برای imageِ buildx معتبر است. روی `.10` اجرا نشد.

---

## ۹. حداقلِ اصلاح برای بازدارنده

| # | بازدارنده | حداقلِ اصلاح | اثباتِ لازم |
|---|---|---|---|
| 1 | D6(i) IPv4 ِ **بی‌پورت** را نمی‌خواند | به scanِ (i) یک grep برای literalِ IPv4 که کلِ یک رشته است اضافه شود (quote/backtick در دو طرف، یا quote و `:`/`/` پس از آن)، زیرِ همان default-deny، با سهمیهٔ دقیقِ اندازه‌گرفته: `"0.0.0.0"` ×1 و `"127.0.0.1"` ×3 (منبعِ هرکدام پیش از افزودن مشخص شود)؛ و متنِ `:841,:847-848` با دامنهٔ واقعی (نامِ بی‌نقطهٔ بی‌پورت خوانده نمی‌شود) هم‌تراز شود | imageی با `const h="192.168.170.8"` + `"http://"+h+":9000"` → `GATE D6 FAIL` (5.1 و 7)؛ bundleِ فعلی → `GATE D6 PASS`؛ evasion و incident همچنان FAIL |

---

## ۱۰. typecheck (یک بار)

```
$ npm run typecheck > typecheck.log 2>&1; echo "NPM_EXIT=$?"
NPM_EXIT=2
error TS count : 70
distinct files : 6   (products.index 18 · sales-reminders 15 · accounting/functions 13 · invoices/functions 13 · audit/index 6 · admin.automation 5)
errors in the branch's 5 changed .ts/.tsx files : 0 each
```

---

## ۱۱. اختلاف با پیوستِ پیاده‌ساز

1. **`BROKEN_PROBES = 0` (`:920`).** مالِ من `1`: IPv4 ِ بی‌پورت (بخشِ ۵). پیوست `:975` «IPv4:port هر جا» را درست می‌گوید ولی IPv4 ِ بی‌پورت را نه آزموده نه نام برده.
2. **«CommonMark» در C5 (`:821`، و `release/emit-blocks.ps1:85-86`).** fenceِ tab‌دار و info string با backtick برخلافِ CommonMark fence شمرده می‌شوند و یک FAIL را پنهان می‌کنند (حملهٔ ۶؛ ساختگی، بازدارنده نیست).
3. **نام‌گذاریِ M1/M2** در پیوست (`:752-753`) و REVIEW-2 برعکسِ متنِ مأموریت است؛ رفتار یکی است و هر دو FAIL.
4. **`:981` «D2 روی stackِ زنده … همچنان بازدارندهٔ اجرای release»**: روی این باکس درست است (بازتولید شد)؛ برای `.10` مأموریت می‌گوید image قابلِ tag است و آزمونِ شکلِ production ِ من سازگار است — آن جمله برای production دیگر پشتوانه ندارد، ولی روی `.10` اندازه‌گیری نشده.
5. **D10 روی صفحهٔ بازنویسی‌کننده / کلیدِ هم‌حروف PASS می‌دهد** (حمله‌های ۳-۴) — پیوست فقط «دو انتسابِ هم‌شکل» را آزموده.
6. **`:966` پیش‌شرطِ runbook «در این دور اضافه نشد»** — موافقم، و بخشِ ۸ نشان می‌دهد نبودنش روی production یعنی قطعی پیش از rollback، نه فقط FAIL ِ gate.
7. **`:942` حذفِ `afrakala-app:rc-*`**: REVIEW-2 (`:311`) حذفشان را «تصمیمِ مالک» خوانده بود؛ پیوست بدونِ ذکرِ تأییدِ مالک حذف کرده. اثرِ عملی ندیدم (هیچ کانتینری از آن‌ها استفاده نمی‌کرد).

آنچه پیوست می‌گوید و تأیید کردم: هر شش اصلاح در هر دو جهت (بخشِ ۳)؛ ۵ ردیفِ C5؛ خروجیِ C3؛ رفتارِ guard در conhost و WT، 5.1 و 7؛ `70/6`، exit `2`؛ `:lan` و `StartedAt` بی‌تغییر.

---

## ۱۲. آنچه نتوانستم تأیید کنم

1. **هیچ‌چیز روی `192.168.170.10`** — نه D10، نه D2، نه مقدارِ واقعیِ `SUPABASE_URL`/`APP_SUPABASE_PUBLIC_URL` (از متنِ مأموریت گرفته شد)، نه اینکه Kong ِ production از مرورگر روی `:8000` دیده می‌شود.
2. **paste با Ctrl+V یا کلیکِ راستِ واقعی** — key eventها در input buffer نوشته شد. تلاش برای pasteِ واقعیِ conhost با `WM_COMMAND 0xFFF1` متنی تحویل نداد؛ مسیرِ bracketed paste ِ WT اندازه‌گیری نشد.
3. **build ِ 5.1 ِ production** (`5.1.26100.9444`) — اینجا `5.1.26100.9278`.
4. **pwsh 7.0–7.2.**
5. **رفتارِ مرورگرِ واقعی** برای imageِ `attack` — اینکه fetch ِ کاشته‌شده واقعاً به `192.168.170.8:9000` می‌رود استنتاج از bundle است؛ مرورگر باز نکردم.
6. **Ctrl+C وسطِ یک gate** — مستقیم نسنجیدم؛ «gateِ اجرانشده» را با shellِ تازه شبیه‌سازی کردم (حملهٔ ۷).
7. **جهتِ STOP ِ C3 روی سندی با migration** — دوباره اجرا نکردم (کدِ آن مسیر در `a9b58b71` تغییر نکرد).

---

## پیوست — پاک‌سازی و سلامتِ stack (`measured`، پایانِ کار)

```
docker rm -f review3-live-{exact,kong,incident,super,slash} review3-fake-{twocfg,override,dupkey,casekey,bracket,empty,nocfg} review3-d2-web review3-pg   -> exit 0
docker rmi review3-d2:lan review3-afrakala:{served99,persian,evasion,attack,clean}   -> exit 0
review3 containers left: 0 · review3 images/tags left: 0 · review3-d3 / review3-live tags: 0
afrakala-app:lan           sha256:296eb4b4899ff75d…   (بدون تغییر)
afrakala-lan-web           Image=sha256:0c3106602cc9…  StartedAt=2026-09-12T17:46:06.169711504Z  Restarts=0  Health=healthy
afrakala-app:lan-rollback  وجود ندارد
پنجره‌های آزمایشیِ REVIEW3-*: همه با تایپِ exit بسته شدند (یکی که گیر کرد با Stop-Process روی همان pid ِ powershell)؛ باقی: 0
clipboard ِ کاربر: ذخیره و برگردانده شد
git status --porcelain (درختِ اصلی): فقط سه فایلِ untrackedِ پیشین + همین سند
```

هیچ خواندن یا نوشتنی روی `192.168.170.10`؛ هیچ deploy، restart یا `compose build` روی stackِ در حال اجرا؛ هیچ `docker commit`/`pause`؛ هیچ کانتینری با `SUPABASE_URL` به `.10`؛ `:lan` جابه‌جا نشد؛ هیچ `npm run build` بیرون از Docker؛ هیچ اصلاحی در کد؛ هیچ push به main/staging؛ هیچ PR (حکم REJECT).
