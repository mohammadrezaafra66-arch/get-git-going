# بازبینیِ مستقلِ شاخهٔ `fix/release-line-runtime-config`

شاخه: `fix/release-line-runtime-config` @ `ae47de4a` · پایه: `origin/main` @ `9bc8d554`
باکس: `D:\AfraKalaTest\app` (تست، `192.168.170.8`) · تاریخ: ۲۰۲۶-۰۹-۱۳
بازبین: مستقل از پیاده‌ساز. معیارها از **خودِ خواسته** استخراج شد، نه از گزارش تکمیل.
گزارش تکمیل **آخر** خوانده شد.

سطوح شاهد: `measured` (دستور + خروجی + exit code جدا از هر pipe) · `quoted` · `cited` ·
`compared` · `assertion` (فقط استدلال، برچسب‌خورده).

---

## ۱. حکم

**REJECT.** — `BROKEN_PROBES=2` (D6(i) و D8 هر دو در شرطی که باید قرمز شوند سبز شدند)، و
`D2` — یکی از نُه نقصِ فهرست — واقعاً بسته نشده است: دستورِ آن، image در حال اجرا را تگ
نمی‌کند، و روی همین باکس همین حالا image دیگری را تگ می‌کند.

قاعدهٔ فاز ۳ صریح است: «اگر probeای وقتی باید fail شود pass کرد، آن یک BROKEN PROBE است و
شاخه بازبینی را پاس نمی‌کند.» پس PR باز نشد.

این حکم منکرِ کارِ انجام‌شده نیست. هستهٔ خواسته — «غیرممکن‌بودنِ ساختاریِ اینکه imageِ
ساخته‌شده روی یک میزبان، پیکربندیِ میزبان دیگری را حمل کند» — با اندازه‌گیریِ خودم **اثبات
شد**: یک image، سه رفتار، از جمله یک میزبانِ ساختگی که هرگز در هیچ build نبوده (بخش ۳).
آنچه نبسته، نیمهٔ دومِ خواسته است: «خط release باید این را روی **آرتیفکت** اثبات کند» —
probe وجود دارد و روی image واقعیِ حادثه قرمز می‌شود، ولی راهِ فرار دارد و **هیچ اتوماسیونی
اجرایش نمی‌کند**.

---

## ۲. جدولِ نقص‌به‌نقص

هر ردیف فقط شاهدی را می‌آورد که **خودم** تولید کردم.

| id | حکم | شاهدِ من |
|---|---|---|
| **D1** | **verified** | `measured`: در runbookی که خودم با emitter فعلی تولید کردم، `### Block 0` در خط ۱۲ است، دو `Expect:` خودش در ۳۵ و ۵۰، و **اولین `Expect:` دیگرِ سند** در ۶۰ — پس پیش از هر مقایسهٔ `Expect:` می‌نشیند. ابطال: sha نامنطبق → `FAIL D1a: HEAD is ae47de4a but this release was built from deadbeef`, `exit 1`؛ درخت کثیف → `FAIL D1a: working tree is not clean` + فهرست, `exit 1`؛ کنترلِ مثبت روی درختِ تمیز با sha درست → `OK D1a`, `exit 0`. فایل migration غایب → `FAIL D1b: 1 of 15 migration file(s) missing:` + `MISSING 20260913101000_533_pg_cron_http_scheduler.sql`, `exit 1`؛ کنترلِ مثبت → `OK D1b: all 15 ...`, `exit 0`. **نام می‌برد**، همان‌طور که خواسته شده بود. |
| **D2** | **NOT VERIFIED — ادعا با اندازه‌گیری نمی‌خواند** | `quoted` (`emit-blocks.ps1`، بلوک image transfer): `docker tag afrakala-app:lan afrakala-app:lan-rollback` و `Expect: afrakala-app:lan-rollback resolves to the image the running container is on`. `measured` روی همین باکس: `docker inspect afrakala-lan-web --format '{{.Image}}'` → `sha256:0c3106602cc9...`؛ `docker images afrakala-app:lan --format '{{.ID}}'` → `296eb4b4899f`؛ جست‌وجوی `0c3106602cc9` در همهٔ تگ‌ها → **هیچ تگی به image در حال اجرا اشاره نمی‌کند**. یعنی این دستور، همین الان، `296eb4b4899f` را فریز می‌کند نه image در حال اجرا، و خطِ `Expect:` آن **کاذب** است. علاوه بر این هیچ assertion و هیچ `exit 1` در این بلوک نیست: فقط دو چاپ با **دو قالبِ متفاوت** (`sha256:` کاملِ ۶۴ کاراکتری در برابر id کوتاهِ ۱۲ کاراکتری)، که حتی چشمِ یک اپراتورِ دقیق هم نمی‌تواند مکانیکی مقایسه کند. نیمهٔ دومِ D2 **verified** است: خطِ بعدیِ `docker tag :lan :lan-rollback` واقعاً حذف شده — `quoted`: «the `docker tag afrakala-app:lan afrakala-app:lan-rollback` line that used to sit here is DELETED». |
| **D3** | **verified** | `compared` در فضای‌نامِ خصوصیِ `afrakala-review` (تگ `:lan` هرگز جابه‌جا نشد): دو نام روی یک image → `FAIL D3: :lan and :lan-rollback are the same image (92c554df96cc).`, `exit 1`؛ rollback به image دیگر (`427aacd91600`) → `OK D3: lan=92c554df96cc rollback=427aacd91600`, `exit 0`. |
| **D4** | **verified (cited/quoted، نه end-to-end)** | `cited/quoted` `release/build.ps1:122` → `$saveTag = "afrakala-app:$shortSha"`، مصرف‌شده در `:123` و `:137` (`docker save '$saveTag' \| gzip`). تگِ `afrakala-app:lan` دیگر ذخیره نمی‌شود، پس آرشیو خودتوصیف است و خطِ `docker tag afrakala-app:<sha> afrakala-app:lan` در runbook روی تگی اجرا می‌شود که واقعاً داخل آرشیو هست. **end-to-end اجرا نشد** — اسکریپت عمداً از شاخهٔ غیر-main رد می‌کند. |
| **D6** | **claim differs — BROKEN PROBE #1** | نیمهٔ مثبت، `compared`: D6(i) روی **خودِ image حادثه** (`afrakala-app:3bc526c4` = `296eb4b4899f`) → `FAIL D6(i): host literal(s) baked into the client bundle: http://192.168.170.8:9000`, `exit 1`؛ روی imageی که **خودم** از این شاخه ساختم (`afrakala-app:review-ae47de4a` = `92c554df96cc`) → `OK D6(i)`, `exit 0`. D6(ii) روی image حادثه → `FAIL`, `exit 1`؛ روی image من → `OK`, `exit 0`. تا اینجا probe واقعاً کار می‌کند و همان رشتهٔ حادثه را **نام می‌برد**. ولی: regexِ بند (i) فقط `https?://IPv4:port` است، پس **هر literalِ میزبان که نامِ میزبان باشد نامرئی است**. `measured`: imageی ساختم که `http://kong:8000` و `https://kwwkppkcihrbeurwudjh.supabase.co` در bundle کلاینتش کاشته شده بود → `OK D6(i)`, `exit 0` **و** `OK D6(ii)`, `exit 0` — کلِ بلوک سبز، با دو literalِ میزبان در bundle. خواسته می‌گوید «no host literal»؛ نامِ میزبان هم literalِ میزبان است. ضعفِ دوم، `measured`: با تگِ ناموجود، `docker run` خطا می‌دهد، `$found` خالی می‌شود و D6(i) `OK` چاپ می‌کند با `exit 0` — و **همین حالتِ پیش‌فرضِ خودِ emitter است** وقتی `-BuildManifest` داده نشود (`$img = 'afrakala-app:<sha: supply -BuildManifest>'`). این یکی را D6(ii) می‌گیرد (`exit 1`)، پس از بلوک فرار نمی‌کند. |
| **D7** | **verified** | `cited/quoted` `release/build.ps1:72-79`: `if (-not (Test-Path $envFile)) { ... "REFUSED: required env file not found: $envFile" ... exit 1 }`، در `release/build.ps1` که نقطهٔ ورودِ release است. دو ضعفِ باقی‌مانده: فقط **وجود** را می‌سنجد نه محتوا — یک `.env.lan` که `APP_SUPABASE_PUBLIC_URL` ندارد از آن رد می‌شود و image غلط تولید می‌کند (بخش ۵، حملهٔ ۳)؛ و بعد از guardهای branch/origin می‌نشیند، پس از شاخهٔ غیر-main هرگز به آن نمی‌رسیم. |
| **D8** | **claim differs — BROKEN PROBE #2** | نیمهٔ مثبت، `measured`: سندی ساختم که آخرین خطِ `VERDICT:`اش FAIL است ولی `## VERDICT: PASS` در خط ۲۰۴۷ دارد → `FATAL: the FINAL verdict in the rehearsal report is not PASS.` + فهرستِ هر سه حکم به ترتیب، `exit 1`، و **هیچ فایلی نوشته نشد** (`Test-Path ... -> False`). `compared`: همان سند با روایتِ **میان‌خطیِ** `VERDICT: PASS` هم رد شد، در حالی که checkِ قدیمی آن را می‌پذیرفت (`(Get-Content -Raw) -match '## VERDICT: PASS'` → `True`). و `rehearsal-e4b.md` همچنان **پذیرفته** می‌شود، `exit 0` — به این دلیل که آخرین خطِ `VERDICT:`اش (`:2047 ## VERDICT: PASS`) واقعاً PASS است، هرچند `:878` FAIL است. تا اینجا حرفِ خواسته رعایت شده. ولی: regex (`^\s*#*\s*VERDICT:`) نمی‌تواند سرتیتر را از **نثر** تشخیص دهد. `measured`: سندی که آخرین سرتیترِ حکمش `## VERDICT: FAIL` است و بعدش یک خطِ نثرِ `VERDICT: PASS is the outcome we were hoping for, but it did not happen.` از ستون ۰ آمده → emit با `exit 0` **پذیرفت** و `RELEASE-20260913-d8c2.md` را نوشت. یک checkی که می‌تواند **بدون درست‌بودن پاس شود** — همان کلاسی که گزارش تکمیل خودش می‌گوید این خط release برای بستنش وجود دارد. |
| **D9** | **verified** | `measured` end-to-end: سه کانتینرِ یک‌بارمصرف از image خودم، سه محیط، و منطقِ D9 **عیناً همان‌طور که emit می‌شود** روی رشتهٔ سرو‌شدهٔ واقعی: `http://kong:8000` → `FAIL D9: served host 'kong' is a bare name with no dot`, `exit 1`؛ `http://localhost:8000` → `FAIL D9: served host 'localhost' is loopback`, `exit 1`؛ `http://10.99.99.99:8000` → `OK D9: served host '10.99.99.99' is browser-reachable`, `exit 0`. ضعفِ باقی‌مانده، `measured`: نامِ مستعارِ **نقطه‌دار** رد نمی‌شود — `http://kong.internal:8000` → `OK D9`, `exit 0`. حرفِ خواسته («not a bare container name») رعایت شده، ولی یک alias نقطه‌دار در شبکهٔ compose همان‌قدر برای مرورگر غیرقابل‌دسترس است. |
| **N1** | **claim differs** | `compared` روی درختِ tracked: `git grep -clE "eyJ...\.eyJ...\..." origin/main` → دو فایل (`docs/verification/convergence/wave-b-browser-pass.txt` و `vite.config.ts`)؛ همان grep روی `HEAD` → **فقط** `docs/verification/convergence/wave-b-browser-pass.txt`. پس کلیدِ `vite.config.ts` (sha256 prefix `6e22`) واقعاً حذف شده. ولی `measured`: یک JWT یکتای دیگر (sha256 prefix `a0bf`، ۱۲ بار) در آن فایلِ tracked باقی است. `git grep` روی `src/ vite.config.ts Dockerfile deploy/ release/` → `exit 1` (هیچ تطابقی). یعنی «صفر JWT در source» برای **کد** درست است، برای **درختِ tracked** نه. این فایل روی `origin/main` هم هست؛ این شاخه آن را نساخته. |
| **N2** | **verified** | `measured` روی image خودم: `public unique_jwt_count=0` و `server unique_jwt_count=0`. هیچ مقداری چاپ نشد. |

---

## ۳. فاز ۲ — جدولِ bundle و سه رفتار، روی imageِ **خودم**

image من: `afrakala-app:review-ae47de4a` = `92c554df96cc`، ساخته‌شده با
`docker build -f Dockerfile -t afrakala-app:review-ae47de4a --build-arg GIT_SHA=ae47de4a ...`
(`BUILD_EXIT=0`). هیچ imageی از پیاده‌ساز استفاده نشد. `docker compose build` هرگز اجرا نشد.

### ۳.۱ شمارشِ literal — کلاینت در برابر SSR (`measured`)

| needle | client | SSR |
|---|---|---|
| `192.168.170.10` | 0 | 0 |
| `192.168.170.8` | **1** | 3 |
| `:8000` | 0 | **1** |
| `:9000` | 0 | 0 |
| `kong:` | 0 | **1** |
| `localhost` | 6 | 27 |

تنها `192.168.170.8` در bundle کلاینت `http://192.168.170.8:11434` است — همان متنِ راهنمای UI
برای Ollama که در probe صریحاً allowlist شده. تأیید شد که **در build حاضر است**، پس allowlist
امروز بارِ واقعی دارد.

آن `kong:` و `:8000` در SSR چیست؟ `measured`: در
`/app/.output/server/_ssr/client-8Stp-cPz.mjs`، و متنِ اطرافش یک **کامنتِ فارسیِ خودِ
`src/lib/runtime-config.ts`** است: «`SUPABASE_URL` روی این استقرار `http://kong:8000` است».
build سمت SSR کامنت‌ها را نگه می‌دارد. bundle کلاینت minify می‌شود پس آنجا صفر است
(`measured`). عملاً بی‌خطر است (SSR سمتِ سرور است و این یک کامنت است)، ولی توجه: فایلی که
قاعدهٔ دائمی‌اش «هیچ مقدارِ وابسته به میزبان وارد bundle نشود» است، خودش یک literalِ میزبان
به bundle SSR می‌برد — و اگر همین جمله در فایلی می‌بود که به کلاینت می‌رود، regexِ IPv4-onlyِ
D6(i) آن را **نمی‌دید**.

### ۳.۲ JWT (`measured`)

هر دو bundle: `unique_jwt_count=0`. هیچ توکنی چاپ نشد.
همچنین `kwwkppkcihrbeurwudjh` (project ref ابری که هنوز در `define` است): client `0`، SSR `0`
— پس آن `define` عملاً به هیچ bundleای نمی‌رسد.
`__APP_RUNTIME_CONFIG__` در bundle کلاینت حاضر است: `/app/.output/public/assets/index-Bvgj7gd2.js`.

### ۳.۳ یک image، سه رفتار (`measured`)

هر سه با `docker run -d --rm`، پورت آزادِ بالا، نه ۳۱۰۰ و نه ۳۰۰۰. هیچ‌کدام `SUPABASE_URL`
به `192.168.170.10` نداشت.

| # | محیطِ زمان اجرا | پورت | `window.__APP_RUNTIME_CONFIG__` سرو‌شده |
|---|---|---|---|
| (a) | `APP_SUPABASE_PUBLIC_URL=http://192.168.170.8:9000`, `VITE_APP_ENV=test` | 31771 | `{"supabaseUrl":"http://192.168.170.8:9000","supabaseAnonKey":"…","appEnv":"test"}` |
| (b) | `APP_SUPABASE_PUBLIC_URL=http://10.99.99.99:8000`, `VITE_APP_ENV=production`, `VITE_TRUSTED_HOSTS=10.99.99.99` | 31772 | `{"supabaseUrl":"http://10.99.99.99:8000","supabaseAnonKey":"…","appEnv":"production","trustedHosts":"10.99.99.99"}` |
| (c) | `SUPABASE_URL=http://kong:8000` فقط | 31773 | `{"supabaseUrl":"http://kong:8000","supabaseAnonKey":"…"}` |

بندِ (b) نکتهٔ اصلی است: `10.99.99.99` یک میزبانِ **ساختگی** است که در هیچ build، هیچ
`.env`، و هیچ جای این repo وجود ندارد، و image بی‌کم‌و‌کاست همان را برگرداند. پس مکانیزم
**واقعاً** زمان‌اجرا است، نه یک build arg تغییرِنام‌یافته. این هستهٔ خواسته را اثبات می‌کند.

بندِ (c) حالتِ D9 است: bundle تمیز، مکانیزم حاضر، و **مقدار** برای مرورگر بی‌فایده.

### ۳.۴ پاک‌سازی (`measured`)

`docker ps -a --filter ancestor=afrakala-app:review-ae47de4a` → خروجیِ خالی.
`docker ps -a --filter ancestor=afrakala-review:planted-nonipv4` → خروجیِ خالی.
تگ‌های خصوصیِ `afrakala-review:*` (از جمله imageِ عمداً آلوده) حذف شدند.
`afrakala-app:review-ae47de4a` به‌عنوان شاهد باقی ماند.

### ۳.۵ stack تست — پیش و پس (`compared`)

| | ابتدا | انتها | ground truth |
|---|---|---|---|
| `afrakala-app:lan` | `296eb4b4899f` | `296eb4b4899f` | `296eb4b4899f` ✔ |
| `afrakala-lan-web` StartedAt | `2026-09-12T17:46:06` | `2026-09-12T17:46:06` | `2026-09-12T17:46:06` ✔ |

هیچ اختلافی. تگِ `:lan` جابه‌جا نشد، هیچ build روی stack اجرا نشد، هیچ `docker commit`،
هیچ `docker pause`، و هیچ ارتباطی با `192.168.170.10`. درختِ گیت در ابتدا و انتها تمیز بود
(`git status --porcelain` خالی).

---

## ۴. فاز ۳ — ابطال‌پذیری

`exit code` در هر مورد **جدا از هر pipe** خوانده شد (اسکریپت در فایل نوشته و با
`powershell -File` اجرا شد، سپس `$LASTEXITCODE`).

| # | probe | ورودیِ باید-fail | نتیجه | exit | حکم |
|---|---|---|---|---|---|
| 3.1 | D1a | build sha = `deadbeef`، HEAD = `ae47de4a` | `FAIL D1a: HEAD is ae47de4a but this release was built from deadbeef` | `1` | سالم |
| — | D1a | درختِ کثیف | `FAIL D1a: working tree is not clean:` + فهرست | `1` | سالم |
| — | D1a | کنترلِ مثبت: sha درست + درختِ تمیز | `OK D1a: HEAD = ae47de4a, tree clean` | `0` | سالم |
| 3.2 | D1b | ۱۴ از ۱۵ فایل روی دیسک | `FAIL D1b: 1 of 15 migration file(s) missing:` / `MISSING 20260913101000_533_pg_cron_http_scheduler.sql` | `1` | سالم، **نام برد** |
| — | D1b | کنترلِ مثبت: هر ۱۵ فایل | `OK D1b: all 15 migration files present` | `0` | سالم |
| — | D1b | مجموعهٔ **خالی** (`$expected = @()`) | `OK D1b: all 0 migration files present` | `0` | **خالی‌گذر** — بی‌معنا پاس می‌شود |
| 3.3 | D3 | دو نام روی `92c554df96cc` | `FAIL D3: :lan and :lan-rollback are the same image (92c554df96cc).` | `1` | سالم |
| — | D3 | کنترلِ مثبت: دو image متفاوت | `OK D3: lan=92c554df96cc rollback=427aacd91600` | `0` | سالم |
| 3.4 | D6(i) | **image واقعیِ حادثه** `296eb4b4899f` | `FAIL D6(i): ... http://192.168.170.8:9000` | `1` | سالم |
| — | D6(i) | کنترلِ مثبت: image من | `OK D6(i)` | `0` | سالم |
| — | D6(ii) | image حادثه | `FAIL D6(ii): __APP_RUNTIME_CONFIG__ is absent` | `1` | سالم |
| — | **D6(i)** | **literalِ کاشته‌شدهٔ غیر-IPv4** (`http://kong:8000` + `https://kwwkppkcihrbeurwudjh.supabase.co` در bundle کلاینت) | `OK D6(i)` — و `OK D6(ii)` هم | `0` / `0` | **BROKEN** |
| — | D6(i) | تگِ ناموجود (= پیش‌فرضِ بدون `-BuildManifest`) | `OK D6(i)` هرچند هیچ چیز اندازه‌گیری نشد | `0` | **معیوب** (D6(ii) می‌گیردش: `exit 1`) |
| 3.5 | D8 | آخرین `VERDICT:` = FAIL، با PASS در `:2047` | `FATAL: the FINAL verdict ... is not PASS` + فهرستِ مرتب؛ فایل نوشته **نشد** | `1` | سالم |
| — | D8 | `rehearsal-e4b.md` (باید پذیرفته شود) | `Written: ... RELEASE-20260913-review.md` | `0` | سالم — **چون آخرین حکمش `:2047 ## VERDICT: PASS` است**، هرچند `:878` FAIL است |
| — | D8 | روایتِ میان‌خطیِ `VERDICT: PASS` پس از FAIL واقعی | رد شد (checkِ قدیمی می‌پذیرفت: `-match '## VERDICT: PASS'` → `True`) | `1` | **بهبودِ واقعی** |
| — | **D8** | **نثرِ ستون-۰** `VERDICT: PASS is the outcome we were hoping for, but it did not happen.` پس از `## VERDICT: FAIL` | **پذیرفت** و `RELEASE-20260913-d8c2.md` را نوشت | `0` | **BROKEN** |
| 3.6 | D9 | `http://kong:8000` (سرو‌شدهٔ واقعی) | `FAIL D9: served host 'kong' is a bare name with no dot` | `1` | سالم |
| — | D9 | `http://localhost:8000` | `FAIL D9: served host 'localhost' is loopback.` | `1` | سالم |
| — | D9 | `http://10.99.99.99:8000` (باید پاس شود) | `OK D9: served host '10.99.99.99' is browser-reachable` | `0` | سالم |
| — | D9 | `http://kong.internal:8000` | `OK D9` | `0` | ضعف (alias نقطه‌دار) |

### `BROKEN_PROBES=2`

۱. **D6(i)** — روی bundle کلاینتی که `http://kong:8000` و
   `https://kwwkppkcihrbeurwudjh.supabase.co` در آن کاشته شده، `exit 0` داد، و `D6(ii)` هم
   `exit 0` — پس **کلِ بلوک سبز می‌شود** و یک literalِ میزبان به production می‌رسد. خواسته
   «no host literal» است، نه «no IPv4 literal». این همان کلاسِ literalی است که
   `vite.config.ts` پیش از این شاخه به‌عنوان fallback داشت
   (`https://kwwkppkcihrbeurwudjh.supabase.co`) — یعنی دقیقاً حالتی که probe باید بگیرد.
۲. **D8** — سندی که حکمِ نهاییِ واقعی‌اش FAIL است را با `exit 0` پذیرفت و سند release تولید کرد.

---

## ۵. فاز ۴ — حمله به شاخه

| # | حمله | حکم | شاهد |
|---|---|---|---|
| ۱ | **هیچ اتوماسیونی D1/D2/D3/D6/D9 را اجرا نمی‌کند** | **confirmed** | `cited` `release/apply-release.ps1:4-5` و `release/lib/apply-release-engine.sh:131-134` (`if [[ "$line" == "# Phase 5"* ]]; then STOPPED_BEFORE_DEPLOY=1 ... break`). engine فقط preflight ثابت و خطوطِ `mig_apply`/`ledger_insert_only` را اجرا می‌کند. `measured` روی runbookی که خودم تولید کردم: `# Phase 5 - image` در خط ۶۱۹، `### Block 28 - image transfer` (D2/D3) در ۶۲۱، `### Block 30 - artifact probe (D6)` (و D9) در ۶۵۹ — **همه بعد از نقطهٔ توقف**. و `Block 0` (D1) در خط ۱۲ هیچ خطِ `mig_apply`/`ledger_insert_only` ندارد، پس engine کاملاً از رویش می‌گذرد و شمارشِ آشتیِ `DOC_APPLY_COUNT` هم آن را نمی‌بیند. پس هر پنج نقص **متنی‌اند که انسان باید دستی اجرا کند**. این همان جنسِ «هیچ‌چیز در خط release به آرتیفکت نگاه نمی‌کرد» است، یک پله بالاتر: حالا نگاه‌کردن **مستند** است، ولی **الزام‌شده** نیست. |
| ۲ | **نامِ متغیرِ زمان‌اجرا روی میزبان مقصد غلط تایپ شود** | **confirmed، با تخفیف** | `measured`: `APP_SUPABASE_PUBLIC_UR` (بدون `L`) → صفحه `HTTP 200` می‌دهد و `window.__APP_RUNTIME_CONFIG__={"supabaseAnonKey":"…"}` — `supabaseUrl` کاملاً **غایب**. بدونِ هیچ متغیرِ URL هم همین. تخفیف: `/api/healthz` آن را می‌گیرد — `{"ok":false,"status":"unhealthy",...,"detail":"SUPABASE_URL/SUPABASE_PUBLISHABLE_KEY not set"}`. ولی `cited` `src/routes/api.healthz.ts:80`: `const url = process.env.SUPABASE_URL;` — healthz **فقط** `SUPABASE_URL` را می‌بیند، هرگز `APP_SUPABASE_PUBLIC_URL` را، که همان مقداری است که مرورگر می‌گیرد. `measured` با `--add-host kong:192.168.170.8` و `SUPABASE_URL=http://kong:9000` و `APP_SUPABASE_PUBLIC_URL` غایب: اتصال به Kong برقرار شد (`HTTP 401` در `5ms` — یعنی قابل‌دسترس بود، فقط کلیدِ dummy رد شد) و مرورگر `{"supabaseUrl":"http://kong:9000"}` گرفت. نتیجه‌گیریِ «healthz سبز می‌شود در حالی که مرورگر آدرسِ غیرقابل‌دسترس دارد» یک **استنتاج** از این دو است، نه اندازه‌گیریِ مستقیم. |
| ۳ | **`APP_SUPABASE_PUBLIC_URL` غایب ولی `SUPABASE_URL` حاضر** | **confirmed، و D9 می‌گیردش** | `measured`: حالتِ (c) — سرو‌شده `http://kong:8000`. `compose` این را با `${APP_SUPABASE_PUBLIC_URL:-}` پاس می‌دهد (`cited` `deploy/lan/docker-compose.yml:67`)، یعنی غایب‌بودنش به رشتهٔ خالی تبدیل می‌شود و `\|\|` در `readServerRuntimeConfig` بی‌صدا سراغِ بعدی می‌رود. D9 این را رد می‌کند (`exit 1`) — **ولی فقط اگر کسی D9 را اجرا کند** (حملهٔ ۱). |
| ۴ | **`VITE_APP_ENV` غلط تایپ شود** | **confirmed، و هیچ probeای نمی‌گیردش** | `measured`: `VITE_APP_EN=test` → سرو‌شده `{"supabaseUrl":"http://10.99.99.99:8000","supabaseAnonKey":"…"}` — `appEnv` **غایب**. `cited` `src/routes/__root.tsx:337`: `getRuntimeConfig().appEnv ?? import.meta.env.MODE` و `MODE` در یک build واقعی همیشه `production` است. پس بنرِ ایمنی **بی‌صدا** می‌گوید production. هیچ‌یک از D1..D9 مقدارِ `appEnv` سرو‌شده را نمی‌سنجد. |
| ۵ | **allowlistِ probe کهنه شود و همه‌چیز را پاس کند** | **refuted (شکلِ قوی) / confirmed (شکلِ ضعیف)** | `quoted`: `$allow = @("http://192.168.170.8:11434")` — تطابقِ **دقیق** روی یک رشته، پس ساختاراً نمی‌تواند «همه‌چیز را پاس کند»؛ و رشتهٔ حادثه (`…8:9000`) در آن نیست، که با ۳.۴ اندازه‌گیری شد. ولی: allowlist یک literalِ میزبانِ **باکس تست** را برای همیشه در آرتیفکتِ production مجاز می‌کند، و `measured` نشان داد آن رشته امروز واقعاً در bundle کلاینت هست (۱ بار) — پس بارِ واقعی دارد و اگر متنِ راهنما عوض شود probe به‌دروغ قرمز می‌شود. |
| ۶ | **runbook می‌تواند بلوک probe را کاملاً رد کند** | **refuted** | `cited`: بلوک D6/D9 بی‌قید emit می‌شود (بیرونِ `if/else`ِ `$BuildManifest`). `measured`: سندِ emit‌شدهٔ بدونِ manifest (`release/out/RELEASE-20260913-d6test.md`) بلوک را **دارد**. بدونِ manifest بلوک بی‌اثر می‌شود ولی **پرصدا** (D6(ii) `exit 1`)، نه خاموش. |
| ۷ | **بلوکِ D6(iii)، وقتی runbook روی باکس تست رهرسال شود، ترافیک به production می‌فرستد** | **confirmed (cited)** | `quoted` `release/emit-blocks.ps1` param: `[string]$D6TargetHost = "192.168.170.10"`، و بلوکِ emit‌شده: `docker run --rm --entrypoint sh -e SUPABASE_URL="http://${target}:8000" $img ...`. یعنی اجرای runbookِ پیش‌فرض روی این باکس یک کانتینر با `SUPABASE_URL` به سمت production بالا می‌آورد. **عمداً اجرا نشد** — در فهرست ممنوعه است. من با `-D6TargetHost 10.99.99.99` تولید کردم. |
| ۸ | **SSR حالا از مسیرِ IP میزبان بیرون می‌رود، نه از نامِ داخلیِ compose** | **assertion** | `cited` `src/lib/runtime-config.ts:87-91`: ترتیب `APP_SUPABASE_PUBLIC_URL` → `VITE_SUPABASE_URL` → `SUPABASE_URL`. پیش از `763dddc0` سمتِ سرور عملاً `SUPABASE_URL` (`http://kong:8000`) را می‌گرفت. حالا SSR هم آدرسِ رو‌به‌مرورگر را می‌گیرد، پس به hairpin routingِ IP میزبان از داخلِ کانتینر وابسته می‌شود. روی این باکس چنین مسیری کار می‌کند (`measured`: `5ms`, `HTTP 401` به `192.168.170.8` از داخلِ کانتینر)، ولی **روی `192.168.170.10` آزمودنی نیست** و آزموده نشد. |
| ۹ | **`define` دیگر تنها دربِ ورودِ مقادیرِ میزبان به bundle نیست** | **confirmed (cited)** | `cited` `node_modules/@lovable.dev/vite-tanstack-config/dist/index.js:592-593`: `const loadedEnv = loadEnv(mode, process.cwd(), "VITE_"); for (...) envDefine[...] = JSON.stringify(value)`، و `cited` `node_modules/vite/dist/node/chunks/config.js:9417`: `for (const key in process.env) if (prefixes.some(...)) env$1[key] = process.env[key]`. یعنی **هر** `VITE_*` که در محیطِ build باشد خودکار به `define` می‌رود، مستقل از بلوکِ `define` دستیِ `vite.config.ts`. امروز بی‌خطر است چون `measured`: هیچ کدِ زنده‌ای در `src/` دیگر `import.meta.env.VITE_SUPABASE_URL` را نمی‌خواند و Dockerfile هیچ `VITE_*`ِ وابسته به میزبان را `ENV` نمی‌کند. ولی قاعدهٔ نوشته‌شده در `vite.config.ts` («هیچ مقدار وابسته به میزبان را به `define` اضافه نکنید») **قاعدهٔ درست نیست**: قاعدهٔ درست «هیچ مقدارِ وابسته به میزبان را با `import.meta.env.VITE_*` **نخوانید**» است. یک `import.meta.env.VITE_SUPABASE_URL` تازه در `src/` حادثه را برمی‌گرداند بدون آنکه کسی `vite.config.ts` را لمس کند. |

### شمارشِ `import.meta.env` (فاز ۱.۳، `measured`)

دستور: `grep -rn "import\.meta\.env" src/` → `exit 0`، **۱۵** تطابق در **۱۱** فایل.
از این ۱۵، شش مورد کامنت/مستند است و **۹ خوانشِ زنده** در ۷ فایل:
`src/lib/mcp/index.ts:8` (`VITE_SUPABASE_PROJECT_ID`)، `src/lib/pwa/build-version.ts:52`
(`VITE_BUILD_ID`)، `src/lib/pwa/register-sw.ts:169` / `src/router.tsx:31` /
`src/routes/_app.sales.search.tsx:468` / `src/routes/__root.tsx:117` (همه `DEV`)،
`src/routes/__root.tsx:337` (`MODE`، به‌عنوان fallback)، `:339` (`VITE_SHOW_ENVIRONMENT_BANNER`)،
`:340` (`VITE_ENVIRONMENT_BANNER_TEXT`).
**هیچ‌کدام `VITE_SUPABASE_URL` یا `VITE_SUPABASE_PUBLISHABLE_KEY` نیست** — این نیمهٔ اصلیِ کار
درست انجام شده. سه موردِ آخر همچنان build-time و وابسته به میزبان‌اند، ولی هیچ‌کدام آدرس
نمی‌سازد و هیچ‌کدام در `compose` به‌عنوان build arg پاس نمی‌شود.

### `getRuntimeConfig()` و شکستِ خاموش (فاز ۱.۴)

`cited`: `supabaseUrl`/`supabaseAnonKey` در دو مصرف‌کننده **بلند** می‌شکنند —
`src/integrations/supabase/client.ts:23-27` (`throw new Error("Missing Supabase configuration…")`)
و `src/lib/storage/upload-with-progress.ts:70-72`. ولی `client.ts` یک `Proxy` تنبل است
(`:42-47`)، پس throw فقط در **اولین دسترسی** رخ می‌دهد نه در بارگذاریِ ماژول: صفحه ۲۰۰ می‌دهد و
تا اولین لمسِ supabase هیچ‌چیز اعتراض نمی‌کند (`measured`، بخش ۵ حملهٔ ۲).
`appEnv` و `trustedHosts` **هیچ‌کدام** بلند نمی‌شکنند: `appEnv` به `MODE` می‌افتد
(`production`)، و `trustedHosts` به `""` می‌افتد که در جهتِ ایمن است (بنرِ قرمز).

### SSR و کلاینت، یک مقدار یا دو؟ (فاز ۱.۵)

`cited`: `RootShell` (`src/routes/__root.tsx:274-289`) خودش `getRuntimeConfig()` را صدا می‌زند و
همان JSON را تزریق می‌کند؛ کلاینت همان global را می‌خواند
(`src/lib/runtime-config.ts:111-116`). پس در یک process یک مقدارند و `measured` هم همین را
نشان داد (هر سه رفتارِ ۳.۳ عیناً مقدارِ محیط را برگرداندند).
شرطِ واگرایی: اگر آن `<script>` inline اجرا نشود (CSP، یا خطای JS پیش از آن) کلاینت `{}`
می‌گیرد و throw می‌کند — بلند، نه خاموش. escapingِ `</script>` و `-->` هم
(`serializeRuntimeConfig`, `:124-126`) درست است.

### دامنهٔ تغییرات (فاز ۱.۲)

`measured` (`git diff --name-status origin/main...HEAD`): ۱۵ فایل. خارج از نُه نقص:
`docs/missions/convergence/local-snapshot-20260912-2153/{README,RESUME,STATE}.md` (۷۸۵ خط،
وضعیتِ orchestrator که در فاز ۰ حفظ شد — با دلیل مستند، بی‌ربط به موضوع شاخه) و
`release/out/RELEASE-20260913-d6test.md` (۷۹۳ خط آرتیفکتِ تولیدی که در repo commit شده و
اکنون کهنه است — بخش ۸ بندِ ۱۰). scope creep در ابزارِ release، همان‌طور که بریف هشدار داده بود.

---

## ۶. فاز ۵ — اختلافِ شمارشِ `/operations/tasks`

**فرضیهٔ RLS رد شد.** علتِ واقعی پیدا و اندازه‌گیری شد.

همهٔ کوئری‌ها read-only، هر سه داخلِ `begin read only; … rollback;`. هیچ ردیفی در
`afrakala` تغییر نکرد.

| اجراکننده | `count(*)` کلِ وضعیت‌ها | `status='pending'` |
|---|---|---|
| superuser (`supabase_admin`، RLS bypass) | **13** | **1** |
| `role authenticated`، JWT `sub=1a15e8c6…` (admin+manager+accountant) | **13** | **1** |
| `role authenticated`، JWT `sub=eb8db50e…` (فقط sales) | **0** | **0** |
| `role anon` | `ERROR: permission denied for table tasks` | — |

`cited` سیاستِ `tasks_select`:
`((assigned_to = uid()) OR (created_by = uid()) OR has_any_role(uid(), ARRAY['admin','manager','accountant']))`.
پس RLS یا **۱۳** می‌دهد یا **۰** — هرگز ۸. برای یک ادمین **صفر ردیف حذف نمی‌کند**.

فیلترِ صفحه هم ۸ نمی‌دهد: `cited` `src/routes/_app.operations.tasks.tsx:206` مقدارِ اولیهٔ
`filterStatus = "pending"` و `:222` `q.eq("status", filterStatus)` → امروز **۱**.
صفحه‌بندی هم نه: `cited` `:19` `const PAGE_SIZE = 20` و کلِ ردیف‌ها ۱۳.

**۸ از کجا می‌آید:** همان صفحه یک فراخوانیِ دومِ داده هم دارد —
`cited` `:235` `supabase.rpc("get_task_kpi_report", { p_days: Number(kpiDays) })` با
`cited` `:211` مقدارِ اولیهٔ `kpiDays = "30"`. `measured`، با همان JWT ادمین:

```
 section | bucket_key | bucket_label | task_count | pending_count | expired_count
 overall | all        | همه وظایف    |          8 |             1 |             7
```

و پنجرهٔ ۳۰ روزه دقیقاً همین را می‌دهد (`measured`):
`within_30_days = 8`, `older = 5`, `total = 13`.

**حکم:** «۸» شمارشِ `overall` گزارشِ KPI است که به `created_at >= now() - 30 days` محدود
است؛ «۱۳» کلِ جدول است. دو مخرجِ مختلف روی یک صفحه، نه RLS و نه از دست رفتنِ داده.
تمامِ فاصله توضیح داده شد: `8 + 5 = 13`. هیچ ردیفی گم نشده و هیچ نشانه‌ای از خواندن از
دیتابیسِ اشتباه نیست (برای کامل‌بودن: `prod_rehearsal_*` همه ۹۱۳ ردیف دارند، `postgres` صفر
— هیچ‌کدام ۸ یا ۱۳ نیست).

از آنجا که فاصله **کامل توضیح داده شد** و علتش خوش‌خیم و بی‌ربط به این شاخه است، این را
به‌تنهایی بازدارندهٔ PR نمی‌دانم. این قضاوتِ من است و مالک می‌تواند ردش کند — چیزی که
بازدارنده بود (فاصلهٔ توضیح‌داده‌نشده که می‌توانست نشانهٔ خواندن از جای اشتباه باشد) از بین
رفته است. حکمِ REJECT از `BROKEN_PROBES=2` و `D2` می‌آید، نه از این بند.

---

## ۷. فاز ۶ — typecheck

```
$ npm run typecheck              (یک بار، exit code جدا خوانده شد)
TYPECHECK_EXIT=0
error TS count : 70
distinct files : 6
```

| فایل | تعداد خطا |
|---|---|
| `src/routes/_app.products.index.tsx` | 18 |
| `src/routes/_app.admin.sales-reminders.tsx` | 15 |
| `src/lib/invoices/functions.ts` | 13 |
| `src/lib/accounting/functions.ts` | 13 |
| `src/lib/audit/index.ts` | 6 |
| `src/routes/_app.admin.automation.tsx` | 5 |

منطبق بر ground truth: **70 خطا در 6 فایل**. و صفر خطا در هر ۹ فایلی که این شاخه لمس کرده
(`measured`، فایل‌به‌فایل). این بند **verified** است.

---

## ۸. اختلاف‌ها با گزارش تکمیل

گزارش تکمیل **بعد از** تشکیلِ حکمِ خودم خوانده شد. ۱۳ اختلاف، در هر دو جهت.

**آنچه گزارش ادعا می‌کند و من بازتولید نکردم:**

۱. **`BROKEN_PROBES=0`** (`runtime-config-completion-20260913.md:100`). اندازه‌گیریِ من:
   **۲**. گزارش D6 را فقط با دو ورودی آزمود (image حادثه و image اصلاح‌شده) و هیچ‌گاه یک
   literalِ **غیر-IPv4** نکاشت — خودش می‌نویسد «هیچ literalی کاشته نشد؛ لازم نبود»
   (`probe-falsification-20260913.md`). دقیقاً همان کاشتنِ نکرده، راهِ فرار را نشان می‌دهد.
۲. **D2 «اصلاح شد … روی image در حال اجرا»**. دستور `afrakala-app:lan` را تگ می‌کند و روی
   همین باکس `:lan` = `296eb4b4899f` در حالی که image در حال اجرا `0c3106602cc9` است و
   **هیچ تگی** به آن اشاره نمی‌کند. نکتهٔ تلخ‌تر: پیوستِ ۱ بندِ ۲ همین گزارش **همین واگرایی
   را خودش اندازه گرفته** (`:lan tag id : 296eb4b4899f` / `running container image :
   0c3106602cc9`) ولی آن را به ادعای D2 وصل نکرده. گزارش با خودش ناسازگار است.
۳. **«صفر JWT در source» (N1)**. یک JWT یکتا (sha256 prefix `a0bf`، ۱۲ بار) در
   `docs/verification/convergence/wave-b-browser-pass.txt`ِ tracked باقی است. برای `src/` و
   ابزارهای release درست است؛ برای «source» به‌معنای درختِ tracked نه.
۴. **D6 بند (i) به‌عنوان «صفر literal میزبان در bundle کلاینت»**. آنچه پیاده شده «صفر
   literalِ `IPv4:port`» است. یک ادعای ضعیف‌تر از آنچه نوشته شده.
۵. **جدولِ ۳ گزارش، `:8000` = `0/0`**. روی image **خودم** (از `ae47de4a`): client `0`، SSR
   **`1`**. `compared`: روی `afrakala-app:rc-banner` (image خودِ گزارش) واقعاً `0/0` است — پس
   جدول برای imageِ خودش درست بود، ولی برای headِ شاخه دیگر درست نیست. علتش commit
   `763dddc0` است که کامنتِ `http://kong:8000` را به `src/lib/runtime-config.ts` آورد و
   build سمت SSR کامنت را نگه می‌دارد.
۶. **بدنهٔ گزارش D8 را «پیدا شد و اصلاح نشد» ثبت می‌کند.** headِ شاخه (`ae47de4a`) آن را
   اصلاح **کرده**؛ تصحیح فقط در پیوستِ ۲ آمده. بدنهٔ سند کهنه است.

**آنچه من یافتم و گزارش ذکر نمی‌کند:**

۷. **هیچ اتوماسیونی D1/D2/D3/D6/D9 را اجرا نمی‌کند** — همه پس از نقطهٔ توقفِ
   `# Phase 5` در `apply-release-engine.sh` می‌نشینند، و `Block 0` هیچ خطِ
   `mig_apply` ندارد پس شمارشِ آشتی هم نمی‌بیندش (بخش ۵ حملهٔ ۱). این مهم‌ترین یافتهٔ
   ذکرنشده است: نیمهٔ دومِ خواسته («خط release باید اثبات کند») مستند شده ولی الزام نشده.
۸. **شمارهٔ بلوک در سندِ emit‌شده خراب است** (`measured`، با emitter فعلی): `Block 28` →
   `Block 30`، یعنی `Block 29` **وجود ندارد**، و `Block 30` **دو بار** می‌آید (بلوکِ probe
   در خط ۶۵۹ و بلوکِ rollback tag در ۷۳۸). در diff، `$blockN++` پیش از بلوکِ D6 هست ولی
   پس از آن نیست. `release/validate-blocks.ps1` این را نمی‌گیرد — `measured`:
   `PASSED — no problems found`, `VALIDATE_EXIT=0` با ۳۵ بلوک. یک سندِ اجراییِ عملیاتی با
   دو بلوکِ هم‌شماره، در سندی که همهٔ ارجاع‌دهی‌اش به شمارهٔ بلوک است.
۹. **D6(i) روی تگِ ناموجود `OK` می‌دهد** — و آن تگِ ناموجود پیش‌فرضِ خودِ emitter است وقتی
   `-BuildManifest` داده نشود.
۱۰. **`release/out/RELEASE-20260913-d6test.md` (۷۹۳ خط) در repo commit شده و کهنه است** —
    `measured`: بلوکِ D9 **کاملاً در آن نیست** (ساخته‌شده در `c8ba695b`، پیش از `ae47de4a`).
    یک آرتیفکتِ تولیدی که به‌عنوان شاهد در سند ارجاع می‌شود ولی وضعیت فعلیِ emitter را نشان
    نمی‌دهد.
۱۱. **`appEnv` با نامِ غلط‌تایپ‌شده بی‌صدا `production` می‌شود** و هیچ probeای آن را نمی‌سنجد
    (بخش ۵ حملهٔ ۴).
۱۲. **بلوکِ D6(iii)ِ emit‌شده به‌طور پیش‌فرض به `192.168.170.10` اشاره می‌کند**، پس رهرسالِ
    runbook روی باکس تست کانتینری با `SUPABASE_URL` به سمت production بالا می‌آورد
    (بخش ۵ حملهٔ ۷).
۱۳. **قاعدهٔ نوشته‌شده در `vite.config.ts` قاعدهٔ درست نیست** — wrapper هر `VITE_*`ِ محیط را
    خودکار به `define` تزریق می‌کند، پس محافظِ واقعی «نخواندنِ `import.meta.env.VITE_*`» است
    نه «دست‌نزدن به بلوکِ `define`» (بخش ۵ حملهٔ ۹).

**آنچه گزارش می‌گوید و من تأیید می‌کنم** (برای انصاف): بازتولیدِ قرمزشدنِ D6 روی image
واقعیِ حادثه (۷.۴) — `compared` و درست؛ ابطالِ D9 با `kong`/`localhost`/IP واقعی — من هم
همان سه نتیجه را گرفتم؛ `70/6` بودنِ typecheck؛ اینکه تنها `192.168.*`ِ bundle کلاینت متنِ
راهنمای Ollama است؛ و اینکه `rehearsal-e4b.md` باید پذیرفته شود چون آخرین حکمش PASS است.

---

## ۹. آنچه نتوانستم تأیید کنم

۱. **هیچ‌چیز روی `192.168.170.10` آزموده نشد.** ممنوع بود و نشد. پس «این image روی
   production درست کار می‌کند» برای من هم یک **استنتاج** است، نه اندازه‌گیری. چیزی که
   اندازه‌گیری کردم قوی‌تر از گزارش است (یک میزبانِ ساختگیِ `10.99.99.99` که در هیچ build
   نبوده)، ولی روی مقصدِ واقعی نه.
۲. **`release/build.ps1` را end-to-end اجرا نکردم.** از شاخهٔ feature عمداً رد می‌کند
   (`exit 1`)، و push به `main` ممنوع است. پس D4 و D7 را `cited/quoted` تأیید کردم، نه با
   اجرای واقعیِ مسیرِ کامل. به‌طور خاص: اینکه tarballِ ذخیره‌شده با تگِ `<sha>` واقعاً بعد از
   `docker load` تگِ `afrakala-app:<sha>` را بازمی‌سازد، **اندازه‌گیری نشد**.
۳. **`release/apply-release.ps1` را اجرا نکردم.** پس اینکه engine واقعاً در `# Phase 5`
   می‌ایستد از `cited` کد و از موقعیتِ خطوط در سندِ emit‌شده نتیجه گرفته شده، نه از یک اجرا.
   این تنها شاهدِ حملهٔ ۱ است و اجرای واقعی محکم‌ترش می‌کرد.
۴. **رفتارِ مرورگرِ واقعی را نسنجیدم.** همهٔ اندازه‌گیری‌های من روی HTMLِ سرو‌شده و روی
   محتویاتِ image است. اینکه Chrome پس از hydration واقعاً به همان میزبان درخواست می‌فرستد
   و هشدارِ hydration mismatch نمی‌دهد، ادعای گزارش است که من بازتولیدش نکردم.
۵. **«healthz سبز می‌شود در حالی که مرورگر آدرسِ غیرقابل‌دسترس دارد»** (حملهٔ ۲) یک
   استنتاج از دو اندازه‌گیری است. اندازه‌گیریِ مستقیمش یک کلید anonِ معتبر و یک میزبانِ
   صرفاً-داخلیِ قابل‌دسترس لازم داشت که بی لمس‌کردنِ stack فراهم نبود.
۶. **قابلِ بازتولید بودنِ «۸» در زمانِ نوشتنِ گزارش** را نسنجیدم. امروز
   `get_task_kpi_report(30)` دقیقاً ۸ می‌دهد و `8 + 5 = 13`، که توضیح را کامل می‌کند؛ ولی
   اینکه همان عدد در همان لحظه از همان مسیر آمده باشد، استنتاج است.
۷. **پایداریِ allowlistِ D6 در طول زمان** را نسنجیدم — فقط ثابت کردم امروز بارِ واقعی دارد و
   تطابقش دقیق است.
۸. **`afrakala-app:review-ae47de4a` (`92c554df96cc`) روی این باکس باقی ماند** به‌عنوان شاهد،
   ۱٫۳۲GB. تگِ `:lan` را لمس نکرد. حذفش تصمیم مالک است.
۹. **بندِ ۱۰ گزارش («stack تست نقطهٔ بازگشت ندارد») را مستقل تأیید نکردم** به‌جز نیمهٔ
   بی‌خطرش: `measured` که هیچ تگی به `0c3106602cc9` اشاره نمی‌کند. آزمودنِ اینکه آن image
   واقعاً در store نیست نیازمند عملیاتی بود که برای stack در حال اجرا ریسک داشت، و نکردم.
   **این یک ریسکِ عملیاتیِ بازِ مستقل از این PR است و به مالک تعلق دارد.**

---

## ۱۰. آنچه برای پاس‌شدن لازم است

به ترتیبِ اهمیت، و هیچ‌کدام را من اصلاح نکردم — من دروازه‌ام، سازنده نه:

۱. **D2** — rollback را روی image **در حال اجرا** بگیر، نه روی تگِ `:lan`، و آن را
   **assert** کن با `exit 1`، در یک قالبِ واحد.
۲. **D6(i)** — regex را از «`IPv4:port`» به «هر originِ میزبان» گسترش بده تا نامِ میزبان
   (`kong:8000`، `*.supabase.co`) را هم بگیرد؛ و روی خروجیِ خالیِ `docker run` صریحاً fail کن.
۳. **D8** — سرتیتر را از نثر جدا کن (مثلاً فقط `^#{1,6}\s*VERDICT:`).
۴. **اجرا** — دستِ‌کم D1 و بلوکِ probe باید توسط چیزی **اجرا** شوند، نه فقط emit.
۵. **شمارهٔ بلوک‌ها** — `$blockN++` جامانده، و یک checkِ تکراری‌نبودن در `validate-blocks.ps1`.
