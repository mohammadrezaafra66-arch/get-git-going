# 00-PROGRESS — programme level

The single place to look to answer "where are we". Updated by the Lead Orchestrator at every phase
boundary. Per-task detail lives in `phase-<N>-PROGRESS.md`.

## HANDOFF STATE

```
Programme:            AfraKala Live Ledger
                      Block rewritten 2026-08-23 by the bookkeeping mission. Every line below
                      is measured; nothing is carried over from the previous text. The phase
                      table and the Owner-Gate log further down were already current — only
                      this block was stale, and it had been stale since phase 6.
Current phase:        NONE IN PROGRESS. Phase 6 and its remediation are COMPLETE; task 6.7
                      remains BLOCKED by OG-4 and is the only open item inside phase 6.
                      Since then, four side-missions closed, none of them a numbered phase:
                        PV-remediation      PR #328, merge 5b65f60  (migrations 368, 369)
                        phase-6 remediation PR #332, merge e209218b (no migration)
                        G-1 anon view leak  PR #333, merge 1acbd730 (migrations 370-372)
                        OG-25 default tap   PR #334, merge faa8b1fa (migrations 373-380)
                        bookkeeping         PR #335 0d58db1d, PR #336 53c8c172 (no migration)
                        Phase 0 owner-gates PR #337, merge 9a661303 (no migration)
                        M3 function EXECUTE PR #338, merge 274bfa3e (migrations 381-383)
                        M9 BYPASSRLS        PR #339, merge f0257d2e (migrations 384-385)
                        M6 route guard      PR #340, merge e66e3759 (no migration, src/ only)
                        M4 viewer guard     migrations 386-387 - see the M4 progress file
                        M5 public surfaces  measurement only, zero migrations -
                                            docs/research/m5-public-surfaces.md
                        M5B anon products   migrations 388-389 - OG-49 CLOSED, OG-50 answered
                                            docs/research/m5b-anon-products.md
                                            OG-49 needed TWO migrations. 388 narrowed anon to
                                            nine product columns; sku still came out, through
                                            find_duplicate_product, a SECURITY DEFINER RPC
                                            whose parameters are the columns 388 left open.
                                            A definer function ignores column grants and RLS
                                            alike, so no view and no column revoke could have
                                            closed it - 389 revokes EXECUTE, from PUBLIC as
                                            well as anon, since the PUBLIC default makes the
                                            anon-only revoke measure as no change.
                                            The e2e run of 2026-08-25 (92 failed, 4.7h) is
                                            INVALID - see OG-54. Baseline stays 47.
                                            NOTE: M6 reported R9 (the repo e2e suite) as
                                            "490 passed, 0 failed". That was WRONG - the
                                            output had been truncated with tail -18 and the
                                            failure block sits above the summary. Re-run on
                                            the deployed build: 535 passed, 41 FAILED. None
                                            of the 41 is an M6 spec. See the M6 progress
                                            file and OG-43.
                      The previous text's "ONE REMAINING MANUAL STEP: the test web image was
                      not rebuilt" is RESOLVED — the image was rebuilt during the G-1 mission.
Current task:         none — do not start phase 7 (blocked by OG-5, HTTPS)
Branch:               staging is the integration branch; work happens on feature/*
Last commit SHA:      274bfa3e on origin/staging (git rev-parse, 2026-08-24). The line above
                      said faa8b1fa until 2026-08-24; three missions had merged since without
                      updating it, which is the same drift the bookkeeping mission fixed once
                      already. The migration table had stopped at 380 for the same reason -
                      rows 381-383 were added by M9, the mission that had applied them.
Live APP_GIT_SHA:     dbe46fe1 — BEHIND HEAD, AND THAT IS CORRECT. NO REBUILD IS OWED.
                      `git diff --name-only dbe46fe1 origin/staging` is 12 files under docs/
                      and 8 under supabase/migrations/ — ZERO under src/. Nothing in that gap
                      reaches the built bundle, so moving the stamp would make it lie about
                      what is in the image. PostgREST was restarted after each migration,
                      which is what actually makes new database objects reachable.
Typecheck:            70 / 70 baseline (D14)
Migrations applied:   45 (336-380) on the test server, all applied by hand.
                      supabase/migrations/ holds 568 files; supabase_migrations.schema_migrations
                      held 523 rows and was frozen at 20260811180000 — it records nothing this
                      programme built. Back-filled to 552 rows on 2026-08-23 with the 29 of the
                      45 whose effect is provable against the live catalogue. The other 16 are
                      deliberately absent and each is listed with its reason in
                      `bookkeeping-record-reconciliation-PROGRESS.md`; a replay re-running them
                      is safe, a replay skipping one that never ran is not.
Open Owner-Gates:     23 open — OG-4, OG-5, OG-6, OG-8, OG-9, OG-11, OG-12, OG-15, OG-17,
                      OG-23, OG-24, OG-26, OG-27, OG-28, OG-29, OG-30, OG-31, OG-32, OG-33,
                      OG-34, OG-35, OG-36, OG-37.
                      13 closed — OG-1, OG-2, OG-3, OG-10, OG-13, OG-14, OG-16, OG-18, OG-19,
                      OG-20, OG-21, OG-22, OG-25.
                      OG-35/36/37 were added 2026-08-23. They are not new decisions — they are
                      three owner ACTIONS that existed only as prose in this file and had no row,
                      so a reader of the log could not see them: open one generated .xlsx inside
                      Asan (phase 5's unmet exit condition), activate test.manager and test.viewer
                      (both `rejected`, which makes a third of the role matrix untestable), and
                      create a bank_accounts row with account_type='cash' (there is none, so the
                      cash branch of all three document types has never been exercised).
                      OG-4, OG-5 and OG-6 had rows but were entirely EMPTY — no date, no status.
                      Filled the same day from measurement.
                      Derived from the log below, gate for gate. Two defects in that log were
                      fixed the same day: OG-25 still read OPEN although the mission that
                      closed it had merged, and OG-24 existed only as a `###` section and had
                      never been added to the table. `OG-7` is NOT a gate — it is the heading
                      of the "Reviewer escalations" section, which is why it has no row.
BLOCKING BEFORE PHASE 6: reverse_document CLOSED (363–365).
BINDING ON PHASE 5:   T14 honoured. T15 (2026-08-19): bank automatic; cash/cheque/reversal
                      manual — both legs of a reversed pair and every cheque document are
                      absent from the journal export (367). Fourth menu
                      `پرداخت‌های خرید و تسویه` for purchase_payment + settlement.
Owner decisions:      T9-T15 in ledger-decisions.md. T15 recorded 2026-08-19. D8 and D17
                      amended the same day.
Blocked tasks:        none. Phase 3 is COMPLETE and honoured all four T13 constraints - proved,
                      not asserted (phase-3-PROGRESS.md, task 3.3/3.4 and the T13-c3 proof).
                      T9 itself must still be RESOLVED BEFORE PHASE 5.
Gate A defects:       OG-14 reverse_document Gate A: **PASS** then remediated — M2/M3/m2 closed
                      (365); M1 deferred to phase 5 as required input; m1 recorded for phase 8;
                      OG-23 raised. Report: `og14-reverse-document-GATE-A.md`.
                      Phase 4 Gate A: **PASS** — 0 BLOCKER, 1 MAJOR, 3 MINOR (`phase-4-GATE-A.md`).
                      Phase 4 M1 (`361-down` gate) already closed. Remaining from that review:
                      **m1** (reader-table), **m2** (root `PROGRESS.md` SHA), **m3** (INSERT policy
                      / D12 → phase 6).
                      Phase 2 Gate A: 16 raised, 12 closed, 1 with the owner (OG-17), 3 deferred
                      (m1, m7 -> phase 6; m3 -> phase 5). M4 + M5 CLOSED 2026-08-18.
                      Phase 5 Gate A: **PASS** 0 BLOCKER / 4 MAJOR / 3 MINOR then remediated
                      (367). M1–M4 and m1–m3 CLOSED. Report: `phase-5-GATE-A.md`.
                      Progress: `phase-5-remediation-PROGRESS.md`.
                      **Do not import the old concatenated sample xlsx into Asan.** Trial from
                      `/admin/asan-export`. OG-23 still open (source-row freeze).
Production touched:   NO - and the T9 production-count question was CLOSED BY OWNER DECISION
                      without contacting it (T13). CLAUDE.md rule 10 stands unweakened.
```

**T9 research complete; phase 3 is unblocked, subject to four constraints.** This entry previously
read "T9 blocks the next dispatch … scope is unmeasured". The research was run
(`docs/research/T9-one-person-one-balance-RESEARCH.md`, merged as `bc0ddafc`) and recommended **(b)**
— *phase 3 may proceed, but must be written so it does not deepen the split*. **The owner adopted (b)
as T13 on 2026-08-19.**

**Phase 3 must respect all four of T13's constraints:** (1) no new `account_kind` → table mapping —
`validate_journal_line_ref` has 6 and needs 0 more; (2) resolve the party to a `person_id` at the
boundary and store it in `payment_vouchers.payee_person_id`, which already exists and is already in
the person-FK registry; (3) **do not copy `pay_purchase_with_voucher`'s unconditional supplier
keying** — it posts `('supplier_payable', _purchase.supplier_id)` even when the payee is an
`external_party`, which is the exact failure T10 forbids; (4) **T9 must be resolved before phase 5**,
because `asan_list_journal_export` and `person_settlement_position` both read all three kinds and
phase 5 is where they become the accountant's numbers.

**The production-count question is closed without contacting production.** The research flagged,
correctly, that its confidence in (b) over (c) rested on how many persons on production are both a
customer and a supplier. The owner answered the model instead of the number: being both is **part of
this business's model, not an anomaly** — the count is low today and will grow. That is a stronger
answer than a count, and the practical decision is identical either way, because `journal_entries`
holds 1 row and `journal_lines` 2, so the contradiction stays **latent at any count**. Production
(`192.168.170.10`) was **not queried, not pinged, not contacted**. Full reasoning in
`ledger-decisions.md` § T13.

**Two corrections the research forced, recorded in `ledger-decisions.md`:** T9's assumption that
`external_parties` might lack a `person_id` was **wrong** — it exists, is `NOT NULL`, all three role
tables carry one, and 29 of 29 persons-referencing FKs are registered `ok`; and T9's claim that the
split is the root cause of `person_settlement_position`'s misleading numbers was **also wrong** — the
cause is that nothing ever debits `customer_credit`, and resolving T9 would not fix it.

**APP_GIT_SHA note — corrected 2026-08-18.** This entry previously read `bfcc723a`, "which
predates the phase-1 merge". That is no longer true: the `afrakala-lan-web` container now reports
**`87c1a921`**, so the image was rebuilt at some point after phase 2 merged. It is now exactly two
commits behind `staging` (`9b837306`), and `git diff --name-only 87c1a921 9b837306` is **entirely
`docs/` and `supabase/migrations/`** — not one file that reaches the built web bundle. A rebuild
would move the SHA stamp and change nothing else. PostgREST was restarted after the migrations,
which is what actually makes new database objects reachable.

**The deploy was attempted and deliberately not forced.** `deploy/lan/build.ps1` refuses to build a
tree that is not clean, because `docker-compose` builds from the working tree, so anything
uncommitted ships into the image while `APP_GIT_SHA` still reports the last commit. This shared
checkout currently holds **8 untracked files belonging to other missions** (`audit/`,
`docs/audits/7-eg-checklist-mission.md`, `docs/execution/production-gap-analysis-mission.md`,
`docs/research/_a…_e`). Building with `-Force` would stamp `9b837306` onto an image that also
contains eight uncommitted files — reintroducing exactly the "state does not match the recorded
commit" drift that this remediation existed to close (§ 0 of
`phase-2-REMEDIATION-PROGRESS.md`). The guard was left to do its job.

**Remaining manual step for a human.** Once the other missions have committed their files and
`git status --porcelain` is empty, on the **test computer**:

```powershell
git switch staging; git pull --ff-only origin staging
powershell -ExecutionPolicy Bypass -File deploy\lan\build.ps1 web
docker compose -f deploy\lan\docker-compose.yml --env-file deploy\lan\.env.lan up -d web
docker inspect afrakala-lan-web --format "{{range .Config.Env}}{{println .}}{{end}}" | Select-String "APP_GIT_SHA"
```

`APP_GIT_SHA` must then equal `git rev-parse --short HEAD`. Nothing in phase 2 or in this
remediation needs that rebuild to function — it closes a reporting discrepancy, not a defect.

## Phase status

| Phase | Status | Started | Finished | Tests | Notes |
|---|---|---|---|---|---|
| 0 Ground and decisions | complete | | 2026-08-18 | n/a | OG-1 confirmed |
| 1 Shared foundations | **complete** | 2026-08-18 | 2026-08-18 | Gate B PASS, Gate A FAIL then remediated | 12 migrations; OG-10 closed, 1 risk open (OG-14) |
| 2 Receipts post | **complete** | 2026-08-18 | 2026-08-18 | 8/8 accept PASS; stress PASS; Gate A FAIL then remediated; cleanup verifier 14/14 PASS | migrations 348-349 + remediation 350-353; OG-16 and OG-17 raised; OG-13 fully closed. 12 of 16 Gate A defects closed, 1 with the owner (OG-17), 3 deferred — `phase-2-REMEDIATION-PROGRESS.md` |
| 3 Payments post | **complete** | 2026-08-19 | 2026-08-19 | 9/9 accept PASS; stress PASS; Gate A FAIL then remediated | migrations 354-355 + remediation 356-358. Gate A: 1 BLOCKER, 2 MAJOR, 3 MINOR -> 4 closed (B1, M2, m1, OG-20), 1 with the owner (M1 -> OG-18), 2 deferred to phase 6 (m2, m3). `phase-3-GATE-A.md`, `phase-3-REMEDIATION-PROGRESS.md` |
| 4 Dual documents | **complete — corrected; Gate A PASS** | 2026-08-19 | 2026-08-19 | 7/7 accept PASS then owner correction accept PASS; stress PASS; cleanup proved clean; Gate A 0 BLOCKER / 1 MAJOR / 3 MINOR | migrations 360-361 plus **362** (no fee). Gate A M1 **closed** (`361-down` gate). New `dual_documents` table (task 4.2). T11 four roles. C-c / OG-21 **overturned by the owner 2026-08-19: no fee exists.** Always exactly two journal lines. Phase 5 not started. `phase-4-PROGRESS.md`, `phase-4-GATE-A.md` |
| **reverse_document** | **complete (OG-14) + Gate A remediated** | 2026-08-19 | 2026-08-19 | accept + Gate A re-probes PASS | 363–364 plus **365** (M2 credit from journal line; M3 admin+accountant only, interim). M1 deferred to phase 5 as required input. m1 leftover recorded for phase 8. OG-22 closed. OG-23 raised (source-row freeze). `asan_list_journal_export` not touched. |
| 5 Asan exports live | **complete** | 2026-08-19 | 2026-08-19 | 5/5 Accept; typecheck 70 | migration **366**. Classifier reads stored `doc_kind`. M1/C-d/C-e/C10/C7 closed. Cheque skip (D8). invoice_ar already 989. Samples in `docs/verification/asan/phase-5-asan-*.xlsx`. Owner must open one in Asan before phase 9. Phase 6 not started. |
| 6 Wizard front end | **complete except 6.7** | 2026-08-19 | 2026-08-19 | RPC accept 3/3; typecheck 70 | No migration. Dual fee step removed from spec. `PaymentReceiptForm` deleted. OG-4 still open. Do not start phase 7. |
| **phase-6 remediation** | **COMPLETE** | 2026-08-22 | 2026-08-22 | 14/14 acceptance PASS; typecheck 70; two independent review subagents | Closes the phase-6 Gate A defects. **P6-B1** cheque receipt could not be recorded — the step-4 gate demanded an account the branch never renders and the RPC refuses; fixed against the live RPC signatures. **P6-B2** a `sales` session reached the wizard — cause measured (only the SSR path fails open, not `rolesLoading`); fixed with the in-component check this repo already uses, **the shared guard deliberately not changed** (see the new Owner-Gate). **P6-M1/M3/m1/m2** the review screen now shows the tracking number, description and all four T11 evidence-only fields, renders Jalali, and no longer claims to come from the server. **P6-m3** the receipts list now names a party from the person file, matching the Asan export — owner answer (c), **no stored name rewritten, no migration**. Evidence: `phase-6-remediation-PROGRESS.md`. |
| **G-1 remediation** — close the unauthenticated read on the `is_viewer_only` view class | **COMPLETE** | 2026-08-22 | 2026-08-22 | A1–A11 all PASS; typecheck 70; **independent review PASS on round 3** (rounds 1 and 2 both returned CHANGE REQUIRED — 8 findings, all closed) | Separate REMEDIATE mission, not a programme phase. Migration **370**. **The leak was total, not partial** — anon's row count equalled the owner-eye count on every view: `vw_account_balances` served a bank name and a 10,289,000,000 IRR balance, `publish_recipients_view` all 24 staff names and roles, `v_promotion_suggestions` 19,880 rows, `product_computed_prices_public` 588. Cause: the eight views are SECURITY DEFINER (PostgreSQL's default) owned by `supabase_admin`, so base-table RLS never reaches the caller, and `is_viewer_only(NULL)` returns false. Fix: `REVOKE ALL … FROM anon` on the six that held a grant (it was `arwdDxt` — anon held INSERT/UPDATE/DELETE/TRUNCATE too, not just SELECT), plus `security_invoker=true` on the **two** views measured to cost no authenticated reader anything. The other four were left alone because `security_invoker` demonstrably breaks a live reader on each. Owner's pre-flight answer was **(د) "you choose"**, which the mission's pre-declared rule translates to (الف) + selective (ب), with (ج) raised as a gate rather than taken. Also resolved a Gate A loose end: the two `v_dynamic_*` 401s were **not** protection — anon held full grants and was blocked only by a missing `EXECUTE` on `_capital_alloc_used`. **Raises OG-25 through OG-28.** **Independent review returned CHANGE REQUIRED and found a real blocker, now closed:** 370 broke `/api/public/products`, an unauthenticated CORS-`*` server handler that reads `product_computed_prices_public` with the anon key and no session — the Phase 0 search enumerated only the flat `api.public.bot.*.ts` filenames and missed the nested `src/routes/api/public/` directory. Fixed **without restoring the anon grant**: the price lookup alone runs as the service role, while the `products` query stays on the anon/RLS path under the deliberate `public_api_read_active_products` policy, so the service-role client never chooses the public surface. Endpoint re-verified `HTTP 200`, 199 products, 193 priced. The review also proved 370's own `DO $chk$` gate would pass over three wrong end states (counted `security_invoker` without checking *which* views, tested `grantee='anon'` instead of `has_table_privilege` so a `PUBLIC` grant was invisible, and covered only 4 of the 6 revoked views when checking `authenticated`) — **migration 371** replaces it, asserting by name and by effective privilege, and each perturbation was re-run against it and is now caught. Evidence: `g1-anon-view-leak-PROGRESS.md`. |
| **OG-25 remediation** — close the anon default-privilege tap | **COMPLETE** | 2026-08-22 | 2026-08-23 | A1–A12 all PASS; typecheck 70; **independent review PASS on round 5** (rounds 1–4 all returned CHANGE REQUIRED — 20 findings, all closed) | Separate REMEDIATE mission, not a programme phase. Migrations **373–376**. **Closes the root cause G-1 identified and deliberately did not fix.** `pg_default_acl` granted `anon` `arwdDxt` on every new table/view and `rwU` on every new sequence in `public`, so G-1 was never a mistake on eight views — it was the schema default, and those eight were merely where someone looked. Headline result, measured both ways with the same probe: before, a freshly created view came out with `anon SELECT = true`; after, `false`, and its `relacl` has no `anon` entry at all. Owner-scoped: **TABLES and SEQUENCES only — `FUNCTIONS` explicitly excluded**, and migration 375 asserts that entry is *still present* so over-reach fails the gate. **Zero existing objects revoked**, proven by md5 of every `relacl` in `public` being identical across all four migrations. The explicit grants (374, 376) name their consumer route and are catalogue no-ops by design — what changes is the record, so the batched REVOKE the owner deferred knows what to keep. **The public-surface list was got wrong once and corrected by changing method, not by repeating it:** a per-route-file classification missed `register.tsx` → `lib/profile-fields/queries.ts` → `profile_field_definitions`, which a transitive import walk found; migration 376 covers it. Both conclusions of the owner's input analysis were confirmed — all eight `api.public.bot.*` routes are service-role, and the anon-dependent surface list was otherwise exactly the two named. Audit: `docs/research/anon-grant-audit.md`, report-only, no REVOKE. **Raises OG-30, OG-31, OG-32.** Evidence: `og25-anon-default-privileges-PROGRESS.md`. |
| 7 OCR | not started | | | | Needs OG-5 (HTTPS) |
| 8 Integrated verification | not started | | | | |
| 9 Production | not started | | | | Needs OG-6 |
| **PV-remediation** — close legacy payment-voucher write path | **Phases 0–3 COMPLETE.** Phase 4 NOT started | 2026-08-21 | 2026-08-21 | T-3.1 6/6; T-3.2 PASS; T-3.3 4/4; T-3.4 PASS; **T-3.5 not run — see below** | Separate REMEDIATE mission, not a programme phase. Merged as **PR #328**, merge commit **`5b65f60`**. Migrations **368** (drops `payment_vouchers_insert_finance`; direct INSERT now `42501`, `create_payment` unaffected) and **369** (`vw_account_balances` + `get_account_ledger` read `journal_lines`; a journal-less voucher moves the figure by `0.00`). **T-2.4 resolved by owner decision D22 — read-only history, not deletion**, because the page is the only list of payment vouchers in the app; its original acceptance assumed full deletion and is corrected in `payment-voucher-remediation-PROGRESS.md` next to T-2.4. Ground truth `ground-truth.md` §13; decisions **D19–D22**. **T-0.2 measured 0 legacy rows** — Owner-Gate 8 never triggered. **T-1.4 zero-diff proven** before either migration was written. Deferred items in `deferred.md`. |

### PV-remediation T-3.4 / T-3.5 (2026-08-21)

**T-3.4 — PASS.** On the merged tip (`staging` @ `5b65f60`, confirmed equal to `origin/staging`):

- `npm run typecheck` → **70**, the D14 baseline.
- CI's failing `Staging Check` was pulled and compared set-by-set against the recorded baseline, not
  by count: **70 unique errors, identical sets**, and **zero errors in either file this mission
  touched** (`src/routes/_app.accounting.payment-vouchers.tsx`, `src/lib/treasury/queries.ts`). That
  comparison mattered here in a way it does not for a docs-only PR, because this branch did change
  TypeScript.
- **Boundary Guard: pass** on the merged PR #328.
- Drift: `git diff --name-only e1fd2ce6 origin/staging` → exactly **11 files**, every one of them
  this mission's. Nothing outside scope.

**T-3.5 — NOT RUN. Remaining manual step for the owner.**

The shared working tree was not clean: **8 untracked entries, none created by this mission** —
`audit/`, `scratch/`, `docs/audits/7-eg-checklist-mission.md`, and
`docs/research/_a_frontend.md` … `_e_export_sql.md`. This mission's own paths were fully committed.
`deploy/lan/build.ps1` (lines 61-67) refuses to build a dirty tree, and **`-Force` was not used** —
the same decision recorded for the earlier deploy attempt in this file.

**The consequence is live and worth stating plainly.** The migrations were applied directly to the
test database, so `192.168.170.8:3100` is currently in a split state:

```
APP_GIT_SHA on afrakala-lan-web : 477c0eda   (the phase-6 wizard merge)
staging tip                     : 5b65f608

database, right now:
  INSERT policies on payment_vouchers      = 0      (368 live)
  vw_account_balances reads journal_lines  = true   (369 live)
  get_account_ledger  reads journal_lines  = true   (369 live)
```

The deployed UI still shows the «سند پرداخت جدید» button on `/accounting/payment-vouchers`, while the
write path behind it is closed. A user who fills that form gets **`42501`** on submit. This is the
ordinary migrations-ahead-of-code window, but it is user-visible, so it is recorded here rather than
left to be discovered. It closes as soon as the tree is clean and the `web` service is rebuilt.

### OG-24 (raised 2026-08-22) — the shared route guards fail open under SSR

Measured during the phase-6 remediation with a session whose only role is `sales`:

```
A) full page load   -> /accounting/payment-vouchers   denied = false
B) client-side nav  -> /unauthorized                  denied = true
```

`resolveAuthWithRetry` in `src/lib/rbac/route-guards.ts` opens with
`if (typeof window === "undefined") return null`, and `requireAnyRole`,
`requirePermission` and `requireAdmin` then each return `{user: null, roles: []}`
**without throwing** — so the server-rendered page is delivered and the initial route is
never re-checked on the client.

**Correction, 2026-08-22, from the phase-2/3 independent review (its D3).** This entry
first said "only this SSR path fails open; the `rolesLoading` path denies correctly."
**That was wrong.** Every guard carries a *second* non-throwing return:

```ts
if (auth.rolesLoading || auth.profileLoading || auth.loading) return { user, roles: auth.roles };
```

That is a client-side fail-open as well. The remediation's measurement never reached it
because the roles were already warm in that session. The defect is therefore wider than
this gate first recorded: **two** fail-open paths, not one.

**Exploitability, measured by the same review — this is UI exposure, not privilege
escalation.** A sales-only session on a fail-open route sees chrome, filters and «فیشی
یافت نشد» — no rows — because RLS holds the data back and `create_receipt` enforces the
same boundary server-side (`42501`). That is why the review capped it at MEDIUM rather
than HIGH. It does not make the hole acceptable; it means the owner is choosing when to
fix a visibility defect, not racing a data leak.

**The named remedy — added 2026-08-22 after the final independent review refused to let this
gate close without one.** "Fix it in the guard" is not the same as "deny during SSR", and the
remediation's argument knocked down a fix nobody proposed. The obvious remedy never touches
the SSR path at all:

> Lift the hand-written client-side check into a shared `<RequireRoles roles={…}>` wrapper —
> or a `useRoleGate(roles)` hook — that holds while `rolesLoading`, reports `rolesError` as a
> load failure rather than a denial, and renders a denial otherwise. Apply it once in the
> `_app` layout, or per route as a one-line wrapper. It is the same ~15 lines already written
> by hand in `_app.accounting.receipts.create.tsx` and in `_app.admin.asan-export.tsx`,
> generalised. Mechanical, and it covers all 150 routes rather than one.

Whether a single `_app`-level application works in this TanStack Start version is **unproven** —
it needs a prototype. That prototype is the first task of the scoped mission, not a reason to
defer again.

**The honest summary of the deferral.** Both independent reviewers judged the *decision* sound
and the *stated reasoning* not: deferring a 150-route refactor out of a four-file remediation is
correct scoping, but it was argued from a false choice, and it leaves `/accounting/receipts` —
a route that same remediation edited — fail-open one directory from the route it hardened.

**Still live on a full page load for a sales-only session** (measured):

```
/accounting/receipts            denied=false
/accounting/receipts/training   denied=false
/accounting/payment-vouchers    denied=false
/accounting/treasury            denied=false
```

**Blast radius: 150 route files** — 62 `requireAnyRole`, 73 `requirePermission`,
15 `requireAdmin`.

**Why the remediation did not fix it there.** `ensureAuthReady` reads the session from
browser storage (`src/lib/auth/session.ts:315`), so the server cannot see who the caller
is. A server-side deny would redirect **every legitimate user to /login on their first
page load**. The wizard route was fixed with the in-component check this repository
already uses in `_app.admin.asan-export.tsx` and five other routes; the shared guards
were left alone deliberately.

**Owner decision needed:** whether to make the guards fail closed — which needs an
architectural answer for how a guarded route defers rendering until the client can
check — as its own scoped mission with its own Gate A. Three of the four accounting
routes tested still show no denial on a full page load.

## Owner-Gate log

| Gate | Asked | Answered | Answer |
|---|---|---|---|
| OG-1 A1–A4 confirmed | | 2026-08-18 | CONFIRMED — ledger-decisions.md:155 |
| OG-2 delete dead posting path | | 2026-08-18 | CONFIRMED — owner authorised the drop |
| OG-3 `invoice_ar` Asan code | | 2026-08-18 | ANSWERED — asan_control_accounts.invoice_ar = 989 (verified live) |
| OG-4 canonical phone format | 2026-08-18 | | **OPEN — and the owner's 2026-08-23 answer to M2 does NOT close it.** That answer settles the *search*: normalise all three forms (`09121234567`, `9121234567`, `+989121234567`) and **rewrite no stored identifier**. It does not name a canonical stored format, which is what this gate asks. Recorded in `ledger-decisions.md` Part 5. Task 6.7 may still be ticked on the search acceptance alone. **OPEN.** Blocks **task 6.7** (party lookup by Asan code or mobile) and mission M2. The owner must decide one canonical stored form for a mobile number — `09121234567`, `9121234567` or `+989121234567`. Measured 2026-08-23: `normalize_identifier` and `person_find_by_identifiers` are already live and `src/features/ledger-wizard/lookup.ts` already calls the latter, so the lookup may already satisfy 6.7's acceptance and only the format decision is missing. **What an agent must not do while this is open:** rewrite stored identifiers. Normalising at the query boundary is reversible; rewriting the data is not, and it is the same mistake owner decision (ج) was taken to avoid on the customer-name divergence. |
| OG-5 HTTPS on the test server | 2026-08-18 | | **OPEN, and it is the widest block in the programme.** Without a Secure Context the browser withholds `getUserMedia`, `crypto.randomUUID` and `crypto.subtle`. Blocks **all of phase 7** and the upload leg of **M1**. Owner options: split-horizon DNS, Let's Encrypt DNS-01, or WireGuard. **There is no polyfill and no workaround** — an agent that finds one has found a way to be wrong. Work that does not need a Secure Context (M1's attachment *ordering*, which is pure database and RPC) proceeds; the browser upload leg is marked `[U]` until this lands. |
| OG-6 authorise production | 2026-08-18 | | **OPEN.** Blocks **phase 9 entirely**. `main` is still at `99f6bd5`, before this programme began and roughly 45,000 lines behind `staging`. Two things are unmet independently of the authorisation itself: the owner has not yet opened a generated `.xlsx` inside Asan (OG-35), and 16 of the 45 applied migrations are deliberately absent from `schema_migrations` because their effect is not provable — so a production replay must run `supabase/migrations/` **whole and in order**, never gap-driven. See `bookkeeping-record-reconciliation-PROGRESS.md`. |
| OG-35 open one generated `.xlsx` inside Asan | 2026-08-23 | | **OPEN — this is phase 5's exit condition and it has never been met.** No test anywhere in this repo can prove Asan accepts the file; only the owner opening one in Asan can. Samples exist at `docs/verification/asan/phase-5-asan-*.xlsx`, generated from the live RPC. **Do not import the old concatenated sample** — take a fresh trial export from `/admin/asan-export`. Recorded as a gate row 2026-08-23; it existed in prose in this file but had no row, so it was invisible to anyone reading the log. |
| OG-36 activate `test.manager` and `test.viewer` | 2026-08-23 | **2026-08-24 — OVERTAKEN BY EVENTS** | **THE PREMISE OF THIS ROW IS NO LONGER TRUE, and several other rows lean on it.** It recorded both accounts as `status='rejected'`, which is why four missions wrote `NOT TESTABLE` against anything needing a `manager` or `viewer` session. Measured 2026-08-24 during the R9 diagnosis: **both are `active`**. `test.viewer@afrakala.local` has `profile_status=active`, `pwd_updated=08-24 18:06`, `last_sign_in=08-24 18:06` — it signed in successfully. `test.manager@afrakala.local` is `active` with `last_sign_in=07-19 09:04`. **Every `NOT TESTABLE — status=rejected` note in this programme is therefore wrong from 2026-08-24 onward**, including the ones in the M4, M6 and M9 progress files and the R1/A7 rows of their acceptance tables. Those measurements were honestly reported against the state of the day; they are now stale and re-taking them is a decision, not a correction — nobody has established that the accounts were activated deliberately or by a side effect of the session-generation script that ran the same evening. **A second thing this row was used to justify is also false:** the claim that the shared password had been reset to an unknown random value. `e2e/security/viewer-restrictions.spec.ts` logs in as `test.viewer` through the auth API with the in-repo password and asserts `status === 200`; it ran 40 times in the 2026-08-24 suite and failed zero times. Original text follows. | **OPEN.** Measured 2026-08-23: both accounts exist with the right roles assigned (`manager`, `viewer`) but `profiles.status = 'rejected'` for both. The `_app` layout redirects a non-active profile **before any route guard runs**, so one third of the role matrix cannot be exercised at all — this is what makes task **8.4** partly untestable, and it is why every mission in this programme records `manager`/`viewer` as `NOT TESTABLE` rather than as passing. **An agent may not change a user's account status** — that is business data. Two clicks in the admin panel. |
| OG-37 create a `bank_accounts` row with `account_type='cash'` | 2026-08-23 | | **OPEN.** Measured 2026-08-23: `bank_accounts` holds exactly **one** row and its `account_type` is `bank`. There is no cash box. So the **cash branch of all three document types has never been exercised end to end**, in any phase, by any test. Roughly two minutes from the admin panel; an agent may not create it, because it is business data. Related and also owner-supplied: there are **zero** endorsable cheques (`payment_receipts` with `document_channel='cheque'` and `reversed_at IS NULL` = 0), so the endorsed-cheque branch is equally unexercised. Both are prerequisites for phase 8. |
| OG-8 drop orphaned trg_post_receipt_on_approve? | 2026-08-18 | **2026-08-25 — ANSWERED YES by the owner (M8 scope), and CLOSED by migration 391** | **CLOSED — dropped.** First, a correction the chain documents carried for three revisions: **it is a trigger FUNCTION, not a trigger.** `pg_proc` holds one row of that name (returns `trigger`, SECURITY DEFINER, oid 49318); `pg_trigger` holds none. Its trigger was `trg_payment_receipts_post_journal`, dropped by migration 336 on 2026-08-09 — so `pg_get_triggerdef` had nothing to prove and `pg_get_functiondef` was the right instrument. **Zero callers, measured in four independent directions, each returning zero:** triggers whose `tgfoid` is this function; other `pg_proc` bodies naming it (`prosrc ILIKE`); `pg_depend` entries referencing it; and references in `src/` (whole-tree grep — the only migrations naming it are the one that created it and 336). **And it was worse than unreferenced: it was broken.** Its body called `public.post_receipt_journal(NEW.id)`, which 336 also dropped — that function returns 0 rows from `pg_proc`. It could not have worked if re-attached; a direct call returned `0A000`. `phase-1-GATE-A.md` called it *a loaded gun* for exactly this reason. Dropping it also removes one more anon-executable SECURITY DEFINER function, the door class OG-31 is about. Rollback `docs/verification/391-down.sql` was written from the captured live body and dry-run proved forward-then-back with the body **byte-identical** and the acl set identical. Evidence: `docs/execution/m8-orphan-fn-and-viewer-restricted-PROGRESS.md`. |
| OG-9 should the document serial reset each Jalali year? | 2026-08-18 | **2026-08-23 — ANSWERED by the owner; evidence recorded 2026-08-25** | **ANSWERED: do not reset the serial** (`ledger-decisions.md` Part 5, given 2026-08-23; the brief's fallback was the same, so this is a confirmation rather than an override). **And the decision costs nothing, because a per-year reset is not merely absent from the mechanism — it is unrepresentable in it.** The serial is `asan_export_numbers`, minted by `asan_assign_document_number` (migration 290) as `COALESCE(MAX(asan_number),0)+1 WHERE doc_type = _doc_type`. The predicate is `doc_type` alone; the table carries no year column; both unique constraints are `(doc_type, source_id)` and `(doc_type, asan_number)`; the advisory lock key is `hashtext('asan_export_numbers:' || _doc_type)`. **Three separate sites would have to change to introduce a reset**, one of them a constraint on a table whose design note is *numbers are never reused, never renumbered, never reordered*. Honouring the decision is zero lines; reversing it would be a schema migration. **The instruction that reached this mission named `document_serial_counters` and `next_serial` — neither exists anywhere in the repository (0 hits, whole tree, both names). See OG-59.** Evidence: `docs/research/m12-serial-and-module.md`. **CONFIRMED LIVE 2026-08-25 (mission 0-LOCAL) — PROVISIONAL struck.** `pg_get_functiondef('public.asan_assign_document_number')` on the live `afrakala` database returns `SELECT COALESCE(MAX(asan_number),0)+1 ... WHERE doc_type = _doc_type` with **no year anywhere in the body**, and the lock key `hashtext('asan_export_numbers:' || _doc_type)` — the live body matches migration 290, so A5.28's drift warning does not bite here. **And the answer is broader than this row recorded:** a SECOND live mechanism exists, `document_numbers` + `assign_document_number` (migration 338), and **it is the one whose own header comment raised OG-9** (338 only mentions the asan function in prose). Its live minting statement is `COALESCE(MAX(serial),0)+1 WHERE doc_type = _doc_type`; the Jalali year appears in the **display string only** (`RCP-1405-000042`), never in the predicate. Its constraint `document_numbers_serial_unique_per_type UNIQUE (doc_type, serial)` carries no year column, so a per-year reset would collide in year two — unrepresentable there too, exactly as this row already argued for `asan_export_numbers`. The owner's answer therefore holds across the whole serial surface, not half of it. Evidence: `docs/research/0local-live-confirmation.md`. |
| OG-10 cheque counterparty may be an external party? | 2026-08-18 | **2026-08-18** | **ANSWERED: YES, both directions.** A cheque received may come from a non-customer; a cheque issued may go to a non-supplier. Implemented in migration **347**: `validate_journal_line_ref` accepts `external_parties` for both `cheque_receivable` and `cheque_payable`. **Closes Gate A M6** (the receipt-side mirror) by the same migration. Design choice (a) - existence in any allowed table - recorded with reasons in phase-1-PROGRESS.md. |
| OG-11 post_receipt_accounting back-fill vs immutability | 2026-08-18 | | raised in task 1.6 |
| OG-12 is 'ledger-documents' the right module string? | 2026-08-18 | **2026-08-23 — ANSWERED by the owner; evidence recorded 2026-08-25** | **ANSWERED: do not rename the module** (`ledger-decisions.md` Part 5, 2026-08-23). **The row's own fear is measured and does not obtain.** Migration 344 raised this asking *if phase 6's wizard registers a different one, THAT string will be unseeded and therefore open to every role*. Measured: **phase 6's wizard registers no module string at all** — `src/features/ledger-wizard/` contains no `has_dynamic_permission`, `hasPermission` or `module:` call of any kind. There is no competing name, so there is no open door. **A full census was run in both directions.** Forward: every module referenced in `src/` has a seed row — the one apparent gap (`hr`) was a **false positive of the census method itself** and is recorded because nearly reporting it was the error; migration 315 seeds it with a `SELECT`-form INSERT that the `awk` range terminated early on. Reverse: **`ledger-documents` is the only one of the 28 seeded modules with ZERO consumers in `src/`** — seeded by 344, amended by 352 (`manager` gains `can_view`/`can_create`), and read by nothing. That is not this gate's question but it is this gate's discovery, raised as **OG-57**. Renaming a string with no readers is pure risk for no benefit, which is a second and independent reason the owner's answer is right. The wizard is **not** unguarded: its route carries `requireAnyRole(['admin', 'accountant','manager'])` plus M6's `staticData` gate, and 352 aligned the seed with those same three roles. Evidence: `docs/research/m12-serial-and-module.md`. **CONFIRMED LIVE 2026-08-25 (mission 0-LOCAL) — PROVISIONAL struck.** `SELECT DISTINCT module FROM role_permissions` returns 28 modules and `ledger-documents` is among them. **No competing ledger-wizard module string exists:** `grep -rnE "has_dynamic_permission|hasPermission|module:" src/features/ledger-wizard/` returns nothing, and the only `ledger*` literals in `src/` are three React Query **cache keys** (`ledger-wizard-accounts`, `-held-cheques`, `-proformas`), confirmed by reading their `useQuery({...})` calls rather than by name. The two-way census re-ran live under one collation (`LC_ALL=C`; psql `ORDER BY` and shell `sort` disagree on the hyphen, which produced a spurious diff on the first attempt and was corrected): **in `src/` but not seeded → empty**, so no module falls through to the fallback; **seeded but no consumer → `ledger-documents` alone**, confirming OG-57 unchanged. Evidence: `docs/research/0local-live-confirmation.md`. |
| OG-13 should manager get can_view on ledger-documents? | 2026-08-18 | **2026-08-18** | **ANSWERED — option (a). CLOSED.** The boundary stands as migration 346 applied it: create = `admin`, `accountant`, `manager`; read the numbering ledger = `admin`, `accountant`. No migration needed. `create_receipt` uses `has_any_role(_uid, ARRAY['admin','accountant','manager'])`, matching `assign_document_number`. Proved end to end in task 2.8: manager creates successfully through gate → numbering → receipt → links → entry → credit → audit, which is what Gate A's M3 said would break. **Correction 2026-08-18 (Gate A M3):** this row was premature — two of OG-13's four surfaces still carried the old answer when it was written. Migration **352** applied answer (a) to both: `document_numbers_select_finance` now admits `manager`, and `role_permissions('ledger-documents','manager')` is `can_view=t, can_create=t`. Both verified in the live catalogue. OG-13 is closed on all four surfaces. |
| OG-14 build reverse_document, or an audited escape hatch? | 2026-08-18 | **2026-08-19** | **ANSWERED — option (b), and CLOSED 2026-08-19 by migrations 363 and 364** (Gate A remediations in **365**). Opposite posted entry; original immutable. Role gate narrowed by OG-22 / 365. |
| OG-15 add viewer_restricted to the two new tables? | 2026-08-18 | **2026-08-18 → 2026-08-26 — CLOSED** | **CLOSED, and fully discharged.** OG-15 named two tables; one was real and is done, the other never existed. **`document_attachments`** — `viewer_restricted` applied by migration **391**, matching the 91-table house pattern field for field. Reported honestly at the time as **defence in depth**: the viewer already read nothing there (permissive `document_attachments_select` admits only admin/accountant/manager), so 391 added `FOR ALL` write coverage and restrictive semantics rather than removing current access. **`document_audit_log`** — **never existed; the name was fabricated.** See **OG-60**. Nothing is owed on it. **`audit_logs`**, the plausible-looking candidate had anyone been tempted to substitute it, **already carries `viewer_restricted`** and is owed nothing either — so no table is left uncovered by OG-15's actual intent, whatever that intent was. **Separately, and NOT as a reconstruction of the fabricated name:** migration **392** put `viewer_restricted` on **`document_status_history`**, chosen on independent measured evidence — a viewer-only account could **read** the status history of any document it uploaded (`d.uploaded_by = auth.uid()`, measured 1 row) **and INSERT into that history** (`{public}` policy checking only `changed_by`, measured `INSERT 0 1`). Both are now closed; the write is denied by policy name. That is a real hole closed on its own merits, not an OG-15 deliverable. Evidence: `docs/research/og60-status-history.md`. |
| OG-16 what does a receipt from a non-customer credit? | 2026-08-18 | **2026-08-18** | **ANSWERED — CLOSED. Superseded by owner decision T10.** The gate offered three options (a) `external_party`, (b) a new `person_credit` kind, (c) require promotion to a customer first. T10 replaces all three: **a person has one file and one balance (T9), and the sign of that balance decides the direction.** If they owe us, a receipt reduces what they owe; if we owe them, it increases what we owe. The user is never asked what the money is for. A friend or relative lending money is not a special case — they become a creditor under the same rule. **Consequence recorded, not patched:** `create_receipt` takes `p_customer_id` and always credits `customer_credit`, which is now known to be too narrow. The fix belongs to the T9 research, not to a patch on the RPC — a second narrow path is worse than one. Full text in `ledger-decisions.md` § T10. |
| OG-17 the credit hold half was never built - should it be? | 2026-08-18 | | **STILL OPEN. Question restated a SECOND time on 2026-08-19, now from measurement.** It began as "a receipt allocated to a proforma is counted twice" (Gate A **M1**), became "is the hold/release symmetry actually maintained" once the owner confirmed the model, and is now: **given that the hold half was never built - should it be built, and if so, in which phase?** **The owner's model stands and the behaviour is correct:** credit is a **revolving limit, not a wallet** - finalising a proforma consumes the limit and paying restores it, so a receipt raising available credit is releasing a consumed limit, not creating money (`ledger-decisions.md` **Part 4**). **What the T9 research measured (`bc0ddafc`) is that one side of the symmetry does not exist.** CHECK: built and running - `create_sales_quote_with_items` reads `get_customer_dynamic_credit`, and `audit_logs` holds **6** `credit_limit_blocked` rows. **HOLD: never built** - `hold_credit` has **zero** SQL callers and appears in `src/` only in generated `types.ts`; all **11** `customer_credit_balance` rows have `held_credit = 0.00` with **0** rows holding anything; **none of the 9 `sales_quotes` triggers touches credit**; `create_sales_quote_with_items` writes a jsonb snapshot but never calls `hold_credit`, never writes `held_credit`, never writes `customer_credit_ledger`. RELEASE: built and running - `increase_credit`. So the system **checks the limit, never reserves against it, then releases against it on payment**. That is why `available_credit` behaves as a monotonically increasing total of receipts, and why M1's measurement looked like a double count. **Not a defect to fix - a decision about what the limit should do.** Deliberately NOT answered. |
| OG-18 does a cheque payment reduce the bank balance on the day it is written? | 2026-08-19 | **2026-08-19** | **ANSWERED — option (a). CLOSED by migration 359.** A cheque does not move the bank balance until it clears. `vw_account_balances` and `get_account_ledger` now exclude `document_channel = 'cheque'`, the direct mirror of what migration 350 did on the receipt side of the Asan export. The LEDGER was always correct — `create_payment` credits `cheque_payable`/`cheque_receivable` and `create_receipt` debits `cheque_receivable`, never `bank` — so 359 aligns the two views WITH the ledger and changes no posting. **Fixed symmetrically:** the identical defect existed on the receipt side (a cheque receipt RAISED the displayed balance), and leaving it would have recreated exactly the asymmetry Gate A objected to over OG-20. **Cash is deliberately NOT excluded** — a cash box is a `bank_accounts` row (D2) and a cash payment really does move it. Verified both directions: cheque payment `total_out` 0 -> 0 (Gate A measured 900,000 here), bank payment 0 -> 250,000; cheque receipt `total_in` unchanged, bank receipt +111,000. |
| OG-19 nothing posts the OTHER side of supplier_payable or customer_credit | 2026-08-19 | **2026-08-19** | **ANSWERED — option (b): THE LEDGER RECORDS MONEY MOVEMENTS ONLY. Purchases and sales do NOT post.** Recorded as owner decision **T14** in `ledger-decisions.md`. The owner will complete the purchase and sales side later, **separately from this programme**. So `supplier_payable` accumulating debits with no credits, and `customer_credit` credits with no debits, is **by design — not an absent counter-posting, and not to be "fixed"**. It follows that `person_settlement_position` and every ledger-derived balance shows **money moved, not the party's full position**: the party with 13,000,000,000 Toman of received purchases reads `balanced` because the purchase was never a ledger event. **This confirms two earlier judgements:** phase 3's contradiction **C5** and its **refusal to invert the sign convention** were both correct — the convention was never inverted and the one-sided accumulation is intended. **BINDING ON PHASE 5:** no phase-5 export or report may present a ledger-derived figure as a party's total balance or total debt; a task needing a full position must raise an Owner-Gate rather than sum the ledger. **STILL OPEN AND UNASSIGNED:** where the complete figure comes from (T9 says one balance; T14 says the ledger holds only part of it) is deferred with the purchase/sales work — no phase owns it. |
| OG-20 payment_vouchers has no delete guard while payment_receipts does | 2026-08-19 | **2026-08-19** | **ANSWERED - CLOSED. Built, not deferred.** Phase 3 deferred this to OG-14 on the grounds that a second stopgap is not the cure; phase-3 Gate A pushed back - it is migration 353's trigger with two identifiers changed, and phase 3 opened the path that made the failure reachable. The owner agreed with Gate A. **Migration 357** adds `trg_payment_vouchers_block_delete_when_posted`, mirroring 353 exactly. Verified with the same M8 probe used on the receipt side: `DELETE REFUSED sqlstate=P0001`, orphaned voucher entries **0**, and `phase-3-stress-cleanup.sql`'s entries-before-vouchers ordering re-proved to still work. This does not pre-empt **OG-14** - `reverse_document` remains the cure for both sides; it stops one more orphan being creatable in the meantime. |
| OG-21 is a صراف who is paid a fee an account holder? | 2026-08-19 | **2026-08-19** | **ANSWERED and CLOSED, 2026-08-19: there is no fee at all.** AfraKala does not charge or record one. The question as asked does not arise. Phase 4's C-c reading (a paid صراف is a third account holder, third journal line on `('external_party', intermediary_id)`) is **overturned**, not because the implementation was faulty but because the business rule it implemented does not exist. صراف / واسط / شخص ثالث / نفر سوم / طرف سوم are the same **record-only** class as the transferrer and recipient. Migration **362** dropped the fee columns, the fee parameters, and the third line. The original C-c write-up is kept in `phase-4-PROGRESS.md`. |
| OG-22 may a manager reverse a posted document? | 2026-08-19 | **2026-08-19** | **ANSWERED and CLOSED by migration 365: accountant and admin only; manager excluded (`42501`).** Create stays OG-13's wider set. **Interim, not final:** the owner said access control will be built in a dedicated phase where every module's permissions are set by role. Recorded in `ledger-decisions.md` so that phase revisits this array. |
| OG-23 freeze party/amount on a posted source row? | 2026-08-19 | | **OPEN.** Migration 343 made the journal immutable; UPDATE policies on `payment_receipts`, `payment_vouchers` and `dual_documents` have **no column list**. Accountants still UPDATE posted receipts (OCR apply of amount; approve/reject `status`; `reverse_document` metadata). Credit unwind no longer reads the mutable party (365). Freezing is a business question — continue; do not idle. |
| OG-24 the shared route guard fails open on a full page load | 2026-08-19 | **2026-08-24** | **PARTIALLY ANSWERED by M6 — the thirteen accounting routes are closed; the other 136 guarded routes are not, and that is OG-41.** Both fail-open paths were confirmed live and both remain in `route-guards.ts`, which M6 did not edit: `resolveAuthWithRetry` returns `null` when `typeof window === "undefined"` and every guard returns without throwing, and every guard also returns on `rolesLoading || profileLoading || loading`. Measured with the project's stored `sales` session: **12 of 13 accounting routes rendered in full on a cold page load** and 3 of 4 rendered while roles were in flight, while client-side navigation correctly redirected on all 13. **The brief's premise was wrong about the worst case:** `/accounting/receipts/create`, the document wizard, was ALREADY closed by the phase-6 remediation and was the only one of the fourteen that was. **Not fixed on the server, deliberately** — `ensureAuthReady` reads the session from browser storage, so an SSR deny sends every legitimate user to `/login` on their first page load, which is the regression phase 6.7 had to undo in three routes. The remedy is a `RouteRoleGate` in the `_app` layout that enforces the check on the client, fed by one `staticData` line per route. **The obvious mechanism was tried first and does not work in this stack:** having each guard return its requirement from `beforeLoad` into router context was built, deployed and measured, and on the client every match's context is empty (`ctxKeys: []` on all three matches) — that code was deleted rather than shipped as a check that silently never fires. **Exposure remains UI-only, re-measured rather than restated:** a `sales` cold load does not even execute the route's data RPC, because the function gates itself with `has_any_role(...) … RAISE EXCEPTION 'forbidden' USING ERRCODE = '42501'`; on `bank-accounts` `sales` reads «حسابی ثبت نشده است.» where `accountant` reads the real bank row. Original text follows. | | **OPEN.** Raised by the phase-6 remediation and written up in full as a `###` section earlier in this file — but never added to this table, so every reader of the log has been one gate short since 2026-08-22. Added here 2026-08-23 by the bookkeeping mission; the section is the authority and is not duplicated. In short: `resolveAuthWithRetry` returns `null` when there is no `window`, and `requireAnyRole` / `requirePermission` / `requireAdmin` then each return `{user: null, roles: []}` **without throwing**, so a server-rendered page is delivered and the initial route is never re-checked. Measured with a `sales`-only session: full page load reaches `/accounting/payment-vouchers` with `denied = false`, client-side navigation correctly reaches `/unauthorized`. Affects 150 route files. |
| OG-25 close the schema-wide `ALTER DEFAULT PRIVILEGES … TO anon`? | 2026-08-22 | **2026-08-22** | **ANSWERED and CLOSED by migrations 373–380** (see the OG-25 remediation row in the phase table). The owner scoped it on 2026-08-22: close the future tap on `TABLES` and `SEQUENCES`, exclude `FUNCTIONS`, grant the real public surfaces explicitly, audit existing objects **report-only** with no batched REVOKE, and hand back any already-broken surface as a new gate rather than fixing it. All five delivered; the batched REVOKE became **OG-30**, `FUNCTIONS` became **OG-31**, and the two broken surfaces became **OG-32** and **OG-33**. **This row said OPEN until 2026-08-23**, when the bookkeeping mission corrected it — the mission that closed the gate recorded its own completion in the phase table and in the migration ledger but never came back to the gate log. Original text follows. **OPEN — larger than the G-1 it was found under.** `pg_default_acl` holds, for `supabase_admin` in schema `public`: tables/views `anon=arwdDxt`, sequences `anon=rwU`, functions `anon=X`. So **every future table, view and function created there is granted to `anon` automatically**. G-1 was therefore not a mistake on eight objects — it is the schema default, and those eight were merely where someone looked. Migration 370 is a point fix: `CREATE OR REPLACE VIEW` preserves the revoked ACL, but a `DROP VIEW` + `CREATE VIEW` (which migrations do whenever a column list changes) **silently re-grants anon**. Not changed autonomously because it affects every future object and some tables must genuinely stay public — the `api.public.bot.*` routes depend on that. Needs its own mission: close the default, then audit every existing object in `public`, not just views. |
| OG-26 the `is_viewer_only` guard opens instead of closing for a NULL uid | 2026-08-22 | **2026-08-24** | **ANSWERED and CLOSED by migrations 386 + 387.** All eight guard-class views now read `WHERE uid() IS NOT NULL AND NOT is_viewer_only(uid())`. Measured before, as `authenticated` with no `sub` claim: 24 / 14 / 210 / 1 rows from four of them; after: zero from all eight. **The owner chose option (ب) — the eight views only, function untouched — from a measured census of 8 views + 91 RLS policies + 1 function**, the deciding fact being that `search_visible_persons` uses the guard positively and would have flipped behaviour for a NULL-uid caller. So the 91 `viewer_restricted` policies are unchanged and **OG-26 remains open for every caller other than these eight views** — a NULL-uid `authenticated` session still satisfies `NOT is_viewer_only(uid())` in all 91. That is deliberate, it is the owner's decision, and it is stated here rather than left implied by the word CLOSED. Signed-in visibility did not move: verified per view per role, and the gate now asserts it as a data-independent property. Original text follows. | **OPEN.** Option (ج) of the G-1 pre-flight. `is_viewer_only(NULL)` returns false, so for a caller with no identity the guard **opens** instead of closing — that is the root of G-1, not a faulty RLS policy. Deliberately not taken autonomously: it changes the behaviour of all eight views plus every other caller of the function at once. Migration 370 closes the anon path without it. |
| OG-27 was G-1 reachable from outside the LAN? | 2026-08-22 | | **OPEN — `[U]`, and it cannot be closed from this session.** What was measured: `docker port` shows Kong listening on `0.0.0.0:9000` and `[::]:9000` on the test host, so any host that can route to `192.168.170.8` reaches it with the published anon key and no session. What could **not** be measured: the company router/NAT configuration, which is out of reach here and was not to be probed from production. If the answer is yes, this was a real disclosure of bank and staff data rather than an internal defect, and whether to notify anyone is the owner's call. |
| OG-34 `ALTER ROLE anon BYPASSRLS` is invisible to every grant-level control | 2026-08-23 | **2026-08-24** | **ANSWERED and CLOSED by migrations 384 + 385 - as an assertion, because a full remedy does not exist.** The gap is real and reproduced: `anon` with the attribute reads 43,516 `audit_logs` rows and the whole of `customers`, `persons` and `journal_entries`, and no gate in this programme could see it. **384 revokes nothing** - `anon` and `authenticated` were already `false` - so the entire deliverable is the assertion. **Prevention is partial, not absent, and 384's header got this wrong before 385 corrected it.** `supautils` is loaded here and reserves roles; measured as the non-superuser `postgres`, `ALTER ROLE anon|authenticated|authenticator BYPASSRLS` all fail with `42501 reserved role`, but `ALTER ROLE products_api_readonly BYPASSRLS` **succeeds**, `CREATE ROLE ... BYPASSRLS` **succeeds**, and a superuser is not constrained at all. Event triggers genuinely cannot help - `event triggers are not supported for ALTER ROLE`, because roles are cluster-wide and event triggers per-database - and `ON login` does not exist on PostgreSQL 15. So the assertion is the control for exactly the paths supautils leaves open. **Its blind spot, stated rather than buried: an assertion only fires when a migration next runs, and that interval is unbounded. See OG-40.** The two smaller siblings this row named are still open and unaddressed - `GRANT ... WITH GRANT OPTION` is not distinguished, and `GRANT CREATE ON SCHEMA public TO anon` is invisible. Original text follows. | **OPEN — outside OG-25's scope by construction, and the single most consequential gap the four review rounds surfaced.** A role attribute is neither a grant nor a default privilege, so migration 380's gate cannot see it and deliberately was not extended to. Measured 2026-08-23 inside `BEGIN … ROLLBACK`: `ALTER ROLE anon BYPASSRLS` changes no relation-level privilege, the gate prints `380 OK`, and `anon` then reads **28 customers, 84 persons and 7 journal entries**. **Why this outranks an ordinary gap:** `docs/research/anon-grant-audit.md` §3 rests its whole safety argument on RLS — all 202 anon-readable tables have it enabled, and "for 202 of 202 the grant layer does nothing". One attribute change falsifies that for all 202 at once. The check is three lines against `pg_roles` and belongs to whatever mission owns role attributes, alongside two smaller siblings the same review found and rated low because `anon` has `rolcanlogin = f` and reaches the database only through PostgREST, which issues neither DDL nor GRANT: `GRANT … WITH GRANT OPTION` is not distinguished, and `GRANT CREATE ON SCHEMA public TO anon` is invisible. |
| OG-33 should an anonymous caller learn when a product was last purchased? | 2026-08-22 | **2026-08-23** | **ANSWERED — no — and CLOSED by migration 381.** The owner decided it explicitly and said not to use the fallback. `REVOKE EXECUTE` from **both `anon` and `PUBLIC`** on `get_recent_purchase_label(uuid)` and `get_recent_purchase_labels(uuid[])`; two revokes were required because PostgreSQL grants functions to `PUBLIC` by default and 712 of the 841 functions in `public` carry that grant, so revoking from `anon` alone changes nothing. Verified live: the RPC now returns `42501` / HTTP 401 to an anon caller. **Migration 383 additionally asserts the closure by function NAME rather than by exact signature**, because the review showed an overload — `get_recent_purchase_label(text)` — reopens the leak while an exact-signature check reports OK; and with OG-31 open, any newly created overload is PUBLIC-executable by default, so that can happen without anyone writing a GRANT. The owner overrode the mission's review cap to have that specific hole closed before shipping. `authenticated`, `service_role`, `postgres` and `supabase_admin` keep explicit grants, so the three `_app` routes using the badge are unaffected. Original text follows. | **OPEN — and unlike OG-32 this path works today, so it is a live disclosure rather than a record-keeping gap.** The public sale-list page's recent-purchase badge calls `get_recent_purchase_label(uuid)` / `get_recent_purchase_labels(uuid[])`. Both are `SECURITY DEFINER` and both are executable by `anon`. Measured 2026-08-22 with the anon key and no session, against a product that has purchases: `{"hours_since":967.17,"last_purchase_at":"2026-07-13T10:01:00.667437+00:00"}` — while `GET /rest/v1/purchases` returns **401** to the same caller, because `anon` holds no grant on that table. So the definer context reaches past a table the caller cannot read and hands out procurement timing for any product UUID it can name. **This is the G-1 defect class surviving in a function instead of a view.** Migration 377 records the grants with their consumer named; it deliberately does not close them, for two reasons: whether an anonymous visitor may learn procurement timing is a business decision, and a `REVOKE … FROM anon` would achieve nothing on its own — PostgreSQL grants functions EXECUTE to `PUBLIC` by default and `proacl` here begins `=X/supabase_admin`, so `PUBLIC` must be revoked too. **Found by the independent review.** The mission had noticed the call and written it in its own progress file without acting on it. |
| OG-30 revoke the `anon` grant from the 216 existing objects? | 2026-08-22 | | **OPEN — deliberately excluded from the OG-25 mission by the owner, who scoped that mission to the future tap plus explicit grants plus a report-only audit.** The numbers are in `docs/research/anon-grant-audit.md`, measured 2026-08-22. **The census is 216 objects across `relkind` r/v/S — 204 tables, 7 views and 5 sequences** (the 5 sequences were missed by the mission's first pass and found by the independent review; they also reconcile `deferred.md`'s "216 of 224", whose original query counted all relkinds and compared the total against tables only). Migration 378 pins all 216 names, so this is a recorded keep/strip list rather than a number. The one the decision turns on: of the **209** objects `anon` may SELECT, it actually sees rows in **five** — `products` 355, `brands` 40, `categories` 12, `profile_field_definitions` 4, and one aggregate view returning zeros. For the other 204 the grant is a layer doing nothing and RLS is carrying the load alone. The keep-list is now written down rather than guessed (migrations 374 and 376 name each grant's consumer). Three things a batched REVOKE must weigh, all recorded with evidence: six objects are safe today only by accident — they error on a nested function or base-table permission, the same pattern G-1 found on `v_dynamic_*`, and they open silently if that ACL changes; five auth-bootstrap objects (`profiles`, `user_roles`, `custom_roles`, `role_permissions`, `log_event`) are reached from every public route and return zero rows, but their post-revoke behaviour is untested and they run on every page load; and 64 tables are empty for the owner too, so "zero rows for anon" proves nothing about them. One thing that got easier: after migration 373 the 211 is a **closed set**, not a moving target. |
| OG-31 close the `FUNCTIONS` default privilege (`anon=X`)? | 2026-08-22 | **2026-08-25 ANSWERED YES by the owner; 2026-08-26 CLOSED by migration 393** | **CLOSED.** The tap is shut for `public` and for `public` only. The working form is the one M3 measured and it was re-derived independently before use: a schema-scoped revoke strips `anon=X` but leaves the bare `=X` that `acldefault()` supplies, so anon keeps EXECUTE through PUBLIC (measured: `P2 anon_exec = t`); only a **global** (`defaclnamespace = 0`) row replaces `acldefault()`. **Two corrections to this row's own blast-radius text, both measured.** (1) It is not only `anon` that a bare global row strips - a new function in `extensions` came out `{supabase_admin=X}`, so **`authenticated` loses EXECUTE too**. (2) The affected schema list is **five, not four**: `pgbouncer` is missing from every previous list, and `pgbouncer.get_auth` carries `proacl = NULL`, i.e. it runs entirely on the inherited PUBLIC grant - stripping it silently is a connection-pooling outage. **So the owner decision this row called blocking did not have to be taken.** 393 pairs the global revoke with a per-schema `GRANT EXECUTE ON FUNCTIONS TO PUBLIC` in `extensions`, `graphql`, `pgbouncer`, `pgsodium`, `pgsodium_masks` and `vault`, which restores the prior default exactly and does not leak back into `public` (measured, P5/P6). The containment is asserted per schema in the gate, not argued. **Nothing existing was revoked**: all 741 currently anon-executable functions are untouched, and `CREATE OR REPLACE` preserves ACLs (asserted), so no code path changes. The batched revoke across those 741 is raised as **OG-61**; a live pricing leak found by the mandated A5.31 sweep is raised as **OG-62**. Live re-check after `docker restart afrakala-lan-rest`: a fresh function in `public` is `anon=f, supabase_read_only_user=f, authenticated=t, service_role=t`; a fresh function in `extensions` and in `pgbouncer` is still PUBLIC-executable. Evidence: `docs/research/og31-function-execute-audit.md`. Original text follows. | **OPEN — and M3 measured it rather than closing it, after getting the answer wrong twice.** It IS closable, in two statements, and the working form is **global, not schema-scoped**: `ALTER DEFAULT PRIVILEGES REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC` (no `IN SCHEMA`) plus `ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE EXECUTE ON FUNCTIONS FROM anon`. Measured 2026-08-23 inside `BEGIN … ROLLBACK`: a freshly created function then comes out `{postgres=X, supabase_admin=X, authenticated=X, service_role=X}` — no `=X`, no `anon=X` — with `anon` and `PUBLIC` both false and `authenticated`/`service_role` intact. The mechanism is that the **global** (`defaclnamespace = 0`) row *replaces* `acldefault()`, while a schema-scoped row does not; four schema-scoped variants were tried first and all failed, and the mission wrongly generalised that to "cannot be done". **Why it was still not shipped:** the global row applies to **every schema**, but **not** `auth`, `storage` or `realtime` — those are owned by `supabase_auth_admin` / `supabase_storage_admin` and a `FOR ROLE supabase_admin` row does not reach them. It reaches **`extensions` (61 functions, all 61 currently PUBLIC-executable — pgcrypto and uuid-ossp territory), `pgsodium` (119), `graphql` (6) and `vault` (3)**, where an `ALTER EXTENSION … UPDATE` that recreates a function would silently strip its PUBLIC EXECUTE. That blast radius is an owner decision, not an agent's. Original text follows. | **OPEN — excluded from OG-25 by the owner.** `pg_default_acl` still carries `public \| f \| {postgres=X/…, anon=X/…, authenticated=X/…, service_role=X/…}`, so every function created by `supabase_admin` in `public` is granted EXECUTE to `anon` automatically. Migration 373 deliberately has no `ON FUNCTIONS` line and migration 375 **asserts that entry is still present**, so an accidental over-reach here fails the gate. Needs its own measurement of what depends on it, starting with `authenticateBot` and the auth path. Related evidence from this mission: PostgreSQL also grants functions `EXECUTE` to `PUBLIC` by default — `refresh_sale_list_prices` has `proacl` beginning `=X/supabase_admin` — so revoking from `anon` alone does not remove `anon`'s ability. Any FUNCTIONS mission must handle the `PUBLIC` grant too or it will achieve nothing. |
| OG-32 the public sale-list page is already broken for an anonymous visitor | 2026-08-22 | | **OPEN — NOT repaired, per the owner's instruction. M5 measured it and the recorded description was wrong in two ways.** **(1) The page is not blank and does not error.** Loaded in a real browser with no session at all, it returns HTTP 404 and renders a clean Persian message — «۴۰۴ لیست فروش یافت نشد / این لیست وجود ندارد یا هنوز منتشر نشده است.» — which is graceful, and **ambiguous between two causes that are both wrong**: the real cause is neither "does not exist" nor "not yet published". **(2) The cause is a PRIVILEGE DENIAL, not RLS silence.** Separated explicitly, which this programme's rule 12 exists for: `anon SELECT sale_lists` and `sale_list_items` both raise `42501` (`401` over HTTP), while `sale_price_types` returns **0 rows with no error** although the owner sees 3. So there is a SECOND cause hiding behind the first — granting the two tables alone would still not render the price-type title. Published lists: **0 of 20, all `draft`**; none was created to make the page interesting. **And the denial is the only thing preventing an unauthenticated WRITE — see OG-48, which any repair must decide first.** |
| OG-38 `supabase_read_only_user` bypasses RLS and can log in - is that wanted? | 2026-08-24 | **2026-08-26 — OWNER ANSWERED IN PART: do NOT `NOLOGIN`; monitor first. STILL OPEN pending the monitoring window.** | **OPEN — and mission 4 remains CONDITIONAL on it.** Owner's answer, recorded verbatim in effect: no known BI tool or reporting script is attached to this role, but *known* is not *certain*; therefore **do not disable it**, run the monitoring option instead and report. **Monitoring is live as of 2026-08-26 11:57 Tehran.** `ALTER ROLE supabase_read_only_user SET log_connections = on` is **not possible** and this was measured rather than assumed — `ERROR: parameter "log_connections" cannot be set after connection start`, because it is a backend-start parameter, so a per-role setting never takes effect. It was enabled globally by `ALTER SYSTEM SET log_connections = on` + `pg_reload_conf()` — no restart, reversible with `ALTER SYSTEM RESET log_connections`. **Proven to capture this role**, not assumed: a probe connection appeared as `127.0.0.1 … supabase_read_only_user@afrakala LOG: connection authorized: user=supabase_read_only_user database=afrakala application_name=psql`. **That captured line is the verification probe itself and must not be counted as a real consumer** — a genuine one will appear later with a different `application_name` or source address. Read the window with `docker logs --since 24h afrakala-lan-db 2>&1 | grep supabase_read_only_user`. Cost: the setting is global, so every connection in the cluster is logged including PostgREST pooling — noise, not risk, and the price of catching short-lived connections that `pg_stat_activity` sampling would miss. **Facts for the eventual decision, measured in mission 4:** the role has **no password**, so it authenticates only through the container's `127.0.0.1 trust` line; it gets **zero rows from all eight guard views** because it carries no JWT; but it reads every base table freely via `pg_read_all_data` + BYPASSRLS (`persons` 84, `payment_receipts` 10). So the question was never the EXECUTE grant — it is whether a passwordless BYPASSRLS reader should exist at all. Original text follows. | **OPEN. Measured, not touched.** `rolbypassrls = true`, `rolcanlogin = true`, `rolsuper = false`, no `valid_until`, member of `pg_read_all_data`. It is a Supabase-managed reporting role and reading everything is its designed purpose, so M9 left it alone and allow-listed it in 384. But it was **not** in the mission brief's do-not-touch list, it holds the attribute, and it can log in - which makes it the second-most powerful credential on this server after `supabase_admin`, and the only login-capable non-superuser besides `postgres` that bypasses RLS. Whether it should keep `LOGIN`, or carry a `valid_until`, or exist at all on a self-hosted installation with no Supabase dashboard, is an owner decision. Nothing depends on the answer today; the assertion in 384 names it explicitly so a future change is visible. |
| OG-39 `products_api_readonly` is not protected by supautils | 2026-08-24 | | **OPEN - the narrow hole M9's correction exposed.** `supautils.reserved_roles` covers `anon`, `authenticated`, `authenticator`, `service_role` and the `supabase_*` roles, so only a superuser can alter them. **`products_api_readonly` is on none of those lists**, and it is a request-facing role: `authenticator` is a member of it and PostgREST switches into it by JWT claim. Measured 2026-08-24 as the non-superuser, login-capable role `postgres`: `ALTER ROLE products_api_readonly BYPASSRLS` **succeeds**. So the one request-facing role supautils does not cover can be handed an RLS bypass by a role that is not a superuser. Two ways to close it - add the role to `supautils.reserved_roles` in `/etc/postgresql-custom/supautils.conf` (a server config change and a restart, outside migration scope), or retire the role if nothing uses it. **Neither was done, because both are owner decisions about server configuration rather than schema.** Migration 385 asserts the end state either way. Note also that `CREATE ROLE <new> BYPASSRLS` is not blocked for anyone, which 384 check 1 catches by name. |
| OG-40 should `ALTER ROLE` be detected continuously rather than at deploy time? | 2026-08-24 | | **OPEN - raised by the independent review, and it names the real limit of what M9 shipped.** 384 and 385 assert at **migration time**, so the interval between someone setting `BYPASSRLS` and anyone finding out is however long it is until the next deploy. `pgaudit` is already in `shared_preload_libraries` on this server and `pgaudit.log` is `none`; setting it to `role` logs `ALTER ROLE`, `GRANT` and `REVOKE` as they happen, and it is on `supautils.privileged_role_allowed_configs`, so even the non-superuser `postgres` role may set it. **Not done autonomously:** turning on audit logging changes log volume and retention on a server whose disk budget this programme has never measured, and it is a running-system configuration change rather than a schema one. The alternative - a scheduled out-of-migration check - needs somewhere to run and somewhere to report, neither of which exists yet. |
| OG-41 should the other 136 guarded routes get the same treatment? | 2026-08-24 | | **OPEN — and unlike most rows here the cost is now KNOWN rather than estimated, which is the point of raising it.** M6 closed the thirteen accounting routes and proved the mechanism; the remaining guarded routes still fail open on a full page load exactly as measured. The surface, re-derived rather than inherited: **149 route files import a guard — 62 `requireAnyRole`, 74 `requirePermission`, 15 `requireAdmin`, all under `_app`**. The transitive walk terminates immediately: nothing outside `src/routes/` imports `route-guards`, so no hook or component carries a guard indirectly. **The cost is one `staticData` line per route** — three lines with its comment — plus nothing else; `RouteRoleGate` already handles all three guard kinds. So the decision is 136 mechanical edits against leaving 136 routes rendering to the wrong role on a cold load. **Two things make it a decision and not a chore.** First, `requirePermission` routes resolve through `hasPermissionEx` against the dynamic permission cache, and M6 exercised only the `anyRole` kind end to end — the `permission` branch of `passes()` is written but unproven against a real route. Second, a route whose `staticData` disagrees with its own `beforeLoad` silently widens or narrows access, and 136 hand-copied lists is where that happens; M6 verified its fourteen mechanically and a rollout should do the same rather than trusting transcription. An alternative worth pricing first: derive the list from `beforeLoad` at build time so the two cannot drift at all. |
| OG-42 the guarded routes M6 did not touch are still fail-open | 2026-08-24 | | **OPEN — recorded so the state is written down rather than implied by OG-41.** This is not a new defect; it is the unchanged remainder of OG-24, and it is listed separately because OG-41 is a question and this is a fact someone may need to find on its own. A user holding only `sales` still reaches every non-accounting guarded route on a full page load, and every guarded route still renders while roles are in flight. The mitigations that make this UI-only rather than an escalation are unchanged and were re-measured during M6: RLS returns no rows, the route RPCs gate themselves and raise `42501`, and `create_receipt` enforces the same boundary. **What would change the severity:** any route that reads through a `SECURITY DEFINER` function with no internal role gate. M6 did not enumerate those, and that enumeration is what should precede any decision to leave this open. |
| OG-43 the repo e2e suite is pinned to a database fixture that no longer exists | 2026-08-24 | | **OPEN — and the 41-failure baseline this row was raised with is SUPERSEDED. The baseline is now 47, of which about 3 are NON-DETERMINISTIC.** Measured on a stable environment 2026-08-24 (`r9-run3.log`, 46.6 min, 47 failed / 19 skipped / 529 passed) and diagnosed in full in `docs/research/r9-diagnosis.md`. **A set comparison, not a count:** the 41 were recovered name-by-name and compared — 40 of them still fail unchanged, 1 no longer fails (`persons/person-profile.spec.ts:121`), and 7 are new, giving 41 − 1 + 7 = 47. **Classification of all 47: A = 23 fixture-READ** (specs hardcode row counts the database no longer holds), **D = 8 environmental** (navigation and click timeouts, and the three flakes below), **E = 16 fixture-WRITE and unrelated** — and that write half is the part this row did not know about: twelve failures are the specs' own setup INSERTs being rejected, `ERROR: null value in column "doc_kind" of relation "journal_entries"` nine times and `"person_id" of relation "customers"` twice, against NOT NULL constraints added by migrations 294/297/320 in Mordad. See **OG-46**. **ZERO of the 47 are an M4 regression, proved structurally: none of the 26 distinct failing spec files references any of the eight `is_viewer_only` guard-class views — `0 of 26`.** | **A SECOND FULL RUN WAS MADE ON 2026-08-25 AND IT IS INVALID — 92 failed / 487 passed in 4.7 hours. It is recorded here so nobody mistakes it for a regression, and it is NOT the baseline. The valid baseline remains 47** (`r9-run3.log`, 46.6 min). Same build (`a19fd811` both runs, `git log ebff94e2..a19fd811` empty and the diff two doc files), same database, six times slower. Two-way set comparison was still done and is the reason the run is legible at all: **46 of the 47 still fail, 1 recovered** (`products/torob-url-and-excel:235`), **42 new** — 30 in `purchase/*`, 8 in `requirements/*` all on their first navigation, 3 in `asan/product-asan-code`, 4 in phase6. Error-signature census across all 92 blocks: **zero `42501`, zero permission denials**; 51 are timeouts. Cause is **OG-54**. Proof that migration 388 was not involved: the product family was run four times on the cleaned machine, twice with 388 reverted via `388-down.sql` and twice with it re-applied — **5 passed in every one of the four**, 13.9 to 18.7 seconds. `payment_receipts` was **10 before and 10 after** the invalid run, so nothing was consumed by it.
| OG-44 who protects `is_viewer_only` itself? | 2026-08-24 | **2026-08-26 — CLOSED by migration 393** | **CLOSED — this mission owns the function, and the gate now asserts it directly.** The row's open question was *which mission owns it*; the security-trio mission is that owner, and 393's single gate carries the assertions without touching the object (A6.34/OG-51 respected - the eight views' predicate is unchanged). Pinned: exactly **one** signature (an overload was the OG-33/383 escape and is caught - D10); `SECURITY DEFINER`; `STABLE`; owned by `supabase_admin`; `search_path` pinned in `proconfig`; **no PUBLIC EXECUTE**; and still executable by `anon`/`authenticated`/`service_role` so the guard cannot be made to fail closed across all 8 views and 93 policies. **And, unlike migration 387, it is asserted behaviourally rather than through a vacuity side-effect**: the function must AGREE with `user_roles` for every user, with a guard requiring both populations to be non-empty. `SELECT true`, `SELECT false`, and an `AND`->`OR` swap in the body all die on it (disturbances D6/D12, constructed and caught). The three sabotages the row says pass every gate in this programme - `SECURITY INVOKER`, `RESET search_path`, `GRANT EXECUTE TO PUBLIC` - were each run against this gate and each caught (D7/D8/D5). **Correction to the row's arithmetic:** the dependants are 8 views + **93** policies + 1 function, not 91 - migrations 391 and 392 each added a `viewer_restricted` policy that calls it. Evidence: `docs/research/og31-function-execute-audit.md`. Original text follows. | **OPEN — raised because M4's own gate deliberately does not.** The owner chose option (ب), so M4 changed eight views and left the function alone. That is coherent, but it leaves the function — which backs **8 views + 91 RLS policies + 1 function** — asserted by nothing. Measured during M4's gate repair: replacing `is_viewer_only` with `SELECT true` collapses every signed-in count on all six readable views to zero, and replacing it with `SELECT false` voids the viewer restriction across all 91 policies. Migration 387 catches both, but **indirectly** — through its vacuity guards, because a constant-returning function empties one of the two user populations — not through an assertion aimed at the function. Nothing anywhere asserts its security properties either: an independent reviewer showed `ALTER FUNCTION … SECURITY INVOKER`, `RESET search_path`, and `GRANT EXECUTE … TO PUBLIC` all pass every gate in this programme. **What deciding it would take:** a decision about which mission owns the function. Adding those assertions to M4's gate would have it assert an object M4 was told not to touch; adding them to a new gate would breach the one-gate cap. Recorded as `[U]` in the M4 progress file with exactly this reasoning. |
| OG-45 `supabase_read_only_user` can SELECT all eight guard-class views | 2026-08-24 | **2026-08-26 — CLOSED as far as a migration can close it (393); the residue is OG-38** | **CLOSED by assertion, and the row's severity is measured down two levels.** Three corrections, all live. **(1) There is no grant to revoke.** The role holds no entry in the `relacl` of any of the eight; its SELECT comes wholly from `pg_read_all_data` membership, so a per-view REVOKE is not an available remedy - only the membership, the LOGIN, or the EXECUTE it lacks, and the first two are OG-38. **(2) "One GRANT away" is exact for six of the eight.** `v_dynamic_customer_capital_balances` and `v_dynamic_salesperson_capital_balances` stop on `_capital_alloc_used(text,uuid)` first, so those two are two grants away. **(3) The prize is empty.** With BOTH grants made inside `BEGIN … ROLLBACK`, all eight returned **0 rows** - the role connects with no JWT, so `auth.uid()` is NULL and migration 386's `uid() IS NOT NULL` predicate closes every one of them. That is **OG-51's mechanism at a different door: M4's OG-26 fix incidentally neutralised this gate's data exposure.** The control that keeps it honest: the same role read `persons` 84 rows and `payment_receipts` 10 rows from the base tables in the same session. It is a full data-read capability; it just has no need of these views. **What 393 does:** pins the absence of `EXECUTE ON is_viewer_only` for this role, and the absence of a PUBLIC grant that would confer it without naming the role (both attacked and caught - D4/D5). **Coupling to record:** if OG-51 is ever answered by exempting NULL-uid callers in the eight views, **this gate becomes live**, because that predicate is the only thing withholding the rows. Evidence: `docs/research/og31-function-execute-audit.md`. Original text follows. | **OPEN — found by M4's independent reviewer, and it is one `GRANT` away from mattering.** M4's own "who can reach these views" census covered `anon`, `authenticated` and `service_role` and **missed** `supabase_read_only_user`, which holds table-level SELECT on **all eight**, including the two `authenticated` cannot reach. It is currently stopped only by lacking EXECUTE on the guard function — `ERROR: permission denied for function is_viewer_only`, identical before and after migration 386, so **M4 has no effect on it either way**. The role can log in, bypasses RLS, and is not a superuser; it is already recorded as **OG-38**, and this row is the specific consequence on this guard class rather than a new gate about the role. **What deciding it would take:** the OG-38 decision about whether that role should keep LOGIN at all. If it does keep it, the question narrows to whether it should hold EXECUTE on `is_viewer_only` — today it does not, and that absence is the only thing standing between it and all eight views. |
| OG-46 the OG-43 harness has TWO halves, and the writing half is the one nobody had named | 2026-08-24 | | **OPEN. Both halves are to be fixed by repairing the HARNESS, not by deleting data — that is the owner's instruction and it is recorded here so no future mission reads "stale fixture" as licence to clean up.** **The reading half** is what OG-43 already describes: specs assert hardcoded row counts, e.g. `e2e/asan/export-bank-deposits.spec.ts:64` expects `select count(*) from payment_receipts` to be 6 while the table holds 10. **The writing half** is new: the specs' own fixture INSERTs are rejected by NOT NULL constraints that arrived after the specs were written — `journal_entries.doc_kind` (9 failures) and `customers.person_id` (2 failures), both from migrations 294 / 297 / 320. Those surface in the log as `Error: Command failed: docker exec afrakala-lan-db … psql`, which reads like an infrastructure fault and is not one. **What the owner has already established:** all 10 `payment_receipts` rows are test data — **but 4 of them are posted and carry live journal entries, and D11 governs those**, so "they are test data" does not make them deletable. **What deciding it needs:** whether the harness is repaired by making its assertions relative rather than absolute (the honest fix, ~20 spec files, and it changes what those specs guarantee) or by giving each spec its own isolated fixture. **No spec and no row is to be modified until that is decided.** | **CLOSED for the reading half, and the writing half is closed SUSTAINABLY rather than quickly — 2026-08-25.** Reading half: 14 literal fixture-state pins (not 5 — the first count missed `dbRows(...).length` pins and every query carrying a `WHERE`), 12 of which failed live, each now computed from an independent query at spec start. `export-numbering` no longer demands an empty register but asserts consecutiveness from its own per-`doc_type` high-water mark, which still fails if the RPC skips, repeats or reorders. `final-verification` 5.2/5 split in two: rows this programme MARKED stay pinned to zero, because a marker is what makes *no fixture survives* a question a query can settle; the six unmarkable whole-table pins became unchanged-by-this-spec. Writing half: `doc_kind` is derived from the live CHECK at run time and `'other'` reached by elimination, and **the fixtures now insert `status='draft'`** — because a posted fixture can never be deleted and would recreate the OG-56 deadlock on every run. The measured cost of that, and it is the honest one: the export RPC selects posted only, so 10 tests that construct a document and assert how the export treats it are `test.describe.fixme` and **deferred to Phase 8** by owner decision, recorded by name in the new-baseline section. **The `customers.person_id` claim in this row remains unverified and stays `[U]`** — no spec in `e2e/` inserts into `customers`, and the new baseline contains no `customers.person_id` failure at all, so whatever produced those two is no longer reproducing.
| OG-47 three of the 47 e2e failures are FLAKE, so the baseline is a range and not a number | 2026-08-24 | | **OPEN — recorded because treating 47 as exact will manufacture false regressions.** Three failures are non-deterministic: `e2e/persons/filters-ui.spec.ts:138`, `e2e/persons/permission-matrix.spec.ts:321`, `e2e/persons/phone-collisions-ui.spec.ts:205`. **A future run landing anywhere in roughly 44–50 is NOT automatically a regression**; the failure SET must be compared, never the count. Evidence that they are flake and not a real defect: all three fail with `expect(page).not.toHaveURL(/\/login/)` — they never obtain a session at all — and all three log in interactively with their own inline helper rather than a storageState file. `e2e/persons/person-profile.spec.ts:121` runs semantically identical login code against the same account and PASSED in the same run, and the outcome alternates by position: 233 fail, 292 fail, 303 pass, 309 fail, 311 pass, 329 pass. **Suspected mechanism, stated as a hypothesis and not as a finding:** the same hydration race the owner fixed in `e2e/auth/generate-role-sessions.spec.ts` on 2026-08-24 — the form is filled at `domcontentloaded`, React hydrates, the fields are cleared and an empty form is submitted. That fix was applied only to the session generator; six specs carry their own copy of the login helper and none of them was patched. **Settling it needs a targeted re-run of those three specs, which writes data, so it was not done during a read-only diagnosis.** |
| OG-48 repairing OG-32 the obvious way also opens an unauthenticated WRITE | 2026-08-24 | | **OPEN — and it has to be decided BEFORE any OG-32 repair, not after.** `src/lib/public/get-public-sale-list.ts:50` calls `supabase.rpc("refresh_sale_list_prices", …)`. Measured: that function is `VOLATILE`, `SECURITY DEFINER`, its body contains `INSERT/UPDATE/DELETE`, and **`anon` holds EXECUTE on it**. It does not run today, proved both from the code and empirically: the `sale_lists` SELECT on line 38 runs FIRST, fails `42501` for `anon`, and line 47 returns null before the RPC is reached — `anon` calls to `refresh_sale_list_prices` were **0 before and 0 after** a real browser page load, and `sale_list_items` stayed at 1837 rows. **So the privilege denial that makes OG-32 "broken" is the only thing standing between an anonymous visitor and a definer-rights write.** A `GRANT SELECT ON sale_lists TO anon` — the most obvious form of the repair — arms it in the same stroke. Deciding it means deciding whether `anon` should hold EXECUTE on that RPC at all. |
| OG-49 anon reads 28 product columns directly, not the 7 the public feed publishes | 2026-08-24 | **2026-08-25 — CLOSED by migrations 388 + 389** | **CLOSED. It took two migrations and the second one is the one that mattered.** Before: `/rest/v1/products?select=*` with the published anon key returned **355 rows x 28 columns**, including `sku` (355/355), `created_by` (355/355), `updated_by` (353/355) and `accounting_code` (5/355), the five `created_by` uuids all resolving to real staff profiles. **Migration 388** narrowed `anon` to nine named columns and closed every direct path — `select=*` now 401, and column, `order=`-only, filter-only and embed shapes all verified as effect per column. **Migration 388 was not enough, and believing it was would have shipped the leak under a green gate.** `find_duplicate_product` is `SECURITY DEFINER` returning `TABLE(id, name, sku)`, and four of its six parameters are precisely the columns 388 leaves open; a definer function executes as its owner and so passes straight through column grants and RLS. **Migration 389** revokes EXECUTE from `anon` **and** from `PUBLIC` — the `anon`-only revoke measures as no change, because PostgreSQL grants functions to `PUBLIC` by default. A catalogue sweep of 27 candidate functions, every guardless `STABLE` one actually invoked rather than pattern-matched, established it was the only such door. Verified live after both: `anon` gets `42501 permission denied for function`, `authenticated` gets `200`, `/api/public/products` still answers 200 with 199 rows and `price: 0`. **OG-29 was never violated** — `products` carries no price or cost column. What was exposed, and is now closed, is internal identifiers and staff uuids. Evidence: `docs/research/m5b-anon-products.md`. |
| OG-50 the three `api/public/hooks/*` webhooks were not measured | 2026-08-24 | **2026-08-25 — MEASURED, no leak** | **ANSWERED, and the answer is that all three are guarded.** M5B read every one. Each checks a Bearer token **before** any write and before any outbound call — the ordering is the part that matters and it was verified statement by statement, not inferred from the presence of a check. `api/messenger/ai-chat.ts`, which M5 had recorded as `[U]` in the same position, is closed on the same pattern. **One operational finding that is not a security finding:** `PRICING_WORKER_TOKEN` and `MARKET_RATES_CRON_SECRET` are not set on this server, so those two workers are inert here — they reject every caller including the intended one. That is fail-closed and therefore safe, but it means the pricing queue and market-rate ingestion are not running on the test box, which is worth knowing before anyone debugs why prices look stale. |
| OG-51 `service_role` gets ZERO rows from all eight guard-class views | 2026-08-24 | | **OPEN — an unrecorded consequence of M4, found because M5's gate went red for a reason that was not a gate bug.** The service-role JWT carries **no `sub` claim** (`{exp, iss, role}` only), so `auth.uid()` is NULL for it and migration 386's `uid() IS NOT NULL` predicate closes every guard-class view to it — **silently, zero rows, no error**. Measured: as `service_role` with no sub, `product_computed_prices_public` = 0 and `v_promotion_suggestions` = 0; with a `sub` added, `product_computed_prices_public` = 588. M4's acceptance measured admin/accountant/sales (all of which carry a sub) and the NULL-uid case, but never `service_role` through PostgREST. **Impact today is zero:** the only server-side reader of any of these views is `src/routes/api/public/products.ts:108`, and it sits behind `PUBLISH_PUBLIC_PRICES = false`; every other reader is client-side and carries a sub. **The trap:** the moment OG-29 is answered "publish" and that flag flips, the feed would publish zeros anyway, with no error and no signal. Deciding it means choosing between exempting `service_role` in the views, making the server-side call present a `sub`, or having the publish flag assert this first. |
| OG-52 `categories.base_margin_percent` is readable by `anon` and is more sensitive than anything closed on `products` | 2026-08-25 | **2026-08-25 — CLOSED by migration 390** | **CLOSED, and the sweep found something larger than the row was raised for.** Before: `anon` held `arwdDxt` on `public.categories` with zero column ACLs and read all 11 columns including `base_margin_percent`, 15.00 on all 12 rows. Migration 390 narrows it to six catalogue columns. **But a column revoke would not have closed it.** The definer sweep found `calculate_adjusted_price(uuid)` — `SECURITY DEFINER`, `anon`-executable, reading `product_computed_prices`, which `anon` cannot read (`42501`, the denial OG-29 and `PUBLISH_PUBLIC_PRICES = false` rest on). Measured live as `anon`: it returned **38,985,000** for a product whose true stored price is **33,900,000**, ratio exactly **1.15**. The margin was recoverable from the output and **the real computed sale price came out with it** — see **OG-55**. Both halves are closed and verified over HTTP: `select=base_margin_percent` 401, `select=*` 401, `order=`-only 401, filter-only 401, `select=id,name` 200 with real data, `rpc/calculate_adjusted_price` 401/`42501` for `anon` and 200 for `authenticated`, which still reads all 11 columns. Evidence: `docs/research/m5c-margin-and-probe.md`. |
| OG-53 `effective_currencies_view` changed its FAILURE SHAPE for `anon` and nothing recorded it | 2026-08-25 | | **OPEN — LEFT OPEN DELIBERATELY, and M5C did not touch it.** Restated so the next reader does not mistake it for an oversight: `anon` selecting `effective_currencies_view` used to return HTTP 200 with zero rows and now returns 401/`42501`. **Access is identical in both states** — `anon` reads nothing either way — so this is not a privilege regression; it is a change of failure SHAPE, and this programme's rule 12 exists because the two get confused. No caller in `src/` reads this view unauthenticated, so impact today is zero. **It stays open because the fix depends on which shape is intended, and that is the owner's call, not an agent's:** if no public surface should read currencies at all, the 401 is correct and this row closes as documentation; if one should, the view needs a grant and the 200-empty shape comes back. |
| OG-55 a SECURITY DEFINER function handed `anon` a real computed sale price, defeating OG-29's flag | 2026-08-25 | **2026-08-25 — CLOSED by migration 390** | **CLOSED, and raised here because it is bigger than the OG-52 row that found it and must not be filed under it.** `calculate_adjusted_price(uuid)` was `SECURITY DEFINER`, `VOLATILE`, and executable by `anon` (explicitly, and again through the `PUBLIC` default `=X`). It reads `product_computed_prices` — the table `anon` is denied with `42501`, and that denial is what `PUBLISH_PUBLIC_PRICES = false` and the whole of OG-29 rest on — multiplies by the category margin and holding-day bucket, and returns the number. Measured live as `anon` inside `BEGIN … ROLLBACK`: **38,985,000** returned against a true stored price of **33,900,000**. So for the whole period this stood, the answer to OG-29's question *may the public feed publish real prices* was already **yes, by accident, for anyone holding the published anon key** — while the feed itself dutifully returned `price: 0`. **How it was found:** the owner instructed M5C to sweep definer functions for `base_margin_percent` before claiming OG-52 closed. That sweep, not the mission's own scope, is what surfaced it — the same discipline migration 389 had to learn the expensive way on `products.sku`. **Two corrections it forced:** the function writes nothing (`prosrc` has no INSERT/UPDATE/DELETE), so M5B's record of it as un-probed-because-VOLATILE was wrong; and the 12 other `anon`-executable definer functions reading `categories` were checked per function, not sampled — all build `jsonb_build_object('id', c.id, 'name', c.name)` and none carries the margin out. Migration 390 check F pins that. |
| OG-56 two E2E journal entries are stuck in the ledger and cannot be deleted by anyone | 2026-08-25 | | **OPEN — and this one was caused by this agent, on 2026-08-25, while repairing OG-46. Recorded first, before anything else in that mission.** The OG-46 brief called the missing `journal_entries.doc_kind` on three fixture INSERTs *a defect, not a decision* and instructed that it simply be filled in. It was filled in with `'other'`, and the result was measured rather than assumed — which is the only reason this row exists. **Adding the column makes the situation worse, not better.** With `doc_kind` absent the INSERT fails and nothing is written. With it present the INSERT succeeds and writes a `status='posted'` entry, and `trg_journal_entry_immutable` then raises `P0001` on **every** UPDATE and DELETE where `OLD.status='posted'` — verified as `supabase_admin`, the superuser, inside `BEGIN … ROLLBACK`: `سند ثبت‌شده قابل تغییر نیست؛ برای اصلاح، سند برگشتی بزنید`. The spec's own `cleanupConstructed()` deletes by `description like MARK%`, which matches, and is refused. So each run would leave two permanent entries in the company's journal, and the previous defect was masking a worse one. **Two rows exist now and cannot be removed:** `E2E_AUDIT_20260729_ASAN_JRN_UNBALANCED` and `E2E_AUDIT_20260729_ASAN_RCP_PAYMENT`, both `source_type='manual'`, `doc_kind='other'`, `status='posted'`, created 2026-08-25 13:09, two `journal_lines` each. The `doc_kind` change was **reverted** so no further rows accumulate, and both spec files now carry a warning at the INSERT saying why it must not be 'fixed' that way. **`status='draft'` first does not rescue it:** the trigger tests `OLD.status`, so the draft→posted UPDATE is allowed and the DELETE afterwards still is not. **What deciding it needs, and it is D11 territory:** a reversing entry (the mechanism the trigger's own message names), or accepting the two rows as permanent test residue and teaching `final-verification` 5.2/5 to exclude them by id, or a migration giving the trigger a test-only escape — which would put a hole in the ledger immutability guarantee and should be the last option considered. Until then the OG-46 writing half is NOT a fill-in-a-column fix: a real repair has to stop those fixtures creating posted entries at all, and that changes what `export-journal` and `export-receipts-payments` assert. | **UPDATE 2026-08-25 — a SECOND consequence, worse than the one first reported, and it was not identified until the repair was measured.** The two rows are `status='posted'`, so **they appear in the ASAN journal export**. Excluding them from `cleanupConstructed` does not exclude them from the export. `export-journal:162` asserts that `rows[0]` of the export is the real corrupted entry; it is now this residue instead, and that test fails for that reason alone. So OG-56 accounts for **2 of the 30 failures in the new baseline** — `final-verification:267` and `export-journal:162` — and both close the moment the rows go. **The first report of this row understated the damage twice:** it said the effect was one red assertion, when in fact `cleanupConstructed()` runs inside `beforeAll`, its DELETE matched these rows, the trigger raised, `dbExecE2e` threw and `beforeAll` died — taking **all 23 tests in both spec files** out of every run until the id exclusion was added. **Owner's decision, applied:** exclude by id in `cleanupConstructed` only, for these two UUIDs only; do not reverse them; do not modify the immutability trigger.
| OG-54 orphaned `chrome-headless-shell` processes silently invalidate any e2e run | 2026-08-25 | **2026-08-25 — mechanism identified by the owner** | **OPEN as a standing harness rule, not as a defect to fix once.** **Count `chrome-headless-shell` before AND after every e2e run.** A `chrome|node` process pattern does not match it — that is the exact pattern this agent used, which is why its first diagnosis reported *only 2 Playwright processes* and concluded the machine was clean. It was not. Sixteen orphans were burning CPU while idle, one with **3.75 hours of accumulated CPU time**, the oldest from 08:45. Their parent was a `node.exe` from the **Python** Playwright driver (`site-packages/playwright/driver/...cli.js run-driver`), not the npx e2e run — so they came from a side tool and no amount of looking at the test harness would have found them. Kill the driver first, then the children. **Cost when missed: the run of 2026-08-25 took 4.7 hours instead of 46.6 minutes — six times slower — and produced 92 failures against a 47 baseline, every one of the 42 extra being a timeout rather than an assertion.** That run is INVALID and is not recorded as a baseline. This is the same class of harm that voided three R9 runs, from a different source. |
| OG-57 `ledger-documents` is seeded and consulted by nothing | 2026-08-25 | | **OPEN — found by M12 while answering OG-12, and it is the inverse of the danger OG-12 was raised about.** Migration 344 seeded the module because *an unseeded module is OPEN, not closed* (`has_dynamic_permission` grants all roles when no row exists). The seed is correct and present, and 352 later gave `manager` `can_view`/`can_create`. **But a census of all 28 seeded modules found `ledger-documents` is the only one with ZERO references in `src/`** — nothing ever asks `has_dynamic_permission('ledger-documents', …)`, so the row governs nothing. **This is not an open door and must not be filed as one:** the row exists, so the function returns the seeded value to anyone who asks, and nobody asks. **Nor is the wizard unguarded** — `_app.accounting.receipts.create.tsx` carries `requireAnyRole(['admin','accountant','manager'])` and M6's `staticData` gate, and RLS is on the underlying tables. What is missing is specifically the **module layer** of CLAUDE.md rule 7 (UI guard + route/server guard + RLS). **What deciding it needs:** either wire the module check into the wizard's UI layer — a behaviour change, outside M12's *change neither* mandate, and it can hide the wizard from a role the route currently admits — or record the row as deliberately reserved for a later surface and stop treating rule 7 as unmet here. Evidence: `docs/research/m12-serial-and-module.md`. |
| OG-58 the chain is being executed on a machine that cannot reach the database | 2026-08-25 | | **OPEN, and it bounds every mission from here on rather than any single one.** The session running the chain is an ephemeral cloud container holding a fresh clone and nothing else. Measured, not assumed: `192.168.170.8` ports 9000 / 5432 / 11434 all unreachable (no route — `192.168.0.0/16` is in the proxy's `noProxy`, so a LAN address is attempted directly and has none); a real `psql` attempt against `postgresql://…@192.168.170.8:5432/afrakala` was killed at the timeout without connecting; `docker info` finds no `/var/run/docker.sock`, so `docker exec afrakala-lan-db` is impossible; `https://api.test.myafrakala.ir/` returns a proxy CONNECT **403 policy denial**. **Consequence:** every remaining Phase 2 mission is gated on live measurement — `pg_get_functiondef`, `has_column_privilege`, a two-sided gate against `afrakala`, an e2e run against the deployed build — and **none of it can be executed or honestly reported from here** (A0.3). A4.18's health check, A4.19's ceiling and A4.20's lock are moot: there is no suite to run. A3.15 independent review is also unavailable, this session being directed not to spawn subagents. **What deciding it needs:** either the chain moves to the test computer, or each mission is split into a repository half done here and a live half done there — in which case the split must be explicit in every report, because a document that reads as measured when it is only inferred is the exact failure A0 exists to prevent. |
| OG-59 `document_serial_counters` and `next_serial` exist in no file in this repository | 2026-08-25 | **2026-08-25 — CLOSED by mission 0-LOCAL, live query** | **CLOSED — not schema drift, no action.** The M12 instruction named these two objects as the mission's measurement targets. `grep -rl` over the whole tree (excluding `.git` and `node_modules`) returns **zero hits for each**. The serial mechanism that does exist is `asan_export_numbers` + `asan_assign_document_number` (migrations 290/291), and it is what OG-9 is about. **Three possibilities and they are not equivalent:** (a) the objects exist live but in no migration — that is schema drift, it means the migration directory does not describe the database, and it matters far more than M12 does; (b) the names come from another system or an earlier design and M12's serial half is `asan_export_numbers` as documented; (c) they are planned and not yet built. **That query has now been run on the live `afrakala` database and both columns came back NULL** — neither object exists. That is branch **(b)**: the names came from outside this system. Branch (a), *exists live but in no migration* — the schema-drift case that would have outranked M8 — is **refuted by real output**, not assumed away. The names originated in a fabricated report on 2026-08-25 and were then quoted back as fact in a later instruction; a whole-tree grep returned zero hits for both, and the agent that received them correctly refused to build on them. The real serial mechanism is `asan_export_numbers` + `asan_assign_document_number` (migrations 290/291) and `document_numbers` + `assign_document_number` (migration 338), which is what OG-9 is about. Cross-reference **A0.9** — fabricated details propagate, and only the zero-hit grep plus this zero-row query stopped these two from becoming a schema-drift investigation. Evidence: `docs/research/0local-live-confirmation.md`. |
| OG-60 `document_audit_log` — the second table OG-15 names does not exist. Which real table was meant? | 2026-08-25 | **2026-08-26 — CLOSED by the owner's own finding, confirmed live** | **CLOSED — the name was FABRICATED. No drift, no missing table, nothing owed on that name.** `document_audit_log` was never a table, never dropped and never renamed. `SELECT to_regclass('public.document_audit_log')` returns **NULL**; no relation of that name exists in any schema of any relkind (the only `%audit%` relations live are `auth.audit_log_entries`, `public.audit_logs` and `public.bot_api_key_audit_log`); and a whole-tree grep finds the string **nowhere but the line in this file recording the OG-15 answer itself**. **Origin:** the name came from a fabricated report on 2026-08-25 — the same session whose M5C completion claim had no commit, no branch and no PR — and was then repeated, unverified, inside the owner's OG-15 answer, which is how it reached a mission brief as a fact. **This is the SECOND fabricated object name to propagate this way.** The first was `document_serial_counters` / `next_serial`, closed the same way as **OG-59** — zero grep hits, `to_regclass` NULL, name invented. Both times the correct behaviour was **A0.9**: a handed fact that cannot be located is recorded and asked about, never built on. No substitute table was guessed here. Cross-reference **OG-59** and **A0.9**. Evidence: `docs/research/og60-status-history.md`. |
| OG-61 revoke `anon`/`PUBLIC` EXECUTE from the existing anon-executable functions in `public`? | 2026-08-26 | | **OPEN — and it now carries M10's scope too.** **M10 was closed on 2026-08-26 as a DUPLICATE of this row**: the repository's only definition of M10 (`m3-function-execute-leak-PROGRESS.md:177`) is the batched EXECUTE revoke across the *existing* functions, which is precisely this gate. Migration 393 did **not** do it — 393 holds no `REVOKE`/`GRANT` on any existing function, only 8 `ALTER DEFAULT PRIVILEGES` lines — so the earlier reading that M10 was *covered by OG-31* does not survive measurement. Deciding this row now settles M10 as well. **The number has moved twice and must be re-derived, not quoted:** M3 measured 746, mission 4 measured 741, and after migration 395 revoked 28 the live figure is **713 anon-executable of 840**. **OPEN — the function-side twin of OG-30, raised by migration 393 which deliberately closed only the FUTURE tap.** Census measured 2026-08-26 on `public`'s 840 functions: **741 anon-executable**, 99 already closed by 381/389/390, **427 SECURITY DEFINER**, and **342 both** — of which 91 are STABLE (safe to call as anon per A5.31) and 251 VOLATILE (never called; bodies read instead). **The number 741 is an upper bound on exposure, not a measure of it.** Of the 18 highest-risk STABLE definer functions — financial, pricing, credit, PII and export domains — **14 refused from inside their own bodies** when called as `anon` (`forbidden`, `unauthenticated`, `requires admin, manager, or accountant`, and Persian refusals from the Asan exports). This codebase overwhelmingly guards at the top of the function, so a batched revoke here is **defence in depth, not a fix**, and it is a much weaker case than OG-30's table-side twin. **What deciding it needs:** the same weighing OG-30 records — a revoke that touches 741 objects will break anything that silently depended on the grant, and the four functions that DID return data to anon are already carved out as OG-62. Note the owner scheduled OG-30 *after* the security-trio mission; this gate is its natural companion and should be decided with it, not before. Evidence: `docs/research/og31-function-execute-audit.md`. |
| OG-62 two definer functions hand an ANONYMOUS caller real sale prices — may they be closed? | 2026-08-26 | **2026-08-26 — ANSWERED YES by the owner (close it, with a complete domain-wide sweep) and CLOSED by migration 395** | **CLOSED, and wider than the row asked.** 28 functions revoked from `anon` AND from `PUBLIC`. Live after the apply: `anon_can_execute_of_28 = 0`, `auth_can_execute_of_28 = 28`, and behaviourally `get_product_sale_price` / `get_product_price_bounds` now raise **42501** to anon instead of returning `79800000` and `(79800000,87300000,91665000,79800000,t)`. A5.32's distinction is respected — this is a privilege refusal, not RLS's silent zero rows. **The list came from a sweep, not a name match (A5.31).** All **91** STABLE anon-executable definer functions in `public` were called as anon, each in its own sub-transaction: 36 refused from inside their own bodies, 47 returned rows, 8 other. **A row count is not a leak** — `has_role(NULL,NULL)` returns one row of `false` — so every function that returned was called AGAIN with real arguments and its VALUES inspected. That second pass is what separates these 28 from the boolean RLS helpers, which were deliberately left alone because they back 93 policies and revoking them would break policy evaluation rather than close anything. **Safety measured before a single REVOKE was written:** all 28 hold their OWN explicit `authenticated` and `service_role` grants (so revoking PUBLIC cannot silently remove them — the trap that has bitten this project four times), none appears in any RLS policy, none is reachable through a view `anon` can read, and each has exactly one signature. `get_product_sale_price` and `get_product_price_bounds` have **no `src/` caller at all** beyond generated types. **The rollback caught a real bug in itself.** Comparing the ACL as a SET showed two functions GAINING a PUBLIC grant they never had — `polymorphic_ref_orphan_report` and `product_videos_waiting` were anon-executable through an explicit `anon=X` only — so a blanket re-GRANT would have left them MORE open than before. It now restores per function. The near-miss is the lesson: the text-md5 mismatch had two causes at once, a harmless aclitem re-ordering and this real defect, and calling it "just ordering" would have buried the defect. Final: 166 ACL entries before, 166 after, zero difference either way. **Attacked with 12 disturbances plus a control, all caught**, including an inherited-role escape where `grants_naming_anon = 0` and anon still reached the function — a gate scanning `proacl` for anon would have passed it; this one tests effect via `has_function_privilege`. `/api/public/products` is byte-identical before and after: 200, 199 products, `price` present on all 199 and 0. Evidence: `docs/research/og62-anon-definer-sweep.md`. Original text follows. | **OPEN, and it is a LIVE disclosure, not a latent one.** Found by the A5.31 definer sweep that migration 393's mission was required to run. Measured as `anon`, no session, inside `BEGIN … ROLLBACK`: `get_product_sale_price(uuid,uuid)` returned **100900000** — a real sale price — and `get_product_price_bounds(uuid,uuid)` returned **(100900000,100900000,105945000,100900000,t)**, i.e. the floor, the current price and a 1.05x ceiling. **This is the OG-55 pattern exactly**, the one migration 390 closed when `calculate_adjusted_price` returned 38,985,000 to a caller whose table SELECT was refused with 42501. The column grants of 388/389/390 are intact; these two walk past them the same way. **It contradicts a decision the owner has already recorded** — the A8 table gives `product_computed_prices` PUBLIC: NOTHING and calls it the OG-29 anchor, and OG-29 is still open, which is the only reason `/api/public/products` publishes `price: 0`. Two more from the same sweep: `get_leaderboard_monthly` returned **5 rows** of employee leaderboard data to anon (five sibling `get_leaderboard*` variants share the shape and were not individually called), and `list_trusted_credit_customers` did not refuse — it ran and matched nothing, so it is **latent**, not a live leak today. **Why it was raised and not fixed:** v7's OG-31 brief scopes this mission to *close the future tap, audit existing report-only, raise gates for anything already broken* — the same scoping under which OG-25's broken surfaces became OG-32 and OG-33 rather than being fixed in place — and A2.9 caps the mission at one assertion gate, which 393 spent. Closing needs its own two-sided gate proving `authenticated` keeps EXECUTE after the PUBLIC revoke; the REVOKE-then-GRANT ordering trap has struck four times (A5.32). **Recommended as the immediate next action**, ahead of OG-61 and OG-30. Evidence: `docs/research/og31-function-execute-audit.md`. |
| OG-63 purchases cannot be registered for 3.5 hours every Tehran night — `CURRENT_DATE` is UTC | 2026-08-26 | **2026-08-26 — ANSWERED by the owner (fix first, scope to `create_purchase` only) and CLOSED by migration 394** | **CLOSED for `create_purchase`.** One line: `p_purchase_date > CURRENT_DATE` became `p_purchase_date > public.tehran_today()`. The body shipped is a byte-for-byte copy of the live `pg_get_functiondef` capture with only that comparison replaced — copied, not retyped, because it carries 31 distinct Persian-guarded error paths (A5.30). Live after the apply: `prosrc` md5 `d4b56b29cf3339f053cccc10805076fc`, equal to the dry-run's, so the delivery path corrupted nothing; Persian intact; no `>\s*CURRENT_DATE` left. Rollback proved byte-identical BEFORE the forward file was applied (`ea45948e…` → forward → down → `ea45948e…`). **The hard part was the gate, not the fix.** The defect is dormant 20.5 hours a day, so a gate that merely called the RPC would PASS against the broken code at almost any hour. This gate **reconstructs the window on demand**: `CURRENT_DATE` follows the session TimeZone while `tehran_today()` does not, so `SET TimeZone='Etc/GMT+12'` puts them a day apart at any wall-clock moment. It asserts under BOTH regimes that today-in-Tehran is ACCEPTED and tomorrow-in-Tehran is still REJECTED, each with a vacuity guard proving the regime was really built. Nothing is written — every probe runs through a `pg_temp` helper whose sub-transaction is rolled back even on success; `purchases` was 198 before and 198 after. **Attacked with 12 disturbances plus a control, all caught.** The one that matters: a body calling `tehran_today()` with **no bare `> CURRENT_DATE`** but an `OR p_purchase_date > (CURRENT_DATE)` appended — structurally clean, still the original bug, caught only behaviourally inside the reconstructed window. A grep-based gate would have shipped it. **Note on the e2e evidence, stated rather than glossed:** mission 4's 14 `purchase/*` failures were produced at 02:5x Tehran, inside the window; this mission's run was at ~11:00 Tehran, outside it, so those tests would pass with or without the fix and **do not discriminate**. The proof is the gate, not the suite. **The rest of the class is NOT fixed** — see OG-64. Evidence: `docs/research/og63-current-date-audit.md`. Original text follows. | **OPEN — a LIVE product defect, found by diagnosing 14 new e2e failures rather than by looking for it.** `create_purchase` line 170 is `IF p_purchase_date > CURRENT_DATE THEN RAISE … PURCHASE_DATE_FUTURE`. `CURRENT_DATE` is the **server** date and the server runs UTC; the purchase form defaults its date field to the **Tehran** calendar day. Tehran is UTC+3:30, so from **00:00 to 03:30 Tehran** the form's own default is rejected as being in the future. Measured at the moment of diagnosis: `utc_now = 2026-08-25 23:23`, `tehran_now = 2026-08-26 02:53`, `CURRENT_DATE = 2026-08-25`, `public.tehran_today() = 2026-08-26`, `tehran_today() > CURRENT_DATE = t`. **A real user in Tehran cannot register a purchase during those 3.5 hours**, and the failure is the RPC's own Persian message, so it looks like a validation refusal rather than a bug. **This is A5.32's recorded trap** — *server timezone is UTC; use `public.tehran_today()` for calendar day* — violated in the function itself. **It is a class, not one function.** 22 functions in `public` reference `CURRENT_DATE`/`current_date` and **not one of them also references `tehran_today`**; six of those compare `> CURRENT_DATE` and so carry the same future-date rejection: `create_purchase`, `get_payables_list`, `get_payables_summary`, `get_receivables_list`, `get_receivables_summary`, `upsert_staff_daily_performance_metric`. The four payables/receivables ones bound a *filter*, so their night-time effect is a silently narrowed list rather than an error — which is worse to detect. **Why it surfaced now and not before:** it is time-of-day dependent. The 30-failure baseline and M8's run were taken earlier in the day, inside the safe 20.5 hours. **This mission's run started at 02:5x Tehran and hit the window.** That also makes it the explanation for all 14 new failures in `purchase/*` (A4.22 set comparison) — and migration 393 was **exonerated by experiment**: with 393 fully reverted via `393-down.sql` and PostgREST restarted, `c1-foundation:20` and `c2-central-rpc:46` failed identically. **What deciding it needs:** whether the fix is `CURRENT_DATE` → `public.tehran_today()` in the six (a behaviour change on live validation, and the four list functions would return different rows), or whether the UI should submit a UTC-aligned date instead. It is a real business rule about which calendar day a purchase belongs to, so it is the owner's. Not fixed here: out of this mission's scope, and A2.9 caps it at the one gate 393 spent. Evidence: `docs/execution/m4-security-trio-og31-PROGRESS.md`. |
| OG-64 21 more functions still compare against the UTC `CURRENT_DATE`; none uses `tehran_today()` | 2026-08-26 | | **OPEN — the class OG-63 belonged to, audited report-only by owner scoping.** 22 functions in `public` reference `CURRENT_DATE`/`current_date` and **not one also references `tehran_today`**. `create_purchase` is fixed; these 21 are not. **The class is not homogeneous, and that matters more than the count.** **(a) One more outright REFUSAL:** `upsert_staff_daily_performance_metric` line 21, `IF p_metric_date IS NULL OR p_metric_date > CURRENT_DATE THEN` — a staff member recording today's metric is refused inside the same 00:00–03:30 Tehran window; line 26's 5-day backdating allowance shifts with it too. **(b) Four are BUCKETING FILTERS, not refusals:** `get_payables_list` (36–38), `get_receivables_list` (35–37), `get_payables_summary` (16–18), `get_receivables_summary` (16–18) each split *today / tomorrow / future* on `= CURRENT_DATE`, `= CURRENT_DATE + 1`, `> CURRENT_DATE + 1`. Inside the window an item due today-in-Tehran is counted as *tomorrow* and the *due today* total reads wrong. **No error, no log, plausible-looking numbers — harder to notice than the purchase outage, not easier.** **(c) The remaining 16** stamp or bound by date without refusing anything: `_latest_active_capital_setting`, `calculate_adjusted_price`, `calculate_credit_score`, `calculate_customer_realtime_credit`, `calculate_dynamic_score`, `compute_daily_capital`, `create_dynamic_scoring_parameter`, `create_dynamic_scoring_parameter_v2`, `generate_birthday_notifications`, `get_customer_dynamic_credit`, `get_product_stats`, `pay_purchase_with_voucher`, `post_mutual_settlement`, `refresh_today_dynamic_capital_after_score_change`, `settle_league_season`, `upsert_dynamic_parameter_weight`. These were **located, not assessed** — only the `CURRENT_DATE` usage was found; none was analysed for correctness, and saying so is the point. **What deciding it needs:** this is a business question, not a mechanical sweep. Converting the four bucketing functions changes which rows appear under *today* on live financial screens; converting `upsert_staff_daily_performance_metric` moves a permission boundary. Fixing them individually as they surface is also defensible. Evidence: `docs/research/og63-current-date-audit.md`. |
| OG-65 the Asan export is already built — three narrow questions block finishing it | 2026-08-26 | **2026-08-26 — ANSWERED by the owner from the REAL Asan `.xlsx`, read cell by cell, and CLOSED by the mission-10 code change (no migration)** | **CLOSED. The measured file settled it, and the code was the side that was wrong.** The template holds `A='Date' B='Code_M' C='Name_Moshtare' D='Shopmare_Peygeri' E='Mablagh' F='Bank_cod'`, with `G..O = ''` and `max_col = 15`. The code wrote `Name_Moshtari`/`Shomare_Peygiri`. Both measured spellings look like misspellings and both are legacy-intentional in Asan — correcting them lands real data in the wrong column of live accounting software. **The specs were the reason the wrong headers survived.** `e2e/asan/export-bank-deposits.spec.ts:27,98` and `e2e/asan/export-shell.spec.ts:274-279` ASSERTED the wrong pair, one commenting that the transliteration was *reproduced exactly*. **A test asserting a wrong value does not merely fail to catch the defect — it defends it**, and makes the correct fix look like a regression. Both corrected, each carrying the reason in place. **Template 2 needed no change** — measured as `کد حساب، کد کالا، شرح، تعداد، بدهکار، بستانکار`, exactly what `JOURNAL_HEADERS` already writes. Untouched. **G–O are empty STRINGS, not nulls**, and that distinction is load-bearing: `aoa_to_sheet` writes no cell at all for `null`, so a null padding yields a six-column file that looks right in a diff and is the wrong width in Asan. **Direction now lives in the SIGN of `Mablagh`** — receipt positive, payment negative — **and only in layout 4**. Layout 3 carries direction in separate `بدهکار`/`بستانکار` columns where a negative would corrupt double-entry; the gate asserts no sign handling has leaked there. The minus is a numeric sign, not a leading character, because `write-xlsx` writes amounts as real numbers. **Scope boundary stated rather than implied:** `asan_list_bank_deposit_export` reads `payment_receipts` and returns no direction column, so **every row the wired source yields is a receipt**; bank payments still route to template 2 via `asan_list_journal_export`. The mapping supports payments, but nothing feeds them into this layout yet — wiring one needs an RPC change, outside the owner's `src/lib/asan/`-only scope. Raised as **OG-67**. **Attacked with 12 disturbances plus a control, all caught**, including the *corrected* spelling, a `null` padding, a negative reaching template 2, and **an amount returned as a formatted string** — the first mission in this chain where A2.12(b)'s numeric-as-string class has a real attack surface, since every earlier gate made only catalogue/SQL checks. **D10 failed to construct on its first form and was rebuilt rather than counted** (A2.12d, second time it has fired). Evidence: `docs/research/og65-asan-bank-template.md`. Original text follows. | **OPEN, and it REFRAMES OG-35: this is not a build mission.** The wiring audit v8 ordered found that **both of v8's templates already exist in the repository**. **Template 2 (`سنددوبل_اسان.xls`) is an EXACT match** for the built layout 3 — `JOURNAL_HEADERS` at `src/lib/asan/layouts.ts:64` is `کد حساب, کد کالا, شرح, تعداد, بدهکار, بستانکار`, same six columns in the same order, and its own comment says it *serves receipts, payments and دوبل*. Nothing to decide, nothing to build. **Template 1 (`واریزوپرداخت_بانکی.xlsx`) is the built layout 4** — `BANK_DEPOSIT_HEADERS` at `layouts.ts:74` — with the same six columns in the same order and **four of six headers identical**. The whole conflict is **three characters** in two headers. Also already built and matching v8: Toman×10 → Rial (`amounts.ts`, `RIAL_PER_TOMAN = 10`), Jalali `YYYY/MM/DD` with Latin digits converted **in Asia/Tehran first** (`dates.ts` — the same timezone class OG-63 got wrong elsewhere, handled correctly here with the reason written down), and `Bank_cod` fed from `bank_accounts.bank_code`. **Genuinely NOT built, and both widen a contract two e2e specs assert:** (a) a **negative `Mablagh` for bank payments** — a search across all of `src/lib/asan/` finds **no sign handling of any kind**, and the bank export is deliberately deposit-only with the accounting document as the default path for payments; (b) **columns G–O emitted empty to `max_col=15`** — the shipped layout is six columns. **The three questions:** (1) `Name_Moshtari`/`Shomare_Peygiri` as shipped and asserted at `e2e/asan/export-bank-deposits.spec.ts:27,98-99` (whose comment says the transliteration is *reproduced exactly*), or `Name_Moshtare`/`Shopmare_Peygeri` as v8 states? Both are owner-sourced and a wrong header lands data in the wrong column of live accounting software, so **A2.6 forbids an agent choosing** — open since 2026-08-25 and still unanswered. (2) Should template 1 also carry bank PAYMENTS with a negative amount? (3) Should G–O be emitted to max_col=15? **A fourth question sits behind them.** The «قالب پیکربندی نشده است» message is **not** a missing configuration — it is a deliberate refusal (`AsanLayoutNotConfiguredError`, `export-modes.ts:69`, thrown from `createUnconfiguredAsanAdapter` at `:107`/`:116`) on the accounting-receipts page. Its five unconfigured adapters are **`docs/asan/ASAN_BRIDGE.md`'s layout family** (`sales_invoice`, `purchase_invoice`, `accounting_voucher`, `bank_receipt`, `bank_payment`) — **not** the seven built exports on `/admin/asan-export`. `export-modes.ts` states that adopting ASAN_BRIDGE.md is the owner's call and *a separate mission requiring approval before any of it is built*. So OG-35 splits in two: **(A)** finish template 1 against the existing implementation — three small questions — or **(B)** configure the receipts page's ASAN_BRIDGE adapters, which v8's template block does not describe. **Nothing was built and no `src/` file was touched.** Evidence: `docs/research/og35-asan-export-audit.md`. |
| OG-66 party search — should it match `city`, and from where? | 2026-08-26 | | **OPEN — and like OG-65 it reframes its mission: most of task 6.7 / M2 is already built and wired.** **Three of the five fields are done:** `search_visible_persons` (migration 299, SECURITY INVOKER) searches `display_name` and `legal_name` and normalises the query into identifier forms before matching `person_identifiers` on `mobile_e164`, `national_id_ir` and `asan_person_code`; it returns `matched_by`, and it is already called from `src/lib/persons/functions.ts:482` and `src/routes/_app.persons.tsx:201`. Mobile normalisation is consistent with OG-4, measured: of 34 stored `mobile_e164` rows, **34 are the leading-zero national form, zero `+98`, zero bare-9**. **`city` is not a missing feature, it is a modelling question.** The person core has **no city column at all** — `persons` carries only `display_name` and `legal_name` — and the only city columns in the database are `customers.city` and `suppliers.city`, i.e. on the ROLE tables. CLAUDE.md's phase rule says any customer or supplier belongs to the Phase-2 unified persons core and forbids separate person systems, so the three options — join out to the role tables, put city on the person core, or leave city out — all move the model in different directions and **none is a conservative default an agent may pick**. `surname` is the same shape and smaller: there is no surname column, so surname search is whatever `display_name`/`legal_name` already gives. **One actionable non-gated gap, deliberately not taken here:** the ledger wizard (`src/features/ledger-wizard/lookup.ts`) matches only `asan_person_code` and `mobile_e164` by exact identifier and never calls `search_visible_persons`, so it cannot find a party by name. Wiring it would give name matching for free, but it changes what a live business surface matches — a name search can return several parties where an identifier search returned one, and the wizard's `pickKind` assumes a single hit — so it needs its own gate rather than a drive-by. **Settled in passing, and it closes a standing `[U]`:** v8 asked whether `lookup.ts` passes the Asan code to `person_find_by_identifiers` or only the mobile. **It passes the Asan code, and tries it FIRST** (`lookup.ts:81-84`, four paths, Asan before mobile in both halves), and the RPC honours the kind rather than assuming mobile — it reads `_kind`, normalises with `normalize_identifier(_kind, …)` and matches `kind = _kind AND value_normalized = _norm`, grading `national_id_ir`/`tax_id_ir`/`company_reg_id_ir`/`iban` as strong and the rest weak. The earlier unverified report was **wrong**. Evidence: `docs/research/m9-party-search-audit.md`. |
| OG-67 nothing feeds bank PAYMENTS into the Asan bank template | 2026-08-26 | | **OPEN — raised by mission 10, which deliberately stopped at the scope line rather than crossing it.** The owner's correction was that the bank file carries receipts AND payments, direction expressed as the sign of `Mablagh`. The **mapping** now does that and the gate proves both directions. **The data source does not.** `asan_list_bank_deposit_export` reads `payment_receipts`, and its result shape carries no direction column at all: `(doc_id, doc_label, doc_date, party_name, person_code, tracking_number, amount, bank_code, bank_title, blocked_reason)`. Bank payments today route to **template 2** through `asan_list_journal_export`. So a payment row can be built but cannot be obtained from live data, and the row type defaults an absent direction to `receipt` — the safe value and the only one the wired source can produce. **What deciding it needs:** whether bank payments should move from template 2 to template 1, and if so, where their rows come from — extending `asan_list_bank_deposit_export` to union a payments source (a migration), or a second RPC. Both are outside mission 10's `src/lib/asan/`-only scope, which is why this is a gate rather than a silent half-build. Note it is also a **routing** change, not just plumbing: payments currently produce a valid accounting document, and moving them changes which file an accountant imports. Evidence: `docs/research/og65-asan-bank-template.md`. |
| OG-68 `afrakala-lan-db` is running DEGRADED — five file binds are dead, so restarting it is a real risk | 2026-08-26 | | **OPEN, and it is a standing PRECONDITION rather than a task: do not restart this container.** **Measured 2026-08-26, inside the container:** all five bind-mounted files on `/docker-entrypoint-initdb.d/` are dead. `ls` reports `Input/output error` for each and lists them with a null stat line: `-????????? ? ? ? ? ? 00-afrakala-pre-supabase-admin.sh`, and the same for `zz-10-afrakala-roles.sh`, `zz-20-afrakala-schemas.sql`, `zz-30-afrakala-jwt.sh`, `zzzz-99-afrakala-final-role-passwords.sh`. **Harmless right now**, and the reason is worth stating so nobody 'fixes' it under pressure: those scripts are consumed by `initdb` only, and only against an empty data directory. The live data lives in the named volume `afrakala-lan_lan-db-data` mounted at `/var/lib/postgresql/data`, which is NOT a bind and is unaffected — which is exactly why the database is healthy (`docker exec … psql` works; `payment_receipts` 10, `purchases` 240) while its binds are broken. **Why a restart is dangerous:** a restart reuses the STORED container config, so Docker must re-create those five binds — the very operation erroring now (`mkdir /run/desktop/mnt/host/d: file exists`). If it fails, the container does not come back, and the test environment goes down with it. `--force-recreate` carries the same exposure. **This is also the same fault that broke `docker cp`** and produced the INVALID e2e run of 2026-08-26 14:51. **Standing rules until this is resolved:** do NOT `docker restart afrakala-lan-db`; do NOT `--force-recreate` it; do NOT `docker compose down` anything that would stop it. Restarting `afrakala-lan-rest` after a migration remains fine — that container carries zero VM-form binds. **What deciding it needs:** a Docker Desktop restart rebuilds the VM mount table and is the standard remedy, but it restarts every other project container on this machine, which the owner has said not to stop — so it is the owner's call and must be scheduled, not done in passing. **Take a `pg_dump` first**; one exists at `deploy/lan/backups/afrakala-pre-restart-20260826.dump` (17,466,432 bytes, md5 `3bd728e6b6c5cf41256b509e886bff9c`, verified identical inside the container). Note for any restore: `pg_dump` warned of circular foreign keys, so a restore needs `--disable-triggers`. **Workaround already in place, so nothing is blocked meanwhile:** fixture and migration delivery moved from `docker cp` to a stdin stream (CLAUDE.md/AGENTS.md rule 1, amended 2026-08-26), proven byte-identical by md5. |
| OG-69 the bank file's G–O columns are 15 wide but not empty STRINGS — does Asan care? | 2026-08-26 | | **OPEN, small, and recorded rather than asserted away.** v9 specified that the real template's G–O are *empty strings* and `max_col = 15`, and asked the gate to assert both. **Measured on the file this code produces, with `openpyxl` — the same tool the owner measured the real template with — `max_col` is 15 (correct) but G–O read back as `None`, not `''`.** SheetJS's `aoa_to_sheet` turns `""` into a cell the writer omits. Forcing explicit empty shared-strings (`ws[addr] = {t:'s', v:''}`) was tried and **did not change what openpyxl sees**; the patch was reverted rather than left as a no-op inside a writer every layout shares. **What the gate asserts instead:** the six named headers byte-for-byte, and 15 columns carrying no header text. Asserting `''` would assert something untrue of the produced file; asserting `None` would assert this implementation rather than the template. Either is the RULE 3 failure this same mission promoted — a test asserting a wrong value defends the bug — so the deviation is written down instead. **What deciding it needs:** does Asan's importer care whether a trailing column is a blank cell or an empty-string cell? The column COUNT is what a positional importer keys on and that is correct, so the likely answer is no — but that is the owner's to confirm against the real import, not an agent's to assume. If it does matter, the fix is a different xlsx writer path, not a change to the layout. |
| OG-29 may the public product feed publish real prices? | 2026-08-22 | **2026-08-24 — MEASURED, still the owner's to answer** | **M5 verified the current state and did NOT change it: no real price is published anywhere public.** `/api/public/products` answers 200 over both HTTP and HTTPS with byte-identical payloads, lists 199 products, and **the `price` key is PRESENT on all 199 and holds `0`** — that is the "key present, value zero" case, not the "key absent" case, and the two are different findings. The zero is deliberate and flag-controlled: `const PUBLISH_PUBLIC_PRICES = false` gates the whole price lookup. The source view `product_computed_prices_public` answers `401 / 42501` to `anon`. The feed leaks nothing else — its seven keys are `capacity, id, is_active, model, name, price, stock_status` and none matches a cost/supplier/stock-qty/staff pattern. **A separate exposure was found beside it and is NOT a price leak — see OG-49** — and a trap that would defeat a future "yes, publish" answer is **OG-51**. Evidence: `docs/research/m5-public-surfaces.md`. | **OPEN, and it is the reason that endpoint still returns zeros.** Measured, not assumed: since commit `eef3a4a1` (2026-08-10) this CORS-`*` endpoint has returned `price: 0` for every product. That commit added a `sale_price_types!inner(code)` filter so an outside caller could not be handed a cheque or partner price, but PostgREST filters `!inner` embeds by the caller's RLS and `anon` sees **zero** rows of `sale_price_types` (`sale_price_types_auth_read` is `{authenticated}`; `sale_price_types_read` needs admin/manager/accountant). So the embed never matched and the feed was silently zeroed. Migration 370 turned that silent zero into an HTTP 500; fixing the 500 by moving the price lookup to the service role would also *un*-zero it, publishing 193 real cash prices for 199 active in-stock products. Those products' names and SKUs are already deliberately public (`products_public_read`) — their prices are not. Not switched on autonomously: it is a new outward-facing disclosure, irreversible once scraped, and a business decision, not a side effect of a security remediation. `PUBLISH_PUBLIC_PRICES` in `src/routes/api/public/products.ts` holds the endpoint at its exact pre-370 observable behaviour; flipping it is one line. **Found by the second independent review, not by the mission's own measurement** — the mission had asserted the response was "identical to before 370", which was false. |
| OG-28 should `sales` see a bank balance, or a full staff roster? (G-2) | 2026-08-22 | | **OPEN.** Measured while choosing where `security_invoker` was safe: turning it on drops `vw_account_balances` for `sales` from 1 row to 0, `publish_recipients_view` for accountant **and** sales from 24 to 1, `v_dynamic_customer_capital_balances` for sales from 14 to 0, and `v_dynamic_salesperson_capital_balances` for sales from 210 to 9. In other words the base-table RLS already says these roles should not see this data, and the SECURITY DEFINER view is what overrides it. Migration 370 deliberately did **not** change any of the four — narrowing a signed-in user's access is a business decision and is outside a G-1 remediation. |

## Contradictions found against ground-truth.md

Anything measured that disagrees with `ground-truth.md`. **Never silently adapt** — record here,
continue with the next independent task, and let the Lead decide.

| # | Expected | Found | Where | Impact |
|---|---|---|---|---|
| 1 | `docs/execution/BACKEND-EXECUTION-phases-1-5.md` dispatched as the mission | File does not exist on disk or anywhere in git history | `find` / `git log --all` | **Phases 1-5 cannot start.** Task list exists in MASTER-CHECKLIST but the dispatched procedure does not |
| 2 | Gate A (Supervising Engineer) and Gate B (E2E) run at every phase end | Neither defined anywhere in `docs/execution/` | `grep -rn "Gate A|Gate B|Supervising Engineer"` | The override's only safeguard cannot be honoured |
| 3 | OG-2 CONFIRMED (per dispatch) | No OG-2 confirmation line anywhere; Owner-Gate log row empty | `grep -rn OG-2 docs/execution/` | Task 1.1 is gated by OG-2 and drops a live trigger + function |
| 4 | `DROP FUNCTION public.post_receipt_journal();` | Live signature is `post_receipt_journal(_receipt_id uuid)` | live `pg_proc` | Task 1.1 as written would error |
| 5 | OG-3: `invoice_ar` Asan code unknown | `asan_control_accounts` already holds `invoice_ar = 989` | live query | OG-3 may already be answered |
| 6 | Task 1.1 drops 2 objects | The trigger fires `trg_post_receipt_on_approve()`, which is what calls `post_receipt_journal`. A **third** object is involved | live `pg_trigger` join | Dropping only the 2 named objects orphans `trg_post_receipt_on_approve()` — decided in task 1.1 |
| 7 | `asan_control_accounts` covers the unresolvable kinds | Only `invoice_ar` has a row; `clearing` and `other` have none | live query | Those two kinds always block the journal export — deliberate per the function comment, recorded for phase 5 |
| 8 | Phase 2, C1–C9 | Nine contradictions between `rpc-contracts.md` §1 and the live schema, each recorded with its decision | `phase-2-PROGRESS.md` § Contradictions found | C1/C3/C5/C6 corrected the contract in place (task 2.1); C2 → OG-16; C4 → migration 348; C7 → phase 5; C8 → phase 6; C9 → the audit spec's row count needs amending in phase 4 |

**Resolutions 2026-08-18:** rows 1-2 superseded — the mission document was re-issued directly by the owner and now defines Gate A / Gate B. Row 3 resolved — OG-2 written to `ledger-decisions.md`. Row 4 resolved — `MASTER-CHECKLIST.md` task 1.1 corrected to `post_receipt_journal(_receipt_id uuid)`. Row 5 resolved — OG-3 answered (989).

## Reviewer escalations (OG-7)

| Task | Reviewer | Objection | Lead's decision |
|---|---|---|---|

## Owner decisions taken 2026-08-25 (phase 1 questions)

Recorded verbatim from the owner. Each one is a decision, not a measurement; the evidence
column says what the repo actually holds today, verified at the moment of recording.

| # | Gate | Owner's answer | What the file actually says today |
|---|---|---|---|
| 1 | **OG-15** add `viewer_restricted` to the two new tables? | **YES — both `document_attachments` and `document_audit_log` are to be restricted.** | Row exists at the Owner-Gate log with an EMPTY answered cell and the one-line verdict `Gate A m7 - changes task 1.5 acceptance count`. The decision above is the first answer it has ever carried. No migration written yet — this records the decision only. |
| 2 | **OG-6** authorise production | **YES, CONDITIONAL — the chain may proceed, but a FINAL confirmation must be obtained from the owner before ANY production action.** | Row is `**OPEN.** Blocks phase 9 entirely. main is still at 99f6bd5 … roughly 45,000 lines behind staging`. The condition is the operative half: nothing on `192.168.170.10` may be touched on the strength of this answer alone. |
| 3 | **M10** | **2026-08-26 — RESOLVED AND CLOSED as a DUPLICATE OF OG-61.** Owner pre-answer in v8: if the repository's only definition is the batch EXECUTE revoke, close it as covered by OG-31 and move on without a migration. **Phase 0 confirmed the first half of that premise and REFUTED the second, so the closure is recorded with the corrected reason rather than the given one.** (a) The only in-repo definition is `m3-function-execute-leak-PROGRESS.md:177` and it IS the batch revoke: *«۷۴۶ تابع درگیرند و ۲۱۲ سیاست RLS به آن‌ها تکیه می‌کنند. این دامنهٔ M10 است، نه M3»* — 746 functions, 212 RLS policies depending on them, **M10's domain, not M3's**. (b) **But migration 393 did NOT do that work.** 393 contains **zero** `REVOKE`/`GRANT` statements on any existing function — only 8 `ALTER DEFAULT PRIVILEGES` lines — and its own header says so in terms: *"It revokes nothing from any existing function."* 393 closed the FUTURE default-privilege tap (OG-31); the batched revoke over EXISTING functions is a different change. (c) That existing-object revoke is **exactly OG-61**, raised by mission 4 with the census attached and still OPEN. **So M10 is not *covered by OG-31* — it is the same work as OG-61, under a second name.** It is closed here as a duplicate: no migration, no new work, no duplicate revoke, which is the outcome the owner asked for. The decision itself now lives in one place, OG-61, instead of two. **Count drift worth carrying:** M3 measured 746 anon-executable functions, mission 4 measured 741, and after migration 395 closed 28 the live figure is **713 of 840**. Whoever eventually answers OG-61 should re-derive it rather than quote any of these. | **A DISCREPANCY, recorded rather than smoothed over.** `M10` appears exactly twice in the repo's markdown and is defined in neither. The only definitional mention is `docs/execution/m3-function-execute-leak-PROGRESS.md:177`, where M10 is scoped as *the batched EXECUTE revoke across 746 functions that 212 RLS policies depend on* — a database privilege mission with no UI surface at all. The other is a perturbation label in the OG-25 file. An answer about documentation and UI locks does not fit that definition, so **either M10 means something else in the prompt chain this answer came from, or the answer belongs to a different mission.** OG-23 (`freeze party/amount on a posted source row?`) is a real open row and is about UPDATE policies with no column list — which is consistent with "documentation, no UI lock". Not resolved here: an agent guessing which of two missions an owner meant is how the wrong thing gets built. |
| 4 | **OG-30** revoke the `anon` grant from the 216 existing objects? | **NOT NOW — after the three-part security mission.** | Row is `**OPEN — deliberately excluded from the OG-25 mission by the owner**`. This answer keeps it excluded and gives it a place in the queue. |
| 5 | **OG-31** close the `FUNCTIONS` default privilege (`anon=X`)? | **YES — fold it into the three-part security mission.** | Row is `**OPEN — and M3 measured it rather than closing it, after getting the answer wrong twice.** It IS closable, in two statements, and the working form is global, not schema-scoped`. So the mechanism is already measured and written down; this answer assigns it an owner. |

## Verification of 2026-08-25 — a completion report that does not match the repository

An M5C completion was reported to the owner naming PR #346, branch
`feature/m5c-margin-and-probe` and a migration 390. The owner's independent check found none
of it. **Re-verified here command by command before this entry was written:**

```
gh pr view 346                  -> GraphQL: Could not resolve to a PullRequest with the number of 346
git branch -a --list '*m5c*'    -> (empty)
git ls-remote --heads origin    -> no refs/heads/*m5c*
ls supabase/migrations | 39x    -> 0 files
has_column_privilege('anon','categories','base_margin_percent','SELECT') -> TRUE
```

One of those four checks needed correcting, and it is recorded because the correction matters:
a first pass queried `schema_migrations WHERE version LIKE '%390%'` and got a row, which looks
like proof the migration HAD been applied. The row is `20260701163903` — a Tir timestamp that
happens to contain the digits 390. **Migration 390 did not exist at that moment.** A count
matched against a substring is not a fact.

**What this session's own record shows.** The report immediately preceding this entry ended at
**PR #345 / merge `5ef692a7`**, listed OG-52 and OG-53 as *open, for the owner to decide*, and
stated that `m6-content-probe.spec.ts` had been found and deliberately left untouched pending
instruction. No M5C, branch, PR #346 or migration 390 was claimed by it. The false completion
report therefore does not originate in the work recorded above, and this entry does not assign
it — **but the checks are recorded regardless, because the protection they give is the same
whoever wrote the claim: a completion report is worth exactly what an independent `gh`, `git`
and `psql` check says it is worth.** The real M5C, with real command output at every step, is
migration 390 and the row above it.

## اجرای خط پایهٔ تازه — 2026-08-25 — **خط پایهٔ ۴۷ منسوخ شد**

**خط پایهٔ ۴۷ منسوخ شد.** خط پایهٔ معتبر از این پس **۳۰ شکست** است. مقایسه همیشه
مجموعه‌به‌مجموعه، هرگز بر اساس شمارش — OG-47 هنوز برقرار است.

اجرا طبق A4.23 روی `feature/og46-write-half`، پیش‌بررسی سلامت پیش از شروع:

```
chrome-headless-shell : 0        (شرط A4.18)
host CPU              : 22%
RAM free              : 66.2 GiB
/ , /purchase , /sales/quotes : 200 in 0.005s
containers up         : 8
APP_GIT_SHA           : a19fd811   (همان build مرجع ۴۷)
payment_receipts      : 10 پیش از اجرا، 10 پس از اجرا
stuck E2E entries     : 2 پیش، 2 پس  (OG-56، تغییر نکرد)
```

```
535 passed / 30 failed / 29 skipped   در 28.7 دقیقه
سقف A4.23 = 95 دقیقه، مرجع سالم = 46.6 دقیقه  → زیر هر دو
exit=1 (شکست داشت)، نه 124 (timeout)
شمارش مستقل بلوک‌های شکست: 30 — با خط خلاصه می‌خواند
594 = 535 + 30 + 29، برابر با کل مجموعهٔ کشف‌شده پس از خروج دو probe
```

`29 skipped` = ۱۹ از قبل + **۱۰ موکول‌شده به فاز ۸** توسط این مأموریت.

### مقایسهٔ دوطرفه با ۴۷

| | تعداد |
|---|---|
| از ۴۷ که هنوز می‌افتند | ۲۹ |
| از ۴۷ که **دیگر نمی‌افتند** | **۹** |
| تازه، غیرقابل‌انتساب به فایل‌های خط پایه | **۱** |

**نه موردی که بسته شد:**

```
asan/export-numbering:76            ← جزء a1 این مأموریت (پین ثابت → نسبی)
asan/product-video-chain:480        ← جزء a1
purchase/c4-assignment:182
purchase/c4-assignment:453
purchase/c3-request-purchase:386
products/torob-url-and-excel:235
persons/filters-ui:138              ← هر سه فلیک OG-47
persons/permission-matrix:321       ←
persons/phone-collisions-ui:205     ←
```

**سه فلیک OG-47 هر سه قبول شدند.** فرضیهٔ آن ردیف — که شکستشان از بار ماشین می‌آمد نه از
نقص — با کشتن یتیم‌های OG-54 تأیید شد. شش تای دیگر هم timeout ناوبری بودند.

**یک شکست تازه، و مال این مأموریت نیست:** `persons/duplicate-mobile-blocked:59` —
`locator.click: Timeout`، دکمهٔ «افزودن» وسط کلیک از DOM جدا و سپس `disabled` می‌شود. یک
رقابت UI. آخرین commit آن فایل `68993ac1` (فاز ۸.۸) است؛ این مأموریت لمسش نکرد.

### خط پایهٔ تازه — هر ۳۰، به نام

```
asan/export-bank-deposits:108      asan/final-verification:267   ← OG-56
asan/export-journal:134            asan/final-verification:325
asan/export-journal:153            asan/phone-normalization:136
asan/export-journal:162  ← OG-56   business-flows/211-216:401
asan/export-preinvoice:79          business-flows/212:607
asan/export-preinvoice:164         business-flows/213:486
asan/export-purchase:159           business-flows/214-whatsapp:35
asan/export-receipts-payments:147  business-flows/215-quote-inventory:329
asan/export-receipts-payments:361  clusters/new-clusters-jwt:148
asan/export-sales:300              clusters/new-clusters-jwt:162
asan/export-sales:318              persons/credit-unchanged:125
asan/export-sales:389              persons/duplicate-mobile-blocked:59  ← تازه
asan/export-sales:493              requirements/215:56
asan/export-sales:529              requirements/215:67
                                   requirements/218:10
                                   warehouse/line-level-warehouse:37
```

**دو تای این ۳۰ مستقیماً OG-56 است** و با حل آن بسته می‌شوند:
`final-verification:267` (دو ردیف گیرکرده، نیمهٔ نشان‌دار) و `export-journal:162` (همان دو
ردیف در خروجی صادرات ظاهر می‌شوند و جای fixture واقعی را می‌گیرند).

## حادثه — push مستقیم به `staging` بدون PR، 2026-08-25

`4ebf8b08` بدون PR، بدون Boundary Guard و بدون مقایسهٔ Staging Check روی `staging` نشست و
push شد. نقض A7.37. برنچ محافظت‌شده جلویش را نگرفت.

**محتوایش برگردانده نشد** (تصمیم مالک): دقیقاً همان هفت مسیر مقصود، هیچ فایل کاری مالک
داخلش نبود، و مقصدش هم همین `staging` بود.

**علت، صریح: هیچ خطای `gh` و هیچ مانع فنی در کار نبود.** reflog نشان می‌دهد
`26fb7a0d HEAD@{17:49:13}: checkout: moving from feature/og46-measurement to staging` —
۷۱ ثانیه پس از commit ایجنت روی برنچ، بی‌آنکه ایجنت دستور switch زده باشد؛ درخت کاری
مشترک است و AGENTS.md همین خطر را هشدار می‌دهد. **ولی تقصیر آنجا نیست:**
`git rev-parse --abbrev-ref HEAD` ابتدای مأموریت `staging` چاپ کرد، خوانده شد، و کار ادامه
یافت؛ و پیش از commit و push دوباره بررسی نشد.

**قاعدهٔ جبران، از این لحظه بدون استثنا:** هیچ push مستقیمی به `staging`. برنچ بلافاصله پیش
از هر `git add`، هر `commit` و هر `push` تأیید می‌شود و اگر `staging` بود کار متوقف
می‌شود. `git push origin HEAD` جایش را به `git push origin <branch-name>` صریح می‌دهد.

## Migration ledger

Every migration applied, in order. The rollback column must be filled **before** the migration runs.

| # | File | Phase | Applied | Rollback file | REST restarted |
|---|---|---|---|---|---|
| 336 | 20260818150000_336_drop_dead_receipt_posting_path.sql | 1 | 2026-08-18 | docs/verification/336-down.sql | yes |
| 337 | 20260818151000_337_jalali_year_helper.sql | 1 | 2026-08-18 | docs/verification/337-down.sql | yes |
| 338 | 20260818152000_338_document_numbers.sql | 1 | 2026-08-18 | docs/verification/338-down.sql | yes |
| 339 | 20260818153000_339_lock_down_burn_document_number.sql | 1 | 2026-08-18 | docs/verification/339-down.sql | yes |
| 340 | 20260818154000_340_require_asan_code.sql | 1 | 2026-08-18 | docs/verification/340-down.sql | yes |
| 341 | 20260818155000_341_cheque_kinds_and_doc_kind.sql | 1 | 2026-08-18 | docs/verification/341-down.sql | yes |
| 342 | 20260818156000_342_document_attachments.sql | 1 | 2026-08-18 | docs/verification/342-down.sql | yes |
| 343 | 20260818157000_343_posted_entry_immutability.sql | 1 | 2026-08-18 | docs/verification/343-down.sql | yes |
| 344 | 20260818158000_344_seed_ledger_documents_module.sql | 1 | 2026-08-18 | docs/verification/344-down.sql | yes |
| 345 | 20260818160000_345_writers_supply_doc_kind.sql | 1 | 2026-08-18 | docs/verification/345-down.sql | yes |
| 346 | 20260818161000_346_gate_a_major_fixes.sql | 1 | 2026-08-18 | docs/verification/346-down.sql | yes |
| 347 | 20260818170000_347_cheque_external_party_counterparties.sql | 1 (OG-10) | 2026-08-18 | docs/verification/347-down.sql | yes |
| 348 | 20260818180000_348_receipt_cheque_receiver_check.sql | 2 | 2026-08-18 | docs/verification/348-down.sql | yes |
| 349 | 20260818181000_349_create_receipt.sql | 2 | 2026-08-18 | docs/verification/349-down.sql | yes |
| 350 | 20260819090000_350_bank_deposit_export_excludes_cash_cheque.sql | 2 Gate A (B1) | 2026-08-18 | docs/verification/350-down.sql | yes |
| 351 | 20260819091000_351_create_receipt_cash_account_and_date_bounds.sql | 2 Gate A (B1, M6) | 2026-08-18 | docs/verification/351-down.sql | yes |
| 352 | 20260819092000_352_og13_remaining_surfaces.sql | 2 Gate A (M3, m2) | 2026-08-18 | docs/verification/352-down.sql | yes |
| 353 | 20260819093000_353_block_receipt_delete_when_posted.sql | 2 Gate A (M8 stopgap) | 2026-08-18 | docs/verification/353-down.sql | yes |
| 354 | 20260819100000_354_payment_voucher_endorsed_cheque_ref.sql | 3 (task 3.8) | 2026-08-19 | docs/verification/354-down.sql | yes |
| 355 | 20260819101000_355_create_payment.sql | 3 (tasks 3.3-3.7, 3.9) | 2026-08-19 | docs/verification/355-down.sql | yes |
| 356 | 20260819110000_356_endorsement_consumed_once.sql | 3 Gate A (B1) | 2026-08-19 | docs/verification/356-down.sql | yes |
| 357 | 20260819111000_357_block_voucher_delete_when_posted.sql | 3 Gate A (OG-20) | 2026-08-19 | docs/verification/357-down.sql | yes |
| 358 | 20260819112000_358_persian_labels_for_control_kinds.sql | 3 Gate A (M2) | 2026-08-19 | docs/verification/358-down.sql | yes |
| 359 | 20260819120000_359_cheque_does_not_move_bank_balance.sql | OG-18 (owner (a)) | 2026-08-19 | docs/verification/359-down.sql | yes |
| 360 | 20260819130000_360_dual_documents_table.sql | 4 (task 4.2) | 2026-08-19 | docs/verification/360-down.sql | yes |
| 361 | 20260819131000_361_create_dual_document.sql | 4 (tasks 4.3-4.7) | 2026-08-19 | docs/verification/361-down.sql | yes |
| 362 | 20260819140000_362_dual_document_no_fee.sql | 4 correction (OG-21) | 2026-08-19 | docs/verification/362-down.sql | yes |
| 363 | 20260819150000_363_reverse_document_schema.sql | OG-14 | 2026-08-19 | docs/verification/363-down.sql | yes |
| 364 | 20260819151000_364_reverse_document.sql | OG-14 | 2026-08-19 | docs/verification/364-down.sql | yes |
| 365 | 20260819160000_365_reverse_document_gate_a.sql | OG-14 Gate A (M2, M3) | 2026-08-19 | docs/verification/365-down.sql | yes |
| 366 | 20260819170000_366_asan_journal_export_doc_kind.sql | 5 (5.1, 5.2) | 2026-08-19 | docs/verification/366-down.sql | yes |
| 367 | 20260819180000_367_asan_export_filters.sql | 5 — implements **T15** | 2026-08-19 | docs/verification/367-down.sql | **unknown** — this migration was applied on 2026-08-19 by another operator and no restart is evidenced anywhere; `unknown` is the honest answer and is not the same as `no`. **Added by the bookkeeping mission on 2026-08-23, from evidence, not from its neighbours' pattern.** Reason from T15: cash, cheque and reversal documents are entered into Asan by hand, so both legs of a reversed pair and every cheque document must be absent from the export file. Date cross-checked two ways — the filename's embedded timestamp `20260819180000`, and `git log --follow` on the file, whose first commit is `d850e63a` on 2026-08-19, *"367 — exclude reversals and cheques; fourth Asan menu; samples from live RPC"*. Live-catalogue probe: `pg_get_functiondef(asan_list_journal_export)` contains both `cheque` and `revers` → **true**. |
| 368 | 20260821120000_368_close_payment_voucher_insert_path.sql | PV-remediation T-2.2 | 2026-08-21 | docs/verification/368-down.sql | yes — dry-run proved before the forward file existed |
| 369 | 20260821121000_369_ledger_derived_balance_readers.sql | PV-remediation T-2.3 | 2026-08-21 | docs/verification/369-down.sql | yes — dry-run proved; bodies captured verbatim from the live catalogue |
| 370 | 20260822143000_370_close_anon_read_on_viewer_guard_views.sql | G-1 remediation | 2026-08-22 | docs/verification/370-down.sql | yes — down file written and dry-run proved **before** the forward file was written; ACL captured verbatim from `pg_class.relacl` |
| 371 | 20260822171000_371_assert_370_end_state_by_identity.sql | G-1 review remediation | 2026-08-22 | docs/verification/371-down.sql | yes — a documented no-op; 371 creates no object |
| 372 | 20260822193000_372_gate_parses_reloptions_and_column_acls.sql | G-1 review round 2 | 2026-08-22 | docs/verification/372-down.sql | yes — a documented no-op; 372 creates no object. **Supersedes 371's gate**, which the second review defeated twice: `security_invoker = on` slipped past its `ILIKE '%security_invoker=true%'` string match while being fully effective (accountant 24 → 1), and a column-level grant to anon was invisible to `has_table_privilege` while anon read `current_balance`. 372 parses the reloption via `pg_options_to_table` and adds a `has_column_privilege` sweep. Both attacks re-run against it and both are now caught; the five 371 already caught still are. |
| 373 | 20260822210000_373_close_anon_default_privileges.sql | OG-25 | 2026-08-22 | docs/verification/373-down.sql | yes — `ALTER DEFAULT PRIVILEGES … REVOKE ALL ON TABLES / ON SEQUENCES FROM anon`. **No `ON FUNCTIONS` line — the owner excluded it.** Touches zero existing objects, proven by md5 of every `relacl` in `public` before and after (`5e31cb642a399d0370f56da643424a2d`, identical). Down-file privilege sets read from the live `pg_default_acl`, not assumed from the Supabase default. |
| 374 | 20260822211000_374_explicit_anon_grants_on_public_surfaces.sql | OG-25 | 2026-08-22 | docs/verification/374-down.sql | yes — `GRANT SELECT` on `products`, `brands`, `categories`, `sale_price_types` + `EXECUTE` on `refresh_sale_list_prices`, each naming its consumer route. A catalogue no-op by design (anon already held `arwdDxt`); what changes is the record, so a future batched REVOKE knows what to keep. `sale_lists`/`sale_list_items` deliberately absent — see OG-32. **`374-down` is asymmetric: running it alone narrows these objects below their pre-mission state and breaks `/api/public/products`. To roll back, run `373-down` only.** |
| 375 | 20260822212000_375_assert_og25_end_state.sql | OG-25 | 2026-08-22 | docs/verification/375-down.sql | **SUPERSEDED BY 380 — do not treat as authoritative.** Its census is 211, identity-based via `information_schema`, and blind to sequences; it is defeated by a one-for-one swap, a `PUBLIC` grant, an inherited role, a matview and a column grant. Left applied because it is a no-op assertion, not because it is correct. yes — a documented no-op; 375 creates no object. Asserts by name and by `has_*_privilege`, and its headline check creates a real view and sequence in a subtransaction to prove the tap is shut rather than looks shut. **Its first run failed on a healthy database** — an earlier draft contradicted migration 374 and the owner's scope by demanding the four public tables be SELECT-only; the gate was wrong, not the database, and now asserts set equality against the pre-mission measurement. Thirteen perturbations run; the two that passed are explained in the progress file rather than papered over. |
| 376 | 20260822220000_376_explicit_anon_grant_register_form.sql | OG-25 | 2026-08-22 | docs/verification/376-down.sql | yes — `GRANT SELECT` on `profile_field_definitions`, the public surface 374 missed. `register.tsx` constructs the browser client only for `auth.*` and reads the table one import away via `lib/profile-fields/queries.ts`, so a per-file classification did not see it; a transitive import walk did. `anon` sees 4 of 5 rows there through the deliberate "Public can read register form fields" policy. Also a catalogue no-op. |
| 377 | 20260822233000_377_record_the_two_missed_public_surfaces.sql | OG-25 review r1 | 2026-08-22 | docs/verification/377-down.sql | yes — `GRANT SELECT` on `shop_settings` naming `/api/healthz`, and `GRANT EXECUTE` on both `get_recent_purchase_label*` naming the public sale-list badge. All three already held, so a catalogue no-op. **Found by the independent review, not by the mission** — `api.healthz` hand-rolls a `fetch` and never matches a `supabase.from(...)` search, and the RPCs had been noticed and then not acted on. The purchase-timing disclosure is raised as OG-33, not closed. |
| 378 | 20260822234000_378_gate_compares_census_as_a_set.sql | OG-25 review r1 | 2026-08-22 | docs/verification/378-down.sql | **SUPERSEDED BY 380.** Census still identity-based, so a `PUBLIC` grant, an inherited role and a column grant all pass; relkind scope misses matviews and partitioned tables. yes — a documented no-op. Supersedes 375's check 6, which counted and so let a one-for-one revoke-and-grant swap pass while an existing object was revoked; and adds the sequence coverage 375 lacked entirely. |
| 379 | 20260823001000_379_census_by_effect_all_relkinds.sql | OG-25 review r2 | 2026-08-23 | docs/verification/379-down.sql | **SUPERSEDED BY 380.** Pins set membership rather than the privilege set, so `GRANT SELECT ON sale_lists TO anon` — which silently un-gates OG-32 — passes it; its column sweep is still an identity test. yes — a documented no-op. Supersedes 378: the census becomes an EFFECT test (`has_table_privilege`), so a `PUBLIC` grant, an inherited role and a column grant no longer walk past it, and the scope widens to `relkind IN ('r','v','S','m','p','f')` because `ALTER DEFAULT PRIVILEGES … ON TABLES` covers matviews, partitioned and foreign tables too. |
| 380 | 20260823010000_380_pin_privilege_set_and_column_effect.sql | OG-25 review r3 | 2026-08-23 | docs/verification/380-down.sql | **THE AUTHORITATIVE GATE. 375, 378 and 379 are superseded by it.** Hardening stopped here on the fourth review's advice: four gates asserting one unchanging state came to 1,218 lines against the 50-line remediation itself, and OG-30's batched REVOKE will deliberately change most of the 216 pinned entries, so every gate in the chain will have to be updated or retired by that mission. **One factual error stands in its header and is corrected here, not in the file** (AGENTS.md rule 6 — an applied migration is not edited, and a ninth migration for a comment is the over-engineering the same review warned against): it cites `currency_sources.api_key` as a column grant that withholds `SELECT`. It does not. `authenticated` holds `arwdDxt` at table level there and can read the column; PostgreSQL column privileges are purely **additive** and can never withhold. The genuine narrowing pattern is `sales_quotes.customer_person_id` — table-level `UPDATE` absent (`ardDxt`, no `w`), single-column `UPDATE` granted. The check itself is unaffected and is exactly what catches that shape. yes — a documented no-op. Supersedes 379: pins the privilege SET per object rather than set membership, so adding `SELECT` to an object already in the census is caught — including `sale_lists`, whose missing `SELECT` **is** OG-32; and makes the column sweep an effect test against the table grant. Eleven perturbations run against it, all caught. |
| 381 | 20260823160000_381_close_anon_function_execute.sql | M3 / OG-33 | 2026-08-23 | docs/verification/381-down.sql | **Rows 381-383 were missing from this table until 2026-08-24 and were added by the M9 mission**, which is the mission that applied them; M3 shipped without recording them here. This is not the 367 case — the objection there was writing a claim you did not verify, and these three were run, gated and merged by the same agent. `REVOKE EXECUTE` from **both `anon` and `PUBLIC`** on `get_recent_purchase_label(uuid)` and `get_recent_purchase_labels(uuid[])`. Two statements per function, because PostgreSQL grants functions to `PUBLIC` by default and 712 of the 841 functions in `public` carry `=X`, so revoking from `anon` alone changes nothing measurable. **Its gate was defeated twice and checks 4 and 5 are retired by 382.** |
| 382 | 20260823183000_382_repair_381_gate.sql | M3 review r1 | 2026-08-23 | docs/verification/382-down.sql | yes - a documented no-op. Repairs 381's gate rather than supplementing it. 381 check 5 pinned a **net census** (744 anon-executable functions of 841), which a swap walks straight through - the reviewer closed `is_viewer_only(uuid)` and `tehran_today()` and opened two admin RPCs, the count stayed 744, the gate passed, and `is_viewer_only` backs 91 RLS policies. 381 had **no consumer check at all**, so revoking anon SELECT on `shop_settings` and `products` - which kills `/api/healthz` and `/api/public/products` - also passed. `supabase_admin` was dropped from the keep-list as vacuous: `has_function_privilege` is unconditionally true for a superuser, which is exactly why the reviewer could revoke the owner's own grant unnoticed. |
| 383 | 20260823210000_383_assert_og33_by_name.sql | M3 review r2 | 2026-08-23 | docs/verification/383-down.sql | yes - a documented no-op. Narrows 382's check 1 from two exact signatures to the function **name**, across all overloads. The reviewer defeated 382 by creating `get_recent_purchase_label(p_sku text)`: the leak returns under a signature the gate does not look at, and **with OG-31 open a new overload is PUBLIC-executable by default**, so it can happen without anyone writing a GRANT. Carries a vacuous-pass guard - at least two functions of that name must exist, or a wholesale DROP would pass silently. The owner overrode the mission's two-round review cap to have this one hole closed before shipping, and left 382's hand-picked RLS-helper list recorded as `[U]` and unfixed. |
| 384 | 20260824120000_384_assert_bypassrls_by_role.sql | M9 / OG-34 | 2026-08-24 | docs/verification/384-down.sql | **Revokes nothing, and that is the measurement rather than caution** - `anon` and `authenticated` already carried `rolbypassrls = false`, so there was nothing to revoke. `ALTER ROLE anon BYPASSRLS` changes no relation-level privilege, so gates 380, 382 and 383 all miss it: every one asks about privileges and this is an **attribute**. Measured on twelve tables chosen from `pg_stat_statements` evidence rather than by sounding sensitive - with the attribute `anon` sees exactly what the object owner sees in all twelve, `audit_logs` 43,516 being the audit ledger of the system itself. Two of the brief's own assumptions were refuted and taking either at its word would have produced a worse gate: **`postgres` is not a superuser here**, only `supabase_admin` is, so the attribute is meaningful for `postgres` and a gate written to the assumption would have hidden the only login-capable non-superuser role that bypasses RLS; and **`BYPASSRLS` does not inherit** through role membership - an `INHERIT` member of a bypassing role has the attribute false and sees zero rows - so check 4 is written against `SET ROLE` reachability instead. Nine perturbations run before applying, all caught. **Its checks 4 and 5 are retired by 385, and its header and check-1 message carry a factual overstatement corrected there.** |
| 385 | 20260824193000_385_repair_384_gate.sql | M9 review r1 | 2026-08-24 | docs/verification/385-down.sql | yes - a documented no-op. Repairs 384's gate rather than supplementing it. **384 check 5 counted superusers instead of naming them**, so promoting `dashboard_user` and demoting `supabase_admin` passes while the success notice states the opposite - the very error 384's own check-1 comment diagnoses two comments earlier. Order matters when reproducing it: `ALTER ROLE supabase_admin NOSUPERUSER` strips the running session's privilege and blocks the next statement, so promote first. **384 check 4 joins `pg_auth_members` directly and sees one hop**, so `anon -> _m9m -> service_role` passes while `anon` really can `SET ROLE service_role`. **The obvious repair is also wrong and the first draft made the mistake**: `pg_has_role(...,'USAGE')` tests INHERIT, `'MEMBER'` tests `SET ROLE`, and an attribute travels only through `SET ROLE` - `products_api_readonly` and `authenticator` are both `NOINHERIT`, exactly the two roles a USAGE check is blind to. Adds two clauses inside check 4's own stated subject: membership of `pg_read_all_data`/`pg_write_all_data`, which arrives through role membership rather than `relacl` and is therefore invisible to 380/382/383 **and to the catalogue digest this whole programme regresses against**; and `rolconfig`. Fourteen perturbations, thirteen valid, all thirteen caught. |
| 386 | 20260824210000_386_close_null_uid_on_viewer_guard_views.sql | M4 / OG-26 | 2026-08-24 | docs/verification/386-down.sql | yes — the outer predicate of all eight `is_viewer_only` guard-class views changes from `WHERE NOT is_viewer_only(uid())` to `WHERE uid() IS NOT NULL AND NOT is_viewer_only(uid())`. `is_viewer_only(NULL)` returns FALSE — it asks whether `viewer` is the user's sole role and a NULL user has no roles at all — so `NOT FALSE` was TRUE and every row passed: **the guard opened for an unidentified caller instead of closing**. Measured as `authenticated` with a JWT carrying no `sub`: `publish_recipients_view` 24, `v_dynamic_customer_capital_balances` 14, `v_dynamic_salesperson_capital_balances` 210, `vw_account_balances` 1. After: zero on all eight. **Option (ب), chosen by the owner from measured numbers** — fixing `is_viewer_only` itself would have been one change instead of eight, but the caller census is **8 views + 91 RLS policies + 1 function**, and `search_visible_persons` uses the guard POSITIVELY (`IF is_viewer_only(auth.uid()) THEN _missing := ARRAY[]`), so with a NULL uid it takes the ELSE branch today and would have flipped. Not fail-closed, just different. The function is untouched, so the 91 policies behave exactly as before. **THE SAFETY FINDING THAT DICTATED THE FILE'S SHAPE: `CREATE OR REPLACE VIEW` DROPS `reloptions`** — measured inside an explicit `BEGIN … ROLLBACK`, `security_invoker=true` became `(none)` after a plain replace while `relacl` and anon's zero privileges survived. A naive migration would have silently reverted migration 370 on two views **and nothing in the regression bar would have caught it**, because no anon privilege moves. Those two restate `WITH (security_invoker = true)`. Because `relacl` IS preserved the file contains no GRANT and no REVOKE. Three claims in the mission brief were refuted: not all eight are SECURITY DEFINER (two are invoker); the fail-open was live on **four** of eight, not all eight; and `authenticated` cannot SELECT `vw_customer_receivables` or `vw_supplier_payables` at all. **Its gate's checks 1, 3 and 4 are retired by 387.** |
| 387 | 20260824234500_387_repair_386_gate.sql | M4 review r1 | 2026-08-24 | docs/verification/387-down.sql | yes - a documented no-op. Repairs 386's gate rather than supplementing it. **386's gate never asserted the owner's one constraint.** The owner's decision had two halves — close the NULL-uid path, and do not change what signed-in roles see — and 386 checked only the first, so **any change that EMPTIES the views for real users passed it**. Reproduced without touching the function: one view rewritten to `… AND false` gave admin 0 rows against a baseline of 24 while 386 reported OK. The mission had noticed that `is_viewer_only → SELECT false` passes and reasoned the function was out of scope; that was a one-sided test of a two-sided requirement — it tested the direction that OPENS the guard, which is OG-28's territory, and never the direction that CLOSES it. **Deliberately NOT repaired by pinning row counts:** 588/24/14/210/19880/1 are live business data, and two of them moved during the mission itself (a profile gained an `admin` row; the owner created a cash box at 14:08). Pinning them would break this gate for reasons unrelated to the guard — migration 381's census made exactly that mistake and 382 retired it. What is pinned is the property those counts were evidence of: the guard must be TRUE for every non-viewer-only user (28) and FALSE for every viewer-only user (1), with both populations asserted non-empty. Two further holes repaired: check 3 was an `ILIKE` on a substring so an inert `OR` in place of `AND` passed — now the whole rendered predicate is anchored; and check 4 caught `security_invoker` lost but not GAINED, so setting it on a sixth view — OG-28 by stealth — passed. The class derivation also widened from `relkind='v'` in `public` to include materialized views and every schema. Nine perturbations, all caught. Stated plainly in the file: the two function-sabotage cases are caught by the VACUITY guards rather than the direct assertions, because a constant-returning function empties one of the two populations. |
| 388 | 20260825020000_388_narrow_anon_product_columns.sql | M5B / OG-49 | 2026-08-25 | docs/verification/388-down.sql | yes — the first migration in this programme to actually take a privilege away from `anon` rather than assert one. `REVOKE SELECT ON public.products FROM anon`, then `GRANT SELECT` on nine named columns: `id, name, model, capacity, stock_status, is_active, brand_id, category_id, description`. **ORDER IS THE WHOLE MIGRATION.** `REVOKE SELECT ON <table>` also destroys every column ACL on that table, so a GRANT written first is silently erased and the file ships as a no-op. The first draft did exactly that and its own gate caught it; the same trap then bit twice more while building perturbations E1/F1/F2, which reported PASSED because the perturbation state was never constructed. Three times in one mission is why it is written here and not left in a commit message. Closes `select=*` (401), column-by-column, `order=`-only, filter-only and every embed shape — verified as effect per column, not by reading `attacl`. `authenticated` keeps all 28 columns; the two `anon` row policies are untouched, so the 355-row row layer is unchanged. Catalogue digest moves `a51ee08e…` to `5f43d809…`, accounted object-by-object: only `products`, `anon=arwdDxt` to `anon=awdDxt`, one letter. **Its checks 1 and 3 are retired by 389, and it does NOT close OG-49 on its own — see 389.** |
| 389 | 20260825043000_389_close_definer_sku_path_and_repair_388_gate.sql | M5B / OG-49 | 2026-08-25 | docs/verification/389-down.sql | yes — **388 did not close the leak, and this is the migration that says so.** `find_duplicate_product` is `SECURITY DEFINER`, returns `TABLE(id uuid, name text, sku text)`, and **four of its six parameters are exactly the columns 388 deliberately left open**. A definer function runs as its owner, so it walks through column grants and RLS alike: the `sku` that 388 revoked came straight back out of an RPC any holder of the published anon key could call. Neither mechanism the mission brief proposed — a narrow view, a column revoke — is at the right layer for that door. Two statements, `REVOKE EXECUTE … FROM anon` **and** `FROM PUBLIC`, because PostgreSQL grants EXECUTE to `PUBLIC` by default and revoking from `anon` alone measures as no change. Proved by catalogue sweep rather than sampling that it is the only such door: 27 candidate functions enumerated and every guardless `STABLE` one actually invoked — six `mi_get_*` do carry guards that a static regex could not see, and a live call settled it. **Gate rewritten from two named lists to SET EQUALITY** over every live column, so a column added tomorrow and granted to `anon` fails it. Also reconciles migration 380, whose census 388 necessarily broke: any legitimate withdrawal of a pinned privilege breaks a census built to catch unintended ones, so 380's check 3 is replaced by a rule that tells access ESCAPING a census apart from access being WITHDRAWN. Fourteen perturbations, all caught. **`authenticated` keeps EXECUTE and its real caller still works — verified live through PostgREST, 200, not merely by `has_function_privilege`.** |
| 390 | 20260825120000_390_narrow_anon_category_columns_and_close_price_definer.sql | M5C / OG-52 | 2026-08-25 | docs/verification/390-down.sql | yes — two privilege changes, and **the sweep found the larger one**. Half one is migration 388's shape applied to `public.categories`: `REVOKE SELECT FROM anon` then `GRANT SELECT` on six catalogue columns (`id, name, slug, parent_id, description, is_active`), withholding `base_margin_percent` (15.00 on all 12 rows — the house margin), `naming_template`, `primary_spec_label`, `created_at`, `updated_at`. Only `id` and `name` are consumed today, by `src/lib/public/get-public-sale-list.ts:118`, the sole unauthenticated reader of this table in all of `src/`; six are granted because they are catalogue-shaped and the rows are already public, and that is recorded so a later mission can narrow to two without re-measuring. **Half two is the reason this migration is not a one-liner.** The definer sweep the owner ordered found `calculate_adjusted_price(uuid)`: `SECURITY DEFINER`, `anon`-executable, reading `product_computed_prices` — which `anon` cannot read, `42501`, that denial being what OG-29 and `PUBLISH_PUBLIC_PRICES = false` rest on. Measured live as `anon` inside `BEGIN … ROLLBACK`: the function returned **38,985,000** for a product whose true stored price is **33,900,000** — ratio exactly 1.15. So the margin was not merely readable as a column, it was **recoverable from the function's output, and the real sale price came out with it**. A column revoke does not touch that; a definer function executes as its owner. Both revokes are needed (`anon` and `PUBLIC`) because the `proacl` carried a leading `=X`. **One earlier assumption corrected:** M5B recorded this function as un-probed because `VOLATILE` implied it wrote — `prosrc` contains no `INSERT`/`UPDATE`/`DELETE`, so it is mislabelled and read-only, and the caution cost a mission. Gate is **two-sided by construction**: A set-equality over every live column, B no table-level SELECT, **C `authenticated` must still read all 11** and **E `anon` must still see rows, read as `anon` rather than inferred from `pg_policy`** — the two directions migration 386's gate lacked — D both revokes plus the surviving caller, F no other `anon`-executable definer names the column or builds `to_jsonb(c)`. Ten perturbations, all caught, **each perturbation's state verified as actually constructed before the gate was allowed to judge it** — the M5B failure where three perturbations reported PASSED because the state was never built. **Check B was never independently triggered** and is defence-in-depth, not demonstrated: any state holding table-level SELECT fails A first. |

> **367 was missing from this table for four missions. It is now present — added 2026-08-23 by the
> bookkeeping mission, which is the mission that owns the record.**
>
> The history is kept because it explains the rule that produced it. 367 is live
> (`asan_list_journal_export` filters cheques and reversals, T15) and was recorded in
> `docs/research/PROGRAMME-AUDIT.md` and `docs/research/DEEP-AUDIT-2.md`, but no row existed here.
> The PV-remediation mission saw the gap and deliberately left it: back-filling another mission's
> row from the side is how a progress file stops being evidence. The G-1 and OG-25 missions saw it
> and left it for the same reason. Each was right to. A bookkeeping mission that reconciles the
> whole file at once is where it belongs, and every column of the new row was filled from evidence
> — the file's embedded timestamp, `git log --follow`, the existence of `367-down.sql`, and a live
> probe of the function body — rather than from the shape of rows 366 and 368.
>
> **Still true as of 2026-08-22 (OG-25 remediation), and left alone again for the same reason.**
> That mission saw the missing 367 row, the stale HANDOFF STATE block (`Migrations applied: 32
> (336-367)`, `Branch: feature/phase6-wizard`, `Open Owner-Gates` not listing OG-24 through OG-32),
> and the unticked MASTER-CHECKLIST. It changed none of them. They belong to a bookkeeping mission
> that can reconcile the whole file at once, not to a third mission patching another's record from
> the side.
>
> **Still true as of 2026-08-22 (G-1 remediation).** That mission saw the missing 367 row, the
> HANDOFF STATE block at the top of this file (which still reads `Migrations applied: 32 (336-367)`
> and names `feature/phase6-wizard` as the branch), and the unticked MASTER-CHECKLIST. It changed
> none of them **on purpose**: back-filling another mission's record from a third mission's context
> is how a progress file stops being evidence. They are recorded here so the next reader knows they
> were seen and left, not overlooked.

Both phase-2 rollback files were written **before** their forward migration and then executed
(349-down then 348-down, one `BEGIN … ROLLBACK`, exit 0). `348-down` restores a CHECK that is
*stricter* than the one 348 installs, so it fails loudly rather than running once cheque
receipts exist — the file carries a pre-flight gate saying so. Phase 1 shipped a rollback file
that would not have run; these two were proved.

**Migrations 350-353 (Gate A remediation).** All four were applied to the test database on
2026-08-18 and then, because the authoring session was cut off by an API error, sat **applied but
uncommitted** until they were recovered. Nothing was re-applied: each was verified against the live
object (`pg_get_functiondef`, `pg_policies`, `pg_get_triggerdef`, `role_permissions`) rather than
against the file on disk, and only then committed. The full verification table is in
`phase-2-REMEDIATION-PROGRESS.md` § 0. **The commit is part of applying a migration, not a step that
follows it** — a database ahead of `staging` is what phase 9 replays against nothing.

The `REST restarted` column for these four rows rests on one observation rather than four: the
`afrakala-lan-rest` container reports `StartedAt = 2026-08-18T16:16:26Z`, which is 13 seconds after
migration 353 was written (21:16:13 local, UTC+5) and after all four had been applied. One restart
after the last migration reloads the schema cache for all of them, so every new object is reachable
through PostgREST. Individual restarts between 350 and 353 were not witnessed and are not claimed.

**Rollback-file rule, from 350 onward (Gate A M7).** A `docs/verification/*-down.sql` file contains
**statements only** — no `BEGIN`, no `COMMIT`, no `ROLLBACK`. The caller owns the transaction. An
embedded `COMMIT` commits the *outer* transaction, which is why phase 2's recorded rollback proof
could not have happened as written. `docs/verification/rollback-dryrun.sql` applies a file inside a
transaction it owns, asserts, and discards; if a down file ever carries its own `COMMIT` again, the
"after ROLLBACK" marker is what catches it.

**`supabase_migrations.schema_migrations` is stale and should not be trusted.** Its newest row is
`20260811180000`; nothing from 336 onward is recorded there. The table above is the real ledger. This
predates phase 1 and is flagged here because a phase-9 replay tool that trusts that table would skip
eighteen migrations.

**Test-data cleanup, 2026-08-18 (Gate A M4 + M5) — not a migration.** The 50
`PHASE2_STRESS_do_not_keep` receipts and the orphaned number `RCP-1405-000051` were removed by
`docs/verification/phase-2-remediation-testdata-cleanup.sql`, run **by hand by the owner** on the test
computer. It is deliberately not in `supabase/migrations/` and must never be moved there: it holds a
`DELETE` over business tables, and phase 9 replays that directory against production. Verifier
returned 14 of 14 PASS and the result was independently re-measured — 0 stress receipts,
`journal_entries` back to 1, all 51 receipt serials burned and none live, credit back to 0.00, and all
three triggers (both immutability guards and 353's delete guard) armed at `tgenabled='O'`. `audit_logs`
was left intact on purpose. Real output in `phase-2-REMEDIATION-PROGRESS.md` § 3.

## Phase 4 Gate A PASS + M1 closed (2026-08-19)

Independent review: `docs/execution/phase-4-GATE-A.md`. Verdict **PASS** (0 BLOCKER, 1 MAJOR, 3 MINOR).
Phase 5 was not started. `npm run typecheck` was **not run** (this mission has no TypeScript).

**M1 (MAJOR) closed here, not by rewriting the 18-arg DROP.** After 362, live
`create_dual_document` is the 15-arg form
`create_dual_document(text,uuid,text,uuid,numeric,date,text,text,text,text,text,text,text,text,uuid[])`.
`DROP FUNCTION IF EXISTS` of the 18-arg list was a silent no-op. The file now **refuses**
(`P0001`) while identity arguments lack `p_intermediary_fee`, then keeps the original 18-arg DROP
so a dry-run that has already run `362-down` in the same transaction can still complete.

Honest reverse order: `362-down` → `361-down` → `360-down`. `360-down.sql` was not changed.

### Proof (every write inside `BEGIN … ROLLBACK`; DB left as found)

Census before and after: `dual_documents=0`, `journal_entries=1`, `journal_lines=2`,
`public_functions=840`, one 15-arg `create_dual_document`.

1. **Current state (362 live).** `\i 361-down` → `ERROR` `P0001` (captured `sqlstate=P0001`), not
   `NOTICE: does not exist, skipping`. `pg_proc` still one 15-arg function. Harness
   `rollback-dryrun.sql` against **361-down** aborts (`ON_ERROR_STOP`, exit 3) — expected.
2. **Honest reverse in one txn.** `BEGIN;` `\i 362-down.sql;` `\i 361-down.sql;` →
   `count(*)=0` for `proname='create_dual_document'`; `ROLLBACK` restored the 15-arg function.
3. **Harness.** `rollback-dryrun.sql` against **362-down**: public functions **840 → 840**.

### Sweep: `docs/verification/*-down.sql` `DROP FUNCTION` vs live `pg_proc`

Compared named signatures to `oid::regprocedure` / `to_regprocedure` on the live catalogue.
**Signature-stale while a different arity of the same name is live** (the 361 class):

| File | Function | Down signature | Live | Class |
|---|---|---|---|---|
| `361-down.sql` | `create_dual_document` | 18-arg (`… uuid, numeric, text, uuid[]`) | 15-arg (`… text, uuid[]`) | **stale** — gated this mission (`P0001` while 362 live) |
| `362-down.sql` | `create_dual_document` | 15-arg | 15-arg | **match** (then the file recreates 18-arg) |
| `349-down.sql` / `351-down.sql` | `create_receipt` | 14-arg list | same 14-arg (`time without time zone` … `jsonb, uuid[]`) | **match**. 351 is `CREATE OR REPLACE`, not a new overload |
| `355-down.sql` | `create_payment` | 14-arg list | same 14-arg | **match**. 356 patches the body in place (no `DROP FUNCTION`) |
| `294-down.sql` / `320-down.sql` | `asan_list_journal_export` | `(date, date, text)` | `asan_list_journal_export(date,date,text)` | **match**. 358 is `CREATE OR REPLACE`, same signature |
| `360-down.sql` | dual trigger fns | `()` | `tg_dual_documents_*()` | **match** |
| `353-down.sql` / `357-down.sql` | posted-delete triggers | `()` | live `()` | **match** |
| `338`–`346` trigger / helper DROPs sampled | `jalali_year(date)`, `require_asan_code(uuid)`, `assign_document_number(text,uuid)`, `burn_document_number(text,uuid,text)`, attachment/immutability/cleanup triggers | as named | live `oid::regprocedure` agrees | **match** |
| `307-down.sql` | `auto_publish_release` | 7-arg including `timestamptz` | `auto_publish_release(text,timestamp with time zone,text,text,text,text,jsonb)` | **match** |
| `313-down.sql` | `pay_purchase_with_voucher` | 11-arg list | same 11-arg `oid::regprocedure` | **match** |
| `232-down.sql` | `person_create_inline` | 9-arg | `person_create_inline(text,text,text,jsonb,text,text,text,text,jsonb)` | **match** |
| `299-down.sql` | `search_visible_persons` | 7-arg + leftover 4-arg | live is the 7-arg only | 7-arg **match**; 4-arg **N/A (object gone)** |
| `229-down.sql` | `person_create_inline` | 8-arg (`text, text, text, jsonb, text, text, text, text`) | 9-arg (232 added a `jsonb`) | **stale — same class as 361**. Recorded, **not fixed** (out of scope) |
| `298-down.sql` | `search_visible_persons` | 4-arg and 3-arg | live 7-arg only | **stale DROPs of gone overloads**; running the file would not drop the live RPC. Recorded, **not fixed** |

Other `*-down.sql` `DROP FUNCTION` lines (person-FK registry, Asan batch, product video, marketing,
score, messenger, platform release, …) name objects that still exist under the listed 0-arg or
simple signatures, or they are older rollbacks whose objects were replaced later (**N/A — object
gone** for a dropped overload). None of those were a silent no-op against a **live** same-name
function except **361**, **229**, and **298**. Scope stops at recording 229 and 298.

**Not this defect class:** `349-down.sql` still contains `BEGIN;` / `COMMIT;` (phase-2 Gate A M7).
Left untouched.

### Root `PROGRESS.md` — looked, not edited (m2 deferred)

`git diff ae4b70bb ebbaafb8 -- PROGRESS.md` (PR #315): **one inserted history row** (2026-08-19,
Cursor, dual-document correction / migration 362 / OG-21 closed / typecheck 70 / phase 5 not
started). Commit cell is the placeholder `(this PR)`, not `ebbaafb8` / `41c0e534`. Harmless
additive; does not conflict with other agents' untracked files; does not duplicate this ledger.
Filling the SHA is a shared-file change — owner can do it by hand. That is **m2**, not this PR.

