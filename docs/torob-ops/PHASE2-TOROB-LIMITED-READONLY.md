# Deprecated: TOROB_LIMITED_READONLY

The Phase-2 job type `TOROB_LIMITED_READONLY` and
`automation/worker-runtime/src/drivers/torob_limited_readonly.py` are **deprecated**.

Live Torob reading is `workers/torob-eye` (`afrakala-lan-torob-eye`).
Leave the old driver in place; do not enqueue new jobs.
