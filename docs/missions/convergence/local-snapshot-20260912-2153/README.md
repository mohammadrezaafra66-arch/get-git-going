# عکسِ محلیِ ۲۰۲۶-۰۹-۱۲ ۲۱:۵۳ — وضعیت orchestrator که فقط روی دیسک بود

این دو فایل **نسخهٔ متعارف نیستند**. نسخهٔ متعارف همان
`docs/missions/convergence/RESUME.md` و `STATE.md` در ریشهٔ همین پوشه است و
**تازه‌تر** است.

## چرا اینجا هستند

در فاز ۰ کار «release-line runtime-config»، یازده فایل untracked جلوی
`git checkout -b ... origin/main` را گرفتند، چون همان مسیرها در `origin/main`
tracked بودند. هر یازده فایل پیش از هر حذفی به
`D:\AfraKalaTest\_preserved-untracked-20260913` کپی و بایت‌به‌بایت راستی‌آزمایی شدند.
نُه تا با نسخهٔ tracked یکی بودند؛ این دو فرق داشتند.

## اندازه‌گیری — کدام تازه‌تر است

|  | tracked (`origin/main`) | این عکس |
|---|---|---|
| `STATE.md` | commit `a5cdc580`، `2026-09-12 23:47:40 +0330` (UTC 20:17)، ۹۸۷ خط | mtime `21:53:15 +0500` (UTC 16:53)، ۶۶۹ خط |
| `RESUME.md` | commit `9268cd9d`، `2026-09-12 23:43:44 +0330`، ۸۸ خط | ۸۵ خط |

یعنی نسخهٔ tracked حدود ۳ ساعت و نیم **جلوتر** است و `STATE.md` آن ۳۱۸ خط بیشتر دارد.

## اشتباهی که رخ داد و اصلاح شد

commit `f690327a` این عکس را روی نسخهٔ متعارف نوشت — یعنی محتوای تازه‌تر را با
قدیمی‌تر جایگزین کرد. با `git revert` برگردانده شد و عکس به اینجا منتقل شد.

تفاوت این دو ممکن است برای بازسازی تاریخچهٔ E-4 مفید باشد (این عکس می‌گوید
«E-4 در checkpoint 1 متوقف شد»، نسخهٔ متعارف می‌گوید «آغاز زنجیرهٔ پنج‌موجی
close-out»). هیچ‌کدام حذف نشده‌اند.
