# پذیرش موج ۴ — فعالیت‌ها و پیگیری (ADR-4)

اسکریپت پذیرش دستی مالک پس از موج ۴، مطابق `EXECUTION-PROMPT.md` §9 Wave 4 و برچسب‌های دقیق §5/§6.

**محیط:** `http://192.168.170.8:3100`  
**حساب پیشنهادی:** `test.sales@afrakala.local` / `AfraTest!1404` (همان الگوی `evidence/W2/ACCEPTANCE.md` و `W3/ACCEPTANCE.md`)  
**نسخهٔ مورد انتظار روی ۳۱۰۰:** `APP_GIT_SHA=0c6eeb08` (healthy) — شامل D2…D7 و FE tip؛ تأیید: `docker exec afrakala-lan-web printenv APP_GIT_SHA` (orchestrator / `evidence/W4/deploy-summary.txt` → `0c6eeb08`، health=healthy، HTTP 200).

## متن خلاصهٔ §9 Wave 4 (عین EXECUTION-PROMPT)

فعالیت با موعد ثبت می‌شود؛ «ذخیره و ایجاد فعالیت دیگر» کار می‌کند؛ صفحهٔ «فعالیت‌ها» و عدد قرمز منو درست است؛ آیکون زرد، قرمز، سبز و طوسی روی معامله‌ها درست است؛ «به تعویق انداختن» موعد اصلی را نگه می‌دارد؛ یادآور با ساعت می‌رسد.

---

## آماده‌سازی (مالک — دستی)

1. وارد شوید با حساب فروش آزمایشی بالا.
2. مسیر میز فروش را باز کنید: `/operations/sales-desk`.
3. مطمئن شوید نسخهٔ صفحه همان SHA است (یا از ادمین بخواهید `printenv APP_GIT_SHA` را ببیند → `0c6eeb08`).

---

## D2 — انواع فعالیت (§6 ترتیب/املا)

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| D2.1 | در فرم فعالیت، فهرست **«نوع فعالیت»** را باز کنید. | ۱۸ عنوان به ترتیب §6: اول **«یادداشت ساده»**، سپس ۱–۱۷ Didar (از جمله «تماس ورودی»، «تماس خروجی»، …، «فاکتورلاین»). | مالک — دستی |

**شواهد خودکار (نه جایگزین تست مالک):** hex 18/18 PASS — `evidence/W4/orch-d2-reverify.txt`; mig 572; commit `355c93ec`; critic D2 CONFIRM — `evidence/W4/critic.md`.

---

## D1 — فیلدهای موعد و نگاشت

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| D1.1 | فعالیتی با **«نوع فعالیت»**، **«تاریخ»** / **«ساعت»** (یا **«ساعت مشخص نیست»**)، و **«مسئول انجام این فعالیت»** بسازید. | ردیف با موعد ذخیره می‌شود؛ مالک فعالیت = مسئول. | مالک — دستی |
| D1.2 | (اختیاری) یادداشت/تماس قدیمی را ببینید. | `kind` عوض نشده؛ یادداشت‌ها به **«یادداشت ساده»** نگاشت شده‌اند. | مالک — دستی |

**شواهد خودکار:** ستون‌ها + note=2→یادداشت ساده — `orch-d1-reverify.txt`; mig 573; commit `9b557dcd`.

---

## D3 — انجام فعالیت و نتیجه

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| D3.1 | روی فعالیت باز، **«این فعالیت انجام شده»** را بزنید. | فیلد **«نتیجه‌ی فعالیت خود را یادداشت کنید»** ظاهر می‌شود. | مالک — دستی |
| D3.2 | نتیجه را پر کنید و **«این فعالیت انجام شد»** بزنید. | فعالیت انجام‌شده می‌شود. | مالک — دستی |
| D3.3 | **«ذخیره و ایجاد فعالیت دیگر»** را از جریان انجام امتحان کنید. | فعالیت جدید با همان مشتری و معامله باز می‌شود. | مالک — دستی |
| D3.4 | با حساب غیرمسئول (در صورت امکان) سعی در ثبت نتیجه کنید. | فقط مسئول بتواند (گارد UI/TS — توجه: critic FINDING: RLS تریگر ندارد). | مالک — دستی |
| D3.5 | **«بازگردانی به انجام نشده»** را بزنید. | وضعیت به انجام‌نشده برمی‌گردد. | مالک — دستی |

**شواهد خودکار:** `ActivityDoneControls.tsx:182,187`; `activities.ts:139-141`; commit `1b554e94`; critic D3 CONFIRM + FINDING — `critic.md`.

---

## D4 — صفحهٔ «فعالیت‌ها» و عدد قرمز منو

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| D4.1 | صفحهٔ **«فعالیت‌ها»** را باز کنید (`/operations/sales-desk/activities`). | سطل‌ها: **«گذشته تا امروز»**، **«تاریخ گذشته (N)»**، **«امروز (N)»**، **«فردا»**، **«تا آخر هفته»**، **«تاریخ دیگر»**. | مالک — دستی |
| D4.2 | فیلترها را عوض کنید. | **«انجام نشده»** \| **«انجام شده»** \| **«همه فعالیت ها»**. | مالک — دستی |
| D4.3 | عدد قرمز کنار آیتم منو را با تعداد فعالیت‌های بازِ امروز/عقب‌افتاده مقایسه کنید. | برابر شمارش باز با موعد ≤ امروز تهران (`tehran_today`). | مالک — دستی |

**شواهد خودکار:** route `…activities.tsx:50-60,136-138`; RPC در `orch-rpc-reverify.txt`; badge `AppSidebar.tsx:438`; mig 574; module `sales-activities` در `CONTRACTS.md`; commit `e90e8d83`.

---

## D5 — چراغ پیگیری روی معاملات

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| D5.1 | در **«میز فروش»** / تب **«کارهای من»** معاملات باز را ببینید. | آیکون زرد (بدون برنامه)، قرمز (عقب‌افتاده)، سبز (امروز)، طوسی (آینده). | مالک — دستی |
| D5.2 | فیلتر **«معاملاتی که فعالیتی روی آن‌ها نیست»** را روشن کنید. | فقط معاملات بدون فعالیت برنامه‌ریزی‌شده. | مالک — دستی |

**شواهد خودکار:** `FollowUpTrafficLightIcon.tsx:5-16`; `MyWorkDeals.tsx:336,355`; commit `9ae0a932`; critic D5 CONFIRM.

---

## D6 — تعویق و یادآور

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| D6.1 | روی فعالیت دارای موعد، **«به تعویق انداختن»** را بزنید و موعد جدید بدهید. | `due_at` عوض می‌شود؛ **موعد اصلی** (`original_due_at`) نگه داشته می‌شود. | مالک — دستی |
| D6.2 | با **ساعت مشخص**، **«افزودن یادآور برای فعالیت»** را فعال کنید؛ بدون ساعت امتحان کنید. | فقط با ساعت مجاز؛ پس از رسیدن موعد در زنگوله (poll) یادآور می‌رسد. | مالک — دستی |

**شواهد خودکار:** postpone keep — `critic-postpone-run.txt` / `activities.ts:190-195`; reminder RPC — `orch-rpc-reverify.txt` / `critic-reminder-probe.txt`; mig 575; no pg_cron — `critic.md`; commit `5cda486e`.

---

## D7 — «فعالیت‌های امروز و عقب‌افتاده» در «کارهای من»

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| D7.1 | تب **«کارهای من»** را باز کنید. | بخش **«فعالیت‌های امروز و عقب‌افتاده»** دیده می‌شود و فعالیت‌های مربوط را فهرست می‌کند. | مالک — دستی |

**شواهد خودکار:** `MyWorkDeals.tsx:215`; commit `9ae0a932`; critic D7 CONFIRM.

---

## چک‌لیست نهایی مالک (یک خط در برابر §9 + برچسب‌های §6)

- [ ] فعالیت با موعد ثبت می‌شود (D1)
- [ ] «ذخیره و ایجاد فعالیت دیگر» کار می‌کند (D3)
- [ ] صفحهٔ «فعالیت‌ها» و عدد قرمز منو درست است (D4 — سطل‌ها «گذشته تا امروز» … «تاریخ دیگر»؛ فیلتر «انجام نشده | انجام شده | همه فعالیت ها»)
- [ ] آیکون زرد، قرمز، سبز و طوسی روی معامله‌ها درست است (D5)
- [ ] فیلتر «معاملاتی که فعالیتی روی آن‌ها نیست» کار می‌کند (D5)
- [ ] «به تعویق انداختن» موعد اصلی را نگه می‌دارد (D6)
- [ ] یادآور با ساعت می‌رسد؛ «افزودن یادآور برای فعالیت» بدون ساعت مجاز نیست (D6)
- [ ] «فعالیت‌های امروز و عقب‌افتاده» در «کارهای من» دیده می‌شود (D7)
- [ ] انواع فعالیت ۱۸ عنوان §6 را دارند (D2 — «یادداشت ساده» + Didar 1–17)

---

## خودکار / شواهد موجود (نه جایگزین تست مالک)

| مورد | مسیر | نوع |
|------|------|-----|
| Critic D2–D7 CONFIRM (+ FINDING RLS) | `evidence/W4/critic.md` | critic |
| D2 hex 18/18 | `orch-d2-reverify.txt` | orch |
| D1 cols + map | `orch-d1-reverify.txt` | orch |
| Typecheck ≤74 | `tsc-after-w4-fe.txt` (DOC: 74× `error TS`) | خودکار |
| Deploy 3100 SHA 0c6eeb08 + healthy + compose PASS + playwright PASS | `deploy-summary.txt`, `compose-safety.txt` | OPS |
| Self-check DOC | `verify/W4-selfcheck.md` | DOC |
| Migrations 572–575 + reverts | `supabase/migrations/20260922050*_57*_*.sql`؛ `revert/572`…`575_*.sql` | DE |

**تأییدنشده توسط DOC:** اجرای کامل سرد مرورگر این اسکریپت توسط مالک؛ گارد DB فقط-صاحب (FINDING critic Medium).
