# پذیرش موج ۳ — معاملات (Deals)

اسکریپت پذیرش دستی مالک پس از موج ۳، مطابق `EXECUTION-PROMPT.md` §9 Wave 3 و برچسب‌های دقیق §5/§6.

**محیط:** `http://192.168.170.8:3100`  
**حساب پیشنهادی:** `test.sales@afrakala.local` / `AfraTest!1404` (همان الگوی `evidence/W2/ACCEPTANCE.md`)  
**نسخهٔ مورد انتظار روی ۳۱۰۰:** `APP_GIT_SHA=42392d3f` (healthy) — شامل FE `bc10ec3f` و فیکس zod C2 `c4bcafe9`؛ تأیید: `docker exec afrakala-lan-web printenv APP_GIT_SHA` (DOC اندازه‌گیری 2026-09-22 → `42392d3f`، exit 0؛ `evidence/W3/app-git-sha-a2.txt`؛ `deploy-summary-a2.txt`).

## متن خلاصهٔ §9 Wave 3 (عین EXECUTION-PROMPT)

در فرم «افزودن معامله»، «مسئول معامله» اجباری و «ایجاد کننده معامله» خودکار است؛ جست‌وجوی «287» محصول X287 را می‌آورد؛ معامله در «کارهای من» مسئولش دیده می‌شود و اعلان «من مسئول شدم» می‌رسد؛ «ناموفق شد» دلیل می‌خواهد؛ «ایجاد پیش‌فاکتور» پیش‌فاکتور پیش‌نویس با همان اقلام و مسئول می‌سازد؛ گزارش «معاملات ثبت‌شده برای دیگران» عدد درست دارد.

---

## آماده‌سازی (مالک — دستی)

1. وارد شوید با حساب فروش آزمایشی بالا.
2. مسیر میز فروش را باز کنید: `/operations/sales-desk`.
3. مطمئن شوید نسخهٔ صفحه همان SHA است (یا از ادمین بخواهید `printenv APP_GIT_SHA` را ببیند).

---

## C1 — برچسب «افزودن معامله»

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C1.1 | در میز فروش / پاپ‌آپ تماس، فرم معامله را باز کنید. | عنوان/دکمه **«افزودن معامله»** (نه «ثبت درخواست»). | مالک — دستی |
| C1.2 | (اختیاری) مسیر اعتبار یا خرید را باز کنید. | هنوز **«ثبت درخواست»** دیده می‌شود (عمداً عوض نشده — `c1-strings.md`). | مالک — دستی |

**شواهد خودکار (نه جایگزین تست مالک):** `evidence/W3/c1-strings.md`؛ critic C1 CONFIRM — `evidence/W3/critic.md`.

---

## C2 — «مسئول معامله» اجباری

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C2.1 | فرم «افزودن معامله» را باز کنید؛ فیلد مسئول را نزنید. | پیش‌فرض **خالی** است (نه «خودم»). | مالک — دستی |
| C2.2 | بدون انتخاب مسئول ثبت کنید. | خطای فارسی **«مسئول معامله الزامی است»** (zod/UI). | مالک — دستی |
| C2.3 | مسئول را انتخاب و ثبت کنید. | معامله ساخته می‌شود. | مالک — دستی |

**شواهد خودکار:** zod `src/lib/sales-desk/schema.ts`؛ unit `critic-c2-zod-unit.txt` (7 pass)؛ DB NULL→`RESPONSIBLE_REQUIRED` — `critic-c2-rereview.md`؛ backfill YES — `evidence/W0/backfill-condition-0.6.md`؛ 3 id — `evidence/W3/c2-backfill-ids.txt`؛ mig 565.

---

## C3 — «ایجاد کننده معامله» خودکار

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C3.1 | در همان فرم، فیلد «ایجاد کننده معامله» را ببینید. | فقط‌خواندنی / نام شما؛ قابل ویرایش نیست (`QuickRequestForm.tsx` readOnly). | مالک — دستی |
| C3.2 | در لیست/کارهای من ستون ایجادکننده و فیلتر را امتحان کنید. | ستون و فیلتر author دیده می‌شود. | مالک — دستی |

**شواهد خودکار:** critic C3 CONFIRM — `critic.md`.

---

## C4 — «کارهای من» و اعلان «من مسئول شدم»

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C4.1 | معامله‌ای با **مسئول = خودتان** بسازید؛ تب **«کارهای من»** را باز کنید. | معامله در فهرست دیده می‌شود. | مالک — دستی |
| C4.2 | معامله‌ای بسازید/مسئول را به کاربر دیگر بدهید (یا برعکس با حساب دوم). | در زنگوله اعلان با عنوان **«من مسئول شدم»** برای مسئول جدید. | مالک — دستی |

**شواهد خودکار:** تب UI `_app.operations.sales-desk.tsx`؛ عنوان در mig 570 / `critic-db-probe.txt` `title_pos=781`.

---

## C5 — گزارش «معاملات ثبت‌شده برای دیگران»

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C5.1 | معامله‌ای بسازید که **ایجادکننده ≠ مسئول** باشد. | در گزارش `/operations/sales-desk/deals-for-others` همان روز تهران شمارش شود. | مالک — دستی |
| C5.2 | عدد گزارش را با تعداد واقعی آن روز مقایسه کنید. | **عدد درست** (گروه‌بندی `Asia/Tehran` — `deals-for-others.tsx`). | مالک — دستی |

**شواهد خودکار:** ماژول `sales-deals-for-others` در `CONTRACTS.md`؛ mig 571؛ critic C5 CONFIRM.

---

## C6 — جست‌وجوی «287» → محصول X287

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C6.1 | در بلوک **«محصولات درخواستی»** عبارت `287` را جست‌وجو کنید. | محصولی با **X287** در نام (مثلاً یخچال LG مدل X287) در نتایج می‌آید. | مالک — دستی |
| C6.2 | حداقل یک قلم با تعداد پیش‌فرض ۱ اضافه کنید. | ردیف در جدول اقلام دیده می‌شود. | مالک — دستی |

**شواهد خودکار:** `evidence/W3/c6-product-287.txt` (۳ ردیف شامل X287)؛ `critic-c6-c9-probe2.txt` ILIKE؛ mig 568.

---

## C7 — موفق / ناموفق و زمان‌ها

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C7.1 | روی معامله باز، **«موفق شد»** بزنید. | وضعیت UI «موفق»؛ `won_at` ست می‌شود (mig 566). | مالک — دستی |
| C7.2 | معاملهٔ دیگری را **«ناموفق شد»** بزنید (با دلیل — C8). | وضعیت «ناموفق»؛ `lost_at` ست می‌شود. | مالک — دستی |

**شواهد خودکار:** `OutcomeButtons.tsx`؛ CHECK ثابت در `critic-db-probe.txt`؛ critic C7 CONFIRM.

---

## C8 — «ناموفق شد» دلیل می‌خواهد

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C8.1 | «ناموفق شد» را بزنید بدون انتخاب دلیل (اگر UI اجازه دهد) یا دلیل را خالی بگذارید. | دیالوگ **«دلیل شکست را انتخاب کنید»**؛ بدون دلیل ثبت نشود. | مالک — دستی |
| C8.2 | دلیل «سایر» + متن توضیح را پر و تأیید «این معامله موفق نشد» را بزنید. | معامله ناموفق می‌شود. | مالک — دستی |
| C8.3 | (اختیاری) گزارش دلایل شکست / تنظیمات دلایل را باز کنید. | تنظیمات `/settings/deal-lost-reasons`؛ گزارش deal-lost. | مالک — دستی |

**شواهد خودکار:** seed «سایر» hex در `critic-db-probe.txt`؛ `LOST_REASON_REQUIRED`؛ mig 567؛ modules در `CONTRACTS.md`.

---

## C9 — «ایجاد پیش‌فاکتور»

| گام | اقدام | نتیجهٔ مورد انتظار | نوع |
|-----|--------|---------------------|-----|
| C9.1 | روی معاملهٔ دارای مشتری و ≥۱ قلم، **«ایجاد پیش‌فاکتور»** را بزنید. | پیش‌فاکتور **پیش‌نویس (draft)** با همان اقلام و **مسئول = مسئول معامله**. | مالک — دستی |
| C9.2 | تب **«پیش‌فاکتورها»** روی صفحهٔ معامله را باز کنید. | پیش‌فاکتور لینک‌شده دیده می‌شود (`interaction_id` — mig 569). | مالک — دستی |

**شواهد خودکار:** `critic-c6-c9-probe2.txt` default draft؛ critic C9 CONFIRM.

---

## چک‌لیست نهایی مالک (یک خط در برابر §9)

- [ ] در فرم «افزودن معامله»، «مسئول معامله» اجباری و «ایجاد کننده معامله» خودکار است (C2+C3)
- [ ] جست‌وجوی «287» محصول X287 را می‌آورد (C6)
- [ ] معامله در «کارهای من» مسئولش دیده می‌شود و اعلان «من مسئول شدم» می‌رسد (C4)
- [ ] «ناموفق شد» دلیل می‌خواهد (C8)
- [ ] «ایجاد پیش‌فاکتور» پیش‌فاکتور پیش‌نویس با همان اقلام و مسئول می‌سازد (C9)
- [ ] گزارش «معاملات ثبت‌شده برای دیگران» عدد درست دارد (C5)

---

## خودکار / شواهد موجود (نه جایگزین تست مالک)

| مورد | مسیر | نوع |
|------|------|-----|
| Critic C1,C3–C9 CONFIRM؛ C2 ابتدا REJECT | `evidence/W3/critic.md` | critic |
| Critic C2 re-review CONFIRM پس از zod | `evidence/W3/critic-c2-rereview.md` | critic |
| Typecheck ≤74 | `evidence/W3/tsc-ops.txt`, `evidence/W3/critic-tsc.txt` (DOC: 74× `error TS`) | خودکار |
| Deploy 3100 SHA 42392d3f + healthy | `app-git-sha-a2.txt`, `deploy-summary-a2.txt`, `web-health-a2.txt` | OPS |
| Playwright smoke (موج۱ smoke روی بیلد) | `playwright-smoke-a2.txt` | OPS (نه اسکریپت کامل §9 Wave 3) |
| Self-check DOC | `verify/W3-selfcheck.md` | DOC |
| Migrations 565–571 + reverts | `supabase/migrations/20260922040*_56*_*.sql`؛ `docs/missions/salesdesk-9-fixes/revert/565`…`571_*.sql` | DE |

**تأییدنشده توسط DOC:** اجرای کامل سرد مرورگر این اسکریپت توسط مالک؛ اعلان واقعی در UI زنگوله (فقط رشته در تعریف تابع تأیید شده — `critic.md` «چه چیزی را نتوانستم بررسی کنم»).
