
## §18. Branch marker pass

Added one-line marker comments to `src/lib/pdf/sale-list-pdf.ts`, `src/routes/_app.pricing.sale-lists_.$listId.tsx`, and `src/routes/_app.pricing.sale-lists_.new.tsx` so GitHub compare on `feature/sale-list-settlement-type` shows the feature files as changed. Migration file is read-only and cannot be re-touched; it is already present at `supabase/migrations/20260522162314_a17c6ada-ae0b-4c05-b72c-68a2602073cf.sql` and applied. No behavioral change.

Note: the underlying feature commit `6a54e07` is already reachable from `main`, so before this pass `git diff main...HEAD` was empty for feature files by design (they were identical to main).
# Sale list: settlement type metadata + PDF brand/product order discoverability

Date: 2026-05-22 16:25
Scope: Frontend + 1 non-destructive migration (already applied earlier)

## 1. Summary

- Added a nullable `settlement_type_id` column on `sale_lists` (metadata only).
- Renders "نوع تسویه:" in the sale list PDF header when set.
- Adds a `<Select>` for settlement type in both the new sale list flow and the
  existing sale list edit/settings tab.
- Surfaces the existing brand/product PDF order dialog with a dedicated card
  and a clear primary action button on the sale list detail page.
- Adds a Persian notice on the new-list flow so users know they can adjust
  brand/product order after creating the list.

## 2. Files Changed

- `supabase/migrations/20260522162314_a17c6ada-ae0b-4c05-b72c-68a2602073cf.sql` (already applied)
- `src/lib/pdf/sale-list-pdf.ts`
- `src/routes/_app.pricing.sale-lists_.$listId.tsx`
- `src/routes/_app.pricing.sale-lists_.new.tsx`
- `src/integrations/supabase/types.ts` (auto-regenerated by Supabase)

## 3. Files NOT Changed (explicit guarantees)

- `src/routes/public.sale-lists.$listId.tsx` — public page untouched.
- `src/lib/public/get-public-sale-list.ts` — public reader untouched.
- `src/lib/pricing/*` — pricing engine untouched.
- `pricing_rules`, `invoices`, `price_calculation_snapshots`,
  `settlement_types`, `sale_list_items`, `sale_list_versions` — no schema change.
- Auth, storage, RLS, policies, RBAC — no change.

## 4. Environment Variables

None added or changed.

## 5. Database Changes

- `ALTER TABLE public.sale_lists ADD COLUMN settlement_type_id uuid NULL`
- FK to `settlement_types(id) ON DELETE SET NULL`
- Index `idx_sale_lists_settlement_type_id`
- Idempotent, reversible, non-destructive.

## 6. Schema Changes

Only the column described above.

## 7. Storage Changes

None.

## 8. Migration Required

File: `supabase/migrations/20260522162314_a17c6ada-ae0b-4c05-b72c-68a2602073cf.sql`

Already applied. Local Self-Host instances must run this migration with
`supabase db push` (or apply the SQL manually) before deploying the new code.

## 9. Local Update Steps

1. Pull the new code from GitHub.
2. Apply the migration above on the Local Supabase Self-host (if not already
   applied).
3. Rebuild the frontend (`npm run build`).
4. Restart the app container.

## 10. RLS / RBAC / Auth Impact

None. New column is owned by existing `sale_lists` RLS policies.

## 11. Audit Log Impact

None.

## 12. Backup Required

Standard pre-migration DB snapshot recommended, but the column is nullable
and non-destructive, so risk is minimal.

## 13. Risk Level

LOW. Metadata-only column; UI is additive; pricing engine untouched.

## 14. Rollback Plan

- SQL:
  - `ALTER TABLE public.sale_lists DROP CONSTRAINT IF EXISTS sale_lists_settlement_type_id_fkey;`
  - `DROP INDEX IF EXISTS public.idx_sale_lists_settlement_type_id;`
  - `ALTER TABLE public.sale_lists DROP COLUMN IF EXISTS settlement_type_id;`
- Frontend: revert this commit (3 files).

## 15. Manual Test Checklist

- Open an existing sale list → page loads, no regression in items/versions.
- Edit tab → "نوع تسویه" select renders, choosing a value + save creates a
  new version and persists the value across reload.
- Generate PDF (preview + download) → header shows "نوع تسویه: ..." when set,
  hidden when not set.
- Generate PDF when settlement type is NULL → no "نوع تسویه" line, no error.
- Prices in PDF / items tab remain identical before and after changing
  settlement type (no recalculation).
- New sale list wizard → step 3 shows settlement type Select + Persian notice
  about PDF order. Created list is saved with the chosen value.
- Detail page → new highlighted card "ترتیب نمایش برندها و محصولات در PDF"
  appears with a primary button that opens the existing order dialog. Saving
  order still works.
- Public sale list page (`/public/sale-lists/:id`) is unchanged.

## 16. Build / Lint Status

TypeScript compiles via the Lovable build pipeline. No new dependencies added.

## 17. Branch Sync Note

Re-applied / verified on branch `feature/sale-list-settlement-type` to ensure
the named branch on GitHub contains the full settlement-type changeset
(migration + frontend + change report). No code logic changed by this note.