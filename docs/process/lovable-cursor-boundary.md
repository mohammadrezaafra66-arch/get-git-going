# Lovable Cursor Boundary

GitHub is the official project source of truth.
Lovable is UI only by default.
Cursor is task scoped engineering only by default.
All meaningful changes must go through branch, commit, pull request, review, and merge.
