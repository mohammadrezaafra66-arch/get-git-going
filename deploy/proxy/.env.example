# دامنه‌ها (placeholder — با مقادیر واقعی روی سرور جایگزین شوند)
APP_DOMAIN=app.afrakala.ir
API_DOMAIN=api.afrakala.ir
STUDIO_DOMAIN=studio.afrakala.ir

# IP ادمین برای دسترسی به Studio (فقط روی سرور تنظیم شود)
ADMIN_ALLOWED_IP=

# Basic auth برای Studio
# نام کاربری دلخواه + hash تولید شده با: caddy hash-password
# هرگز رمز/hash واقعی در ریپو commit نشود.
STUDIO_BASIC_AUTH_USER=
STUDIO_BASIC_AUTH_HASH=