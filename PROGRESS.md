# PROGRESS — دفترچهٔ مشترک افراکالا

این فایل دفترچهٔ مشترک دو ابزار (Claude Code و Codex) و خودِ توست.
هدف: هر ابزار بداند ابزار دیگر چه کرده، تا کار همدیگر را خراب نکنند.

## قاعدهٔ استفاده (برای هر دو ابزار)
- **قبل از شروع هر کار:** این فایل + `git log --oneline -10` را بخوان تا بدانی چه شده.
- **بعد از هر کارِ تمام‌شده:** یک ردیف جدید به «تاریخچه» اضافه کن (جدیدترین بالا).
- فرمت هر ردیف: `تاریخ | ابزار | کاری که شد | commit`
- این فایل کوتاه و واقعی بماند؛ گزارش‌های بلند در `docs/`.
- قوانین کامل کار (ایمنی دیتابیس، محیط، تأیید) در `AGENTS.md` / `CLAUDE.md`.

## وضعیت فعلی
- **جریان شاخه‌ها:** `feature/*` → PR → `staging` → PR → `main`. کار مستقیم روی
  `staging` یا `main` ممنوع. شاخهٔ `feature/navigation-modernization` در
  ۲۰۲۶-۰۸-۱۴ بازنشسته و حذف شد. جزئیات در `CLAUDE.md`.
- **کامپیوتر تست:** `192.168.170.8:3100`، پوشهٔ `D:\AfraKalaTest\app`، شاخهٔ
  `staging`، دیتابیس `afrakala`. دادهٔ هویتی‌اش در ۲۰۲۶-۰۸-۱۴ بی‌نام شد.
- **سرور اصلی:** `192.168.170.10:3000`، پوشهٔ `C:\afrakala`، شاخهٔ `main`،
  دیتابیس **`postgres`** (نه `afrakala`). فقط `git pull` → `build web` →
  `up -d web`؛ هرگز توسعه، هرگز تغییر داده، هرگز مهاجرت بدون تأیید صریح مالک.
- **کانتینرها روی هر دو:** `afrakala-lan-db` / `afrakala-lan-web`
- **آخرین rebuild موفق:** branding myafrakala.ir (پس از commit/deploy)
- **⚠️ نکتهٔ استقرار:** `docker compose up -d --force-recreate` و حتی `docker rm -f` + `up -d` روی این سیستم گاهی کانتینر را از **image قدیمی** می‌سازند؛ `APP_GIT_SHA` درست ولی کد قدیمی. مسیر مطمئن: `$env:GIT_SHA`/`$env:BUILD_TIME` را ست کن و `docker compose ... up -d --build web` را در یک فرمان اجرا کن، سپس با `APP_BUILD_TIME` + grep نماد تأیید کن.
- **دانش سازمانی:** ۱۳۹ chunk نمایه‌سازی شده
- **⚠️ نکتهٔ هارنس تست (۲۰۲۶-۰۸-۰۴):** `npx playwright test --config=playwright.auth.config.ts` را **کامل اجرا نکن**. `e2e/auth/save-admin-session.spec.ts` روی `page.pause()` تکیه دارد؛ در اجرای headless این تابع بلاک نمی‌کند و spec یک storageState **خالی** روی `e2e/auth/admin.storage.json` می‌نویسد — و کل رگرسیون از همان فایل به‌عنوان نشست پیش‌فرض استفاده می‌کند. اگر پیش آمد، نشست را دوباره بساز (رمز موقت روی یک حساب **آزمایشی** از Auth Admin API، سپس گرفتن توکن). فقط specهای غیرتعاملی را نام‌به‌نام اجرا کن.
- **⚠️ حساب نشست تست:** حساب ادمین نشست باید `profiles.status='active'` باشد وگرنه اپ به `/pending-approval` می‌فرستد و **هر spec رابطی قرمز می‌شود** بدون اینکه ربطی به کد داشته باشد. `test.admin@afrakala.local` (`rejected`) و `afra-admin@local.test` (`inactive`) هر دو از این در می‌افتند. در ۲۰۲۶-۰۸-۰۴ `afra-admin@local.test` عمداً `active` شد تا هارنس کار کند (مقدار پیشین: `status='inactive'`, `is_active=false` — برای برگرداندن ثبت شده).
- **ارائه‌دهندگان AI:** `gpt-messenger` (اولویت ۱، فعال) · `ollama` (اولویت ۱۰، فعال)

## مأموریت‌های در جریان (هر عامل فقط ردیف‌های خودش را دست بزند)

> **⛔ هشدار فوری (۲۰۲۶-۰۸-۰۸، از عامل p1-dual-role) — هر ادغام شخصی در کل سامانه
> الان می‌افتد.** مهاجرت **۳۱۹** جدول `mutual_settlements` را با ستون `person_id`
> ساخته که کلید خارجی به `persons` است، ولی آن را در فهرست سیاست‌های `person_merge`
> ثبت نکرده. `person_merge` عمداً فهرست کارش را از `pg_constraint` می‌خواند و روی هر
> کلید ناشناخته **متوقف می‌شود**، پس الان با این خطا برمی‌گردد:
> «ادغام متوقف شد: ستون «mutual_settlements.person_id» … سیاست ادغام برای آن تعریف
> نشده است». زنده در یک تراکنش برگشتی بازتولید شد؛ `e2e/persons/merge-ui.spec.ts` و
> `merge-ui-guard.spec.ts` هر دو قرمزند. **این دقیقاً همان تلهٔ مهاجرت ۲۸۵/۲۸۷ است
> که در همین فایل ثبت شده.** محافظ درست کار می‌کند — جایگزینش ادغامی است که بی‌صدا
> ردیف تسویه را به شخصی که دیگر وجود ندارد وصل بگذارد. **عمداً خودم رفعش نکردم:**
> تصمیمِ «وقتی دو شخص ادغام می‌شوند با ردیف تسویه چه باید کرد» یک تصمیم حسابداری
> است و دفتر حسابداری قلمرو عامل `ledger-mutual-settlement` است. لطفاً همان عامل
> (یا مالک) ستون را در رجیستری `person_merge` ثبت کند.


- [x] nav-invoices-cleanup / فاز ۱ — حذف PRIMARY_MODULE_PATHS — **انجام شد** (۱۰۳ خط دادهٔ مرده حذف؛ `primaryModule` حالا از `PRIMARY_MODULES` مشتق می‌شود؛ یک فهرست به‌جای دو تا). ⚠️ این فاز و فاز ۲ ناخواسته داخل commit عامل دیگر (`75f56867`) رفتند، چون آن عامل `git add` فراگیر زد.
- [x] nav-invoices-cleanup / فاز ۲ — وصل ۳۸ صفحه — **انجام شد** (۳۷ صفحه وصل شد؛ ۳۸اُمی `/integrations/didar` بود که در فاز ۳ بازنشسته شد و جایش `/operations/didar` نشست ⇒ در مجموع ۳۸). گیت خودکار: هر مسیر هم seed دارد هم روت واقعی ⇒ **صفر لینک شکسته، صفر تکراری**. ۷ allowlist نقش که با گارد واقعی route نمی‌خواند اصلاح شد (مهم‌ترینش: کل بخش انبار برای فروشنده دیده می‌شد ولی route ردش می‌کرد) و `adminOnly` از ۳ seed گیمیفیکیشن برداشته شد چون حسابدار را از صفحه‌ای که گاردش قبولش دارد پنهان می‌کرد.
- [x] nav-invoices-cleanup / فاز ۳ — یکی‌کردن دیدار — **انجام شد** (`112d4e10`). `/operations/didar` نگه داشته شد و در ناوبری ثبت شد؛ `/integrations/didar` به redirect تبدیل شد (نه ۴۰۴ خام، طبق الگوی موجود `_app.accounting.daily-capital.tsx`). **قبل از بازنشستگی نجات داده شد:** آمار واردسازی هر سه موجودیت (نه فقط مخاطبین) و جدول ۱۰۰ رکورد آخر. بقیه‌اش منحصربه‌فرد نبود — دکمه‌های همگام‌سازی **stub** بودند (۶۰۰ms صبر + توست «به‌زودی») و نشانگر اتصالش `bot_api_keys` را می‌خواند که RLS دارد و **صفر policy**، پس همیشه «متصل نیست» می‌گفت.
- [x] nav-invoices-cleanup / فاز ۴ — **نسخهٔ مرحله‌ای اجرا شد (تصمیم مالک).** مهاجرت **۳۲۳** اعمال شد: `waybill_items`/`waybills`/`invoice_items` (هر سه صفر ردیف) + ۵ تابع مختص آن‌ها حذف شدند؛ `get_product_timeline` و `v_promotion_suggestions` **بازنویسی شدند نه حذف** (از تعریف زندهٔ `docs/verification/pre-323/` وصله خوردند، رفتار view بایت‌به‌بایت حفظ شد). ۶ روت + ۳ کامپوننت + ۳ لینک مرده حذف شد. dry-run در `BEGIN…ROLLBACK` یک باگ در ادعای خودِ مهاجرت گرفت (`pg_get_functiondef` کامنت‌ها را هم برمی‌گرداند) و `WaybillCustomFieldsInput` نزدیک بود اشتباهی حذف شود چون **`PaymentReceiptForm` زنده از آن استفاده می‌کند**. typecheck **۷۰**، build سبز، صفر لینک شکسته. **جدول `invoices` عمداً دست‌نخورده — پیگیری معوق پایین.**
- [x] nav-invoices-cleanup / تحویل از عامل ledger — صفحهٔ `/accounting/mutual-settlement` در ناوبری ثبت شد (گارد واقعی `admin`+`accountant`). عامل ledger ساختش ولی نمی‌توانست ثبتش کند چون فایل‌های ناوبری این دور مال این مأموریت بود.

- [x] nav-invoices-cleanup / **شرط ۱ از سه شرطِ حذف جدول `invoices` برطرف شد — مهاجرت ۳۲۷.** `post_receipt_accounting` (تنها تابعی که در دفتر حسابداری می‌نویسد) دیگر به `invoices` ارجاع **کدی** ندارد. فقط حلقهٔ مردهٔ `-- Allocate to invoices` و چهار متغیر مخصوصش حذف شد؛ `diff` مقابل تعریف زنده ثابت می‌کند گارد نقش، `increase_credit()`، سند دفتر و هر دو خط سند دست‌نخورده‌اند و کلید `invoice_updates` (همیشه `[]`) در خروجی ماند چون رابط حسابدار می‌خواندش. **گیت برابری روی دیتابیس زنده با فیش واقعی ۱۲٬۰۰۰٬۰۰۰ و JWT ادمین: خروجی old-vs-new بایت‌به‌بایت یکسان** (هر دو سمت ROLLBACK). حلقه اثباتاً مرده بود: `payment_receipt_links` صفر ردیف با `invoice_id` و `invoices` صفر ردیف. **⚠️ تصحیح:** عددهای قبلی «۲۵ / ۲۱ تابع» با الگوی کلمه‌ای گرفته شده بودند و کامنت‌ها را هم می‌شمردند؛ عدد درست روی شکل کد `public.invoices` است: **۱۸ → ۱۷**.

- [x] nav-invoices-cleanup / **گیت دائمی رجیستری FK اشخاص — مهاجرت ۳۲۸ (رفع ریشه‌ای).** یک event trigger روی `CREATE/ALTER/DROP TABLE` که اگر مجموعهٔ FKهای `persons` با رجیستری `person_merge` نخواند، **کل مهاجرت را rollback می‌کند** و نام ستون را می‌گوید. هر دو جهت را می‌گیرد: `UNREGISTERED` (تلهٔ ۲۷۱/۲۸۷/۳۱۹ که سه بار ادغام کل سامانه را خواباند) و `STALE` (کلید رجیستری بدون ستون — همان چیزی که حذف `invoices` می‌سازد). ۴ تست داخل `BEGIN…ROLLBACK` سبز، از جمله اثبات اینکه سناریوی `invoices` را می‌گیرد. قاعدهٔ ترتیب در `AGENTS.md`/`CLAUDE.md` بند ۹ ثبت شد. زنده: ۳۰ کلید = ۳۰ FK، صفر مشکل.

- [x] nav-invoices-cleanup / **شرط ۳ بخش ۱ — مهاجرت ۳۲۹.** هر دو کلید خارجی به `invoices` (`payment_receipt_links` و `delivery_receipts`) حذف شد — سدِ سختِ حذف جدول برداشته شد؛ مهاجرت پیش از اقدام assert می‌کند که هیچ ردیفی `invoice_id` غیرتهی ندارد. ستون‌ها نگه داشته شدند. سه تابع بدون هیچ فراخوان (`cancel_invoice`، `send_invoice_to_accountant`، `set_invoice_accounting_marker`) حذف شدند، و یتیمِ جامانده از ۳۲۳ (`InvoiceAccountingMarkers.tsx`) پاک شد. سه تابع با مصرف‌کنندهٔ زنده عمداً ماندند. ارجاع کدی **۱۷ → ۱۴**، typecheck **۷۰**، جدول `invoices` دست‌نخورده.

- [x] nav-invoices-cleanup / **شرط ۳ بخش ۲ — مهاجرت ۳۳۰: چهار تریگر روی جدول‌های زنده.** این‌ها تیزترین موارد بودند (تریگرِ `payment_receipts`/`payment_receipt_links` که با حذف `invoices` از بین نمی‌روند و در زمان اجرا می‌شکستند). **دو نگهبان مالی حذف نشدند بلکه سخت‌گیرتر شدند:** شاخهٔ فاکتور یک **سقف** بود و ستون + CHECK هنوز اجازه‌اش می‌دهند، پس به‌جای پاک‌کردن، صریحاً رد می‌شود. `recompute_employee_scores_on_receipt` عمداً به `sales_quotes` وصل نشد (تصمیم محصولی؛ وگرنه امتیاز ۵۰ پیش‌فاکتور جابه‌جا می‌شد). گیت برابری: تخصیص معتبر ACCEPTED در هر دو، قاعدهٔ ۱ با **همان کد و همان پیام و همان عدد**، و مسیر فاکتور در هر دو رد. ارجاع کدی **۱۴ → ۱۰**، هر چهار تریگر متصل.

- [x] nav-invoices-cleanup / **پایان — جدول `invoices` حذف شد (۳۳۱ + ۳۳۲)، هر سه شرط بسته شد.** ۳۳۱ هفت تابع خواننده را بازنویسی کرد؛ دو تای آن‌ها با ویرایش کورکورانه می‌شکستند (`get_receivable_detail` نام مستعار `i` را واقعاً استفاده می‌کرد، و `calculate_salesperson_collected_sales` باید **یک ردیف صفر** برگرداند نه صفر ردیف). ۳۳۲ دو سدِ پنهان را رفع کرد: `vw_customer_receivables` به جدول وابسته بود (CASCADE صفحهٔ مطالبات را می‌برد) و از ۱۱ تریگر، `set_updated_at` (۷۳ جدول) و `tg_credit_derive_customer_person` (۷ جدول) **مشترک** بودند و محافظت شدند. **اثبات سه‌طرفه:** merge پیش از حذف OK، پس از حذف OK، و در حالت شاهد (حذف بدون de-register) **گیت ۳۲۸ جلویش را گرفت**. نهایی: توابع ارجاع‌دهنده **۲۵ → ۰**، رجیستری **۲۹ متوازن**، `set_updated_at` روی ۷۳ تریگر، typecheck **۷۰**، build سبز.

- [x] nav-invoices-cleanup / **مأموریت تکمیل شد — مهاجرت ۳۳۳ و گزارش نهایی.** آخرین یتیم برداشته شد: جدول `waybill_custom_fields` (صفر ردیف) و صفحهٔ «فیلدهای بیجک» که پیکربندیِ قابلیتی بود که دیگر وجود ندارد. ⚠️ کامپوننت `WaybillCustomFieldsInput` **عمداً نگه داشته شد** — با وجود نامش عمومی است و `PaymentReceiptForm` زنده آن را روی `payment_receipt_custom_fields` رندر می‌کند. گزارش نهایی در `docs/execution/nav-invoices-cleanup-mission-COMPLETE.md` (نام از `-STATUS` تغییر کرد). نهایی: typecheck **۷۰**، build سبز، گیت ناوبری ۷ ماژول/۱۲۴ مسیر/صفر لینک شکسته. **مانده‌ی اصلی: build+deploy روی LAN و بررسی مرورگری — چک‌لیست در بخش ۸ گزارش.**

- [x] nav-invoices-cleanup / **استقرار + رگرسیون کامل e2e + انتشار نسخه #28.** بیلد LAN با هر سه سیگنال تأیید شد (`APP_GIT_SHA=6215e1e0`، build-time، و **image id عوض شد** `9214ab53`→`5c2590cd`). ⚠️ کانتینر پیش از این **۶۲ کامیت** عقب بود. **اجرای اول e2e باطل بود:** توکن `admin.storage.json` ۲۲ ساعت قبل منقضی شده بود و ۱۱۴ قرمز کاذب داد — الگو: specهای `chromium-admin` قرمز ولی همان spec برای بقیهٔ نقش‌ها سبز. پس از `generate-role-sessions` نتیجهٔ معتبر: **۴۷۰ سبز / ۳۰ قرمز / ۱۹ skip / ۷ اجرانشده**. طبقه‌بندی: ۱ خط‌پایهٔ مستند (`credit-uses-person`) · **۴ قرمز تازه ناشی از کار خودم** (لینک‌های سایدبارِ فاز ۲ باعث strict-mode violation در specهایی که locatorشان محدود نشده) · ۲۲ کار عامل‌های دیگر (۲۰ آسان + ۲ clusters؛ اثبات شد هیچ‌کدام از حذف `invoices` نیست — هر ۴ تابع خروجی آسان `references_dropped_table = f`) · ۳ محیط/هارنس. **هیچ قرمزی به حذف زیرسیستم برنمی‌گردد.** نسخهٔ **#28** با ۸ آیتم منتشر شد؛ کشف جانبی: مسیر `docker compose up --build` مرحلهٔ انتشار `up.ps1` را بی‌صدا رد می‌کند. جزئیات در بخش ۱۳ گزارش.

- [x] nav-invoices-cleanup / **۴ قرمز تازهٔ خودم رفع شد.** ⚠️ فرض اولیه که این ۴ هم‌ریشه با «۲ تأمین‌کنندهٔ دارای کد آسان» باشند **درست نبود** — هیچ‌کدام از این ۴ اصلاً به آسان/تأمین‌کننده/کد ربطی ندارد؛ آن مورد ریشهٔ دستهٔ ۲۰تایی آسان است. ریشهٔ مشترک این ۴: لینک‌های سایدبارِ فاز ۲ حالا در هر صفحه هستند و locatorهای محدودنشده آن‌ها را هم می‌گیرند. رفع با محدودکردن به landmark `<main>` — نه «شمارش زندهٔ انتظار»، چون آن کار اندازهٔ فعلی سایدبار را داخل تست می‌پخت و با افزودن آیتم بعدی دوباره می‌شکست. تأیید: **۱۱ سبز / ۱ skip** روی هر چهار فایل.

- [x] pricing-and-stock-warning-fixes / **✅ نرخ ارز کهنه رفع شد — بازانتشار قیمت ۵۴ محصول (تصمیم مالک).** با `publishProductPrices` خودِ پروژه اجرا شد (منطق قیمت‌گذاری بازنویسی نشد)، `source='fx_settlement_backfill'`. اول **یک محصول به‌عنوان canary** اجرا و در دیتابیس تأیید شد، بعد ۵۳ تای بقیه. نتیجه: **۵۴ محصول، ۱۶۲ نوع‌قیمت، صفر خطا.** ⚠️ **نکته‌ی مهم که حین اجرا معلوم شد:** برای بخشی از این محصولات **ردیف پایه هم غلط بود** (مثلاً `AFK-2026-00069` نقدی: ۳۱٬۷۹۳٬۵۰۰٬۰۰۰ → ۳۱۱٬۷۰۰٬۰۰۰)، یعنی بازمحاسبه‌ی نرخ ارز در ۲۰۲۶-۰۸-۰۹ فقط ۸۶ محصول را گرفته بود نه همه را. تأیید نهایی: ردیف با نرخ ۱۹٬۰۰۰٬۰۰۰ = **۰**، و نسبت «قیمت تسویه به قیمت پایه» در کل سامانه از **۱۰۷٫۷۵** به **۱٫۰۶۷** رسید (صفر ردیف بالای ۱۰ برابر). تأیید بصری روی بیلد مستقرشده: کارت‌های تسویه‌ی `AFK-2026-00017` حالا ۶۴ تا ۷۰ میلیون‌اند نه ۶٫۶ میلیارد. چون قیمت‌ها واقعاً عوض شدند، ردیف تاریخچه و اعلان حسابداران هم ثبت شد (انتظارش را داشتیم) و روی کارت‌ها تا مدتی «قیمت قبلی» عدد غلط قدیمی را نشان می‌دهد — این درست است، نه باگ.
> **🟡 باقی‌مانده (شدت کم):** ۱۹۲ ردیف تسویه روی ۳۲ محصول هنوز نرخ **۱۸۰٬۲۰۰** را دارند (نرخ فعلی ۱۸۶٬۱۰۰) — فقط ~۳٪ اختلاف، نه ۱۰۸ برابر. عمداً دست نخورد چون خارج از تصمیم مالک بود.
> **🟡 ریشه هنوز باز است:** تا وقتی `effective-currencies.ts` فقط ردیف پایه را بازمحاسبه می‌کند، هر تغییر نرخ ارز دوباره همین واگرایی را می‌سازد. رفع ریشه‌ای تصمیم جدا و تست جدا می‌خواهد.

> **~~🔴 هشدار داده‌ی زنده (۲۰۲۶-۰۸-۱۰)~~ — رفع شد، بالا را ببین. متن اصلی برای سابقه:** قیمت‌های «به تفکیک تسویه» در جستجوی سریع فروش تا ۱۰۸ برابر باد کرده بودند.
> در ۲۰۲۶-۰۷-۲۸ نرخ دلار اشتباه **۱۹٬۰۰۰٬۰۰۰** ثبت شد و در ۲۰۲۶-۰۸-۰۹ به **۱۸۶٬۱۰۰** اصلاح شد. اصلاح نرخ فقط
> ردیف‌های **پایه** را بازمحاسبه می‌کند — این عمدی و در کد هم کامنت شده:
> `src/lib/pricing/effective-currencies.ts` ← `// Baseline rows only — currency recompute targets the settlement-NULL row.`
> نتیجه: **۹۶۰ ردیف روی ۵۴ محصول** هنوز نرخ ۱۹٬۰۰۰٬۰۰۰ را حمل می‌کنند. در `/sales/search` قیمت نقدی درست
> است ولی کارت‌های تسویه زیرش عدد نجومی نشان می‌دهند — مثلاً «موتور برق ۲۱ اسب»: نقدی **۱۷۲٬۱۰۰٬۰۰۰**،
> کارت تسویه **۱۸٬۵۲۸٬۸۰۰٬۰۰۰**. (۱۹۲ ردیف دیگر روی ۳۲ محصول نرخ قدیمی ۱۸۰٬۲۰۰ دارند — فقط ~۳٪ اختلاف.)
> **عمداً رفع نشد:** هر دو راه‌حل (بازانتشار قیمت ۵۴ محصول، یا تغییر سیاست بازمحاسبه) داده/رفتار قیمت را
> می‌نویسند و تصمیم مالک است. با مالک مطرح شد. **این ربطی به باگ کارگاه قیمت ندارد و با آن رفع نمی‌شود.**

- [x] pricing-and-stock-warning-fixes / **استقرار LAN انجام شد (۲۰۲۶-۰۸-۱۰).** `eef3a4a1` روی `192.168.170.8:3100` نشست. هر سه سیگنال تأیید شد: `APP_GIT_SHA=eef3a4a1` = HEAD، `APP_BUILD_TIME=2026-08-10T10:50:25Z`، و image id کانتینر = `afrakala-app:lan` (کانتینر ۱۰:۵۲:۳۴ بعد از ساخت image در ۱۰:۵۱:۵۷ استارت شد). `/api/version` هم `eef3a4a1` می‌دهد. علاوه بر stamp، **خودِ کد** هم در باندل سرو‌شده گرفته شد (`قیمت فروش (نقدی)` و `quote-stock-advisory` داخل `.output/public` کانتینر) — چون stamp درست با کد قدیمی قبلاً در این پروژه دیده شده. نسخه ۲۹ منتشر شد. ⚠️ `build.ps1` به‌خاطر یک فایل untracked عاملِ دیگر (`docks/audits/7-eg-checklist-mission.md`) رد می‌کرد؛ مسیر تک‌فرمانیِ توصیه‌شده در همین فایل استفاده شد (ست‌کردن `GIT_SHA`/`BUILD_TIME` + `up -d --build web`). چون فایل‌های tracked تمیز بودند، stamp پسوند `-dirty` نگرفت و دقیقاً HEAD است.

- [x] pricing-and-stock-warning-fixes / **بررسی `/sales/search` — باگ کارگاه قیمت آنجا نیست.** صفحه مسیر کوئری جدا ندارد؛ از `get_sales_search_products()` می‌خواند که فیلتر نوع‌قیمت را درست دارد و آرایه‌ی همه‌ی نوع‌ها را برمی‌گرداند. تأیید داده‌ای: ۵۰ محصول، **۰ مغایرت** بین قیمت نقدیِ این RPC و مقدار کارگاه قیمتِ اصلاح‌شده. تأیید زنده در مرورگر روی همان بیلد مستقرشده با نشست ادمین: انتخابگر نوع قیمت پیش‌فرض **نقدی** است و عدد بزرگ «قیمت نقدی ۶۴٬۲۰۰٬۰۰۰» دقیقاً برابر مقدار دیتابیس. **پس `BASE_SALE_PRICE_TYPE_CODE` اینجا چیزی برای رفع ندارد.** ولی عدد غلطی که مالک دیده بود واقعی است — بالا را ببین.

- [x] pricing-and-stock-warning-fixes / **دو باگ رفع شد — بدون مهاجرت (هر دو سمت کلاینت).** ⚠️ **علت واقعی هیچ‌کدام آن نبود که فرض شده بود، و یک باگ سومِ جدی‌تر پیدا شد.** (۱) **کارگاه قیمت من:** هیچ‌جا «چکی» هاردکد نشده بود — کوئری هیچ فیلتری روی `sale_price_type_id` نداشت و «آخرین ردیف بر اساس `computed_at`» را برمی‌داشت، در حالی که `publishProductPrices` برای هر محصول یک ردیف به‌ازای **هر** نوع‌قیمت می‌نویسد. پس عدد نمایش‌داده‌شده تصادفی بود: **۷۷ محصول از ۳۱۶** غلط بودند (۴۶ همکاری، ۳۱ چکی). فیلتر `cash_price` اضافه شد (ثابت مشترک `BASE_SALE_PRICE_TYPE_CODE`) + برچسب‌ها دقیق شدند. گیت: ۳۱۶ محصول، **۰ مغایرت** با قیمت نقدی واقعی. همان باگ در endpoint عمومی `api/public/products.ts` هم بود (با `CORS *`) و رفع شد. (۲) **هشدار موجودی حسابدار:** هشدار از قبل وجود داشت ولی پشت شرط `isAccepting` بود و طبق `update_sales_quote_status` حسابدار **فقط `rejected`** می‌تواند بزند، پس هرگز آن دیالوگ را باز نمی‌کرد. پنل هشدار (نه مانع) روی خودِ صفحه اضافه شد با بازاستفاده از همان RPC `check_quote_stock_availability`. (۳) 🔴 **باگ سوم:** `const rpc = supabase.rpc` در `lib/warehouses/queries.ts` متد را از کلاینت جدا می‌کرد ⇒ هر فراخوانی با `Cannot read properties of undefined (reading 'rest')` می‌افتاد **پیش از هر درخواست شبکه**، و چون خطا به «نتیجهٔ خالی» ترجمه می‌شد، بررسی موجودی برای **همه** — از جمله گاردِ `disabled` دکمهٔ پذیرش مدیر — بی‌صدا مرده بود. با `.bind(supabase)` رفع شد. **⚠️ همان الگو در `lib/treasury/queries.ts` و `lib/accounting/mutual-settlement.ts` هم هست و عمداً رفع نشد** — `bind` کردنشان `post_mutual_settlement` و `pay_purchase_with_voucher` را زنده می‌کند که سند مالی می‌نویسند (شرط توقف مأموریت). تست مرورگری واقعی با نشست حسابدار روی SQ-2026-000021: هشدار با اعداد درست دیده شد، «رد» فعال ماند، و پیش‌فاکتور بدون کمبود هیچ هشداری نداد. typecheck **۷۰**، build سبز.

- [x] internal-products-pricing-api / **کامل شد — مهاجرت ۳۳۴ (فقط افزودنی، فقط‌خواندنی).** یک API داخلی برای مالک روی همان PostgREST/Kong موجود: دو view (`api_products_pricing` یک ردیف per محصول با آرایهٔ همهٔ قیمت‌ها؛ `api_product_price_rows` تخت برای اکسل) + نقش تازهٔ `products_api_readonly` که **دقیقاً ۲ relation** می‌بیند و **صفر** privilege نوشتنی در کل دیتابیس دارد. **فاز ۱ روشن کرد کدام قیمت زنده است:** `product_computed_prices` (۲۵۳۸ ردیف) منبع «قیمت فعلی» است نه `product_sale_price_history` (۳۹۸۵، فقط قبلی/زمان)؛ `price_lists`/`price_list_items` **صفر ردیف = یتیم**؛ هر ۲۰ `sale_lists` هنوز `draft` و آزمایشی‌اند. ⚠️ **دو تلهٔ واقعی که dry-run گرفت:** (۱) `v_latest_active_purchase_prices` عمداً `security_invoker=true` است، پس نقش تازه از داخلش `permission denied` می‌خورد — flag دست نخورد و منطقش با کامنت هشدار تکرار شد؛ (۲) `ALTER DEFAULT PRIVILEGES` روی `supabase_admin` به **هر view تازه** حقوق کامل به `anon`/`authenticated` می‌دهد، یعنی بدون `REVOKE` صریح هر کاربر لاگین‌شدهٔ اپ **قیمت خرید** را می‌دید — آن دو خط REVOKE در مهاجرت حذف نشود. کف قیمت از تابع موجود `get_product_price_bounds()` خوانده می‌شود (دوباره نوشته نشد). **گیت برابری با اپ:** خروجی `get_sales_search_products()` با JWT ادمین شبیه‌سازی‌شده — ۷۹۲ ردیف قیمت، **۰ مغایرت مقداری**. ۸ تست زندهٔ `curl` از بیرون: با credential درست ۲۰۰، بدون آن ۴۰۱، روی جدول خام `products`/`purchase_prices` هر دو ۴۰۳. سرعت: ۳۵۵ محصول کامل ۲٫۳MB در ۰٫۱۱s. **توکن در هیچ فایل commit‌شده‌ای نیست** — با `deploy/lan/scripts/issue-products-api-credential.ps1` ساخته و بیرون از ریپو در `%USERPROFILE%\.afrakala\` ذخیره می‌شود. typecheck **۷۰**. راهنما: `docs/asan/internal-products-pricing-api-guide.md`.

> **🔴 پیگیری معوق — جدول `invoices` هنوز باید حذف شود** (شرط ۱ ✅ برطرف شد؛ شرط‌های ۲ و ۳ باز). «invoices table itself still needs dropping, blocked on ledger-mutual-settlement agent finishing its `post_receipt_accounting` changes, and must register the FK change with whatever system reads `pg_constraint` for `person_merge` before dropping — migrations 271 and 287 both broke merge this same way.» **دلیل دوم و مستقل:** گزارش پایانی عامل ledger تأیید می‌کند `post_receipt_accounting` — **تنها تابعی که در دفتر حسابداری می‌نویسد** — هنوز داخل بدنه‌اش به `invoices` وصل است و عمداً دست‌نخورده رها شده؛ حذف جدول **مسیر ثبت فیش را می‌شکند**، و چون خطا در زمان اجرا رخ می‌دهد نه هنگام drop، مهاجرت سبز می‌شود و خرابی بعداً ظاهر می‌شود.

<!-- ردیف قدیمی فاز ۴ (بررسی نجات منطق `1d21a038`) در همین ردیف بالا ادغام شد. --> داده در خطر نیست (هر ۴ جدول صفر ردیف، `held_credit=0`)، ولی **۲۵ تابع** در بدنه‌شان به `invoices` ارجاع دارند نه ۳ تا — از جمله `post_receipt_accounting`، `enforce_payment_receipt_link_limits`، `get_receivable_detail` و **`person_merge`**. حذف جدول بدون drop کردن FK روی `payment_receipt_links` (شیء `payment_*`، در فهرست لمس‌نکردنی همین مأموریت و در حال ویرایش توسط عامل ledger) ممکن نیست. worktree ایزوله ساخته شد و backup گرفته و تأیید شد (۷ جدول، بیرون از مخزن). جزئیات کامل در `docs/execution/nav-invoices-cleanup-mission-STATUS.md` بخش فاز ۴.

> **⛔ چرا فاز ۴ شروع نشد.** این working tree بین ۶ عامل مشترک است و کار commit‌نشدهٔ من **دو بار** پاک شد (فاز ۳ هر دو بار از دیسک رفت و مجبور شدم بازش بنویسم؛ بار سوم در یک فراخوان اتمیک نوشتم و بلافاصله commit کردم). فاز ۴ یعنی `pg_dump` + DROP چهار جدول + DROP چند تابع + حذف route/فرم. اگر مهاجرت اعمال شود ولی حذف کد وسط کار برگردانده شود (یا برعکس)، اپ روی سرور تستِ **مشترک با ۵ عامل دیگر** به جدولی ارجاع می‌دهد که دیگر وجود ندارد. این دقیقاً مرز توقف (الف) و (ج) مأموریت است.

- [x] ledger-mutual-settlement / فاز ۱ — supplier_payable — **انجام شد** (مهاجرت ۳۱۲ اعمال شد؛ dry-run ۵/۵؛ down تست شد)
- [x] ledger-mutual-settlement / فاز ۲ — پرداخت شخص ثالث — **انجام شد** (مهاجرت ۳۱۳ + فرم؛ dry-run ۱۰/۱۰ داخل rollback، هیچ مبلغ واقعی commit نشد)
- [x] ledger-mutual-settlement / فاز ۳ — صفحهٔ تسویه — **انجام شد** (مهاجرت ۳۱۹ + صفحهٔ `/accounting/mutual-settlement`؛ dry-run ۸/۸ داخل rollback). ⚠️ در سایدبار ثبت **نشده** — فایل‌های ناوبری مال عامل nav است؛ تحویل به او.
- [x] ledger-mutual-settlement / فاز ۴ — غنی‌سازی شرح — **انجام شد** (مهاجرت ۳۲۰؛ dry-run ۹/۹ داخل rollback؛ نشان «شرح ساده» در پیش‌نمایش)
- [x] messenger-rpc-fix — **انجام شد** (گزینهٔ ب؛ مهاجرت **۳۱۶**؛ dry-run ۶/۶؛ E2E JWT واقعی سبز؛ down تست شد؛ فرانت دست‌نخورده)
- [x] db-hygiene / فاز ۱ — seed role_permissions — **انجام شد** (مهاجرت **۳۱۵**؛ فقط سه ماژول واقعی `accounting`/`hr`/`market-rates`؛ `bank`/`customers`/`cheques` عمداً seed نشدند چون اصلاً کلید ماژول نیستند. dry-run اثبات کرد ۳ کاربر واقعی `sales` روی `accounting.view` از fallback مجوز می‌گرفتند و حالا نمی‌گیرند؛ ۸۴ ادعای پس از اعمال، صفر اختلاف با seed)
- [x] db-hygiene / فاز ۲ — یکپارچگی ارجاع — **انجام شد** (مهاجرت **۳۱۷**؛ دو تریگر BEFORE INSERT/UPDATE معادل عملکردی FK روی `stock_movements.ref_id` و `journal_lines.account_ref_id`. نگاشت‌ها از خودِ نویسنده‌های زنده استخراج شد نه حدس؛ `invoice_ar`/`clearing`/`other` و `manual` عمداً بدون اعتبارسنجی‌اند چون حساب کنترلی‌اند و مقصدی ندارند (مهاجرت ۲۹۴). یتیم موجود: **صفر**. تابع تشخیصی `polymorphic_ref_orphan_report()` فقط گزارش می‌دهد. dry-run ۱۲/۱۲ سبز)
- [x] db-hygiene / فاز ۳ — رفع تست export-sales — **انجام شد** (`ASAN_PERSON_CODE_COUNT = 11` حذف شد؛ شمار در `beforeAll` گرفته می‌شود و `afterAll` با همان مقایسه می‌کند. اثبات: با شمار ۱۱ و با شمار ۱۲ (ردیف آزمایشی موقت، بلافاصله حذف شد) هر دو سبز؛ کد قدیمی روی ۱۲ ریاضی‌وار قرمز می‌شد. eslint فایل ۱۱→۹ (دو خطای خودم رفع شد، ۹ خطای موروثی دست‌نخورده)
  - ⚠️ **تداخل بین‌عاملی، نه رگرسیون من:** `customers.accounting_code` مشتری «خان محمدی» الان **خالی** است، پس تنها پیش‌فاکتور قابل‌خروجی (`bcbe3ce6`) بلوکه می‌شود و ۵ تست فایل با «there must be at least one exportable invoice» قرمزند. هیچ‌کدام از تغییرات من نمی‌تواند `accounting_code` را پاک کند؛ `restoreAsanPersonCodes` هم چون از همان ستون بازیابی می‌کند نمی‌تواند ترمیمش کند. **ترمیم نشد** چون مقدار واقعی کد حسابداری را نمی‌دانم (حدس‌زدن = نوشتن دادهٔ مالی جعلی).
- [x] db-hygiene / فاز ۴ — حذف ۲۷۱d7c44 — **انجام شد** (مهاجرت **۳۱۸**؛ سه ردیف: person «محمدزین الدین» + customer + person_context_link. صفر تراکنش در **همهٔ** ۲۹ ستون FK به `persons` و صفر ارجاع به ردیف customer — با پیمایش زندهٔ `pg_constraint` سنجیده شد نه فرض. نمونهٔ دو‌نقشی **۱۳۵ac0e1 «مختارشاهمرادی» دست‌نخورده** (۲ شناسه + ۱ ردیف سرمایه) و gate مهاجرت این را ادعا می‌کند.
  - ⛔ **اشتباه من، ثبت‌شده:** نسخهٔ اول `318-dry-run.sql` فایل down را با `\i` صدا می‌زد؛ آن فایل `COMMIT` خودش را دارد و تراکنش dry-run را بست، سپس `ON_ERROR_STOP 0` باعث شد سه DELETE با وجود امتناع gate اجرا و **کامیت** شوند. با همان down اسکریپت برگردانده شد، dry-run اصلاح شد (مراحل rollback درون‌خط، تست امتناع داخل SAVEPOINT)، دوباره اجرا شد (۵/۵ سبز، دیتابیس دست‌نخورده)، و بعد مهاجرت از مسیر درست `--single-transaction` اعمال شد. وضعیت نهایی درست است ولی مسیر رسیدن به آن یک بار غلط بود.
- [x] db-hygiene / فاز ۵ — حذف xlsx + رفع ۲ spec — **انجام شد** (`docs/asan/reference/اشخاص.xlsx` و `کالا.xlsx` با `git rm` حذف شدند. **skip ساکت نشد**: هر تست skip‌شده دلیلش را با `test.skip(true, FIXTURE_REMOVED)` در گزارش Playwright نشان می‌دهد و بالای هر دو فایل یادداشت کامل «چه چیزی دیگر اثبات نمی‌شود» هست. طبق گزینهٔ (الف) مأموریت فقط بخش وابسته به فایل حذف شد: تست‌های RLS، سیم‌کشی نقش‌ها، ردیف‌های مجوز و کل سوئیت مرورگری `/admin/asan-import` زنده ماندند، و تست نرمال‌ساز **دو نیم شد** تا قواعد folding روی ورودی ساختگی پوشش خود را از دست ندهد. اجرا: **۱۲ skip / ۹ سبز / ۰ قرمز**.
  - ⚠️ **کاهش پوشش، پذیرفته‌شده و نه پنهان:** دیگر اثبات نمی‌شود که parser فایل واقعی مالک را می‌خواند، که نگاشت هدرمحور در برابر جابه‌جایی ستون‌ها مقاوم است، و که زنجیرهٔ stage→classify→commit روی دادهٔ واقعی کار می‌کند. خودِ گاردها در مهاجرت‌های ۲۸۵/۲۸۶ سرجایشان‌اند؛ چیزی که رفت **اثباتِ** آن‌ها مقابل فایل واقعی است.
- [x] p1-dual-role / فاز ۱ — trigger context_link — **انجام شد** (مهاجرت ۳۱۴ اعمال شد؛ dry-run ۸/۸؛ down نوشته شد)
- [x] p1-dual-role / فاز ۲ — جستجوی شخص با شماره — **انجام شد** (هر ۵ مسیر ساخت؛ بدون RPC تازه؛ آزمون زنده: جستجو شخص را یافت، افزودن نقش mirror ساخت، تعداد اشخاص ۷۹ ثابت ماند)
- [x] p1-dual-role / فاز ۳ — نمایش دو نقشی — **انجام شد** (کارت نقش‌ها در `/persons/$personId` از قبل بود؛ لینک متقابل تأمین‌کننده↔مشتری اضافه شد؛ خواندن mirror مشترک شد)
- [x] p1-dual-role / فاز ۴ — یک کد آسان یکتا — **انجام شد** (مهاجرت ۳۲۱، ایندکس جزئی روی `person_id`؛ dry-run ۶/۶؛ هم‌خوانی mirror با `value_raw` صفر اختلاف)
- [x] p1-dual-role / فاز ۵ — رفع unlink + مجوز ویرایش — **انجام شد** (مهاجرت ۳۲۲؛ گزینهٔ الف: `customer_clear_person` حذف شد و جایگزینی از طریق `customer_set_person` ماند؛ ممیزی `person_id` اضافه شد؛ `sales`/`purchase_specialist` هم در UI هم در RLS باز شد — آزمون زنده: sales قبل ۰ ردیف، بعد ۱ ردیف)
- [x] new-clusters-frontend / نامزدی ارتقا — **انجام شد** (`/sales/promotion-nominations` + cancel RPC؛ JWT سبز)
- [x] new-clusters-frontend / استعلام‌ها — **انجام شد** (`/messages/inquiries` + update_inquiry_status؛ tick best-effort — توقف 42P10)
- [x] new-clusters-frontend / لیگ گیمیفیکیشن — **انجام شد با توقف schema** (صفحهٔ لیگ + تب RPC؛ start/settle با title_fa بلاک)
- [x] eg-checklist — **انجام شد** (چک‌لیست رسمی هشت‌بندی E و G + دلتای زنده)

## تاریخچه (جدیدترین بالا)

| تاریخ | ابزار | کار | commit |
|---|---|---|---|
| 2026-08-29 | Claude Code | **بازکردن رنج امتیازدهی اعتبار مشتری تا مقیاس دادهٔ واقعی + بازمحاسبهٔ امتیازهای موجود.** مهاجرت‌های **۴۱۱** و **۴۱۲**. هفت پارامتر مشتری بازتر شدند (سابقه همکاری ۲۴۰→۳۶۰ ماه؛ خرید ۳ماهه ۲→۱۰ میلیارد، ۱ساله ۵→۲۵، ۳ساله ۱۰→۵۰؛ سود ۳ماهه ۰٫۵→۲٫۵، ۱ساله ۱→۵، ۳ساله ۳→۱۵ میلیارد). واحد **تومان** بود (`input_type='toman'`) و پیش از نوشتن فایل زنده تأیید شد، پس ×۱۰ ریالی انجام نشد. 🔴 **درس عملیاتی اصلی — این مهاجرت فقط رنج را عوض نکرد، سقف اعتبار مشتری‌ها را هم بازمحاسبه کرد:** `raw_score` هنگام نوشتن فریز می‌شود، پس تغییر رنج به‌تنهایی هیچ ردیف موجودی را عوض نمی‌کند (صفحهٔ ادمین درست می‌خواند در حالی که همهٔ امتیازهای ذخیره‌شده کهنه‌اند — همان خرابی‌ای که شکل موفقیت دارد). بازمحاسبه با self-update ارزش‌نگهدار (`SET actual_value = actual_value`) انجام شد که `trg_a_compute_raw_score` را دوباره fire می‌کند چون `BEFORE INSERT **OR UPDATE**` است؛ پیش از اعتماد، داخل تراکنش rollback‌شده اثبات شد: `۰٫۳۰۰ → (فقط تغییر رنج) ۰٫۳۰۰ → (self-update) ۰٫۰۶۰`. **ولی این بازمحاسبه از جدول خودش بیرون می‌زند:** روی `dynamic_entity_scores` تریگر `trg_refresh_dyn_capital_after_score_change` هم هست، پس **۵۲ ردیف امتیاز، ۵۲ بار آن را fire کرد و سقف اعتبار واقعی ۹ مشتری را در `customer_capital_allocations_dynamic` بازنویسی کرد** (به‌علاوهٔ ۵۲ ردیف `audit_logs`). چون بازکردن رنج هر امتیاز نرمال‌شده را **پایین** می‌آورد (میانگین `purchase_1y` از ۰٫۳۲۶ به ۰٫۰۶۵)، سقف‌ها **کم شدند** — مالک صریحاً همین را تأیید کرده بود. مشتری‌ای که به سقف چسبیده بود از `۱٫۰۰۰` به `۰٫۲۰۰` رسید، یعنی همان فضای رشدی که هدف کار بود؛ مشتری نهم صفر ردیف در این هفت پارامتر داشت و درست دست‌نخورده ماند. ⚠️ **نقص جانبی که حین کار پیدا و رفع شد:** راهنمای زیر فیلد از `input_hint` می‌آید و UI آن را **عیناً** چاپ می‌کند (فقط اگر NULL باشد رنج محاسبه‌شده را نشان می‌دهد)، پس ۴۱۱ با «بین ۱ تا ۲۴۰ ماه» در برابر سقف ۳۶۰ منتشر شد و ۴۱۲ اصلاحش کرد. دروازه: `e2e/scoring/credit-ranges-widened-and-scores-recomputed.spec.ts` با ۵ تست، و هر سه ادعا **قبل از اعتماد روی حالت خراب اجرا شد و FAIL داد** (حذف بازمحاسبه ⇒ «۴۷ ردیف فریزمانده»، یک `max` غلط ⇒ «found 6»، راهنمای برگردانده‌شده ⇒ ۱ تست قرمز). دفتر مهاجرت هر دو ردیف ثبت شد (قانون ۲b): ۶۰۰ ردیف = ۶۰۰ فایل، `og81` سبز. typecheck **۷۰** (بدون افزایش). درس ماندگار به‌عنوان قانون ۱۰ در `CLAUDE.md`/`AGENTS.md` ثبت شد. | `edb2078b` |
| 2026-08-19 | Cursor | **تصحیح فاز ۴ — سند دوطرفه طبق تعریف مالک: کارمزد وجود ندارد.** مهاجرت **۳۶۲**. مالک همان روز OG-21 را بست: افراکالا کارمزدی ثبت نمی‌کند؛ صراف/واسط/شخص ثالث همان نقش‌های فقط-ثبت روی فیش‌اند. تابع ۱۸پارامتری و ستون‌های کارمزد حذف شدند؛ همیشه دقیقاً دو ردیف دفتر. نام انتقال‌دهنده و گیرنده اختیاری ماند. پیوست اسکن هنوز `0A000` است (فاز ۶). typecheck **۷۰**. فاز ۵ شروع نشد. | (this PR) |
| 2026-08-18 | Claude Code | **فاز ۲ برنامهٔ دفتر زندهٔ حسابداری — `create_receipt`: یک فیش با یک تراکنش ثبت و سند می‌خورد.** مهاجرت‌های **۳۴۸** و **۳۴۹**. تابع جدید `create_receipt` همهٔ کار را در **یک تراکنش** انجام می‌دهد: اعتبارسنجی، الزام کد آسان، شماره‌گذاری سند، درج فیش، اتصال به پیش‌فاکتور، سند حسابداری متوازن `doc_kind=receipt` با وضعیت `posted`، افزایش اعتبار مشتری و ردیف حسابرسی. 🔴 **نقصی که کشته شد:** مسیر قدیمیِ فرم چهار درج جدا می‌زد و در صورت خطا `DELETE` می‌کرد روی جدولی که **هیچ policy حذف ندارد** — پس ۲۰۴ برمی‌گشت و هیچ سطری پاک نمی‌شد و یک فیش یتیم بدون ردیف حسابرسی می‌ماند. با اجبار همان خطا اثبات شد: پس از یک اتصال ناموفق، تعداد فیش‌ها، اتصال‌ها، اسناد و **شماره‌های سند** هیچ‌کدام تغییر نکرد. ۳۴۸ فقط شرط `receiver_exclusive` را برای «چک» شل می‌کند (چک نه حساب مقصد دارد نه طرف خارجی) — دقیقاً شل‌کننده، پس هیچ نویسندهٔ فعلی نمی‌شکند. **بازاستفاده به‌جای بازنویسی:** `require_asan_code`، `assign_document_number`، `increase_credit` و تریگر سقف تخصیص `trg_payment_receipt_links_enforce_limits`. ✅ ۸ از ۸ فرمان پذیرش با فراخوانی واقعی زیر JWT شبیه‌سازی‌شده؛ نقش `sales` → `42501`، `accountant` و **`manager`** → موفق (OG-13 بسته شد). تست فشار ۵۰ فیش هم‌زمان ⇒ ۵۰ شمارهٔ متمایز، بدون تکرار، بدون سند نامتوازن، بدون یتیم. 🔍 **m2ی Gate A که فاز ۱ نتوانسته بود اندازه بگیرد بسته شد:** دو تراکنش هم‌زمان روی **یک** `source_id` هر دو `RCP-1405-000051` گرفتند و فقط یک ردیف ساخته شد. typecheck **۷۰**. ⚠️ ۹ تناقض بین قرارداد و اسکیمای زنده ثبت و **تصمیم‌گیری** شد (نه سازگاری خاموش) — از جمله اینکه `status=posted` اصلاً وجود ندارد و اگر ساخته می‌شد سقف تخصیص پیش‌فاکتور را بی‌صدا از کار می‌انداخت؛ همه در `docs/execution/phase-2-PROGRESS.md`. Owner-Gate جدید **OG-16** باز است ولی چیزی را مسدود نمی‌کند. | `97a763bf` |
| 2026-08-15 | Claude Code | **پرشدن خودکار قیمت واحد در دیالوگ افزودن آیتم پیش‌فاکتور + دو اصلاح ظاهری در جست‌وجوی سریع فروش.** بدون مهاجرت. 🔴 **باگ ریشه‌ای که رفع شد:** دیالوگ افزودن آیتم از ویو `product_computed_prices_public` می‌خواند که `WHERE settlement_type_id IS NULL` را هاردکد کرده — یعنی همیشه **قیمت پایه** — ولی کف مجاز سمت سرور با `settlement_type_id IS NOT DISTINCT FROM p_settlement_type_id` خوانده می‌شود. هر وقت قیمت تسویه بالاتر از پایه بود، عددی که کاربر می‌دید زیر کف واقعی بود و پیش‌فاکتور هنگام ذخیره رد می‌شد. حالا قیمت از `get_sales_search_products` می‌آید — تنها مسیر تسویه‌محوری که به نقش `sales` داده شده (جدول `product_computed_prices` فقط admin/manager/accountant یا `pricing:view` را می‌پذیرد). کوئری `product_sale_price_history` که هیچ فیلتری روی `settlement_type_id` نداشت حذف شد. محافظ مالی دست‌نخورده: `sale_price_type_id` همچنان روی آیتم ثبت می‌شود، پس شرط `IF _sptid IS NOT NULL` در سرور برقرار می‌ماند. ⚠️ **محدودیت شناخته‌شده:** آن RPC بارکد را جست‌وجو نمی‌کند و `p_limit` در خود تابع به ۵۰ محدود است، پس اگر محصول در نتایج نبود دیالوگ صریحاً همین را می‌گوید و ادعا نمی‌کند قیمتی ثبت نشده. **جست‌وجوی سریع فروش:** قیمت اصلی هم‌اندازهٔ بقیه شد و هر کارت تسویه حالا هر دو بُعد را می‌نویسد («نقدی · پیش واریز») — قیمت روی سه‌تایی (محصول، نوع‌قیمت، تسویه) ذخیره می‌شود، پس هر ترم تسویه یک ردیف به‌ازای هر نوع‌قیمت دارد و برچسب قبلی سه کارت هم‌نام با عددهای متفاوت می‌ساخت. 🔴 **نمایش قیمت پیش‌واریز در کارگاه و جست‌وجو نوشته، تست، و به تصمیم مالک پس گرفته شد** (`35fc8c8b`) — هرگز روی تولید build نشد؛ کارگاه دست‌نخورده است. 📌 **یافته‌های تحقیق که هنوز باز‌ند:** صف `pricing_recompute_queue` روی تولید ۷۵٬۶۱۰ مورد pending دارد از ۳ خرداد و **هرگز یک بار هم پردازش نشده** (`PRICING_WORKER_TOKEN` تنظیم نیست، هیچ تسک زمان‌بندی‌شده‌ای صدایش نمی‌زند) — ولی قیمت‌ها متوقف نشده‌اند و از مسیر همگام محاسبه می‌شوند؛ حدود نیمی از کاتالوگ تولید (۱۵۳ از ۳۲۱) هیچ قیمت تسویه‌ای ندارد چون از ۲۷ تیر دوباره منتشر نشده؛ تسک `AfraKala LAN Auto Start` هنوز اسکریپتش را از پوشهٔ بازنشستهٔ `C:\AfraKalaServer\get-git-going01lan\` روی هر logon اجرا می‌کند. | `bfcc723a` |
| 2026-08-14 | Claude Code | **رفع بنر قرمز سرور اصلی + هم‌ترازی main/staging با تولید.** متغیر `VITE_TRUSTED_HOSTS` اضافه شد تا هر نصب آدرس واقعی خودش را اعلام کند. سرور اصلی روی `cde5095e` build شد؛ کلید anon بدون تغییر ماند و نقش کاربر در مرورگر تأیید شد. `main` و `staging` هر دو به `643eebc5` رسیدند. ۴ PR منسوخ بسته و ۱۱ شاخهٔ مرده حذف شد. | `643eebc5` |
| 2026-08-11 | Claude Code | **انتقال تولید از `cb7c070f` (۱۵ خرداد) به شاخهٔ کاری + مهاجرت ۳۳۵ (هم‌گرایی محیط‌ها).** با اجازهٔ صریح مالک. ۲۹۹ مهاجرت اول روی یک **کپی دقیق** از تولید اجرا شد: دور اول ۲۵۷ موفق / ۴۲ ناموفق؛ پس از شش دور اصلاح ⇒ **۲۹۵ / ۰ / ۴ skip**. چهار skip دادهٔ محض تست‌اند (از جمله فایلی که تخصیص سرمایهٔ «تست 30M» را از طرف یک UUID سرور تست ثبت می‌کرد). شش مهاجرت با «نسخهٔ تولید» اجرا شدند که تنها تفاوتشان کنارگذاشتن شرط‌های سرشماری مخصوص تست است (مثل `expected 11 coded customers`). 🔴 **کشف اصلی: دیتابیس تست از روی فایل‌های خودش بازتولید نمی‌شود.** مهاجرت‌های **۳۲۴، ۳۲۵، ۳۲۶ در هیچ شاخه‌ای commit نشده‌اند**؛ بدنهٔ ۴۶ تابع (از جمله `has_role` / `has_any_role` / `has_dynamic_permission` که هنوز با `'admin'::app_role` مقایسه می‌کردند در حالی که ستون `text` شده) با فایل‌ها فرق داشت ⇒ روی تولید **هیچ جدولی خوانده نمی‌شد**؛ دو ایندکس `product_interaction_events` در هیچ فایلی نیست؛ چهار جدول `*_backup_*` فقط در کامنت مستند و دستی ساخته شده بودند. مهاجرت ۳۳۵ همهٔ اینها را حمل می‌کند + `USAGE` روی `auth`/`extensions` (بدون آن هر policy با `auth.uid()` می‌شکست و صفحهٔ اشخاص خالی می‌آمد) + مالکیت schema `storage` (علت crash دوهفته‌ای آپلود) + FK صریح به `profiles` برای embed در PostgREST (صفحهٔ تخصیص سرمایه روی **هر دو** سرور خالی بود). اثبات: کپی تولید + ۲۹۹ مهاجرت + ۳۳۵ ⇒ با تست یکسان در جدول/ستون/ایندکس/view/enum/trigger و ۸۱۸ از ۸۱۹ بدنهٔ تابع (یکی فقط یک خط خالی). دادهٔ تولید دست‌نخورده؛ تنها کاهش `knowledge_documents` ۴۲→۰ که مهاجرت ۱۴۳ آرشیوش می‌کند و ۲۷۹ فارسی ۲۸ تای‌شان را بازیابی می‌کند. ۳۳۵ روی تست هم اعمال شد. ⚠️ تولید هنوز **دفتر مهاجرت ندارد**. | — |
| 2026-08-10 | Claude Code | **pricing-and-stock-warning-fixes — دو باگ گزارش‌شده + یک باگ پنهان.** بدون مهاجرت. کارگاه قیمت: کوئری فیلتر نوع‌قیمت نداشت ⇒ ۷۷/۳۱۶ محصول عدد غلط (همکاری/چکی) نشان می‌دادند؛ فیلتر `cash_price` + برچسب‌های صریح؛ همان باگ در `api/public/products.ts` عمومی. هشدار موجودی حسابدار: از قبل بود ولی پشت `isAccepting` که حسابدار (فقط `rejected`) هرگز بازش نمی‌کند ⇒ پنل هشدار روی صفحه با بازاستفاده از همان RPC. 🔴 **باگ سوم:** `const rpc = supabase.rpc` بدون `bind` ⇒ کل بررسی موجودی برای همه بی‌صدا مرده بود، از جمله گارد پذیرش مدیر. همان الگو در treasury و mutual-settlement **عمداً رفع نشد** (مسیر نوشتن مالی). تأیید مرورگری با نشست واقعی حسابدار. typecheck **۷۰**، build سبز. | — |
| 2026-08-10 | Claude Code | **internal-products-pricing-api — مهاجرت ۳۳۴ (افزودنی محض).** دو view فقط‌خواندنی روی PostgREST موجود + نقش `products_api_readonly` (دقیقاً ۲ relation، صفر privilege نوشتنی، RPC هم‌سطح `anon`). فاز ۱ زنده: «قیمت فعلی» از `product_computed_prices` می‌آید نه `product_sale_price_history`؛ `price_lists`/`price_list_items` **یتیم (۰ ردیف)**؛ ۲۰ `sale_lists` همه `draft`. dry-run دو تله گرفت: `v_latest_active_purchase_prices` عمداً `security_invoker=true` است (flag دست نخورد، منطق تکرار شد) و `ALTER DEFAULT PRIVILEGES` بدون REVOKE صریح **قیمت خرید را به `anon`/`authenticated` می‌داد**. کف قیمت از تابع موجود `get_product_price_bounds()`. برابری با `get_sales_search_products()`: ۷۹۲ ردیف، **۰ مغایرت**. ۸ تست `curl` بیرونی سبز (۲۰۰ / ۴۰۱ / ۴۰۳). credential کامیت نشد — اسکریپت صدور + فایل بیرون‌ریپو. typecheck **۷۰**. | — |
| 2026-08-08 | Cursor | **eg-checklist — چک‌لیست رسمی هشت‌بندی E/G.** فقط‌خواندنی. بخش رسمی به `full-accounting-audit.md` افزوده شد؛ خلاصه در `eg-formal-checklist-COMPLETE.md`. دلتای زنده: `role_permissions.accounting`=۷ (مهاجرت ۳۱۵)؛ `supplier_payable` در CHECK (۳۱۲). نقص‌های ساختاری E/G همچنان باز. | `6746d8bd` |
| 2026-08-08 | Cursor | **messenger-rpc-fix — پایان مأموریت (گزینهٔ ب).** مهاجرت **۳۱۶** `set_messenger_group_member_role` (SECURITY DEFINER، الگوی زندهٔ `add_messenger_group_member`). مهاجرت ۲۲۵ گیت اعمال نشد تا `add_*` عوض نشود. dry-run ۶/۶، down در ROLLBACK، E2E با JWT واقعی: ساخت گروه ← افزودن ← تغییر نقش به purchaser/viewer (تأیید ردیف DB) ← حذف عضو ← cleanup. فرانت از قبل درست بود. PostgREST ری‌استارت شد. | `28da7d3c` |
| 2026-08-08 | Cursor | **new-clusters-frontend — فرانت سه خوشه.** صفحات `/sales/promotion-nominations`، `/messages/inquiries`، `/gamification/league` + تب موتور فصل در admin leagues؛ ناوبری در PRIMARY_MODULES. JWT `e2e/clusters` **۵/۵**. **توقف schema:** `start_league_season`/`settle` با تریگر `validate_league_season` (title_fa)؛ `tick_inquiries` با 42P10 داخل `expire_pending_documents`. بدون مهاجرت. جزئیات: `docs/execution/new-clusters-frontend-mission-COMPLETE.md`. ⚠️ محتوای این کار داخل `75f56867` نشست (پیام commit مربوط به مأموریت دیگری است — محتوای tree همان فرانت خوشه‌هاست). | `75f56867` |
| 2026-08-08 | Cursor | **messenger-rpc-fix فاز ۱ — تشخیص.** فرانت در `GroupMembersDialog.tsx:166` با پارامترهای `p_group_id`/`p_user_id`/`p_role` صدا می‌زند. روی دیتابیس زنده فقط `add_messenger_group_member` و `is_messenger_group_member` هستند؛ `set_messenger_group_member_role` در `pg_proc` نیست. مهاجرت قدیمی ۲۲۵ در گیت هست ولی روی LAN اعمال نشده. جدول `messenger_group_members` سیاست UPDATE ندارد ⇒ مسیر مستقیم UPDATE هم بی‌فایده است. | — |
| 2026-08-07 | Claude Code | **`/updates` خودکار شد.** یادداشت انتشار از تاریخچهٔ گیت هنگام build تولید و در استقرار خودکار منتشر می‌شود؛ **هیچ تأیید دستی لازم نیست**. مهاجرت **۳۰۷** `auto_publish_release` (فقط `service_role`، idempotent روی `git_sha`، audit با `actor_id=NULL`). قانون تازه در `AGENTS.md`/`CLAUDE.md`: هر کامیت کاربرپسند باید تریلر `Release-note-fa:` داشته باشد — بدون آن چیزی منتشر نمی‌شود (عمداً، تا متن انگلیسی فنی به کاربر نرسد). دو نقص حین آزمون واقعی پیدا و رفع شد: قلاب boot در کانتینر **هرگز اجرا نمی‌شد** (کانتینر `.output/server/index.mjs` را اجرا می‌کند نه `server/node-entry.mjs`)، و برش «فقط موارد تازه» به‌خاطر اختلاف طول sha (۸ در برابر ۴۰) بی‌صدا باز می‌شد. آزمون سرتاسری: نسخهٔ **۱۴** منتشر شد، اجرای دوباره چیزی نساخت. typecheck **۷۰**. | `8b455da8` |
| 2026-08-07 | Claude Code | **UNIFY P0 — پایان مأموریت.** ۰.۳ اعمال شد (مهاجرت ۳۰۴): ۳۲۳ خرید e2e + ۳۲۳ آیتم + ۳۲۰ idempotency + ۱۵۸ fulfillment + **۳۲۳ stock_movement** حذف؛ خریدها ۳۳۴→**۱۲**؛ `journal_entries`/`payment_receipts`/`sales_quotes` **دست‌نخورده**. **۰.۲ متوقف شد** (گزارش منبع `dual-role-person-analysis.md` وجود ندارد؛ ۳ جفت از ۴ جفت اصلاً نیستند؛ تنها جفت واقعی «مختارشاهمرادی» دوتایی organization/individual است با ۲ خرید و ۷۷ رکورد قیمت ← شرط توقف مالی خودِ فاز). **۰.۵ لازم نبود** (فایل xlsx هدف در **صفر** کامیت تاریخچه است؛ هیچ rewrite/force-push انجام نشد). دامپ P0.3 عمداً کامیت نشد (قاعدهٔ ۴) و به `.gitignore` اضافه شد. بدون تغییر سورس. | `15c5920d` |
| 2026-08-07 | Claude Code | **UNIFY P0.1 اعمال شد + بررسی «api».** مهاجرت **۳۰۳** روی دیتابیس تست اجرا شد (`--single-transaction`, exit 0): persons ۷۹→۷۷، suppliers ۱۵→۱۳، ۶ حساب هارنس e2e و fixture E2E264 سالم. `afrakala-lan-rest` ری‌استارت شد. بررسی `6cd30201` «api»: نوشتهٔ محصول فقط متنِ پیش‌پرشده است نه رابطه (`product_suppliers`=۰ در هر دو سو) — **باقی‌ماندهٔ تست، پیشنهاد حذف با مهاجرت ۳۰۴**. نقص جانبی ثبت شد: تأمین‌کنندهٔ معرفی‌شده `status='pending'` ولی `is_active=true` و پیکرها روی دو ستون متفاوت فیلتر می‌کنند. typecheck **۷۰**. | `8f256649` |
| 2026-08-06 | Cursor | **تاریخچه به‌روزرسانی — `/updates` + مدیریت ادمین.** مهاجرت **۳۰۲** `platform_releases` (RLS، شماره ترتیبی هنگام انتشار، seed چهار نسخه). toast «مشاهده تغییرات». typecheck **۷۰**. | `e2eda7b6` |
| 2026-08-06 | Cursor | **برندینگ — نام پلتفرم متمرکز `myafrakala.ir`.** `src/config/branding.ts` + جایگزینی کروم UI/SEO/PWA/PDF. بدون مهاجرت. typecheck **۷۰**. | `dda3c4c7` |
| 2026-08-05 | Cursor | **آسان — تکمیل خروجی دسته‌ای فروش با بازه تاریخ و انتخاب.** قابلیت هسته از قبل بود؛ UX: اعمال/پاک‌کردن بازه، انتخاب فقط قابل‌خروجی، تأیید شماره‌گذاری، سقف ۱۰۰۰، پیش‌نمایش صریح. بدون مهاجرت. typecheck **۷۰**. | `1f9425e6` |
| 2026-08-05 | Cursor | **محصولات — لینک ترب اختیاری + خروجی اکسل کاتالوگ.** مهاجرت **۳۰۱** `products.torob_url` (nullable، CHECK http/s). فیلد در فرم/جزئیات. دکمهٔ «خروجی اکسل» روی `/products` با فیلترهای جاری (سقف ۵۰۰۰). typecheck **۷۰**. | `ef0c64ad` |
| 2026-08-05 | Cursor | **Persons Phase 6 — تست سخت‌سازی.** Phone-collisions browser e2e، mobile/RTL overflow gate (۳۲۰/۳۷۵/۳۹۰/۴۳۰)، permission matrix (UI+JWT)، رگرسیون فاز ۱–۵. گارد کلاینت import/merge/edit + wrap overflow. بدون مهاجرت. Phase6 **۴۶ سبز / ۱ skip**؛ `e2e/persons`+RLS **۱۴۵ سبز / ۱ قرمز مستند (`credit-uses-person`) / ۳ skip**؛ typecheck **۷۰**؛ drift **۰**؛ leftover **۰**. | `1d294f1e` |
| 2026-08-05 | Cursor | **Persons Phase 5 P2 — گسترش پرونده هویت.** Deep links (customer/supplier/staff/accounting)، merge/collision panels، audit summary با redaction، metadata Jalali. بدون مهاجرت. JWT **۱۱/۱۱**؛ UI dossier **۶/۶**؛ `e2e/persons` **۹۴ سبز / ۱ قرمز مستند (`credit-uses-person`) / ۲ skip**. typecheck **۷۰**. | `cb8e0edf` |
| 2026-08-05 | Cursor | **Persons Phase 4 P1 — UI مدیریت نام‌های دیگر.** مهاجرت **۳۰۰** (سخت‌کردن INSERT به admin/manager + `can_read_person`؛ audit DELETE). `PersonAliasesManager` روی پروفایل/ویرایش. typecheck **۷۰**. `e2e/persons` **۷۷ سبز / ۱ قرمز مستند / ۲ skip**. | `54526f49` |
| 2026-08-05 | Cursor | **Persons Phase 3 P1 — فیلترهای امن فهرست.** مهاجرت **۲۹۹** گسترش `search_visible_persons` (context OR / active / missing AND؛ viewer-only missing را نادیده می‌گیرد). URL state روی `/persons`. بدون اندیس تازه. JWT **۱۳/۱۳**؛ filters-ui **۴/۴**؛ `e2e/persons` **۷۰ سبز / ۱ قرمز مستند / ۱ skip**. typecheck **۷۰**. | `845195d3` |
| 2026-08-05 | Cursor | **Persons Phase 2 P1 — جستجوی امن شناسه/نام.** مهاجرت **۲۹۸** `search_visible_persons` (SECURITY INVOKER؛ تقویت SELECT aliases به `can_read_person`). فهرست + `searchPersons` از RPC. بدون اندیس تازه. JWT **۱۰/۱۰**؛ UI search سبز؛ `e2e/persons` **۵۴ سبز / ۱ قرمز مستند (`credit-uses-person`) / ۱ skip**. typecheck **۷۰**. drift **۰**. | `16f95fb5` |
| 2026-08-05 | Cursor | **Persons Phase 1 P0 — پروندهٔ فقط‌خواندنی `/persons/$personId`.** گارد `persons.view` (نه update). فهرست: «مشاهده» → پروفایل، «ویرایش» فقط برای admin/manager. بدون SQL/مهاجرت/RLS. `e2e/persons` **۴۲ سبز / ۱ قرمز مستند / ۱ skip**. typecheck **۷۰**. | `d6a1edbe` |
| 2026-08-04 | Claude Code | **گیت M3 — سوئیت کامل یک رگرسیون واقعی از فاز ۳.۳ گرفت (مهاجرت ۲۸۷)**. اجرای کامل شد **۲۲۸ سبز / ۸ قرمز / ۴ skip**؛ شش‌تا از هشت‌تا قرمزهای مستندند، **ولی دوتا تازه بودند و مالِ من**. هر دو spec ادغام با «ادغام متوقف شد: ستون «asan_import_person_rows…»» می‌افتادند. علت: **مهاجرت ۲۸۵ ستون `matched_person_id` را اضافه کرد یعنی یک کلید خارجی تازه به `persons`، بدون ثبت در رجیستری**، و `person_merge` عمداً فهرست کارش را از `pg_constraint` می‌خواند و روی هر کلید ناشناخته **متوقف می‌شود** — پس از `aab2c158` به بعد **هر ادغامی در کل سامانه می‌افتاد**. این محافظ درست کار کرده، نه اینکه چیزی خراب شده باشد: جایگزینش ادغامی است که بی‌صدا ردیف import را به شخصی که دیگر وجود ندارد وصل بگذارد. **دقیقاً همان تلهٔ مهاجرت ۲۷۱ در فاز ۴ P1+D8، دو مأموریت بعد و با همان لباس.** **چرا دیده نشده بود: فاز ۳.۳ فقط `e2e/asan/` را اجرا کرده بود** و هیچ‌چیز آن سوئیت به ادغام دست نمی‌زند، پس یک گیت فازِ سبز دو کامیت کنار یک ادغامِ شکسته زندگی کرد — همان دلیلی که بند ۲.۹ سوئیت کامل را به **پایان هر مأموریت** گره زده. ثبت به‌صورت `generic` نه `identity_root` (ستون یکتا نیست و حالت مالی حمل نمی‌کند). **مهاجرت تعریف زنده را وصله می‌زند نه بازنویسی (قاعدهٔ ۲.۳):** `person_merge` حدود ۱۵ کیلوبایت با ۹ رشتهٔ فارسی است؛ یک خط اسکی کنار anchorی که **دقیقاً یک بار** خوردنش assert می‌شود درج شد و snapshot در `docs/verification/pre-287/` ماند. **⛔ dry-run یک باگ در گاردِ خودِ مهاجرت گرفت:** بررسی «تعریف نباید هیچ `?` داشته باشد» فوراً آتش گرفت و **غلط بود** — `person_merge` واقعاً `IF NOT (_registry ? _key)` دارد و `?` عملگر وجودِ کلید jsonb است، یعنی دقیقاً مکانیزمی که رجیستری با آن خوانده می‌شود؛ اگر آن بررسی طوری نوشته شده بود که پاس شود، بی‌صدا هرگز از چیزی محافظت نمی‌کرد. حالا **مقایسهٔ تعداد قبل/بعد** است. ۲۸۷ همان ادعایی را هم اضافه کرد که ۲۸۵ باید می‌داشت: **هر کلید خارجی به `persons` باید برای `person_merge` شناخته‌شده باشد**، تا ستون بعدی این اشتباه را بی‌صدا تکرار نکند. `e2e/persons` برگشت به **۳۶ سبز / ۱ قرمز** (همان قرمز مستند) و `person_fk_drift_report()` صفر ردیف. | `8b48c4b2` |
| 2026-08-04 | Claude Code | **M3.4 آسان — ورود کالا از آسان (مهاجرت ۲۸۶)**. همان معماری «ثبت موقت ← تأیید» مهاجرت ۲۸۵ با استفادهٔ دوباره از `asan_import_batches` (`kind='products'`)، ولی **یک قاعده وارونه است و همان قاعدهٔ مهم است: هیچ کالای تازه‌ای ساخته نمی‌شود**. آسان ۷۲۵۶ قلم دارد و افراکالا ۳۵۵ قلم؛ ردیفی که نظیر ندارد `unmatched` می‌ماند. اصلاً کلاس‌بندی `new` **وجود ندارد** (در CHECK نیست)، و `asan_commit_product_batch` فقط INSERT ننوشتن نیست — **تعداد کالاها را قبل و بعد می‌شمارد و اگر تکان بخورد کل ثبت را برمی‌گرداند**، پس تضمین به «مواظب بودنِ» کد آینده وابسته نیست. **نرمال‌ساز پیش از اعتماد، اندازه‌گیری شد:** کل ۷۲۵۶ شرح در یک تراکنش برگشتی مقابل هر ۳۵۵ کالا تطبیق داده شد و **دقیقاً همان چیزی را داد که R1.5 اندازه گرفته بود** — ۳ تطبیق نام نرمال‌شده (همان سه جفت)، ۰ تطبیق نام دقیق، ۶۰ شرح تکراری درون خودِ آسان روی ۱۲۲ ردیف، و ۷۲۵۶ کد یکتا. **جدول‌های تبدیل حرف عمداً با escape اسکی `U&'\uXXXX'` نوشته شدند**: یک بایت خراب در جدول فولد **خطا نمی‌دهد**، بی‌صدا یک حرف را فولد نمی‌کند و عوض می‌کند کدام کالاها تطبیق بخورند. فارسی فقط در پیام‌های انسانی ماند و پس از اعمال، صفر `?` تأیید شد. **جایی که سند با دادهٔ اندازه‌گیری‌شده می‌جنگد و تحقیق برنده است:** سند بارکد را «قوی‌ترین کلید تطبیق» می‌نامد ولی بارکد **در هر دو طرف ۰٪** پر است، پس اصلاً استراتژی‌ای نیست که بشود امتحان کرد؛ ترتیب شد کد آسان ← نام نرمال‌شده ← بی‌تطبیق. سند می‌گوید «۳۷۴ کالا دارم»، عدد زنده **۳۵۵** است. **چرا تست حالتش را می‌سازد:** روی این دیتابیس فایل واقعی **۳ `unchanged` و ۷۲۵۳ `unmatched` و صفر `update`** می‌دهد (مهاجرت ۲۸۳ همان سه کد را از قبل وصل کرده)، پس مسیر اتصال اصلاً اجرا نمی‌شد؛ تست یک کالای واقعی را **عمداً جدا می‌کند**، دوباره طبقه‌بندی می‌کند (می‌شود `update`)، تأیید و ثبت می‌کند — و خودِ عملیاتِ تحت آزمون کد را برمی‌گرداند. **⛔ باگی که تست گرفت و طراحی ندیده بود:** شرط `applied_at IS NULL` در تریگر نگهبان حیاتی است؛ بدون آن نگهبان **نوشتنِ دفترداری خودِ تابع ثبت** را رد می‌کرد و هر ثبت با یک خطای فارسی می‌افتاد که شبیه «نگهبان دارد درست کار می‌کند» بود. **۲۸۶ ویرایش و دوباره اعمال شد نه با ۲۸۷** — چون هرگز commit یا منتشر نشده بود و فایل idempotent است؛ صریحاً به‌عنوان عدول ثبت شد. **تلهٔ دوم در هارنس نه در کد:** `e2e/helpers/db.ts` هر SQL حاوی کلمهٔ `update` را — حتی داخل رشته — رد می‌کند. **یک ناهنجاری صادقانه گزارش شد نه توجیه:** در همان اجرا که دو باگ بالا قرمز بودند، ادعای آخر نرمال‌ساز هم ۰ داد؛ پس از رفع، تکرارپذیر نیست و ابزار تشخیص در تست باقی ماند — **به‌عنوان «توضیح‌داده‌نشده» ثبت شد نه «رفع‌شده»**. **زمان‌بندی:** ۷۲۵۶ ردیف در **۴۸۴ میلی‌ثانیه** (۱۵ درخواست ۵۰۰تایی) و طبقه‌بندی در **۳.۲ ثانیه** با پنج دستور مجموعه‌ای نه حلقهٔ ردیفی. spec **۸/۸ سبز**، typecheck **۷۰**، دادهٔ آزمایشی پاک (۳۵۵ کالا، ۳ متصل، ۰ دسته). **گیت M3:** بند ۲ با کوئری اثبات شد (`asan-import` ۷ ردیف برای ۷ نقش، `can_view` دقیقاً admin+accountant، ۰ جدول بدون RLS) — و همان کوئری یافتهٔ جانبی داد: `persons` نقش‌های `purchase_specialist` و `site` و `warehouse` نقش `site` را ندارند (از قبل، خارج از دامنه، گزارش شد). | `da6a6f60` |
| 2026-08-04 | Claude Code | **M3.3 آسان — رابط `/admin/asan-import` (پایان فاز ۳.۳، بدون مهاجرت)**. سمت دیتابیس (مهاجرت ۲۸۵) و parser از قبل در `aab2c158` بود؛ چیزی که کم بود همان مسیر ادمین بود. جریان: بارگذاری ← پیش‌نمایش ← ثبت موقت ← طبقه‌بندی ← تأیید نهایی؛ **تا لحظهٔ دکمهٔ آخر هیچ‌چیز در `persons` نوشته نمی‌شود**. صفحه دو قاعدهٔ اصلی را **بازپیاده‌سازی نمی‌کند** — همان‌جا در مهاجرت ۲۸۵ می‌مانند تا فراخوان مستقیم PostgREST هم به آن‌ها بخورد — فقط آن‌ها را دیدنی می‌کند و **هیچ کنترلی که backend ردش کند نشان نمی‌دهد**: ردیف دارای تعارض اصلاً دکمهٔ «تأیید» ندارد، و هر ردیف تطبیق‌یافته می‌گوید کدام سمت قاعدهٔ «بازنویسی نکن» می‌افتد. **⛔ تلهٔ اصلی: بدیهی‌ترین flag غلط بود.** `adminOnly` در رجیستری یعنی «admin **یا manager**» (`selectors.ts:35`)، پس استفاده از آن صفحه را از **حسابدار** — که اصلاً برای او ساخته شده — پنهان می‌کرد و به **مدیر بخش**ی نشان می‌داد که backend ردش می‌کند؛ به‌جایش `ROLE_ALLOWLIST_BY_ROUTE` با `["admin","accountant"]`. `asan-import` به `ModuleKey` و ماتریس ایستای `PERMISSIONS` هم اضافه شد تا fallback آفلاین با ردیف‌های `role_permissions` که ۲۸۵ برای **هر ۷ نقش** کاشته بود یکی باشد. `types.ts` **جراحی** ویرایش شد نه با `prettier --write`. **تصمیم صادقانه:** سند «انتخاب per-field یا per-row» می‌خواهد؛ قاعدهٔ RPC از هر دو سخت‌گیرانه‌تر است (مقدار پرشدهٔ افراکالا هرگز بازنویسی نمی‌شود)، پس تنها فیلدی که کنترل per-field می‌توانست بر آن حکم کند فیلد **خالی** است — per-row همان سطح صادقانه است. ردیف‌ها سمت سرور صفحه‌بندی می‌شوند (۵۰ تایی) و دستهٔ ثبت‌شده هنگام mount دوباره برداشته می‌شود تا refresh، ۴۸۸ ردیف را بی‌صاحب نگذارد. **گیت: `e2e/asan/` ۲۱/۲۱ سبز روی build مستقر** — ۸ مورد API‌ای قبلی + ۴ مورد مرورگری تازه (ادمین می‌بیند و **صرفِ باز کردن صفحه هیچ چیز نمی‌نویسد** — هم شمار اشخاص هم شمار دسته‌ها بررسی شد؛ حسابدار می‌بیند؛ فروشنده و ناشناس نمی‌بینند، با ادعا روی **محتوای صفحه** نه URL چون پوسته پیش از حل‌شدن گارد رندر می‌شود) + ۱ tripwire روی متن منبع تا بعداً کسی بی‌صدا گارد یا allowlist را گشاد نکند. typecheck دقیقاً **۷۰**، eslint فایل‌های لمس‌شده تمیز، دادهٔ آزمایشی پاک (اشخاص ۷۰، ۰ دسته)، هر سه سیگنال استقرار برابر HEAD. | `41225d80` |
| 2026-08-04 | Claude Code | **فاز ۱۱ مأموریت P1+D8 — انتخاب حالت خروجی و کلید جزئیات ردیف (تصمیم D8-6). بدون مهاجرت.** **گیت سخت پاس شد و «شبیه هم» نبود، بایت‌به‌بایت بود:** نگاشت خروجی موجود عیناً به `src/lib/export/receipt-export-rows.ts` منتقل شد و مقابل نسخهٔ `git show HEAD:…` روی **۶ فیش واقعی** روی استقرار زنده مقایسه شد — همان ۲۶ ستون با همان ترتیب، مقادیر deep-equal، و **sha256 یکسان** روی فایل xlsx نوشته‌شده (`565ae450…`). **همین استخراج یک نقص گرفت که دقیقاً همان تضمین را می‌شکست:** برچسب وضعیت «ردشده» است نه «رد شده»؛ نوشتنش از حافظه یک سلول را در هر ردیف ردشده بی‌صدا عوض می‌کرد. **بند ۳ سند «تأیید کن نه اعتماد» واقعاً تأیید شد:** خروجی XLSX حقیقی است — magic bytes `504b0304`، اجزای واقعی `xl/` و round-trip از `XLSX.read`. **«خروجی آسان» عمداً هیچ فایلی تولید نمی‌کند** و `AsanLayoutNotConfiguredError` با پیام فارسی می‌دهد: هیچ فایل نمونهٔ import آسان وجود ندارد — دو `.xlsx` ریشهٔ ریپو **خروجی** آسان از اشخاص و کالا هستند (جهت مخالف)، و تنها تصویر untracked یک عکس نامرتبط از خطای Docker است. `docs/asan/ASAN_BRIDGE.md` بخش B5 ستون‌های نامزد را فهرست می‌کند ولی از روی اسکرین‌شات‌هایی که این جلسه هرگز ندیده، و خودش هم می‌گوید چیدمان بانکی باید مقابل دیالوگ زندهٔ آسان راستی‌آزمایی شود؛ حدس‌زدن یعنی فایلی که بی‌صدا و با ظاهر معتبر وارد نرم‌افزار حسابداری زندهٔ مالک شود. **جزئیات ردیف:** فیش پرداخت خودش ردیف کالا ندارد، پس از زنجیرهٔ واقعی `payment_receipt_links → sales_quotes → sales_quote_items` استفاده شد؛ فیش بدون ردیف متصل همچنان **یک** ردیف با ستون‌های خالی می‌دهد، چون حذفش جمع مبالغ را بی‌صدا کم می‌کرد (۶ از ۶ فیش حفظ شد). **دامنه‌ای که برآورده نشد و پنهان نشد:** سند می‌گوید کلید روی «خروجی پیش‌فاکتور و فیش/پرداخت» اعمال شود — ولی **هیچ خروجی اکسلِ پیش‌فاکتوری در این کدبیس وجود ندارد** (فقط فیش‌ها و لیست قیمت فروش)؛ ساختنش کار `docs/asan/ASAN_BRIDGE.md` B4/B5 است که مأموریت جداگانه و منوط به تأیید مالک است. typecheck ۷۰، build سبز، eslint تمیز. | `feabcdb3` |
| 2026-08-04 | Claude Code | **فاز ۱۰ مأموریت P1+D8 — وظایف بازاریابی تکرارشونده (نیاز ۲۲۴، مهاجرت ۲۷۷ + ۲۷۸)**. سامانهٔ وظیفهٔ دوم ساخته **نشد**: تنها شیء تازه جدول قالب است و هر نمونه یک ردیف عادی `tasks`. **مهم‌ترین یافته پیش از طراحی:** قلاب گیمیفیکیشن از قبل **کامل بود و هرگز تغذیه نشده بود** — KPI فعال `promotions_completed` (وزن ۲) در `compute_employee_score` رویداد `promotion_completed` را می‌شمارد و `gamification_kpi_rules` قاعدهٔ فعال ۱۵ XP دارد، ولی **هیچ‌چیز در کل سامانه این رویداد را تولید نمی‌کرد (۰ ردیف)**؛ پس تصمیم ۳۴ بدون یک خط مسیر امتیازدهی تازه اجرا شد. **یافتهٔ دوم:** ۵۶ ردیف `marketing_channels` اسم پلتفرم نیستند بلکه **حساب × پلتفرم**‌اند («خانم احمدی ۰۹۹۱۹۴۸۴۲۱۰ استوری بله») — همان دانه‌بندی درست یک کار روزانه، پس قالب به ردیف کانال کلید خورد و جدول کانال گسترش داده **نشد**. **تصمیم صریح CHECK:** برخلاف فاز ۹ که صف موجود `store` را استفاده کرد، اینجا `marketing` به CHECK افزوده شد چون `get_task_kpi_report` عملکرد را **به تفکیک صف** می‌دهد و ریختن کار بازاریابی در صف `sales` نرخ تکمیل فروش را خراب می‌کرد؛ `expired` هم افزوده شد چون «منقضی» با «لغو» یکی نیست. گشادکردن فهرست مقدار مجاز کاملاً permissive است. **عدم انتقال به فردا ساختاری است:** اندیس یکتای جزئی روی `(reference_id, due_date, assigned_to)` + قفل advisory روزانه + تریگری که تغییر `due_date` را **کاملاً** رد می‌کند (وگرنه مسئول با `tasks_self_update` کار دیروز را به امروز می‌کشید). **قواعد در تریگر است نه در RPC**، چون PATCH مستقیم PostgREST هر قاعدهٔ فقط-در-RPC را دور می‌زند. **⛔ دو چیز که dry-run مجبور کرد درست شود:** (۱) تولید برای روز **آینده** وظایف زندهٔ **امروز** را منقضی می‌کرد (`expired: 7`) ⇒ رفع با `LEAST(v_date, tehran_today())`؛ (۲) ادعای «کسی نمی‌تواند وظیفهٔ دیگری را تیک بزند» **پاس کاذب** بود (بعد از گیت انقضا اجرا می‌شد، هیچ وظیفهٔ pending نمی‌ماند و با «وظیفه یافت نشد» سبز می‌شد). **⛔ نقصی که فقط تأیید «روی سیم» گرفت (مهاجرت ۲۷۸):** تیک دوبارهٔ وظیفهٔ انجام‌شده `HTTP 200` می‌داد و UI می‌گفت «ثبت شد» — چون نوشتن `'done'` روی `'done'` **گذار نیست** و شاخهٔ «وضعیت تغییر نکرده» زودتر از بررسی وضعیت پایانی اجرا می‌شد. امتیاز دوباره داده **نشد** (تریگر AFTER گارد داشت)، یعنی نقصِ صداقت بود نه داده. **گیت:** ۱۷ ادعای dry-run + **تست دو-نشستی واقعی قفل** (دو تراکنش هم‌زمان ⇒ دقیقاً یک وظیفه، دومی `locked: true`) + **۱۱ ادعای زنده روی PostgREST مستقر با JWT واقعی**: ساخت قالب توسط غیرادمین ۴۰۳، دیدن وظیفهٔ دیگری ۰ ردیف، ناشناس ۰ ردیف، جابه‌جایی `due_date` ۴۰۳ فارسی، تیک غریبه ۴۰۳، تیک مشروع ⇒ امتیاز ۰ ← ۲.۰۳۳ با `promotions_completed` contribution ۲ و حضور در لیدربرد. **زمان:** هیچ تصمیم تاریخی از ساعت سرور (UTC) گرفته نمی‌شود — همه از `public.tehran_today()`؛ کلاینت هم با `Intl` نه offset ثابت. **پاک‌سازی کامل:** ۲۰۸ رویداد امتیاز (دقیقاً عدد پیش از تست)، ۰ وظیفه، ۰ قالب. **حفرهٔ جانبی بسته شد:** `anon` روی `marketing_channels` همهٔ فعل‌های DML از جمله `TRUNCATE` را داشت (هم‌خانوادهٔ ۲۵۹). job از cron میزبان با endpoint توکن‌دار اجرا می‌شود (این دیتابیس `pg_cron` ندارد) — همان الگوی worker قیمت‌گذاری. backup گرفته و آزموده شد. typecheck ۷۰، build سبز، eslint تمیز، deploy با هر سه سیگنال. | `462232ab` · `f4fb94d6` |
| 2026-08-04 | Claude Code | **فاز ۹ مأموریت P1+D8 — بسته‌بندی اجباری تلویزیون، اجرا در عمق (نیاز ۲۲۳، مهاجرت ۲۷۶)**. **گام اول سند جواب مثبت داد:** شناسهٔ پایدار وجود دارد — `categories.slug='tv'` با constraint یکتا (۱۶ محصول)؛ کلید بر slug گذاشته شد نه نام فارسی و نه uuid خام، چون slug بین محیط‌ها قابل‌حمل است. **یافتهٔ سخت‌تر:** مدل «خدمات هر ردیف» (ممیزی B2.3) **اصلاً وجود نداشت** — `sales_quote_items` هیچ ستون خدمتی ندارد و هیچ جدول services/options/addons در اسکیما نیست. پس طبق هشدار صریح سند **حداقلِ همان مدل** ساخته شد نه مکانیزم یک‌بارمصرف: سه جدول کوچک که «تلویزیون باید بسته‌بندی شود» در آن‌ها فقط یک **ردیف** است، پس هفت نوع خدمت باقی‌ماندهٔ ممیزی و هر قاعدهٔ آیندهٔ دیگر (مثل «کولر گازی باید نصب شود») فقط INSERT‌اند و مهاجرت نمی‌خواهند. **پنج لایهٔ اجرا، همه در دیتابیس:** تریگر AFTER INSERT خدمت را می‌چسباند (چه RPC بنویسد چه فراخوان مستقیم API)، تریگر BEFORE DELETE/UPDATE حذف و تنزل و جابه‌جایی را رد می‌کند، `update_sales_quote_status` هنگام پذیرش اول دوباره اعمال و بعد راستی‌آزمایی می‌کند، و یک ردیف `tasks` برای انبار ساخته می‌شود. **⛔ دو چیزی که گیت مجبور کرد درست شود:** (۱) گارد حذف نباید `ON DELETE CASCADE` را ببندد — حذف کل **ردیف** تلویزیون مشروع است و فقط کندن خدمت از ردیفی که می‌ماند ممنوع است؛ تریگر این دو را با نبودِ ردیف والد تشخیص می‌دهد، وگرنه فروشنده هرگز نمی‌توانست تلویزیون را از پیش‌نویس بردارد. (۲) `tasks_assigned_queue_check` فقط `sales|shipping|store|accounting` را می‌پذیرد، پس صف انبار همان `store` موجود است — ساختن `warehouse` یعنی گشادکردن CHECK که کد دیگری به آن تکیه دارد. **HARD GATE با ۱۳ ادعا پاس** و علاوه بر آن **تأیید روی سیم پس از استقرار**: با JWT واقعی ادمین روی PostgREST زنده، `DELETE` و `PATCH {"is_mandatory":false}` هر دو **HTTP ۴۰۳** با پیام فارسی گرفتند و خواندن ناشناس **۴۰۱**؛ دادهٔ آزمایشی با ۰ باقی‌مانده پاک شد. **دو نکتهٔ صادقانه:** backfill صفر ردیف گرفت و این **درست است نه خرابی** (۵۳ ردیف موجود در ۴ دسته‌اند و هیچ‌کدام تلویزیون نیست)، و ردیف متن‌آزاد خدمت اجباری نمی‌گیرد چون محصول و در نتیجه دسته ندارد — خاصیت طراحی دسته‌محور است نه فراموشی. UI الزام را به‌صورت **جملهٔ قفل‌شده** نشان می‌دهد نه چیپ قابل‌حذف (کنترلی که حذف‌شدنی به نظر برسد ولی backend ردش کند، کاربر را به بی‌اعتمادی به فرم عادت می‌دهد)، در هر سه جا: فرم جدید پیش از ذخیره، صفحهٔ جزئیات، و **PDF چاپی** چون انبار از کاغذ کار می‌کند. backup گرفته و سلامتش آزموده شد. typecheck ۷۰، build سبز، eslint تمیز، deploy با هر سه سیگنال تأیید شد. | `98401a24` |
| 2026-08-04 | Claude Code | **فاز ۸ مأموریت P1+D8 — PWA (تصمیم D8-7): manifest، آیکون، service worker با استراتژی به‌روزرسانی امن، و سخت‌سازی دوربین**. هیچ مهاجرتی نداشت. **اول مانع سند بررسی شد و ادعای ممیزی رد شد:** `cache-buster.ts` «هر service worker را در هر بارگذاری unregister نمی‌کند» — `clearAllCaches()` فقط دو فراخوان‌کننده دارد، و مهم‌تر اینکه `BUILD_TAG` یک **ثابت دستی** در `build-info.ts` است (`2026-06-08T…`) که هیچ `define`ای جایگزینش نمی‌کند، پس از ۸ خرداد تاکنون در هر استقرار `prev === BUILD_TAG` بوده و **آن شاخه اصلاً اجرا نشده**. semantics آن عمداً عوض نشد (زنده‌کردنش یعنی پاک‌شدن کش همهٔ کاربران در هر استقرار — تغییر رفتاری خارج از دامنه) و **به‌عنوان تصمیم باز به مالک گزارش می‌شود**؛ به‌جایش `VITE_BUILD_ID` جدا ساخته شد و حلقهٔ unregister فقط `/sw.js` خودمان را رد می‌کند و worker بیگانه را مثل قبل حذف می‌کند. **تضمین ساختاری worker:** فقط `/assets/` (content-hash)، `/fonts/` و `/icons/` کش می‌شوند و برای هر چیز دیگر اصلاً `respondWith` صدا زده نمی‌شود ⇒ ناوبری و API دست‌نخورده ⇒ **نمی‌تواند build کهنه سرو کند**، که همان رگرسیونی است که سند هشدار داده بود. **⛔ تلهٔ طراحی که وسط کار پیدا شد:** طرح بدیهی «`registration.update()` استقرار جدید را بفهمد» **بی‌صدا شکست می‌خورد** — `update()` همان script URL را دوباره می‌گیرد و بایت‌ها را مقایسه می‌کند، ولی `sw.js` بین دو build عوض نمی‌شود (فقط `?v=`)، پس تبی که `/sw.js?v=A` ثبت کرده **هرگز** استقرار جدید را نمی‌فهمد. رفع: تشخیص از `GET /api/version` هر ۱۵ دقیقه + هنگام برگشت به تب، که **سود دومش از خودِ PWA مهم‌تر است — روی http امروز هم کار می‌کند** (استقرار LAN که اصلاً service worker ندارد). بارگذاری مجدد همیشه با کلیک کاربر است، نه خودکار. **آیکون:** هیچ لوگویی در ریپو نبود، پس `scripts/generate-pwa-icons.mjs` نشان هندسی از روی رنگ خودِ اپ (`#007d7e`) می‌سازد؛ عمداً متن رندر نمی‌شود چون نیازمند نصب سراسری Vazirmatn برای librsvg است. مالک با گذاشتن `source-logo.png` جایگزین می‌کند. **دوربین:** فشرده‌سازی و جهت EXIF در **یک** helper داخل `CameraCaptureButton` (پس هر چهار فرم بدون تغییر جداگانه بهره می‌برند)، و نوار پیشرفت + تلاش مجدد با XHR چون **supabase-js با `fetch` آپلود می‌کند و `fetch` رویداد پیشرفت آپلود ندارد**؛ ۴xx عمداً retry نمی‌شود. **⛔ نقص build که پیدا شد و عمداً رفع نشد:** `npm run build` روی ویندوز یک HTML SSR می‌سازد که به دو chunk ورودی اشاره می‌کند که vite هرگز نمی‌نویسد ⇒ کلاینت boot نمی‌شود؛ با revert کامل `vite.config.ts` عیناً تکرار شد پس ساختهٔ این مأموریت نیست و build داخل Docker سالم است — **ولی یعنی اجرای محلی اپ روی ویندوز ممکن نیست**. تست: **۷ سبز / ۳ skip / ۰ قرمز** روی build مستقر؛ سه skip رفتار زمان‌اجرای worker است که **ادعای pass نمی‌شود** چون flag مرورگر برای secure جلوه‌دادن مبدأ در Chromium همراه Playwright اعمال نشد. typecheck ۷۰، build سبز، eslint فایل‌های لمس‌شده تمیز. deploy با هر سه سیگنال تأیید شد. | `6308ddc5` · `65acdc20` |
| 2026-08-04 | Claude Code | **فاز ۷ مأموریت P1+D8 — انتخاب انبار در سطح ردیف (تصمیم D8-8، مهاجرت ۲۷۴ + ۲۷۵)**. **مهم‌ترین یافتهٔ پیش از تغییر:** گاردِ «بیش از موجودی نفروش» از قبل در `apply_stock_movement` هست و پیامش **هم کالا و هم انبار را نام می‌برد** — پس ادعای سوم HARD GATE با **گسترش همان مسیر** برآورده شد نه با ساختن گارد دوم کنارش (همان «سامانهٔ موازی» که قاعدهٔ ۱۴ منع می‌کند). پنج مصرف‌کنندهٔ انبارِ سند فهرست و گزارش شدند؛ سه‌تا بازنویسی شد، دوتا دست‌نخورده. **قاعدهٔ حل:** `COALESCE(انبار ردیف، انبار سند، پیش‌فرض)` در **یک** تابع `effective_line_warehouse` — و چون همهٔ ردیف‌های موجود به یک انبار سند حل می‌شوند، گروه‌بندی بر (کالا، انبار) از نظر ریاضی عیناً همان گروه‌بندی قبلی بر کالا است، یعنی پیش‌فاکتور تک‌انباره اصلاً نمی‌فهمد چیزی عوض شده. **backfill:** از ۵۰ پیش‌فاکتور فقط ۱۱ و از ۲۰۴ خرید فقط ۵ اصلاً انبار داشتند، پس بیشتر ردیف‌ها به‌درستی NULL ماندند؛ سنجهٔ سند «بدون انبار **جایی که سند انبار داشته**» است نه «بدون NULL» — ۱۲ ردیف پیش‌فاکتور + ۵ ردیف خرید پر شد، **۰ یتیم**. HARD GATE با ۵ ادعا پاس: (۱) پیش‌فاکتور تک‌انباره old-vs-new **در یک تراکنش** بایت‌به‌بایت یکسان (۱ حرکت، `-10`)؛ (۲) دو ردیف در دو انبار ⇒ **۲ حرکت مجزا** `-5` و `-4`؛ (۳) انبار کم ⇒ **رد** با «موجودی کافی نیست: «یخچال…» در انبار **«انبارتست2»** فقط ۲ عدد موجود دارد و درخواست ۹ عدد است»؛ (۴) کسر جزئی صفر؛ (۵) backfill بدون یتیم. **۲۷۵ چرا لازم بود:** بدون آن ستون فقط از API مستقیم پر می‌شد و **فرم** نمی‌توانست بفرستد — همان «ساخته شد ولی وصل نشد». `create_sales_quote_with_items` ۳۲۸ خط با ۹ خط فارسی است، پس طبق قاعدهٔ ۴ از حافظه بازنویسی نشد: تعریف زنده گرفته و **داخل کانتینر** با `sed` وصله خورد (هرگز از لولهٔ PowerShell — همان چیزی که ۲۰۲۶-۰۷-۱۱ فارسی ۴۴ تابع را نابود کرد)، اسکریپت اطمینان داد هر anchor **دقیقاً یک بار** بخورد وگرنه abort کند، و فقط **دو خط** فرق دارد؛ پس از اعمال هر ۹ خط فارسی سالم و بدون BOM. گیت ۲۷۵: امضا بدون تغییر، انبار ردیف از مسیر واقعی RPC ذخیره شد، و سازگاری رو به عقب (ردیف بدون کلید ⇒ NULL). UI: ستون «انبار» در جدول ردیف‌ها با پیش‌فرض «انبار سند» (پس حالت تک‌انباره هیچ تعامل اضافه نمی‌خواهد)، پیام کمبود **نام انبار** را می‌گوید، و جدول «تفکیک موجودی به‌ازای انبار» اضافه شد. **فرم خرید عمداً سطح‌ردیف نگرفت** چون `create_purchase` تک‌کالایی است. ستون‌های سطح سند نگه داشته شدند (دو-ستونی گذار)؛ down عمداً ستون‌ها را drop نمی‌کند چون انتخاب‌های کاربر بازسازی‌شدنی نیستند. backup گرفته و آزموده شد. typecheck ۷۰، build سبز، eslint ۵ فایل تمیز. | `c2dc86e5` |
| 2026-08-04 | Claude Code | **فاز ۶ مأموریت P1+D8 — امتیاز دستی با مدت انتخابیِ مدیر و پیش‌نمایش (تصمیم D8-5، مهاجرت ۲۷۳)**. این فاز قبلاً **متوقف شده بود** چون پایه‌اش شکسته بود؛ مالک تصمیم گرفت منظور D8-5 **گیمیفیکیشن کارمند** است، پس اول پایه وصل شد. **یافتهٔ مرکزی:** فرم «ثبت امتیاز دستی» ردیف می‌نوشت، پیام موفقیت می‌داد، و `calculate_employee_score` **هرگز `manual_adjustment` را نمی‌خواند** (تنها `event_type` خوانده‌شده `promotion_completed` بود با **۰ ردیف**) ⇒ هیچ عددی تکان نمی‌خورد. دقیقاً همان الگوی «قابلیتی که ساخته شد ولی وصل نشد». **مدت اثر** به‌ازای هر ثبت (۱ تا ۶۰ ماه) در `payload` ذخیره شد نه ستون تازه، چون `payload` از قبل جای دادهٔ مخصوص هر `event_type` است. **شکل زوال: کاهش خطی و تنها شکل** — با `effect_months=1` رفتار دقیقاً همان حالت قبلی می‌شود، و همین دلیل انتخاب ۱ برای رکوردهای قدیمی است. **چرا پیش‌نمایش نمی‌تواند دروغ بگوید:** محاسبه به `compute_employee_score` (خالص، بدون نوشتن) منتقل شد و `calculate_employee_score` (ذخیره) و `preview_manual_score_adjustment` (با ثبتِ فرضی) **هر دو همان را صدا می‌زنند** — برابری ساختاری است نه تصادفی؛ امضای RPC دست‌نخورده ماند پس هر ۸ فراخوان داخلی و wrapper فرانت‌اند سالم‌اند. **هیچ امتیاز زنده‌ای تکان نخورد:** تنها ردیف موجود (+۱۲۳۵ برای کارمند «1»، دادهٔ آزمایشی) در تیر ثبت شده و با مدت ۱ ماه اثرش **صفر** است؛ عدد دقیق پیش از اعمال اندازه‌گیری و گزارش شد. **⚠️ نقص XP که این فاز قابل‌دسترس می‌کرد:** جریمهٔ دستی `total_score` را پایین می‌برد و با پایان اثر بالا می‌آورد، و کد قدیمی بابت همان امتیاز **بار دوم XP** می‌داد (مزرعهٔ XP) — با یک `GREATEST` روی `last_score_converted` بسته شد. **⛔ دو تله که گیت گرفت:** (۱) نسخهٔ اول CHECK یک payload بدون `effect_months` را **قبول می‌کرد**، چون کلید غایب ⇒ NULL و **PostgreSQL فقط FALSE را رد می‌کند نه NULL**؛ بدتر اینکه ۰ و ۶۱ درست رد می‌شدند و حفره را بسته جلوه می‌داد. (۲) `273-down.sql` اولش `BEGIN/COMMIT` داشت و وقتی از داخل هارنس dry-run با `\i` صدا زده شد، **تراکنش هارنس را commit کرد**؛ آسیبی نرسید (۰ ردیف آزمایشی، ۲۰۸ رویداد بدون تغییر، همهٔ XPها دست‌نخورده) ولی ریشه رفع شد و down دوباره درست آزموده شد. HARD GATE با **۱۲ ادعا** پاس روی توابع زنده: برابری عددی old-vs-new **داخل یک تراکنش** (تا `now()` ثابت بماند)، ۶ payload بدشکل رد، و **پیش‌نمایش == امتیاز پس از submit، بایت‌به‌بایت** (`-49.966666666666666666660` هر دو) به‌علاوهٔ برابری سطح و ردیف ذخیره‌شده. UI: فیلد مدت، پیش‌نمایش با جدول ماه‌به‌ماه و دکمهٔ ثبتِ غیرفعال تا دیده‌شدن پیش‌نمایش، و «تعدیل دستی» با ماه‌های باقی‌مانده در تفکیک امتیاز. backup گرفته و سلامتش آزموده شد. typecheck ۷۰، build سبز، eslint ۴ فایل لمس‌شده تمیز. | `9285728e` |
| 2026-08-04 | Claude Code | **فاز ۵ مأموریت P1+D8 — آستانه‌های نسخه‌دار سطح امتیاز (تصمیم D8-4، مهاجرت ۲۷۲)**. چهار باند مساوی مالک: ۸۰–۱۰۰ عالی، ۶۰–۷۹ قابل اعتماد، ۴۰–۵۹ متوسط، ۰–۳۹ پرریسک. **یافتهٔ حیاتی پیش از طراحی:** `weighted_score` مقیاس **۰ تا ۱** دارد نه ۰ تا ۱۰۰ (`raw_score` بین ۰.۰۰۰ و ۱.۰۰۰ است؛ مقادیر واقعی مثل ۰.۲۳۴۰۵۰). اگر باندها بدون این بررسی روی همان عدد اعمال می‌شدند، **همهٔ مشتری‌ها همیشه «پرریسک»** می‌شدند. رابط از قبل `weighted * 100` را نشان می‌دهد، پس آستانه‌ها در دامنهٔ ۰–۱۰۰ ذخیره شدند و تابع جست‌وجو تبدیل را انجام می‌دهد. (ضمناً معلوم شد `dynamic_entity_scores.entity_id` برابر `customers.id` است نه `person_id`.) **الگو عیناً از `dynamic_parameter_weights`:** همان `valid_from`/`valid_to`، همان ستون‌های حسابرسی، همان سیاست‌ها، و مهم‌تر از همه **EXCLUDE با gist** که هم‌پوشانی را غیرممکن می‌کند. باندها با `numrange` نیمه‌باز `[)` ذخیره شدند نه دو ستون min/max — با دو ستون، عدد **۳۹.۵** بین «۰–۳۹» و «۴۰–۵۹» بی‌خانمان می‌ماند. ستون `entity_type` عمداً اضافه نشد (مالک یک مجموعه باند خواسته، نه دو تا). HARD GATE با ۵ ادعا پاس: **(۱) برابری عددی — checksum امتیاز ۱۴ مشتری × ۲ دوره بایت‌به‌بایت یکسان** (این فاز فقط برچسب اضافه می‌کند، به ریاضیات دست نمی‌زند)؛ (۲) باند کنار عدد؛ (۳) مرزها دقیقاً سر جای خودشان و بدون شکاف؛ (۴) باند هم‌پوشان با `23P01` رد می‌شود؛ **(۵) پایداری تاریخی: امتیاز ۸۵ در این دوره «عالی» و در دورهٔ بعد با نسخهٔ تازه «پرریسک» — یعنی عوض‌کردن باندها برچسب دورهٔ قبل را دست نمی‌زند**، که خواستهٔ اصلی D8-4 است. **نکتهٔ صادقانه:** چون seed طبق دستور سند از دورهٔ جاری شروع می‌شود، امتیازهای تیرماه هیچ نسخهٔ باندی ندارند و بی‌برچسب می‌مانند (رابط عمداً چیزی از خودش نمی‌سازد)؛ اگر مالک برچسب تاریخی بخواهد، عقب‌بردن `valid_from` یک UPDATE ساده است. UI: برچسب فارسی رنگی کنار عدد، که مقدارش **از دیتابیس** می‌آید و در کلاینت بازمحاسبه نمی‌شود تا با جدول نسخه‌دار واگرا نشود. typecheck ۷۰، build سبز، eslint فایل‌های لمس‌شده تمیز. | `0864e684` |
| 2026-08-03 | Claude Code | **فاز ۴ مأموریت P1+D8 — کارمندان به مدل شخص وصل شدند، فقط افزودنی (تصمیم D8-3، مهاجرت ۲۷۰ + ۲۷۱)**. **اول شکل واقعی گزارش شد:** `profiles` ۴۱ ردیف (کلیدش `auth.users.id`؛ نام هر ۴۱ پر ولی **شماره فقط ۱۴**)، ۱۲ جدول کارمندی، و `persons` بدون هیچ ستونی به `auth.users`. **هیچ‌کدام از ۱۱ جدول `employee_*` لمس نشد.** زمینهٔ اتصال `staff_link` است که از قبل در `PERSON_CONTEXT_KINDS` و CHECK دیتابیس تعریف شده بود ولی **۰ ردیف** داشت — نوع تازه‌ای ساخته نشد (قاعدهٔ ۱۴). تطبیق هویت با **همان** `person_find_by_identifiers` که `person_import_batch` استفاده می‌کند، نه matcher دوم. **نتیجه: ۴۱ = ۲ لینک‌شده + ۳۹ ساخته‌شده** (اشخاص ۳۰ ⇒ ۶۹). **⚠️ دو موردِ لینک‌شده نیاز به تأیید مالک دارند:** شماره‌شان با شخص موجود می‌خورد ولی **نامشان نمی‌خورد** — «پورچیستا سعادت مبارکی» ⇒ شخص «12»، و «حانیه ماهرو» ⇒ «محمدزین الدین» (مشتری با ۳ میلیارد اعتبار). با این حال لینک شدند چون از ۲۴۱ به بعد موبایل **کلید هویت سراسری** سامانه است و کنار گذاشتنش به‌نفع مقایسهٔ نام یعنی همان matcher دومی که سند منع کرده؛ ضمناً premise مدل شخص یکپارچه این است که یک انسان هم‌زمان کارمند و مشتری باشد. خطرناک هم نیست چون فاز افزودنی و برگشت‌پذیر است و **گیت اثبات کرد هیچ عدد اعتباری تکان نخورد** (checksum کل `customer_credit_ledger` یکسان) — اعتبار روی `customers` کلید می‌خورد نه `profiles`. `NOT NULL` عمداً اعمال **نشد** و هیچ FK جابه‌جا نشد. **⛔ رگرسیونی که رگرسیون کامل گرفت:** اجرای کامل پس از ۲۷۰ دو قرمز تازه داد (هر دو spec ادغام). علت: `profiles.person_id` یک **کلید خارجی تازه به `persons`** است و `person_merge` عمداً فهرست کارش را از `pg_constraint` می‌خواند و هر کلید ثبت‌نشده در رجیستری سیاستش را **متوقف** می‌کند — یعنی محافظِ فاز ۸.۱ درست کار کرد، نه اینکه چیزی خراب شود. با **۲۷۱** (اصلاح رو‌به‌جلو، ۲۷۰ ویرایش نشد) ستون به‌عنوان `generic` ثبت شد — نه `identity_root`، چون `profiles.person_id` یکتا نیست و profile هیچ حالت مالی حمل نمی‌کند. هر دو spec سبز شدند. HARD GATE با **۷ ادعا** پاس: ۴۱/۴۱ لینک، تفکیک ۲+۳۹، ۴۱ ردیف `staff_link`، هیچ شخصی بین دو profile مشترک نیست، ستون nullable ماند، اعتبار یکسان، و `person_fk_drift_report()` خالی. typecheck ۷۰، هیچ فایل TS تغییر نکرد. | `a19e2627` |
| 2026-08-03 | Claude Code | **فاز ۳ مأموریت P1+D8 — یک شخص = یک طرف حساب خارجیِ فعال (تصمیم D8-2، مهاجرت ۲۶۹)**. **تکراری‌ها: صفر** — کل جدول ۱ ردیف دارد و `person_id` از ۲۴۲ به بعد NOT NULL است، پس هیچ ادغام یا اصلاح لینکی لازم نبود. ⚠️ این شمارش دو بار گرفته شد: بار اول **وسط اجرای e2e** بود و عدد فرق داشت، چون spec طرف حساب موقت «E2E_AUDIT_…» می‌سازد و در afterAll پاک می‌کند؛ نتیجه‌گیری از دادهٔ در حال تغییر غلط است. **اندیس جزئی، نه constraint ساده:** فاز ۸.۳ برای مشتری/تأمین‌کننده constraint ساده گذاشته بود چون «هیچ مسیری `is_active=false` نمی‌کرد»، ولی اینجا آن استدلال برقرار **نیست** — صفحهٔ طرف حساب‌ها کلید فعال/غیرفعال دارد. با constraint ساده، یک طرف حسابِ غیرفعالِ قدیمی تا ابد جلوی ثبت طرف حساب تازه برای همان شخص را می‌گرفت. **مسیر ساخت:** `person_create_inline` برای supplier و customer از قبل الگوی «اگر هست همان را استفاده کن» داشت؛ شاخهٔ `accounting_party` تنها شاخه‌ای بود که بدون بررسی INSERT می‌کرد — همان الگو آینه شد. **گاردی که سند نخواسته بود ولی لازم بود:** `person_merge` برای «هر دو شخص پروندهٔ مشتری/تأمین‌کننده دارند» گارد فارسی داشت ولی برای طرف حساب خارجی نداشت؛ با وجود اندیس، ادغام وسط «گام A» با `unique_violation` خام می‌شکست به‌جای پیام قابل‌فهم. **⚠️ خطایی که نزدیک بود اعمال شود:** نسخهٔ اولِ `person_create_inline` از روی حافظه بازنویسی شد و غلط بود — امضای واقعی `p_visibility_scope` دارد نه `p_phone`، تابع `SECURITY DEFINER` نیست، و ساخت شخص را به `person_create_full` می‌سپارد؛ اعمالش **امضای RPC را می‌شکست**. با diff مقابل snapshot زنده گرفته شد و نسخهٔ نهایی از خودِ متن زنده ساخته شد. HARD GATE با **۸ ادعا** پاس: طرف حساب فعال دوم رد (`23505`)، غیرفعال دوم و سوم مجاز، **فعال‌کردن دوبارهٔ دومی هم رد** (یعنی کلید UI هم نمی‌تواند فرار کند)، گارد فارسی ادغام موجود، و **امضای RPC بدون تغییر**. `e2e/persons` ۳۶ سبز / ۱ قرمز (همان Baseline)، typecheck ۷۰، هیچ فایل TS تغییر نکرد. | `7e0c9740` |
| 2026-08-03 | Claude Code | **فاز ۲ مأموریت P1+D8 — سقف سرمایه دیگر قابل override نیست (تصمیم D8-1، مهاجرت ۲۶۸)**. مالک گفته بود «هر دو سطح قفل». **تفکیک ورودی/خروجی:** `daily_capital_inputs` (موجودی بانک و نقد، چک‌ها، مطالبات بیرونی، ذخیرهٔ ریسک، و **`manual_adjustment`**) **قابل ویرایش می‌ماند** — اینها واقعیتی است که حسابدار اعلام می‌کند؛ ولی `system_suggested_capital`، `final_capital` و `final_amount` هر کارشناس که **خروجی فرمول‌اند** فقط‌خواندنی شدند. نکتهٔ مهم: راه درستِ «عدد دیگری لازم دارم» از قبل وجود دارد و `manual_adjustment` نام دارد — یک ورودی ثبت‌شده و قابل حسابرسی، نه بازنویسی خروجی. **چهار یافته پیش از تغییر:** (۱) دو سامانهٔ موازی سرمایه وجود دارد و سامانهٔ **زنده** (`..._dynamic`، ۱۸۲ ردیف) اصلاً ستون override ندارد؛ مکانیزمی که D8-1 نام می‌برد در جدول قدیمی با **۰ ردیف** است. (۲) `UPDATE` مستقیم روی `final_capital` تا امروز **خطا نمی‌داد** — چون سیاست UPDATE وجود ندارد، RLS صفر ردیف می‌داد و نتیجه `UPDATE 0` بود؛ «بی‌صدا هیچ‌کاری نکردن» با «رد شدن» یکی نیست. (۳) **`anon` روی هر ۷ جدول سرمایه همهٔ فعل‌های DML را داشت، از جمله `TRUNCATE` که اصلاً تابع RLS نیست** — همان الگویی که مورد ۲۵۹ برای `purchases` بست. (۴) **گیت تست کشف کرد که جدول قدیمی کدِ مرده است، نه کم‌استفاده:** هر دو تابعش در هر فراخوان با `type "public.text[]" does not exist` و `operator does not exist: text = app_role` می‌افتادند — دو cast جامانده از تبدیل `user_roles.role` از `app_role` به `text`. چون این توابع به‌هرحال بازنویسی می‌شدند و بدون اصلاح، قفلِ همین فاز روی آن مسیر **قابل اثبات نبود**، هر دو اصلاح شدند. **رفع:** دو تریگر نگهبان در سطح دیتابیس (نه UI) با پیام فارسی، حذف پارامترهای override از `save_daily_capital_snapshot`، رد صریح `final_amount` دستی و `override_reason` در `save_salesperson_capital_allocations`، و پس‌گرفتن DML نقش `anon`. ستون‌های `override_reason` با کامنتِ تاریخ deprecated شدند و **۳ ردیف تاریخی override دست‌نخورده ماندند** (تریگر فقط BEFORE است). HARD GATE با **۱۳ ادعا** پاس شد، از جمله: UPDATE مستقیم رد، INSERT نامتوازن رد، ولی `UPDATE` روی `is_active` همچنان کار می‌کند (چون `trg_archive_prior_allocations` به آن وابسته است و شکستنش رگرسیون خاموش می‌شد)، سمت ورودی همچنان نوشتنی، و **۱۸۲ ردیف تخصیص با checksum یکسان**. backup پیش از فاز گرفته و سلامتش واقعاً آزموده شد (`gzip -t` + نشانهٔ پایانی dump). typecheck **۷۰** — هیچ فایل TS تغییر نکرد چون این توابع هیچ فراخوانی در فرانت‌اند ندارند. | `30e309a5` |
| 2026-08-03 | Claude Code | **فاز ۱ مأموریت P1+D8 — چهار حفرهٔ امنیتی P1** (جزئیات کامل در `docs/execution/p1-d8-progress.md`). **۱.۱ (۲۶۴):** تنها ریسکِ «بحرانی و فعالِ» ممیزی. سیاست `customers` مالکیت‌محور بود ولی `persons` و فرزندانش فقط نقش‌محور ⇒ **هر فروشنده‌ای موبایل/ایمیل/کد ملی/شبای همهٔ مشتریان را می‌خواند**، حتی مشتریانی که خودِ جدول `customers` از او دریغ می‌کرد. با فراخوان مستقیم PostgREST و JWT واقعی فروشنده اثبات و بسته شد (شواهد قبل/بعد در `docs/verification/`). قاعده در **یک** تابع `SECURITY DEFINER` متمرکز شد، چون تکرار inline در سه سیاست همان چیزی بود که اجازهٔ واگرایی داده بود. **۲۶۵ — رگرسیونی که تست‌ها گرفتند:** پس از ۲۶۴ اجرای `e2e/persons` شد ۹ قرمز (Baseline: ۱). هر مسیر ساخت شخص با «new row violates row-level security policy» رد می‌شد. علت: `RETURNING` سیاست SELECT را روی ردیف تازه اعمال می‌کند و `can_read_person` که `STABLE` است و خودش از `persons` می‌خواند، ردیفِ در حال درج را در snapshot خود نمی‌بیند. در psql ایزوله شد (INSERT ساده موفق، `INSERT ... RETURNING` رد). قاعده به `can_read_person_scoped(id, scope)` تقسیم شد که scope را ورودی می‌گیرد و هرگز از جدول نمی‌خواند؛ منطق دسترسی بایت‌به‌بایت همان ۲۶۴ ماند. **۱.۲ (۲۶۶):** سند یک باگ نام برده بود، خواندن تعریف زنده **سه** تا نشان داد — و آنکه نام برده شده بود اهمیت کمتری داشت: شاخهٔ «همان ماه» نسخه را درجا بازنویسی می‌کرد، ولی شاخهٔ `ELSE` نسخهٔ تازه را با `valid_from = اول ماه جاری` **عطف به ماسبق** درج می‌کرد و **هر ۱۶ ردیف وزنِ موجود از همین مسیر رد می‌شدند**، نه از شاخهٔ نام‌برده. به‌علاوه پس از هر تغییر، تخصیص سرمایه بی‌صدا دوباره اجرا می‌شد. حالا نسخهٔ در حال اجرا بسته می‌شود و مقدار تازه از **اول ماه بعد** اعمال می‌گردد؛ نسخهٔ هنوز اجرانشده قابل اصلاح است (همان «رویداد اصلاح» سند، با ردیف audit جدا). گیت: ۲ نسخه، `dynamic_entity_scores` بایت‌به‌بایت یکسان، هر چهار جدول سرمایه بدون تغییر — و برای معنادار بودنِ این ادعا اول یک ردیف `daily_capital_settings` امروز درج شد تا مسیر اجرای خودکار واقعاً فعال باشد. صفحهٔ `credit-rules` هم اصلاح شد چون با `valid_to is null` فقط نسخهٔ **آینده** را می‌خواند و آن را «فعلی» نشان می‌داد. **۱.۳ (۲۶۷):** `payment-receipt-documents` تنها bucket با هر دو مقدار NULL بود؛ سقف ۲۰MB و فهرست نوع دقیقاً از وعدهٔ خودِ UI گرفته شد. `application/octet-stream` عمداً مجاز **نشد** (کل فهرست را بی‌اثر می‌کرد) و در عوض کلاینت اصلاح شد تا نوع را از پسوند بسازد. با ۶ آپلود واقعی روی Storage API تأیید شد: `image/*` و `text/*` و PDF پذیرفته، exe و octet-stream و ۲۱MB رد (HTTP ۴۰۰) — که هم کار کردن wildcard در `storage-api v1.11.13` را ثابت می‌کند و هم لازم بودن اصلاح کلاینت. **۱.۴:** healthz ثابتِ `{ok:true}` جای خود را به probe واقعی داد: دیتابیس وابستگی سخت (۵۰۳)، پل واتساپ/افراپیام فقط `degraded` تا قطعی‌اش کانتینر را restart نکند؛ موازی، ≈۲ ثانیه بدترین حالت. با stop واقعی `afrakala-lan-rest` تأیید شد: ۲۰۰ ← **۵۰۳** ← ۲۰۰. typecheck **۷۰** (بدون تغییر)، build سبز، eslint فایل‌های لمس‌شده ۰ خطا، هر چهار مهاجرت اسکریپت down دارند. deploy روی LAN با هر سه سیگنال تأییدشده (`APP_GIT_SHA=cb9eb2b1` برابر HEAD). | `e6469750` · `cb9eb2b1` |
| 2026-08-03 | Claude Code | **P0 ممیزی ۲۲۰–۲۲۶** (پنج رفع فوری، هیچ‌کدام وابسته به تصمیم‌های D8). **۱:** سقف ۱۵۰ واتساپ در **هر سه لایه** برداشته شد — پیش‌فرض ۱۵۰، نفرستادن `limit` از کارت، و سقف zod روی ۳۰۰. برداشتن فقط یکی، نتیجه را روی ۳۰۰ می‌بست و شبیه «درست شد» به نظر می‌رسید. پیش‌فرض حالا `WHATSAPP_TOP_PRODUCTS_LIMIT` است (runtime، بدون rebuild) با fallback ۱۰۰۰ = سقف اندازه‌گیری‌شدهٔ مبدأ. `purchase-advisor` **عمداً** روی ۱۵۰ ماند چون خروجی‌اش وارد prompt مدل زبانی می‌شود. **۲:** `search` از proxy عبور داده شد — تابع از قبل پشتیبانی می‌کرد و فقط zod بلوکش کرده بود؛ جست‌وجو در مبدأ روی کل مجموعه و **قبل از** limit اجرا می‌شود. شکاف شناخته‌شده: مبدأ فقط نام محصول را می‌گردد، نه متن پیام/فرستنده/گروه. **۳ (مهاجرت ۲۶۳):** bucket `delivery-receipts` با وعدهٔ همیشگی فرم هم‌راستا شد (۲۵MB→۱۰۰MB + mp4/quicktime/webm)؛ تا امروز هر آپلود ویدیو در Storage رد می‌شد و `invoices.product_video_required` بی‌اثر بود. mkv از UI حذف شد نه اینکه به bucket اضافه شود. **۴:** `CameraCaptureButton` **کنار** input موجود، نه رویش — `capture` روی خود input در iOS Safari و Android Chrome گالری را حذف می‌کند و یک قابلیت را با دیگری عوض می‌کرد. **۵:** `/purchase` **در هیچ منویی نبود** (تنها ورودی‌اش یک کارت در `/collaboration`)، پس اول seed رجیستری اضافه شد و بعد میان‌بر موبایل — `getNavigationEntryByRoute` میان‌بر حل‌نشده را بی‌صدا می‌اندازد. `/purchases/create` عمداً میان‌بر نشد چون `purchases.create` فقط admin/manager است. typecheck **۷۰** (بدون تغییر)، build سبز، lint فایل‌های لمس‌شده تمیز، e2e ۲۱۴: ۴ سبز / ۱ قرمز که **از قبل** قرمز بود. **دو فایل عمداً commit نشدند** (`roles.ts` و `WhatsappTopProductsCard.tsx`) چون کار commit‌نشدهٔ ۲۱۹/C5 را هم حمل می‌کنند؛ روی دیسک و روی کانتینر مستقر هستند. | `6ab05d6e` |
| 2026-08-02 | Claude Code | مورد ۲۱۹ فاز **C5 — یکپارچه‌سازی مجوزها و بستن مسیرهای دورزدن** (آخرین فاز توسعه). **۲۵۷:** `role_permissions` می‌گفت `sales` و `purchase_specialist` می‌توانند خرید ثبت کنند، ولی RLS و `create_purchase` هرگز اجازه نمی‌دادند؛ چون `purchases:create` فقط گارد مسیر `/purchases/create` را کنترل می‌کند، فروشنده می‌توانست کل فرم را پر کند و تازه در انتها رد شود. حالا فقط admin/manager. **۲۵۸:** `purchased` و `partially_purchased` مشتق شدند — هم RPC ردشان می‌کند و هم یک **تریگر داده‌محور** (نه بر پایهٔ فلگ session که کلاینت بتواند جعلش کند): وضعیت فقط وقتی می‌تواند مشتق شود که ردیف‌های Fulfillment پشتیبانش باشند؛ `create_purchase` اول Fulfillment را می‌نویسد و بعد وضعیت را، پس مسیر درست پاس می‌شود و مسیر دستی نه. ماتریس Transition هم اضافه شد (تا الان اصلاً وجود نداشت). قیمت نهایی دستی حذف شد. **۲۵۹:** `anon` روی `purchases`/`purchase_items`/`purchase_requests` همهٔ فعل‌های DML را داشت (Default Privileges سوپابیس)؛ حذف شد، و از `authenticated` فقط SELECT ماند به‌علاوهٔ دو UPDATE واقعیِ اپ (ثبت پرداخت حسابدار، ویرایش درخواست خودِ کاربر) — با Evidence از تک‌تک call siteها. **۲۶۰ (یافتهٔ مهم تست):** `v_assignee = v_caller` وقتی درخواست بدون مسئول است NULL می‌شود، `not NULL` هم NULL، و گارد اصلاً اجرا نمی‌شد — یعنی **هر کاربر واردشده‌ای می‌توانست وضعیت درخواست بدون مسئول را عوض کند**. باگ از قبل بود ولی C4 با عادی‌کردن حالت «بدون مسئول» قابل‌بهره‌برداری‌اش کرد. با `IS NOT DISTINCT FROM` رفع شد. **۲۶۱/۲۶۲:** گارد «کاربر غیرفعال» ساخته شد ولی **پس گرفته شد** — حساب `test.admin` که کل تست‌ها با آن اجرا می‌شود `is_active=false, status='rejected'` است و روزانه کار می‌کند؛ یعنی این فلگ‌ها سیگنال معتبر مجوز نیستند و اعمالشان فقط کاربر شاغل را قفل می‌کند. پاک‌سازی `profiles` فاز مستقل است. تست: **۲۶۴/۲۶۴ پایگاه‌داده** (۸ Suite)، **۱۴/۱۴ e2e فاز C5**، Regression ۱۲۱ سبز / ۳ قرمز (همان Baseline)، typecheck ۷۰. **بدون commit به‌خواست مالک.** | — |
| 2026-08-02 | Claude Code | مورد ۲۱۹ فاز **C4 — «اولین مدیر فعال» حذف شد**. `create_purchase_request` هر درخواست را به قدیمی‌ترین مدیر فعال می‌داد؛ نتیجه‌اش این بود که ۴۲ از ۴۴ درخواست به یک نفر تخصیص یافته بود، نه چون کسی او را انتخاب کرده بود بلکه به‌خاطر یک `ORDER BY`. حالا زنجیرهٔ صریح: انتخاب مدیر ← مسئول پیش‌فرض ← کارشناس خرید فعال (با ترتیب قطعی `created_at, id`) ← **بدون مسئول** (حالت واقعی، با اعلان به همهٔ مدیران، دقیقاً یک‌بار برای هرکس). **۲۵۴:** کلید `default_purchase_assignee_id` در همان `shop_settings` کلید/مقدار موجود (نه ستون موازی)، `is_valid_purchase_assignee` (فعال + `status='active'` + یکی از purchase_specialist/manager/admin — سخت‌گیرانه‌تر از کد قبلی که فقط `is_active` را می‌دید، چون ۴ حساب فعالِ در انتظار تأیید وجود دارد)، و بازنویسی `create_purchase_request` با خروجی jsonb. **۲۵۵:** `assign_purchase_request` (تخصیص/تغییر/برداشتن، فقط admin/manager، قفل ردیف + کنترل هم‌زمانی خوش‌بینانه)؛ تاریخچه در `audit_logs` طبق الگوی موجود `product_owner_assigned` — **جدول History جدید ساخته نشد** چون `purchase_request_status_history` فقط شکل وضعیت دارد. **۲۵۶ (اصلاح رو‌به‌جلو):** Supabase به‌صورت پیش‌فرض EXECUTE را به `anon` می‌دهد و ۲۵۴/۲۵۵ فقط از PUBLIC پس گرفته بودند؛ بدتر اینکه بازتعریف `get_purchase_requests` مجوز anon را که ۲۵۳ بسته بود دوباره باز کرد. ۲۵۴/۲۵۵ دست‌نخورده ماندند. دو نقص دیگر که تست‌ها پیدا کردند: مقدار `expected_current_assignee_id` هنگام Submit از prop خوانده می‌شد و Refetch پس‌زمینه می‌توانست کل مکانیزم را خنثی کند (حالا هنگام باز شدن Dialog فریز می‌شود)، و کارت در ۷۶۸px سرریز می‌کرد. تست: **۲۰۱/۲۰۱ پایگاه‌داده** (۷ Suite)، **۱۹/۱۹ e2e فاز C4**، Regression ۱۰۷ سبز / ۳ قرمز (چهار مورد خارج از Scope به سه کاهش یافت؛ ۲۱۵ حالا سبز است)، typecheck ۷۰. **بدون commit به‌خواست مالک.** | — |
| 2026-08-02 | Claude Code | مورد ۲۱۹ فاز **C3 — دکمهٔ «خرید انجام شد» به سند خرید واقعی وصل شد**. پیش از این، دکمه فقط یک قیمت نهایی می‌گرفت و وضعیت را دستی عوض می‌کرد؛ یعنی درخواست می‌توانست «خرید انجام شد» باشد بدون آنکه هیچ سند خریدی وجود داشته باشد. حالا همان فرم استاندارد `/purchases/create` داخل یک Drawer باز می‌شود و وضعیت **فقط به‌خاطر ثبت سند** حرکت می‌کند. **۲۵۲:** مسیر درخواست در `create_purchase` (تخصیص، قفل‌ها به ترتیب ثابت، وضعیت مشتق شامل `partially_purchased`، `final_price` از روی خودِ اسناد بازمحاسبه می‌شود نه از دست کاربر). مجوز: مسئولِ درخواست، یا override مدیر/ادمین. **۲۵۳:** `get_purchase_requests` خلاصهٔ تأمین را برمی‌گرداند و **ستون‌های مالی برای نقش `sales` اصلاً فرستاده نمی‌شوند** (نه اینکه در UI پنهان شوند)؛ `receipt_count` به زیرپرس‌وجوی اسکالر منتقل شد تا داده‌های تأمین آن را باد نکنند. تست: ۳۸/۳۸ پایگاه‌داده، ۹/۹ امنیت، ۱۲/۱۲ e2e فاز C3. **بدون commit به‌خواست مالک.** | — |
| 2026-08-01 | Claude Code | **۲۴۵ — تلفن ثابت دوباره قابل اشتراک شد** (اصلاح ۸.۴ به درخواست مالک). ۲۴۱ هر سه نوع موبایل/تلفن‌ثابت/ایمیل را سراسری یکتا کرده بود؛ ولی تلفن ثابت یک **مکان** را مشخص می‌کند نه یک شخص، و همان سند فاز ۸ در ۸.۲ «تلفن ثابت مشترک بین دو نفر» را نمونهٔ Class B می‌نامد — قانونی که اگر اشتراک تلفن ثابت ممنوع باشد اصلاً معنا ندارد. حالا: `uq_person_identifiers_contact_global` فقط `('mobile_e164','email')`، و اندیس جدید `uq_person_identifiers_landline_confirmed` فقط بین ردیف‌های **تأییدشده**. **نکتهٔ مهم:** تریگر `validate_person_identifier` هم باید عوض می‌شد — اگر فقط اندیس باریک می‌شد، تریگر همچنان تلفن ثابت مشترک را رد می‌کرد و محدودیت در `\d` شل ولی در عمل پابرجا می‌ماند. همین‌طور `CONTACT_IDENTIFIER_KINDS` در `identifiers.functions.ts`، وگرنه **اپ از اسکیما سخت‌گیرتر** می‌شد (آینهٔ همان باگی که در ۸.۸ پیدا شد). موبایل و ایمیل دست‌نخورده. ۷ تست dry-run سبز (اشتراک تلفن ثابت مجاز، اشتراک موبایل/ایمیل رد، دو تلفن ثابت تأییدشده رد)، ۳۷ تست e2e سبز، typecheck ۷۰ | `245` |
| 2026-08-01 | Claude Code | اشخاص یکپارچه **فاز ۸ — بستن مدل هویت یکپارچه** (هر ۴ تصمیم مالک اجرا شد). **۸.۱ (۲۳۹+۲۳۹b):** RPC ادغام `person_merge` + صفحهٔ بررسی `/persons/merge`. فهرست کارِ ادغام **در زمان اجرا از `pg_constraint` خوانده می‌شود** و با یک «رجیستری سیاست» تطبیق داده می‌شود؛ هر کلید خارجیِ ثبت‌نشده ادغام را متوقف می‌کند. سه یافتهٔ آزمون: `sales_quotes` تنها جدولی بود که GRANT UPDATE نداشت (با grant **ستونی** فقط روی `customer_person_id` حل شد)؛ چهار جدول append-only (دفتر اعتبار، snapshot امتیاز، …) اصلاً policy UPDATE ندارند ⇒ به‌جای بازکردن دفتر پول یا DEFINER کردن کل تابع، دو helper کوچک که هر (جدول،ستون) غیرِ کلیدخارجیِ persons را رد می‌کنند؛ چهار جدول فرزند شخص **هیچ policy DELETE نداشتند** (اضافه شد). **۸.۲:** تنها جفت در انتظار = `+989122270261` بین «محمدرضا افرا» (۱ خرید) و «تست دستی من» (۰ ارجاع). روی کاغذ Class C (هر دو تأمین‌کننده دارند) ولی رکورد آزمایشی است ⇒ شناسه از رکورد آزمایشی حذف و جفت `dismissed` شد؛ سابقهٔ شخص واقعی آلوده نشد. **۸.۳ (۲۴۰):** `uq_customers_person_id` و `uq_suppliers_person_id` — **constraint ساده نه اندیس جزئی**، چون هیچ ردیف soft-delete وجود ندارد و هیچ مسیری `is_active=false` نمی‌کند. **۸.۴ (۲۴۱):** یکتایی سراسری موبایل/تلفن/ایمیل — **تصمیم B3 مهاجرت ۲۲۸ را وارونه می‌کند**؛ پیام فارسی در تریگر `validate_person_identifier` (پوشش همهٔ مسیرهای نوشتن) و **افشای مشروط نام مالک** فقط وقتی `visibility_scope` اجازه دهد. **۸.۵ (۲۴۲):** تنها مسیر ایجاد طرف حساب خارجی به RPC منتقل شد، `person_id` به NOT NULL رسید؛ `context_kind` **`accounting_party`** (که از قبل در داده بود) نه یک نوع موازی جدید. **۸.۶ (۲۴۳): گیت برابری عددی پاس شد — هیچ عدد اعتباری تکان نخورد** (۱۲ مشتری × ۴ تابع خواندنی + ۸ مشتری × ۳ تابع نویسنده، بایت‌به‌بایت یکسان؛ خان محمدی ۱۰٬۱۰۰٬۰۰۰٬۰۰۰ و محمدزین‌الدین ۳٬۰۰۰٬۰۰۰٬۰۰۰). **۸.۷ (۲۴۴): هیچ ستونی حذف نشد** — با سرشماری ستون‌به‌ستون ثابت شد هر ۱۹ ستون قدیمی هنوز **ورودی** مدل شخص‌اند (۱۹ تریگر مشتق، ۸ CHECK، ۴ view، ۱۴۵ ارجاع کد)؛ بازنشستگی آن‌ها یعنی وارونه‌کردن جهت اشتقاق = فاز مستقل. **۸.۸:** ۳۷ تست e2e سبز (۲۳ قبلی + ۶ spec جدید فاز ۸)، typecheck ۷۰ | `9c57a254` |
| 2026-08-01 | Claude Code | اشخاص یکپارچه فاز ۷ — انتقال **همهٔ ۱۵ کلید خارجی باقی‌مانده** به شخص. ۲۳۵ (گروه A): `product_suppliers` ۳۱ و `purchase_prices` ۲۴۱ ردیف. ۲۳۶ (گروه B): `payment_receipts` ۴+۲، `delivery_receipts`، و **پیش‌نیازی که وجود نداشت: `external_parties.person_id`** اضافه و پرشد (۱ ردیف) تا مرجع طرف حساب خارجی روی فیش/سند قابل حل شود؛ همچنین دو نقص نهفتهٔ فاز ۵ که dry-run پیدا کرد رفع شد (CHECK سند پرداخت و فرمول `person_fk_drift_report`). ۲۳۷ (گروه C — اعتبار): هر ۷ جدول اعتباری، ۹ ردیف. **گیت برابری عددی پاس شد: هیچ عدد اعتباری تغییر نکرد** (هر ۱۲ مشتری، شامل خان محمدی ۱۰٬۱۰۰٬۰۰۰٬۰۰۰ و محمدزین‌الدین ۳٬۰۰۰٬۰۰۰٬۰۰۰). **توابع اعتبار عمداً بازنویسی نشدند** — `customers.person_id` یکتا نیست، پس کلیدکردن اعتبار روی شخص روزی که دو مشتری یک شخص داشته باشند مانده‌ها را بی‌صدا جمع می‌زند. ۲۳۸ (گروه D): `invoices`، `didar_activities` + sweep نهایی. الگو: ستون‌های **مشتق** با تریگر (مثل ۲۳۱) ⇒ هیچ مسیر نوشتنی تغییر نکرد. ۲۳ تست e2e سبز، typecheck ۷۰ | `3a9e81b4` |
| 2026-08-01 | Claude Code | اشخاص یکپارچه فاز ۶ — بستن حفره‌های ساخت شخص: هر سه مسیر باقی‌مانده (`SupplierForm`، `CustomerForm`، `SupplierReferralModal`) به `person_create_inline` منتقل شدند و migration ۲۳۲ پارامتر `p_legacy_fields` را با whitelist به RPC افزود تا فیلدهای مخصوص جدول قدیمی (contact_name/trust_level/status و responsible_id/link_group/birth_date) از دست نروند. سپس **۲۳۳: `person_id` روی `suppliers` و `customers` به `NOT NULL` رسید** (۲ تأمین‌کنندهٔ یتیم «api» و «تست دستی من» پرشدند؛ اشخاص ۲۵ → ۲۷؛ **هیچ ادغام خودکاری انجام نشد**). ۲۳۴: صف `person_merge_candidates` با تشخیص خودکار شماره مشترک (۱ مورد در انتظار بررسی). رفع ۴ باگ رابط: بخش شناسه‌ها روی صفحهٔ ایجاد شخص، اتصال label به input، لینک ردیف پیش‌فاکتور به جزئیات، و **ریدایرکت اشتباه به /login در سه مسیر** (`/sales/quotes/new`، `/pricing/quick-price`، `/pricing/settlement-types`) که guard دستی به‌جای `requireAnyRole` داشتند. ۱۳ تست e2e سبز، typecheck همان ۷۰ خطای قدیمی | `235d3c83` |
| 2026-08-01 | Claude Code | اشخاص یکپارچه فاز ۵ (۲۳۱): کلید خارجی بر پایهٔ شخص روی سه جدول کسب‌وکاری — `sales_quotes.customer_person_id`، `purchases.supplier_person_id`، `payment_vouchers.payee_person_id`. این ستون‌ها **مشتق** هستند: تریگر `BEFORE INSERT/UPDATE` آن‌ها را از کلید قدیمی بازمی‌سازد (همان قرارداد «دیتابیس مرجع است» در ۲۲۸)، بنابراین **هیچ مسیر نوشتنی تغییر نکرد** و `create_sales_quote_with_items` و `pay_purchase_with_voucher` دست‌نخورده کار می‌کنند. پرکردن: ۴۷ از ۴۸ پیش‌فاکتور و ۶ از ۶ خرید (فاکتور پرداخت خالی است). `NOT NULL` عمداً اعمال نشد — دلیل کامل در بالای فایل migration. تابع `person_fk_drift_report()` برای پایش. ۶ تست دود پاس، backup پیش از اجرا گرفته شد، rollback در `docs/verification/231-down.sql` | `885d2ddb` |
| 2026-08-01 | Claude Code | اشخاص یکپارچه فاز ۴ (۲۳۰): ورود یکپارچهٔ اشخاص (`person_import_batch`) + تطبیق هویت بر پایهٔ شناسه‌های نرمال‌شده، اندپوینت `POST /api/persons/import`، انتقال ایمپورت مشتریان به همان مسیر، و **پرکردن `person_id`**: ۱۳ تأمین‌کننده و ۱۲ مشتری همگی به ۲۵ شخص تازه وصل شدند (۲۵ لینک زمینه، بدون یتیم). ۵۱ تست در چهار فاز پاس، deploy روی LAN | `18b88720` |
| 2026-07-31 | Claude Code | اشخاص یکپارچه فاز ۳ (۲۲۹): ثبت شخص به‌صورت درجا از داخل فرم خرید — افزودن `suppliers.person_id`، RPC اتمیک `person_create_inline` (شخص + شناسه + ردیف قدیمی + لینک زمینه در یک تراکنش)، کامپوننت `PersonModal`، دکمهٔ «تأمین‌کنندهٔ جدید» در `PurchaseForm`، انتقال `QuickAddCustomerDialog` به همان RPC، هم‌راستاسازی گارد تکراری شناسه با قانون جدید یکتایی؛ ۱۳ تست پاس، deploy روی LAN | `9a828d97` |
| 2026-07-31 | Claude Code | اشخاص یکپارچه فاز ۲ (۲۲۸): جدول `person_aliases` با تطبیق بدون‌حساسیت به نیم‌فاصله، تابع `normalize_identifier` در plpgsql + تریگر (دیتابیس مرجع نهایی نرمال‌سازی)، اصلاح یکتایی شناسه‌ها (کد ملی/اقتصادی/شبا سراسری، موبایل/ایمیل فقط پس از تأیید)، محدودکردن انتخاب سطح دسترسی در `PersonForm`، هشدار روی `is_required`؛ ۲۶ تست پاس، deploy روی LAN با `APP_GIT_SHA=0201160e-dirty` | `cf3bb9a5` |
| 2026-07-31 | Claude Code | اشخاص یکپارچه فاز ۱ (۲۲۶+۲۲۷): رفع گیت نقش روی ساخت شخص (sales/accountant حالا می‌توانند شخص internal_general بسازند)، افزودن RPC اتمیک `person_create_full`، اتصال `createPerson` به آن و بستن حفرهٔ نیمه‌نوشت؛ ۱۱ تست RLS/اتمیک‌بودن پاس، build سبز، typecheck همان ۷۰ خطای قدیمی | `1eac0518` |
| 2026-07-30 | Codex | نیاز ۲۱۵: ساخت/اجرای business-flow واقعی Playwright برای کنترل موجودی فقط در پذیرش، پوشش detail/list، atomicity، idempotency، exact stock، عدم دسترسی فروشنده، cleanup و regression ۲۱۱ تا ۲۱۵؛ build سبز و typecheck فقط همان ۷۰ خطای قدیمی | بدون commit |
| 2026-07-30 | Codex | نیازهای ۲۱۱/۲۱۶: رفع دو باگ RPC/migration رد پیش‌فاکتور، اجرای business-flow واقعی Playwright با نتیجه `1 passed`، و ثبت وضعیت Fully Covered/PASS در گزارش کیفیت | بدون commit |
| 2026-07-30 | Codex | نیازهای ۲۱۱/۲۱۶: افزودن business-flow واقعی Playwright برای چرخه رد پیش‌فاکتور/اعلان؛ اجرا فعلاً روی نبود storageStateهای accountant/salesperson متوقف است | بدون commit |
| 2026-07-29 | Codex | نیاز ۲۱۵: افزودن تست مرورگری واقعی برای ساخت پیش‌فاکتور بیش از موجودی، رد قطعی‌سازی با پیام جزئیات، و cleanup داده‌های تست؛ suite ۲۱۱-۲۱۸ با ۲۰ تست پاس شد | بدون commit |
| 2026-07-29 | Codex | نیاز ۲۱۵: حذف کنترل موجودی از زمان ایجاد پیش‌فاکتور، حفظ کنترل در قطعی‌سازی، افزودن تست rollback-only و deploy روی LAN؛ suite ۲۱۱-۲۱۸ با ۱۹ تست پاس شد | بدون commit |
| 2026-07-29 | Codex | ساخت و اجرای suite E2E Playwright برای نیازهای ۲۱۱ تا ۲۱۸؛ ۱۷ تست پاس شد و گزارش در `docs/audits/211-218-playwright-report.md` ساخته شد | بدون commit |
| 2026-07-29 | Codex | نیازهای ۲۱۷ و ۲۱۸: تأیید وجود ویزیتورها/انتخاب ویزیتور در پیش‌فاکتور، افزودن گزینه «رسید اسکرین‌شات از همراه بانک» به فیش واریزی و اعمال migration روی LAN | همین commit |
| 2026-07-29 | Codex | نیازهای ۲۱۳ و ۲۱۴: refresh خودکار تخصیص اعتبار بعد از تغییر امتیازها، اتصال دقیق جدول محصولات پرتکرار واتساپ، و استفاده مشاور خرید از تقاضا/فروشندگان واتساپ | همین commit |
| 2026-07-29 | Codex | نیاز ۲۱۲ روی LAN فعال شد: backup، اعمال migration کنترل اعتبار/تعهد/موجودی، restart PostgREST، و تأیید فرانت cf9482ff | `cf9482ff` |
| 2026-07-29 | Codex | نیاز ۲۱۲: توقف ثبت پیش‌فاکتور برای موجودی ناکافی، معوقه، کسری اعتبار و نبود اعتبار؛ افزودن مسیرهای تعهد/تأیید حسابداری و migration سخت‌گیرانه RPC | همین commit |
| 2026-07-29 | Codex | نیاز ۲۱۱: دلیل کامل رد پیش‌فاکتور، اعلان ماندگار برای فروشنده، و لینک اعلان به پیش‌فاکتور | همین commit |
| 2026-07-29 | Codex | جبران انطباق با `AGENTS.md`: خواندن `PROGRESS.md`/`README.md`/معیارهای پذیرش، ثبت وضعیت، و گزارش نبود test script | `1ca72316` |
| 2026-07-29 | Claude Code | یکپارچه‌سازی مستندات راهنما: ادغام `AGENTS.md` قدیمی با محتوای جدید، ساخت `CLAUDE.md` هم‌محتوا، بازیابی `PROGRESS.md`، حذف `AGENTS (2).md` | `6e3fb472` |
| 2026-07-29 | Codex | مسیریابی ارائه‌دهندهٔ AI بر اساس کاربرد (`ai_usage_routes`)، راهنمای فرم ارائه‌دهنده، پاسخ RTL و زمینهٔ زمانی دستیار | `1ca72316` |
| 2026-07-28 | Claude Code | نیازمندی‌های ۱۹۴–۲۰۹ (۵ فاز): ترمیم ۴۴ تابع خراب، PDF پیش‌فاکتور، ویزیتور، اعتبار/بیعانه، عرض و drag سایدبار، دلیل رد، تخفیف زیر لیست | `9dfdea30` |
| 2026-07-28 | Claude Code | رفع سه باگ: نمایش عکس محصول + بستهٔ کامل عکس، خطای محاسبهٔ قیمت (app_role)، ویرایش/رسید فضای خرید | `3c0dba89` |
| 2026-07-28 | دستی (کاربر) | نمایه‌سازی دانش سازمانی اجرا شد (۱۳۹ chunk) · GPT در ai-providers غیرفعال شد | — |

<!-- ردیف جدید را اینجا اضافه کنید. مثال:
| 2026-07-29 | Codex | فلان صفحه ساخته شد | abc12345 |
-->

## کارهای دستی باقی‌مانده (فقط کاربر می‌تواند انجام دهد)
- **موجودی انبار:** `warehouse_stock` تقریباً خالی است (۴ ردیف برای ۳ انبار) ⇒ «قطعی‌کردن» اکثر پیش‌فاکتورها با «موجودی کافی نیست» شکست می‌خورد. دادهٔ کسب‌وکاری است.
- **تخصیص سرمایهٔ مشتریان:** هیچ مشتری‌ای تخصیص ندارد ⇒ قانون اعتبار/بیعانه (۱۹۷/۱۹۸) ساخته شده ولی تا تعریف تخصیص، خودبه‌خود فعال نمی‌شود.
- **✅ انجام شد در فاز ۶:** هر سه فرم به `person_create_inline` منتقل شدند و `person_id` روی هر دو جدول `NOT NULL` شد. `createCustomer` در `lib/customers/functions.ts` بدون فراخوان بود و `@deprecated` علامت خورد.
- **✅ انجام شد در فاز ۷:** هر ۱۹ کلید خارجی هویتی حالا ستون شخص دارد (۱۳ به customers، ۳ به suppliers، ۲ به external_parties، به‌علاوهٔ سه شاخهٔ payee که یک ستون مشترک دارند). `person_fk_drift_report()` هر ۱۷ مرجع را پوشش می‌دهد.
- **✅ انجام شد در فاز ۸:** یکتایی `customers.person_id`/`suppliers.person_id` (۲۴۰)، یکتایی سراسری شماره (۲۴۱)، `external_parties.person_id` به NOT NULL و فرمش به RPC (۲۴۲)، توابع اعتبار روی شخص با اثبات برابری عددی (۲۴۳)، و **رابط ادغام اشخاص** در `/persons/merge` (۲۳۹). صف `person_merge_candidates` دیگر بدون UI نیست و **۰ مورد در انتظار** است.

### باقی‌مانده — نیازمند تصمیم یا اقدام مالک
- **✅ حل شد در ۲۴۵ — یکتایی تلفن ثابت:** مالک تأیید کرد. `uq_person_identifiers_contact_global` حالا فقط `('mobile_e164','email')` را پوشش می‌دهد و تلفن ثابت به رفتار B3 مهاجرت ۲۲۸ برگشت (اندیس جدید `uq_person_identifiers_landline_confirmed`، فقط بین شناسه‌های تأییدشده). دو همکار می‌توانند یک خط دفتر مشترک داشته باشند؛ دو ادعای **تأییدشده** روی یک خط همچنان رد می‌شود.
- **یکتا نبودن `external_parties.person_id`:** عمداً UNIQUE نشد (تصمیم ۱ فقط مشتری و تأمین‌کننده را پوشش می‌داد). فرض: طرف حساب خارجی یک «نقش» است و یک شخص می‌تواند چند نقش داشته باشد. اگر مالک نظر دیگری دارد، افزودن UNIQUE ساده است.
- **رکوردهای آزمایشی باقی‌مانده:** شخص «تست دستی من» و ردیف تأمین‌کننده‌اش هنوز هستند (بدون تراکنش و بدون شناسه). پاکسازی دادهٔ کسب‌وکاری است، نه اصلاح هویت.
- **ستون‌های هویتی قدیمی حذف نشدند (۸.۷):** هر ۱۹ ستون هنوز **ورودی** ستون‌های مشتق‌اند. حذفشان یعنی وارونه‌کردن جهت اشتقاق (شخص مرجع شود و قدیمی مشتق، یا اشتقاق برداشته و همهٔ نویسنده‌ها بازنویسی شوند) — یک فاز مستقل با ریسک واقعی، نه یک گام پاکسازی.
- **بازبینی دستی انسانی (پیشنهاد):** صفحهٔ `/persons/merge` با یک جفت واقعی، پیام خطای شمارهٔ تکراری در فرم شخص، و یک چرخهٔ کامل فیش واریزی با طرف حساب خارجی تازه‌ساخته‌شده.
