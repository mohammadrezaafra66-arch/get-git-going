# پرامپت اجرایی جامع — موارد ۱۳۲ تا ۱۳۸.۱ (پروژه AfraKala)

پروژه: `D:\AfraKalaTest\app`
این سند شامل ۱۲ فاز است. همه را به ترتیب و پشت‌سرهم اجرا کن.

---

## بخش ۰ — قوانین سراسری (قبل از هر کاری بخوان)

### ۰.۱ موتور اجرا (مهم‌ترین بخش)
برای **هر فاز** دقیقاً این چرخه را اجرا کن:

1. **بررسی**: فایل‌های مربوط به فاز را بخوان و وضعیت فعلی را تأیید کن.
2. **پیاده‌سازی**: فقط تغییرات همان فاز.
3. **تأیید خودکار** (هر سه مورد اجباری):
   - `npm run typecheck`
   - `npm run build`
   - چک‌های اختصاصی همان فاز (در متن هر فاز آمده)
4. **commit** با پیام `feat(<شماره فاز>): <خلاصه>` — هر فاز یک commit جدا.
   - **فقط فایل‌هایی را stage کن که خودِ همان فاز تغییر داده است.** هرگز `git add -A` یا `git add .` نزن.
   - فایل‌های مستندات و پرامپت (`*.md` در ریشه و `docs/`) را در هیچ فازی commit نکن؛ دست‌نخورده رها کن.
5. **گزارش کوتاه ۵ خطی**: فایل‌های تغییریافته / کار انجام‌شده / نتیجه typecheck / نتیجه build / commit SHA.
6. **بدون پرسیدن، بلافاصله فاز بعدی را شروع کن — مگر اینکه به مرز batch رسیده باشی (بخش ۰.۵).**

### ۰.۲ شرایط توقف (فقط این چهار مورد)
اگر یکی از این‌ها رخ داد، **متوقف شو و گزارش بده** — سراغ فاز بعد نرو:
- `typecheck` یا `build` شکست بخورد و بعد از ۲ تلاش اصلاح، همچنان شکسته بماند.
- انجام فاز نیازمند تغییر فایلی خارج از دامنهٔ آن فاز باشد.
- یک فرض کلیدی فاز با کد واقعی نخواند (مثلاً جدول/تابع/کامپوننت ادعاشده وجود نداشته باشد).
- تعارض واقعی با تغییرات فاز قبلی پیدا کنی.
- به آخرین فاز batch جاری رسیده باشی (بخش ۰.۵).

در غیر این صورت **هرگز برای تأیید توقف نکن**.

### ۰.۳ قواعد ثابت
- روی branch فعلی `security/rls-permissive-select-fix` بمان. به `main` سوییچ نکن.
- **`git push` نزن.** در پایان batch، commitها را آماده و فهرست کن و منتظر تأیید صریح بمان.
- **migrationها فقط به‌صورت فایل ساخته و commit شوند.** هیچ SQL ای را خودکار روی دیتابیس زندهٔ `afrakala` اجرا نکن. در انتهای کار فهرست migrationهای نیازمند اعمال را جدا اعلام کن.
- `npm run dev` یا rebuild سرور را خودت اجرا نکن.
- از `any` بی‌دلیل استفاده نکن. اگر `types.ts` قدیمی بود، به‌جای regenerate از cast موضعی حداقلی استفاده کن و در گزارش ذکر کن.
- تمام متن‌های UI فارسی و RTL و هماهنگ با کامپوننت‌های موجود (`Card`, `Badge`, `Alert`, `Button`, `PageHeader`, shadcn).
- کنترل دسترسی: از الگوی موجود پروژه (`requirePermission` / `requireAnyRole` از `@/lib/rbac/route-guards` و `hasPermissionEx` / `hasAnyRole` از `@/lib/rbac/roles`) استفاده کن. لیست دستی نقش‌ها در UI ننویس.

### ۰.۴ ترتیب اجرا و دلیلش
فازهای ۱ تا ۴ همگی روی فایل واحد `src/routes/_app.sales.search.tsx` کار می‌کنند، پس **باید پشت‌سرهم و هرگز موازی** اجرا شوند. سپس دامنهٔ اعتبار، بعد حسابداری، و در آخر سنگین‌ترین کارهای دیتابیسی.

| فاز | مورد | دامنه | ماهیت تغییر | ریسک |
|---|---|---|---|---|
| ۱ | ۱۳۲ | پاپ‌آپ یادآوری در جستجوی فروش | UI افزایشی | کم |
| ۲ | ۱۳۸ | مخفی‌سازی قیمت‌ها تا «مشاهده کامل» | تغییر رفتار موجود UI | کم فنی / **بالا عملیاتی** |
| ۳ | ۱۳۸.۱ | افکت واکنش شناور | UI افزایشی | کم |
| ۴ | ۱۳۷ | دکمهٔ جستجوی سریع فروش در هدر | UI افزایشی | کم |
| ۵ | ۱۳۳ | پنل تشخیص صفر بودن اعتبار | UI افزایشی | کم |
| ۶ | ۱۳۳.۱ | صفحهٔ آموزش ایجاد اعتبار | صفحهٔ جدید | کم |
| ۷ | ۱۳۴ | چهار نوع فیش واریزی | **تبدیل دادهٔ مالی موجود + constraint** | **بالاترین** |
| ۸ | ۱۳۵ | مارکرهای پیش‌فاکتور | ستون جدید روی جدول اصلی `invoices` | متوسط-بالا |
| ۹ | ۱۳۶ | خروجی اکسل لیست قیمت فروش | افزایشی، بدون DB | کم |
| ۱۰ | ۱۳۲.۲ | تاریخچهٔ فیلدی محصول | افزایشی، فقط خواندن | کم |
| ۱۱ | ۱۳۲.۱ | ثبت دستی عملکرد روزانه | **بازنویسی تابع امتیازدهی موجود** | بالا |
| ۱۲ | ۱۳۲.۳ | پرسش و پاسخ AI از دانش‌نامه | فقط اشیای جدید | حجیم ولی کم‌خطر |

**توجه به فاز ۲:** ریسک فنی‌اش کم است ولی رفتار روزمرهٔ همهٔ فروشنده‌ها را عوض می‌کند. قبل از rebuild سرور باید به تیم فروش اطلاع داده شود.

### ۰.۵ مرزهای batch (اجباری)
این سند برای اجرا در **چهار session جداگانه** طراحی شده. دلیل فنی: context window در یک session واحد قبل از فازهای پایانی پر می‌شود و کیفیت افت می‌کند.

| Batch | فازها | در پایان |
|---|---|---|
| اول | ۰ تا ۶ | توقف + گزارش |
| دوم | ۷ تا ۱۰ | توقف + گزارش |
| سوم | فقط ۱۱ | توقف + گزارش |
| چهارم | فقط ۱۲ | توقف + گزارش |

**در ابتدای اجرا از کاربر بپرس کدام batch را باید اجرا کنی، و فقط همان را اجرا کن.** داخل هر batch، auto-continue بین فازها کاملاً برقرار است و برای تأیید توقف نکن. در پایان آخرین فاز batch، گزارش کامل بده و متوقف شو.

---

## فاز ۰ — پیش‌پرواز

1. تأیید کن روی branch ‏`security/rls-permissive-select-fix` هستی.
2. `git status` بگیر و طبق این قاعده تصمیم بگیر:
   - اگر فقط فایل‌های مستندات/پرامپت (`*.md` در ریشه یا زیر `docs/`) untracked یا تغییر کرده‌اند → **متوقف نشو**؛ فقط در گزارش فهرست‌شان کن و در طول کار دست‌نخورده رهایشان کن.
   - اگر فایلی زیر `src/` یا `supabase/` تغییر کرده یا untracked است → **متوقف شو و گزارش بده** (ممکن است کار نیمه‌تمام قبلی باشد که با فازهای ما تداخل کند).
3. `npm run typecheck` و `npm run build` را به‌عنوان خط پایه اجرا کن. اگر همین حالا شکسته‌اند، **متوقف شو و گزارش بده** (نباید خرابی قبلی به پای فازهای ما نوشته شود).
4. بررسی کن آیا `<SalesReminderPopup />` از قبل در `src/routes/_app.sales.search.tsx` رندر شده یا نه. نتیجه را یادداشت کن (ورودی فاز ۱).

---

## فاز ۱ — مورد ۱۳۲: فعال‌سازی پاپ‌آپ یادآوری در `/sales/search`

> **توجه:** این مورد عیناً همان «مورد ۱۲۹» است. ممکن است قبلاً اجرا شده باشد.

**اگر در فاز ۰ دیدی که پاپ‌آپ از قبل رندر شده:** هیچ تغییری نده، فقط چک‌های تأیید را بزن و در گزارش بنویس «از قبل انجام شده» و برو فاز ۲.

**در غیر این صورت:**
1. در `src/routes/_app.sales.search.tsx` اضافه کن:
   `import { SalesReminderPopup } from "@/components/sales/SalesReminderPopup";`
2. داخل `return` اصلی، بلافاصله بعد از `<div className="space-y-5">` و قبل از `<PageHeader ...>` قرار بده:
   `<SalesReminderPopup />`
3. کامپوننت `SalesReminderPopup.tsx` را **بازنویسی نکن** — این رفتارها همین حالا داخلش پیاده شده‌اند: نمایش یک یادآوری فعال در هر ورود، toast حدود ۲ ثانیه، ترتیب چرخشی بین آیتم‌ها، و fail-silent وقتی جدول/داده وجود ندارد.
4. `Toaster` در ریشهٔ اپ موجود است؛ فقط اگر واقعاً نبود اضافه کن.
5. آیتم منوی `/admin/sales-reminders` با عنوان «یادآوری‌های فروش» از قبل در `nav-items.ts` هست — تأیید کن، اضافه نکن.
6. نام فایل‌های موجود (شامل migration با پیشوند ۱۲۸) را تغییر نده.

**چک‌های تأیید:** grep تأیید کند `<SalesReminderPopup />` در JSX صفحه رندر شده + typecheck + build.

---

## فاز ۲ — مورد ۱۳۸: قیمت‌ها فقط بعد از «مشاهده کامل»

**هدف:** در نتایج اولیهٔ `/sales/search` هیچ قیمتی دیده نشود؛ قیمت‌های هر محصول فقط پس از کلیک روی «مشاهده کامل» همان محصول نمایش داده شوند.

> **تصحیح مهم:** state جدید لازم **نیست**. در کامپوننت کارت محصول همین حالا `const [detailsOpen, setDetailsOpen] = useState(false)` و تابع `handleToggleDetails` وجود دارند. از همان‌ها استفاده کن.

1. رفتار فعلی `handleToggleDetails` را **دست‌نخورده** نگه دار: هنگام باز شدن، دو رویداد `product_details_opened` و `price_checked` را از طریق `trackProductInteraction` ثبت می‌کند. این ثبت بازدید باید کاملاً حفظ شود.
2. تمام بلوک‌های قیمتی کارت را داخل شرط `detailsOpen` ببر. شامل:
   - قیمت اصلی (متن بزرگ `text-3xl` با کلاس `price-flash`) و برچسب «قیمت {عنوان}»
   - `PriceChangeBadge`
   - گرید قیمت‌های ثانویه (سایر انواع قیمت فروش)
   - قیمت‌های تسویه‌ای (settlement rows)
   - هر عدد دیگری که قیمت محسوب می‌شود
3. اطلاعات غیرقیمتی همیشه نمایش داده شوند: نام محصول، SKU، برند، دسته‌بندی، وضعیت موجودی، برچسب‌ها، توضیح کوتاه و خود دکمهٔ «مشاهده کامل».
4. در حالت بسته، حداکثر یک متن کوچک و کم‌رنگ به‌جای قیمت: «برای مشاهده قیمت‌ها، مشاهده کامل را بزنید.» — بیشتر از این شلوغ نکن.
5. متن دکمه بر اساس وضعیت تغییر کند: بسته → «مشاهده کامل»، باز → «بستن جزئیات». کلیک دوباره قیمت‌ها را مخفی کند.
6. فقط محصولی که باز شده قیمت نشان دهد؛ بقیه بسته بمانند.
7. با جستجوی جدید، وضعیت باز/بستهٔ کارت‌ها به‌درستی reset شود (چون کارت‌ها remount می‌شوند این معمولاً خودکار است — تأیید کن).
8. مراقب باش این موارد خراب نشوند: فیلترها، حالت label-mode، مرتب‌سازی موجودی، pagination، دکمهٔ نمودار، دکمهٔ تأمین‌کننده، `StockAlertButton`.
9. تغییر backend لازم نیست. در گزارش ذکر کن که این gating فقط در سطح UI است و اگر نیاز امنیتی جدی باشد، فاز بعدی می‌تواند RPC جدا برای قیمت‌ها بسازد.

**چک‌های تأیید:** typecheck + build + تأیید اینکه هیچ رندر قیمتی خارج از شرط `detailsOpen` باقی نمانده.

---

## فاز ۳ — مورد ۱۳۸.۱: افکت واکنش شناور روی «مشاهده کامل»

1. کامپوننت جدید بساز: `src/components/sales/FloatingReactionBurst.tsx`
2. رفتار: با هر trigger، بین **۵ تا ۹** آیتم شناور تولید کند که از نزدیکی دکمه/کارت به سمت بالا حرکت کنند و محو شوند.
3. آیکون‌ها از `lucide-react` (بدون emoji در سورس): `Star`, `Heart`, `Sparkles`, `Moon`.
4. جزئیات انیمیشن: مسیر، اندازه و delay هر آیتم کمی random باشد؛ کل افکت بین ۱ تا ۲ ثانیه؛ رنگ‌ها ملایم و هماهنگ با تم پروژه.
5. `pointer-events: none` روی لایهٔ افکت اجباری است تا کلیک‌پذیری UI خراب نشود.
6. بدون کتابخانهٔ جدید. فقط CSS + React. با `useState` burst بساز و بعد از پایان انیمیشن پاک کن (نشت state نداشته باشد).
7. trigger: داخل `handleToggleDetails` **فقط هنگام باز شدن** (نه بستن)، بعد از حفظ کامل رفتار قبلی: ثبت بازدید (فاز ۲) + نمایش قیمت‌ها (فاز ۲) + سپس افکت.
8. کلیک روی چند محصول مختلف، burstهای مستقل بسازد و تداخل بد نداشته باشد.
9. با `@media (prefers-reduced-motion: reduce)` افکت غیرفعال یا بسیار ساده شود.
10. در موبایل هم درست دیده شود و layout را نشکند.

**چک‌های تأیید:** typecheck + build + تأیید وجود `pointer-events-none` و بلوک `prefers-reduced-motion`.

---

## فاز ۴ — مورد ۱۳۷: دکمهٔ «جستجوی سریع فروش» در هدر

1. فایل هدر اصلی: `src/components/layout/AppHeader.tsx` (اگر ساختار فرق داشت، `DashboardHeader.tsx` را هم بررسی کن). دکمه باید **در نوار بالای صفحه و نزدیک بخش جستجو** باشد، نه فقط داخل sidebar.
2. دکمه: `Button` موجود پروژه + آیکون مناسب از lucide-react (`ScanSearch` یا `Zap`) + متن «جستجوی سریع فروش».
3. ناوبری: `Link` تایپ‌دار `@tanstack/react-router` با `to="/sales/search"` مطابق الگوی پروژه.
4. دسترسی: فقط برای کاربرانی که مجوز دیدن جستجوی فروش را دارند. **اول** بررسی کن `/sales/search` چه guard ای دارد (`beforeLoad` در `_app.sales.search.tsx`) و **دقیقاً همان شرط** را برای نمایش دکمه به کار ببر. اگر guard ندارد، از الگوی permission ماژول فروش استفاده کن. لیست دستی نقش ننویس.
5. ریسپانسیو: در دسکتاپ با متن کامل؛ در موبایل فقط آیکون با tooltip یا متن کوتاه. layout نباید بشکند.
6. جستجوی سراسری موجود در هدر نباید خراب یا جابه‌جا شود.

**چک‌های تأیید:** typecheck + build + تأیید اینکه دکمه در کامپوننت هدر (نه sidebar) اضافه شده.

---

## فاز ۵ — مورد ۱۳۳: پنل «چرا سقف اعتبار صفر شده است؟»

**زمینهٔ تأییدشده:** عدد سقف از RPC ‏`calculate_customer_realtime_credit` می‌آید و در `DynamicScoringSection.tsx` از `realtimeQ.data.final_limit` نمایش داده می‌شود.

> **تصحیح ۱:** تایپ `RealtimeCreditResult` در `useDynamicScoring.ts` **کامل است** و این فیلدها را دارد: `weighted_score`, `params_evaluated`, `params_active`, `final_limit`, `raw_allocation`, `credit_limit`, `binding_constraint`, `capital_date_used`, `is_capital_stale`, `salesperson_allocated_capital`, `share_ratio`, `breakdown`. **تایپ را تغییر نده.**
>
> **تصحیح ۲:** فیلد `total_active_weight` در `realtimeQ` **وجود ندارد**؛ در نتیجهٔ `calcQ` (تایپ `CalculatedScoreResult`) است. اگر خواستی به آن اشاره کنی، از `calcQ` بخوان، نه `realtimeQ`.
>
> **تصحیح ۳:** `binding_constraint` پنج مقدار دارد: `overdue`, `no_salesperson`, `no_capital`, `credit_limit`, `formula`. دو مقدار آخر حالت عادی‌اند و نباید به‌عنوان خطا نمایش داده شوند، ولی کد نباید روی آن‌ها crash کند.

1. کامپوننت جدید: `src/components/credit/CreditZeroReasonPanel.tsx` و استفاده از آن داخل `DynamicScoringSection.tsx`، بلافاصله زیر بخش «سقف اعتبار — محاسبه زنده».
2. **شرط نمایش:** فقط وقتی `final_limit === 0` است یا وضعیت محاسبه هشداردهنده است (مثل `is_capital_stale === true`). در حالت عادی اصلاً رندر نشود.
3. تشخیص علت‌ها (همهٔ موارد صادق را نشان بده، نه فقط اولی):
   - `binding_constraint === "overdue"` → «مشتری دارای بدهی معوق است؛ تا زمان رفع معوقه سقف اعتبار صفر می‌شود.»
   - `binding_constraint === "no_salesperson"` → «این مشتری به هیچ کارشناس فروشی متصل نیست؛ ابتدا کارشناس مسئول مشتری را مشخص کنید.»
   - `binding_constraint === "no_capital"` → «برای کارشناس مسئول این مشتری سرمایه تخصیص داده نشده است؛ باید تخصیص سرمایه روزانه اجرا شود.»
   - `weighted_score <= 0` → «امتیاز وزنی مشتری صفر است؛ ممکن است مقادیر امتیازدهی ذخیره نشده باشند یا وزن‌های پارامترها برای این دوره معتبر نباشند.»
   - `params_active > 0 && params_evaluated > 0 && weighted_score === 0` → «پارامترها پر شده‌اند، اما وزن مؤثر آن‌ها صفر است؛ تاریخ اعتبار وزن‌ها یا تنظیمات وزن‌دهی را بررسی کنید.»
   - `salesperson_allocated_capital <= 0` → «سرمایه تخصیص‌یافته به کارشناس مسئول صفر است؛ حتی با امتیاز مشتری، سقف اعتبار قابل محاسبه نیست.»
   - `raw_allocation <= 0` → «سهم خام مشتری از سرمایه صفر شده است؛ امتیاز مشتری یا مجموع امتیازهای مشتریان همان کارشناس را بررسی کنید.»
   - `is_capital_stale === true` → «محاسبه با سرمایه قدیمی انجام شده است؛ تخصیص سرمایه روز جاری را دوباره اجرا کنید.»
4. یک بلوک عددی جمع‌وجور هم نشان بده: `weighted_score`، `params_evaluated / params_active`، `raw_allocation`، `salesperson_allocated_capital`، `share_ratio`، `capital_date_used`، `binding_constraint` (با ترجمهٔ فارسی کنارش).
5. متن‌ها فارسی، ساده و قابل فهم برای ادمین/حسابدار. از `Alert` یا `Card` با رنگ هشدار **ملایم** استفاده کن؛ صفحه شلوغ نشود.
6. اعداد با `toFaDigits` و تاریخ‌ها با helper تاریخ موجود پروژه فرمت شوند (همان الگویی که در `DynamicScoringSection` استفاده شده).

**چک‌های تأیید:** typecheck + build. در گزارش بنویس برای سناریوی خان‌محمدی (امتیاز وزنی صفر + سرمایهٔ کارشناس صفر + سرمایهٔ قدیمی) کدام پیام‌ها نمایش داده می‌شوند.

---

## فاز ۶ — مورد ۱۳۳.۱: صفحهٔ «آموزش ایجاد اعتبار»

> **تصحیح مسیر:** صفحهٔ لیست مشتریان `src/routes/_app.sales_.customers.tsx` است (نه `_app.sales.customers...`). برای ساخت مسیر `/sales/customers/credit-training` طبق قرارداد flat-file routing پروژه، فایل جدید باید `src/routes/_app.sales_.customers_.credit-training.tsx` باشد. قبل از ساخت، الگوی فایل‌های همسایه (`_app.sales_.customers_.create.tsx`) را ببین و دقیقاً از آن پیروی کن.

1. در صفحهٔ `_app.sales_.customers.tsx`، **کنار دکمهٔ «مشتری جدید»** یک دکمهٔ جدید اضافه کن:
   - متن: «آموزش ایجاد اعتبار»
   - آیکون: `GraduationCap` یا `BookOpen` از lucide-react
   - `variant="outline"` و هم‌اندازهٔ دکمهٔ کناری
   - ناوبری با `Link` تایپ‌دار به `/sales/customers/credit-training`
2. route جدید بساز و در صورت نیاز `routeTree.gen.ts` را regenerate کن (طبق روال پروژه).
3. صفحه داخل layout فعلی باشد و از `PageHeader`, `Card`, `Badge`, `Alert`, `Button` و آیکون‌های lucide استفاده کند.
4. بالای صفحه دکمهٔ «بازگشت به مشتریان» به `/sales/customers`.
5. **محتوای صفحه دقیقاً این ساختار را داشته باشد:**

   **عنوان:** «آموزش ایجاد اعتبار برای مشتریان»
   **توضیح کوتاه:** «در این صفحه مراحل لازم برای اینکه سقف اعتبار مشتری به‌درستی محاسبه شود توضیح داده شده است.»

   **بخش ۱ — پیش‌نیازهای مشتری:** مشتری باید فعال باشد؛ باید کارشناس مسئول داشته باشد؛ اطلاعات پایه درست ثبت شده باشد؛ اگر بدهی معوق داشته باشد اعتبار صفر می‌شود.

   **بخش ۲ — ثبت پارامترهای امتیازدهی:** وارد صفحهٔ اعتبار مشتری شوید؛ بخش «امتیازدهی پویا — ماه جاری» را کامل کنید؛ برای هر پارامتر مقدار مناسب وارد کنید؛ هر مقدار باید ذخیره شود؛ **فقط پر کردن فرم کافی نیست، باید دکمهٔ ذخیرهٔ هر پارامتر زده شود.**

   **بخش ۳ — تنظیم وزن پارامترها:** هر پارامتر باید وزن فعال داشته باشد؛ اگر وزن‌ها برای دورهٔ فعلی معتبر نباشند امتیاز وزنی صفر می‌شود؛ تاریخ اعتبار وزن‌ها باید با ماه محاسبه هماهنگ باشد؛ اگر مجموع وزن‌های فعال صفر باشد سقف اعتبار محاسبه نمی‌شود.

   **بخش ۴ — تخصیص سرمایه روزانه:** بعد از ثبت امتیازها و وزن‌ها باید تخصیص سرمایه روزانه اجرا شود؛ سرمایه ابتدا بین کارشناسان فروش تقسیم می‌شود؛ سپس سهم مشتریان هر کارشناس محاسبه می‌شود؛ اگر سرمایهٔ تخصیص‌یافته به کارشناس صفر باشد اعتبار مشتری هم صفر می‌شود.

   **بخش ۵ — دلایل صفر شدن اعتبار (لیست):** بدهی معوق؛ نداشتن کارشناس مسئول؛ صفر بودن سرمایهٔ کارشناس مسئول؛ صفر بودن امتیاز وزنی؛ نامعتبر بودن وزن پارامترها برای ماه جاری؛ اجرا نشدن مجدد تخصیص سرمایه بعد از تغییرات؛ ساخته نشدن رکورد تخصیص اعتبار برای مشتری.

   **بخش ۶ — مسیر پیشنهادی (گام‌های شماره‌دار):** ۱) مشتری را ایجاد یا ویرایش کنید. ۲) کارشناس مسئول را مشخص کنید. ۳) وارد صفحهٔ اعتبار مشتری شوید. ۴) همهٔ پارامترهای امتیازدهی را وارد و ذخیره کنید. ۵) وزن پارامترها را بررسی کنید. ۶) تخصیص سرمایه روزانه را اجرا کنید. ۷) صفحهٔ اعتبار مشتری را refresh کنید. ۸) بخش «سقف اعتبار — محاسبه زنده» را بررسی کنید.

   **بخش ۷ — خطای رایج:** «ممکن است همهٔ فیلدهای مشتری پر شده باشند، اما چون وزن‌ها برای دورهٔ فعلی معتبر نیستند یا سرمایهٔ کارشناس صفر است، سقف اعتبار همچنان صفر نمایش داده شود.»

6. صفحه ریسپانسیو باشد و در موبایل متن‌ها خوانا بمانند.
7. مخاطب ادمین/حسابدار است، نه برنامه‌نویس: از اصطلاحات فنی مثل `binding_constraint` فقط در صورت لزوم و همراه با توضیح فارسی استفاده کن.
8. دسترسی صفحه هم‌سطح دسترسی صفحهٔ مشتریان باشد.

**چک‌های تأیید:** typecheck + build + تأیید ثبت مسیر جدید در routeTree.

---

## فاز ۷ — مورد ۱۳۴: چهار نوع مستقل فیش واریزی

**وضعیت فعلی تأییدشده:** در `src/shared/components/PaymentReceiptForm.tsx` فقط دو نوع وجود دارد (`payment`, `prepayment`) و اگر `receipt_type === "payment"` باشد انتخاب حداقل یک پیش‌فاکتور اجباری است (خطای «حداقل یک پیش‌فاکتور برای اتصال انتخاب کنید»). صفحهٔ ثبت: `src/routes/_app.accounting.receipts.create.tsx` که فرم را رندر می‌کند.

**مقادیر جدید:**
| مقدار | عنوان فارسی | رفتار |
|---|---|---|
| `invoice_payment` | پرداخت پیش‌فاکتور | انتخاب حداقل یک پیش‌فاکتور **اجباری** |
| `debt_payment` | پرداخت بدهی | بدون پیش‌فاکتور ثبت شود |
| `prepayment` | پیش‌واریز | بدون پیش‌فاکتور ثبت شود |
| `positive_credit` | اعتبار مثبت مستقل | بدون پیش‌فاکتور، به‌عنوان اعتبار مثبت قابل گزارش |

> **هشدار ایمنی — این تنها فازی است که دادهٔ مالی موجود را تغییر می‌دهد و برگشت‌ناپذیر است.** تبدیل `payment → invoice_payment` روی رکوردهای واقعی `payment_receipts` اجرا می‌شود.
> - در بالای فایل migration، به‌صورت کامنت، دستور بکاپ لازم را بنویس تا کاربر قبل از اعمال اجرا کند:
>   `CREATE TABLE payment_receipts_backup_<تاریخ> AS SELECT * FROM public.payment_receipts;`
> - در همان کامنت، SQL بازگشت (rollback) را هم بنویس: نحوهٔ برگرداندن مقادیر به `payment` و بازگرداندن constraint قبلی.
> - در گزارش پایانی صریح بنویس که این migration بدون بکاپ نباید اعمال شود.

1. **قبل از هر کاری**، تمام نقاط استفاده از `receipt_type` را در پروژه پیدا کن: فرم، صفحهٔ لیست `_app.accounting.receipts.tsx`، صفحهٔ جزئیات `_app.accounting.receipts.$receiptId.tsx`، migrationهای `payment_receipts` و `payment_receipt_links`، و هر RPC/trigger/report/journal که به آن شرط دارد. فهرست را در گزارش بیاور.
2. **نام دقیق constraint فعلی را از migrationهای موجود پیدا کن** (فرض نکن `payment_receipts_receipt_type_check` است). migration جدید بساز که constraint را با چهار مقدار بالا جایگزین کند.
3. مهاجرت داده در همان migration: `payment` → `invoice_payment`؛ `prepayment` بدون تغییر باقی بماند. ترتیب درست: اول داده‌ها را تبدیل کن، بعد constraint جدید را بگذار.
4. schema فرم: `z.enum(["payment","prepayment"])` → `z.enum(["invoice_payment","debt_payment","prepayment","positive_credit"])`. مقدار پیش‌فرض `invoice_payment`.
5. Select «نوع فیش» چهار گزینه با عناوین فارسی جدول بالا نشان دهد.
6. متن راهنمای زیر Select بر اساس انتخاب:
   - `invoice_payment`: «این فیش باید به یک یا چند پیش‌فاکتور مشتری متصل شود.»
   - `debt_payment`: «برای پرداخت بدهی نیازی به انتخاب پیش‌فاکتور نیست.»
   - `prepayment`: «این مبلغ به عنوان پیش‌واریز مشتری ثبت می‌شود.»
   - `positive_credit`: «این مبلغ به عنوان اعتبار مثبت مستقل برای مشتری ثبت می‌شود.»
7. بخش «اتصال به پیش‌فاکتورها» فقط وقتی `receipt_type === "invoice_payment"` نمایش داده شود.
8. اعتبارسنجی تخصیص (تعداد ≥ ۱، مجموع ≤ مبلغ فیش، هر تخصیص ≤ ماندهٔ پیش‌فاکتور) فقط برای `invoice_payment` اجرا شود. پیام خالی بودن: «برای پرداخت پیش‌فاکتور، حداقل یک پیش‌فاکتور انتخاب کنید.»
9. درج رکورد در `payment_receipt_links` فقط برای `invoice_payment`.
10. **هر شرط `receipt_type === "payment"` در کل کد را تک‌تک بررسی کن**: اگر منظور «پرداخت متصل به پیش‌فاکتور» است → `invoice_payment`؛ اگر منظور «هر دریافت پول از مشتری» است → subset درست از چهار نوع. رفتار قبلی نباید بی‌سروصدا عوض شود؛ هر تصمیم را در گزارش بنویس.
11. منطق سند حسابداری/Journal اگر روی `payment`/`prepayment` شرط دارد به‌روزرسانی شود: `invoice_payment` تسویهٔ پیش‌فاکتور متصل، `debt_payment` کاهش بدهی بدون لینک، `prepayment` پیش‌واریز، `positive_credit` اعتبار مثبت مستقل.
12. label فارسی نوع فیش در لیست و جزئیات فیش‌ها به چهار حالت مجهز شود.
13. اگر گزارش‌ها یا فیلترهای فیش، فیلتر نوع دارند، گزینه‌های جدید اضافه شود.
14. تایپ‌ها: اگر type generated تغییر نمی‌کند، type محلی فرم را اصلاح کن.

**چک‌های تأیید:** typecheck + build + تأیید اینکه هیچ `"payment"` یتیمی در منطق شرطی باقی نمانده.
**سناریوهای تست دستی (در گزارش بنویس):** `invoice_payment` بدون پیش‌فاکتور → خطا؛ با پیش‌فاکتور → ثبت + ساخت لینک؛ سه نوع دیگر بدون پیش‌فاکتور → ثبت موفق؛ رکوردهای قدیمی `payment` بعد از migration → `invoice_payment`.

---

## فاز ۸ — مورد ۱۳۵: مارکرهای «ثبت شد» و «ارسال شد» پیش‌فاکتور

**فایل‌های تأییدشده:** لیست `src/routes/_app.sales_.invoices.tsx` — جزئیات `src/routes/_app.sales_.invoices_.$invoiceId.tsx`.

1. **اول** schema فعلی `invoices` و الگوی ارتباط user/profile در پروژه را بررسی کن (FK به `profiles` یا `auth.users`) و **همان الگوی غالب پروژه** را رعایت کن.
2. migration جدید:
   - ستون‌ها روی `public.invoices`: `accounting_registered_at timestamptz null`, `accounting_registered_by uuid null`, `accounting_sent_at timestamptz null`, `accounting_sent_by uuid null` (FK طبق بند ۱).
   - ایندکس‌ها: `idx_invoices_accounting_registered_at`, `idx_invoices_accounting_sent_at`.
   - اگر پروژه trigger audit برای این جدول دارد، تغییر این ستون‌ها هم ثبت شود.
3. RPC امن: `set_invoice_accounting_marker(p_invoice_id uuid, p_marker text, p_checked boolean)`
   - `p_marker` فقط `registered` یا `sent` را بپذیرد، وگرنه خطا.
   - `p_checked = true`: برای `registered` مقدار `accounting_registered_at = now()` و `accounting_registered_by = auth.uid()`؛ برای `sent` معادل‌های خودش.
   - `p_checked = false`: **فقط** `admin` یا `accountant` مجاز باشد؛ فیلدهای مربوطه `null` شوند.
   - اگر پیش‌فاکتور `canceled` است، هیچ مارکر جدیدی ثبت نشود.
4. دسترسی: `admin` و `accountant` تیک زدن و لغو؛ `manager` در صورت سازگاری با سیاست پروژه فقط تیک زدن؛ `sales` فقط مشاهده مگر اینکه الگوی موجود پروژه خلافش را نشان دهد. **قبل از تصمیم، الگوی نقش‌های موجود در RPCهای مشابه را بررسی کن.**
5. در صفحهٔ لیست، query را گسترش بده تا چهار ستون جدید را بگیرد.
6. در ستون عملیات دو کنترل اضافه کن: «ثبت شد» و «ارسال شد».
   - دارای مقدار → حالت تیک‌خورده با رنگ موفق.
   - بدون مقدار → حالت `outline`.
7. کلیک «ثبت شد»: RPC با `p_marker="registered"`, `p_checked=true` → invalidate/refetch لیست → toast «ثبت در حسابداری علامت خورد.»
8. کلیک «ارسال شد»: RPC با `p_marker="sent"`, `p_checked=true` → invalidate/refetch → toast «ارسال پیش‌فاکتور علامت خورد.»
9. لغو (برای admin/accountant): با `AlertDialog` تأیید بگیر — «آیا می‌خواهید این علامت را لغو کنید؟» — سپس RPC با `p_checked=false`.
10. Tooltip یا متن ریز نشان دهد چه کسی و چه زمانی تیک زده. اگر join به `profiles` ساده بود نام کاربر، وگرنه حداقل تاریخ.
11. در صفحهٔ جزئیات پیش‌فاکتور همین دو وضعیت را در کارت اطلاعات نمایش بده («ثبت در حسابداری آسان» و «ارسال برای مشتری»)؛ اگر ساده بود همان دکمه‌ها را هم بگذار.
12. این دو مارکر **مستقل** از `invoices.status` هستند؛ `draft`, `pending_accountant`, `final`, `canceled` دست‌نخورده بمانند.
13. اگر `sent` زده شد ولی `registered` خالی است: اجازه بده، اما هشدار کوچک نشان بده — «این پیش‌فاکتور هنوز به عنوان ثبت‌شده در حسابداری علامت نخورده است.»
14. audit log برای هر تغییر با این actionها: `invoice_accounting_registered_checked`, `invoice_accounting_registered_unchecked`, `invoice_accounting_sent_checked`, `invoice_accounting_sent_unchecked`.

**چک‌های تأیید:** typecheck + build.
**سناریوهای تست دستی (در گزارش):** تیک ثبت شد و باقی ماندن بعد از refresh؛ استقلال «ارسال شد»؛ لغو با admin/accountant → null؛ عدم امکان لغو با نقش غیرمجاز؛ عدم پذیرش مارکر روی پیش‌فاکتور canceled.

---

## فاز ۹ — مورد ۱۳۶: خروجی اکسل لیست قیمت فروش

**فایل‌های تأییدشده:** ایجاد لیست جدید `src/routes/_app.pricing.sale-lists_.new.tsx` — جزئیات/ویرایش `src/routes/_app.pricing.sale-lists_.$listId.tsx` — فهرست `src/routes/_app.pricing.sale-lists.tsx`.

> **تصحیح:** کتابخانهٔ `xlsx` نسخهٔ `^0.18.5` **از قبل در پروژه نصب است**. هیچ dependency جدیدی اضافه نکن و سراغ fallback CSV نرو — خروجی باید `.xlsx` واقعی باشد.

1. helper جدا بساز: `src/lib/export/sale-price-list-excel.ts`
   - ورودی: آرایهٔ typed از ردیف‌های لیست قیمت (تایپ صریح تعریف کن، `any` ممنوع).
   - خروجی: ساخت workbook و دانلود در مرورگر (بدون ذخیره روی سرور).
   - طوری بنویس که در صفحهٔ ویرایش هم قابل استفادهٔ مجدد باشد.
2. در صفحهٔ ایجاد لیست جدید، دکمهٔ «خروجی اکسل» با آیکون `FileSpreadsheet` یا `Download` کنار دکمه‌های اصلی (ذخیره/ایجاد لیست) یا در toolbar بالای جدول محصولات.
3. دکمه فقط وقتی فعال باشد که ردیفی برای خروجی وجود دارد. اگر لیست هنوز ذخیره نشده، از همان state/جدول فعلی خروجی بگیرد.
4. ستون‌های خروجی: ردیف، کد کالا/SKU، نام کالا، برند، دسته‌بندی، وضعیت موجودی، قیمت خرید، قیمت فروش، نوع قیمت/لیست قیمت، حاشیه سود یا درصد سود، تاریخ ایجاد خروجی، توضیحات.
   - ستون‌هایی که داده‌شان در state/query موجود نیست حذف شوند.
   - **اجباری:** SKU، نام کالا، قیمت فروش.
5. نام فایل: `sale-price-list-YYYY-MM-DD.xlsx`.
6. کیفیت فایل: نام شیت `Sale Price List`؛ headerها فارسی؛ عرض ستون‌ها خوانا؛ **قیمت‌ها به‌صورت عدد واقعی** (نه رشتهٔ فارسی‌شده) تا در اکسل قابل جمع و فیلتر باشند.
7. UX: بدون محصول → toast هشدار «محصولی برای خروجی گرفتن وجود ندارد.»؛ موفق → «خروجی اکسل آماده شد.»؛ خطا → «خطا در ساخت خروجی اکسل.»
8. بررسی کن آیا همین دکمه در صفحهٔ ویرایش/جزئیات لیست هم منطقی است؛ اگر بله با همان helper اضافه کن و در گزارش ذکر کن.

**چک‌های تأیید:** typecheck + build + تأیید اینکه مقادیر قیمت در workbook از نوع number هستند.

---

## فاز ۱۰ — مورد ۱۳۲.۲: تاریخچهٔ دقیق فیلدی محصول

**وضعیت فعلی تأییدشده:** در `src/routes/_app.products.$id.tsx` کامپوننت `ProductHistoryCard` وجود دارد که از `audit_logs` با `entity_type = "product"` می‌خواند؛ و `src/lib/products/audit.ts` توابع diff فیلدها/برچسب‌ها/attributes را دارد (`diffProductFields`, تایپ `ProductAuditDiff`).

1. کامپوننت جدید: `src/components/products/ProductFieldHistoryDialog.tsx`
2. رفتار: `audit_logs` محصول را بخواند، همهٔ تغییرات را **flatten** کند و هر تغییر را به‌صورت یک ردیف نشان دهد با ستون‌های: فیلد، مقدار قبلی، مقدار جدید، ویرایش‌کننده، زمان تغییر، نوع تغییر (`field` / `label` / `attribute`).
3. فیلترها: بر اساس فیلد، بر اساس کاربر، بازهٔ تاریخ، و نوع تغییر.
4. نمای «تاریخچهٔ فیلد خاص»: با کلیک روی نام یک فیلد، فقط تاریخچهٔ همان فیلد نمایش داده شود (تجربهٔ مشابه تاریخچهٔ سلول در Google Sheets).
5. در صفحهٔ محصول:
   - `ProductHistoryCard` فعلی حفظ یا ارتقا شود (حذف نشود).
   - دکمهٔ «تاریخچه دقیق تغییرات» اضافه شود که این dialog را باز کند.
6. در `src/routes/_app.products.index.tsx` اگر با طراحی جدول سازگار است، دکمهٔ تاریخچه کنار هر محصول اضافه شود؛ اگر شلوغ می‌شود انجام نده و دلیلش را در گزارش بنویس.
7. **مقاوم‌سازی:** اگر رکورد audit قدیمی diff ناقص یا خالی داشت، UI نباید crash کند — پیام «جزئیات تغییر ثبت نشده» نمایش داده شود.
8. خروجی فارسی، RTL و هماهنگ با طراحی فعلی.
9. دسترسی: مطابق دسترسی صفحهٔ محصول عمل کن (چیز جدیدی اختراع نکن).
10. برچسب‌های فارسی فیلدها از `FIELD_LABELS` موجود در `src/lib/products/audit.ts` بازاستفاده شود، نه تعریف دوباره.

**چک‌های تأیید:** typecheck + build + تأیید مدیریت حالت diff ناقص.
**تست دستی (در گزارش):** ویرایش نام/وضعیت موجودی/برچسب/attribute یک محصول → باز کردن dialog → دیدن from/to و actor/time → تست فیلتر فیلد.

---

## فاز ۱۱ — مورد ۱۳۲.۱: ثبت دستی عملکرد روزانهٔ کارشناسان

**زمینه:** ماژول گیمیفیکیشن با `employee_score_events`، `employee_scores`، leaderboard و KPI rules موجود است. فرم ثبت دستی فعلی فقط `manual_adjustment` ثبت می‌کند. نیاز جدید، ثبت **دیتای عملیاتی روزانه** است.

> **قبل از شروع اجباری:** تعریف زندهٔ تابع `calculate_employee_score` و ساختار `employee_score_events` و `gamification_kpis` را از migrationهای موجود استخراج کن. اگر تابع با نام دیگری وجود دارد یا امضایش فرق می‌کند، **متوقف شو و گزارش بده** — چیزی را حدسی بازنویسی نکن.

### دیتابیس
1. migration جدید: `supabase/migrations/<YYYYMMDDHHMMSS>_132_1_employee_daily_manual_metrics.sql`
2. جدول `public.employee_daily_manual_metrics` با ستون‌ها:
   `id uuid primary key default gen_random_uuid()`، `employee_id uuid not null references public.profiles(id)`، `metric_date date not null`، `sales_amount numeric not null default 0`، `profit_amount numeric not null default 0`، `inbound_calls integer not null default 0`، `outbound_calls integer not null default 0`، `talk_minutes numeric not null default 0`، `note text null`، `created_by uuid null references auth.users(id)`، `updated_by uuid null references auth.users(id)`، `created_at timestamptz not null default now()`، `updated_at timestamptz not null default now()`
3. unique index روی `(employee_id, metric_date)`.
4. RLS فعال شود:
   - `admin` / `manager`: select / insert / update.
   - insert فقط برای امروز تا حداکثر ۵ روز گذشته.
   - update فقط وقتی `metric_date >= current_date - interval '5 days'`.
   - delete: فقط `admin` (یا کلاً ممنوع — تصمیمت را در گزارش بنویس).
5. RPC: `upsert_employee_daily_manual_metric(p_employee_id, p_metric_date, p_sales_amount, p_profit_amount, p_inbound_calls, p_outbound_calls, p_talk_minutes, p_note)`
   - نقش `admin`/`manager` را چک کند.
   - محدودیت ۵ روز را enforce کند (پیام خطای فارسی روشن).
   - insert/update انجام دهد و `updated_by`/`updated_at` را ست کند.
   - در `audit_logs` ثبت کند.
   - امتیاز همان کارمند را recalculation کند.
   - رویدادهای متناظر در `employee_score_events` با این `event_type`ها ثبت کند: `manual_daily_sales`, `manual_daily_profit`, `manual_inbound_calls`, `manual_outbound_calls`, `manual_talk_minutes` — و مراقب باش ثبت دوبارهٔ همان روز، رویداد تکراری نسازد (upsert/جایگزینی).
6. تابع `calculate_employee_score` را طوری اصلاح کن که علاوه بر منابع فعلی، `employee_daily_manual_metrics` را هم بخواند؛ برای بازه‌های `daily`/`weekly`/`monthly`/`total` این نگاشت اعمال شود: فروش ← `sales_amount`، سود ← `profit_amount`، تماس ورودی ← `inbound_calls`، تماس خروجی ← `outbound_calls`، دقایق مکالمه ← `talk_minutes`. **منابع فعلی نباید حذف شوند.**
7. این KPIها حفظ شوند: `inbound_calls`, `outbound_calls`, `talk_minutes`, `total_sales`, `total_profit`, `sales_per_talk_minute`, `profit_per_talk_minute`.
8. اگر `total_profit` قبلاً auto-disable شده، در همین migration دوباره فعال شود (چون حالا سود دستی داریم).

### فرانت
9. صفحهٔ جدید: `src/routes/_app.gamification.admin.manual-daily-metrics.tsx` روی مسیر `/gamification/admin/manual-daily-metrics` — فقط `admin`/`manager` (با همان الگوی guard صفحات همسایهٔ `_app.gamification.admin.*`).
10. UI شامل: انتخاب کارشناس، انتخاب تاریخ، فروش، سود، تماس ورودی، تماس خروجی، دقایق مکالمه، توضیح، دکمهٔ ثبت/ویرایش، و جدول رکوردهای اخیر با فیلتر تاریخ و کارشناس.
11. محدودیت UI: اگر تاریخ انتخابی بیش از ۵ روز گذشته است، فرم `disabled` شود و پیام واضح نمایش داده شود: «ویرایش فقط تا ۵ روز گذشته مجاز است».
12. بعد از ثبت موفق: toast موفقیت + invalidate کردن queryهای leaderboard، gamification analytics و employee scores تا امتیاز فوراً به‌روز شود.
13. آیتم منو در `src/components/layout/nav-items.ts` زیر بخش گیمیفیکیشن/مدیریت با عنوان «ثبت دستی عملکرد روزانه».
14. اگر `primary-modules.ts` وجود دارد و لازم است، route را در ماژول مناسب ثبت کن.

**چک‌های تأیید:** typecheck + build + تأیید ثبت مسیر در routeTree و وجود آیتم منو.
**تست دستی (در گزارش):** ورود به صفحه؛ ثبت داده برای امروز؛ دیده شدن رکورد در جدول؛ recalculation امتیاز؛ تغییر leaderboard؛ و ممنوع بودن ویرایش برای تاریخ ۶ روز قبل.

---

## فاز ۱۲ — مورد ۱۳۲.۳: پرسش و پاسخ هوش مصنوعی از دانش‌نامه (RAG)

**زمینهٔ تأییدشده:** دانش‌نامه (`/knowledge`، `/knowledge/manage`، جدول `knowledge_documents`) و AI chat مبتنی بر Ollama در `src/routes/api/messenger/ai-chat.ts` موجودند. semantic search برای messenger هست ولی RAG دانش‌نامه نیست.

> این سنگین‌ترین فاز است. اگر pgvector یا زیرساخت embedding در دسترس نبود، **همه‌چیز باید graceful disabled شود، نه شکست بخورد**.

### دیتابیس
1. migration: `supabase/migrations/<YYYYMMDDHHMMSS>_132_3_knowledge_ai_rag.sql`
2. `create extension if not exists vector;` در صورت نبودن.
3. جدول `knowledge_document_chunks`:
   `id uuid primary key default gen_random_uuid()`، `document_id uuid not null references knowledge_documents(id) on delete cascade`، `chunk_index integer not null`، `content text not null`، `embedding vector(1536) null`، `created_at timestamptz default now()`، `updated_at timestamptz default now()`
4. ایندکس مناسب vector search — **دقیقاً از الگوی `message_embeddings` موجود در پروژه تقلید کن** (نوع ایندکس و عملگر فاصله باید یکسان باشند).
5. RPC: `search_knowledge_chunks(p_query_embedding vector, p_limit int default 6)` با خروجی: `chunk_id`, `document_id`, `title`, `category`, `content`, `similarity`.
6. RLS: کاربران عادی فقط از اسناد `is_published = true` استفاده کنند؛ `admin`/`manager` همه را ببینند؛ اگر منطق `access_level` در پروژه وجود دارد رعایت شود.

### بک‌اند
7. endpoint جدید: `src/routes/api/knowledge/ask.ts` (از الگوی فایل و احراز هویت `api/messenger/ai-chat.ts` پیروی کن):
   - کاربر را احراز هویت کند.
   - `question` را بگیرد، embedding بسازد، chunkهای مرتبط را از Supabase بگیرد.
   - prompt طوری ساخته شود که مدل **فقط** بر اساس context پاسخ دهد.
   - اگر پاسخ در اسناد نبود: «در اسناد موجود پاسخی پیدا نکردم.»
   - پاسخ فقط فارسی، کوتاه، دقیق و سازمانی؛ حدس زدن ممنوع.
   - `sources` برگردانده شود: `document_id`, `title`, یک excerpt کوتاه از متن منطبق، و `similarity`.
8. embedding: اگر `OLLAMA_EMBEDDING_API_URL` یا مدل embedding در env تنظیم شده باشد استفاده شود؛ اگر نبود، قابلیت graceful disabled شود و پیام مناسب برگردد. **هیچ سرویس خارجی بدون env صدا زده نشود.**
9. برای تولید پاسخ از همان تنظیمات فعلی Ollama استفاده کن: `OLLAMA_API_URL`, `OLLAMA_MODEL`, `OLLAMA_API_KEY`.

### chunking و ایندکس
10. utility: `src/lib/knowledge/chunking.ts` که محتوای اسناد را به chunkهای قابل جستجو تقسیم کند (اندازه و overlap منطقی؛ در گزارش ذکر کن).
11. عملیات `rebuildKnowledgeEmbeddings` (server function یا admin action) که اسناد منتشرشده را chunk کند، embedding بسازد و در `knowledge_document_chunks` upsert کند.
12. در `/knowledge/manage` برای `admin`/`manager` دکمهٔ «بازسازی ایندکس هوش مصنوعی» با وضعیت loading و toast نتیجه.

### فرانت
13. صفحهٔ جدید: `src/routes/_app.knowledge.ask.tsx` روی مسیر `/knowledge/ask` شامل: textarea سؤال، دکمهٔ پرسیدن، نمایش پاسخ، نمایش منابع استفاده‌شده (با لینک به سند)، وضعیت loading، و پیام خطای روشن وقتی AI/embedding غیرفعال است.
14. در `/knowledge` دکمهٔ «پرسش از دانش‌نامه» اضافه شود.
15. در صورت نیاز آیتم منو در `nav-items.ts`.

**چک‌های تأیید:** typecheck + build.
**تست دستی (در گزارش):** انتشار یک سند قوانین فروش → rebuild ایندکس → پرسش مرتبط (پاسخ مبتنی بر سند + نمایش منبع) → پرسش نامرتبط (باید بگوید در اسناد پاسخی پیدا نکردم).
**در گزارش حتماً فهرست کامل envهای لازم را بنویس.**

---

## گزارش پایان هر batch

1. جدول خلاصه: هر فاز → وضعیت (انجام شد / از قبل بود / متوقف شد) → commit SHA.
2. فهرست کامل فایل‌های تغییریافته یا ساخته‌شده، گروه‌بندی‌شده بر اساس فاز.
3. **فهرست migrationهای ساخته‌شده که هنوز روی دیتابیس زندهٔ `afrakala` اعمال نشده‌اند** — با ترتیب دقیق اعمال، و برای فاز ۷ حتماً با یادآوری الزام بکاپ.
4. فهرست envهای جدید مورد نیاز (فاز ۱۲).
5. نتیجهٔ نهایی `npm run typecheck` و `npm run build`.
6. فهرست commitهای آمادهٔ push (**بدون اجرای push**) و درخواست تأیید.
7. هر تصمیم مبهمی که خودت گرفتی و بهتر است بازبینی شود.
8. صفحاتی که برای دیده شدن تغییرات نیاز به rebuild فرانت دارند.
9. تأیید اینکه هیچ فایل مستندات/پرامپتی وارد commitها نشده است.