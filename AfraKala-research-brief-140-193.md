# بریف تحقیق عمیق — آیتم‌های ۱۴۰ تا ۱۹۳ (افراکالا)

> **این سند یک مأموریت تحقیق است، نه مأموریت اجرا.**
> هدف: تولید یک گزارش مستند و قابل‌اتکا که وضعیت واقعی هر ۵۴ آیتم را مشخص کند، تا بر پایه‌اش پرامپت اجرایی نوشته شود.
> نحوه استفاده: این فایل را در ریشه پروژه ذخیره کن و به Claude Code بگو:
> `Read AfraKala-research-brief-140-193.md completely and execute it. Research only — write no application code.`

---

## بخش ۰ — قواعد بازی (RULES OF ENGAGEMENT)

این قواعد بر هر چیز دیگری در این سند اولویت دارند.

### ۰.۱ ممنوعیت‌های مطلق

- **هیچ کد اپلیکیشنی ننویس.** نه فیچر، نه fix، نه refactor، حتی اگر باگ واضحی دیدی.
- **هیچ migration ننویس و هیچ migration اجرا نکن.**
- **هیچ عملیات نوشتن روی دیتابیس انجام نده.** فقط `SELECT`، `\d`، `pg_get_functiondef`، و کوئری‌های introspection. هیچ `INSERT/UPDATE/DELETE/CREATE/ALTER/DROP/TRUNCATE`.
- **branch را عوض نکن، merge/rebase/cherry-pick نکن، چیزی commit یا push نکن.**
- **هیچ داده‌ای را حذف نکن** — به‌خصوص محصولات `QA-` با SKU `AFK-2026-00402..00421` که هنوز تصمیم‌شان گرفته نشده.
- **container ها را restart نکن**، `docker compose down/up` نزن.

تنها فایلی که مجاز به نوشتنش هستی، فایل‌های گزارش داخل `docs/research/` است.

### ۰.۲ استاندارد شواهد (EVIDENCE STANDARD)

**هر ادعا باید شاهد داشته باشد.** ادعای بدون شاهد را اصلاً ننویس.

شاهد قابل‌قبول فقط یکی از این‌هاست:

| نوع ادعا | شاهد لازم |
|---|---|
| «این UI وجود دارد» | مسیر فایل + شماره خط + نام کامپوننت |
| «این route وجود دارد» | نام فایل route + مسیر URL تولیدشده + وجود/عدم وجود guard |
| «این تابع دیتابیس این کار را می‌کند» | نام تابع + شماره migration + خروجی `pg_get_functiondef` (فقط بخش مربوطه) |
| «این جدول وجود دارد / ندارد» | خروجی کوئری `information_schema.tables` یا `\d table_name` |
| «این دسترسی برای این نقش هست» | ردیف‌های واقعی `role_permissions` + کد guard در فرانت |
| «این کار نمی‌کند» | یا خطای واقعی، یا خط کدی که مانع می‌شود، یا نبودِ اثبات‌شدهٔ کد |
| «این داده وجود دارد» | خروجی `SELECT count(*)` یا نمونهٔ واقعی |

**اگر بعد از جستجو چیزی پیدا نکردی، بنویس چه چیزهایی را جستجو کردی** (الگوهای `rg`، نام جداول، نام route ها). «پیدا نکردم» بدون ذکر روش جستجو، جواب معتبر نیست.

### ۰.۳ طبقه‌بندی وضعیت (VERDICT TAXONOMY)

برای هر آیتم دقیقاً یکی از این ۵ برچسب را بده — نه بیشتر، نه مبهم:

- **`✅ کامل`** — ساخته شده، به بک‌اند وصل است، در برنچی که سرور از آن build شده وجود دارد، و دسترسی‌اش درست است.
- **`⚠️ ناقص`** — ساخته شده ولی یک حلقه از زنجیره شکسته است (UI هست بک‌اند نیست / بک‌اند هست UI نیست / کامپوننت هست ولی mount نشده / روی main هست روی برنچ سروری نیست / دسترسی seed نشده).
- **`🔶 جزئی`** — بخشی از نیازمندی پوشش داده شده، بخشی نه. باید دقیقاً بنویسی کدام بخش هست و کدام نیست.
- **`❌ وجود ندارد`** — نه در کد، نه در دیتابیس. باید بنویسی چه چیزهایی را جستجو کردی که به این نتیجه رسیدی.
- **`❓ نامشخص`** — نتوانستی قطعی کنی. باید بنویسی دقیقاً چه چیزی مانع شد و برای قطعی‌شدن چه لازم است.

### ۰.۴ قوانین ضدخوش‌بینی (این‌ها از اشتباهات واقعی این پروژه آمده‌اند)

این چهار قانون را در هر آیتم اعمال کن:

1. **کامپوننتِ mount‌نشده = وجود ندارد.**
   قبلاً `SalesReminderPopup.tsx` روی هر دو برنچ وجود داشت، ولی روی برنچ سروری هیچ‌جا رندر نمی‌شد. برای هر کامپوننتی که پیدا می‌کنی، با `rg` بگرد ببین **واقعاً کجا استفاده شده**. اگر هیچ import/render فعالی ندارد، وضعیتش `⚠️ ناقص` است نه `✅`.

2. **UI بدون اتصال به داده = وجود ندارد.**
   اگر صفحه‌ای هست ولی داده‌اش hardcode/mock است یا hook‌اش خطا می‌دهد، `⚠️` است.

3. **کد روی `main` ≠ کد روی سرور.**
   دو جریان commit موازی وجود دارد. برای هر یافته باید مشخص کنی روی **کدام برنچ** است. اگر روی برنچ سروری نیست، برای کاربر عملاً وجود ندارد.

4. **ماژول seed‌نشده در `role_permissions` = دسترسی باز برای همه.**
   تابع `has_dynamic_permission` یک fallback خطرناک دارد: اگر ماژول در `role_permissions` هیچ ردیفی نداشته باشد، به همه نقش‌ها دسترسی می‌دهد. برای هر ماژولی که بررسی می‌کنی، چک کن آیا اصلاً seed شده یا نه — و اگر نشده، این را به‌عنوان ریسک امنیتی گزارش کن.

### ۰.۵ منابعی که نباید به آن‌ها اعتماد کنی

- **`schema_full_export.sql` قدیمی است** (حدود ۲۵ جدول عقب‌تر از schema زنده). هرگز به‌عنوان مرجع استفاده نکن. مرجع، دیتابیس زنده است.
- **دیتابیس واقعی `afrakala` است، نه `postgres`.** یک دیتابیس `postgres` کهنه با کپی قدیمی داده و schema منسوخ وجود دارد. اگر اشتباهاً به آن وصل شوی، همه نتایج غلط می‌شوند. در هر اتصال `-d afrakala` را صریح بنویس.
- **نام فایل migration را با شماره‌اش اشتباه نگیر.** glob مثل `*150*` با timestamp‌های حاوی «150» هم match می‌کند. همیشه محتوای فایل را باز کن.

---

## بخش ۱ — تثبیت محیط (اولین کار، قبل از هر تحقیقی)

خروجی این بخش را به‌عنوان «فصل ۰» گزارش بنویس. تا این بخش تمام نشود، سراغ آیتم‌ها نرو.

### ۱.۱ محل پروژه و وضعیت git

```powershell
cd D:\AfraKalaTest\app
pwd
git remote -v
git branch -a
git branch --show-current
git log --oneline -5
git status --short
```

اگر این مسیر وجود نداشت، مسیر جایگزین را چک کن: `C:\Users\AFRA\AfraKala\get-git-going`. هر کدام که remote آن `mohammadrezaafra66-arch/get-git-going` بود، درست است. مسیر نهایی را در گزارش بنویس.

### ۱.۲ اندازه‌گیری واگرایی دو برنچ (حیاتی)

```powershell
git fetch --all
git rev-list --left-right --count main...security/rls-permissive-select-fix
git log --oneline main..security/rls-permissive-select-fix
git log --oneline security/rls-permissive-select-fix..main
git diff --stat main security/rls-permissive-select-fix
```

در گزارش بنویس:
- چند commit هر طرف جلوتر است
- **لیست کامل فایل‌هایی که بین دو برنچ فرق دارند** (این لیست را نگه دار، در تحقیق آیتم‌ها لازمت می‌شود)
- کدام فایل‌های حساس در این لیست هستند: `routeTree.gen.ts`، هر چیزی در `supabase/migrations/`، `src/routes/_app.sales.search.tsx`

### ۱.۳ تشخیص اینکه سرور از کدام برنچ build شده

سرور روی `http://192.168.170.8:3100` است. راه‌های تشخیص:
- پوشه‌ای که build سرور از آن ساخته می‌شود را پیدا کن (docker-compose را بخوان: سرویس frontend، volume/build context)
- در آن پوشه `git branch --show-current` و `git log -1 --oneline`
- اگر دسترسی نداشتی، از روی feature‌های اختصاصی هر برنچ استنتاج کن و روش استنتاجت را بنویس

**این مهم‌ترین متغیر کل تحقیق است.** هر آیتمی که روی برنچ سروری نباشد، برای کاربر «ساخته نشده» است حتی اگر روی main باشد.

### ۱.۴ اتصال به دیتابیس

الگوی درست (اجتناب از مشکل quoting در PowerShell):

```powershell
$pw = (docker exec afrakala-lan-db printenv POSTGRES_PASSWORD).Trim()
@"
SELECT current_database(), current_user, version();
"@ | docker exec -i -e PGPASSWORD=$pw afrakala-lan-db psql -U supabase_admin -d afrakala
```

اگر `supabase_admin` جواب نداد، `-U postgres` را امتحان کن. **از `psql -c` استفاده نکن** — با SQL چندخطی و nested quote در PowerShell خراب می‌شود.

### ۱.۵ نقشهٔ کلی سیستم (بگیر و در گزارش بگذار)

```sql
-- فهرست کامل جداول
SELECT table_name FROM information_schema.tables
WHERE table_schema='public' AND table_type='BASE TABLE' ORDER BY 1;

-- فهرست کامل view ها
SELECT table_name FROM information_schema.views WHERE table_schema='public' ORDER BY 1;

-- فهرست کامل توابع
SELECT p.proname, pg_get_function_identity_arguments(p.oid) AS args
FROM pg_proc p JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname='public' ORDER BY 1;

-- ماتریس دسترسی کامل
SELECT role, module, action FROM public.role_permissions ORDER BY module, action, role;

-- ماژول‌هایی که در ماتریس seed نشده‌اند (ریسک fallback باز)
SELECT DISTINCT module FROM public.role_permissions ORDER BY 1;

-- سیاست‌های RLS
SELECT tablename, policyname, cmd, permissive, roles, qual::text
FROM pg_policies WHERE schemaname='public' ORDER BY tablename, policyname;
```

و از سمت کد:

```powershell
Get-ChildItem src\routes -Filter *.tsx -Recurse | Select-Object -ExpandProperty Name | Sort-Object
Get-ChildItem supabase\migrations -Filter *.sql | Sort-Object Name | Select-Object -Last 20
```

فهرست route ها و فهرست جداول را در گزارش بگذار — این‌ها مرجع بقیهٔ تحقیق‌اند.

---

## بخش ۲ — روش تحقیق برای هر آیتم

برای هر آیتم، **چهار لایه** را جدا بررسی کن و در گزارش جدا بنویس. اگر هر لایه‌ای شکسته باشد، کل آیتم `✅` نیست:

| لایه | چه چیزی را چک می‌کنی |
|---|---|
| **L1 — رابط کاربری** | آیا صفحه/دکمه/فرم وجود دارد؟ کجا mount شده؟ آیا واقعاً از منوی سیستم قابل رسیدن است؟ |
| **L2 — منطق فرانت** | hook/query/mutation کدام است؟ کدام تابع بک‌اند را صدا می‌زند؟ آیا داده واقعی است یا mock؟ |
| **L3 — بک‌اند/دیتابیس** | جدول‌ها، ستون‌ها، توابع RPC، trigger ها، constraint ها. آیا واقعاً وجود دارند؟ |
| **L4 — دسترسی** | route guard چیست؟ در `role_permissions` چه seed شده؟ RLS چه اجازه‌ای می‌دهد؟ |

### الگوهای جستجوی مفید

```powershell
# پیدا کردن route ها
rg -n "createFileRoute" src/routes | rg -i "<keyword>"

# پیدا کردن استفاده واقعی یک کامپوننت (قانون ۰.۴.۱)
rg -n "<ComponentName>" src --type tsx

# پیدا کردن فراخوانی RPC ها
rg -n "\.rpc\(" src -A 2

# پیدا کردن guard ها و چک دسترسی
rg -n "requirePermission|hasPermissionEx|has_dynamic_permission|has_role|has_any_role" src

# پیدا کردن نام جدول در کد
rg -n "from\(['\"]<table_name>" src
```

### مدیریت حجم کار (مهم)

۵۴ آیتم در یک نشست جا نمی‌شود. این ترتیب را رعایت کن:

1. بخش ۱ (تثبیت محیط) را کامل کن و **بلافاصله در فایل بنویس**.
2. پکیج‌های کاری را **به ترتیب A تا K** برو.
3. **بعد از پایان هر پکیج، نتیجه‌اش را روی دیسک بنویس** — منتظر پایان همه نمان. اگر context تمام شد، کار از دست نمی‌رود.
4. جایی که پکیج‌ها به هم وابسته نیستند، می‌توانی از subagent های موازی استفاده کنی — ولی هر subagent باید همین استاندارد شواهد را رعایت کند و خروجی‌اش را در فایل خودش بنویسد.
5. در پایان، فایل خلاصه (`00-SUMMARY.md`) را بساز.

---

## بخش ۳ — پکیج‌های کاری

### نگاشت آیتم ← پکیج (هیچ آیتمی نباید جا بیفتد)

| پکیج | آیتم‌ها |
|---|---|
| A — سرمایه روز و امتیاز کارشناس | ۱۴۰(ب)، ۱۴۱٫۱، ۱۴۱٫۲، ۱۴۱٫۳، ۱۵۳، ۱۵۴، ۱۵۵ |
| B — ورود دستی متریک + گیمیفیکیشن | ۱۴۳، ۱۵۶، ۱۵۷، ۱۵۸، ۱۵۹، ۱۶۰، ۱۶۱، ۱۶۲ |
| C — جستجوی سریع فروش | ۱۴۲، ۱۴۶ |
| D — فروش، پیش‌فاکتور، ویزیتور | ۱۴۷، ۱۴۸، ۱۵۲، ۱۷۴، ۱۷۵، ۱۷۸، ۱۷۹، ۱۸۴، ۱۸۵، ۱۸۶، ۱۸۷، ۱۸۸، ۱۸۹، ۱۹۰ |
| E — مارکتینگ و پیشنهاد تبلیغات | ۱۴۹، ۱۶۳، ۱۶۴، ۱۶۵، ۱۶۶، ۱۶۷، ۱۶۸، ۱۹۲ |
| F — فیش واریزی، OCR، GPT، خروجی اکسل | ۱۴۰(الف)، ۱۴۴، ۱۹۳ |
| G — اشخاص، ایمپورت، خرید | ۱۴۵، ۱۶۹، ۱۷۰، ۱۷۱، ۱۷۲ |
| H — انبار و موجودی | ۱۷۳، ۱۷۶، ۱۷۷، ۱۸۳ |
| I — خزانه و صندوق | ۱۸۰، ۱۸۱، ۱۸۲ |
| J — گزارش‌های مالی سررسیدی | ۱۵۰، ۱۵۱ |
| K — واحد پول | ۱۹۱ |

---

### پکیج A — سرمایه روز و امتیازدهی کارشناس فروش

**زنجیرهٔ هدفی که کاربر می‌خواهد** (این را به‌عنوان معیار سنجش استفاده کن):

```
حسابدار سرمایه روز را دستی وارد می‌کند
        ↓
سیستم بر اساس امتیاز هر کارشناس فروش، سهم او از سرمایه روز را خودکار محاسبه می‌کند
        ↓
سیستم بر اساس امتیاز هر مشتری، سهم آن مشتری را از سهمِ کارشناسِ خودش محاسبه می‌کند
        ↓
میزان مجاز استفاده هر مشتری از سرمایه مشخص می‌شود
```

**حلقهٔ دوم (کارشناس فروش) مشکوک‌ترین حلقه است** — شواهد قبلی نشان می‌دهد تخصیص فقط در سطح مشتری وجود دارد. این را قطعی کن.

بررسی کن و گزارش بده:

- **A1 (۱۵۵)** — ورود دستی سرمایه روز توسط حسابدار: کدام route؟ کدام جدول (`daily_capital_settings`؟ ستون‌هایش؟)؟ آیا نقش `accountant` دسترسی دارد (هم route guard هم RLS)؟ آخرین رکوردهای واقعی این جدول را نشان بده (`capital_date`, `total_capital`, `scoring_mode`).
- **A2** — `scoring_mode` چه مقادیری می‌گیرد و هر مقدار چه رفتاری دارد؟ در کد کجا شاخه می‌خورد؟
- **A3 (۱۵۳)** — امتیازدهی به کارشناس فروش: تابع `calculate_employee_score` را کامل بخوان و **فرمولش را مرحله‌به‌مرحله به فارسی توضیح بده**. ورودی‌ها از کدام جداول می‌آیند؟ خروجی کجا ذخیره می‌شود؟ چه چیزی آن را trigger می‌کند (cron؟ فراخوانی دستی؟ trigger؟)؟
- **A4 (۱۴۱٫۲)** — منابع امتیاز کارشناس دقیقاً چیست؟ فهرست کامل فاکتورها با نام جدول/ستون منبع. وزن‌ها کجا تعریف شده‌اند — جدول قابل‌تنظیم یا hardcode؟ اگر جدول است، **مقادیر واقعی فعلی‌اش را نشان بده**.
- **A5 (۱۴۱٫۱ + ۱۵۴)** — **آیا اصلاً جدول یا تابعی برای تخصیص سرمایه در سطح کارشناس فروش وجود دارد؟** با `rg` و introspection دیتابیس بگرد. اگر فقط `customer_capital_allocations_dynamic` هست و معادل کارشناس‌محورش نیست، صریح بنویس این حلقه وجود ندارد.
- **A6 (۱۴۱٫۳)** — تخصیص در سطح مشتری: تابع تخصیص کدام است؟ فرمول چیست؟ آیا سهم مشتری از «سهم کارشناسش» محاسبه می‌شود یا مستقیم از کل سرمایه روز؟ (این تفاوت، قلب سؤال ۱۴۱٫۳ است.)
- **A7** — جدول `dynamic_parameter_weights`: ساختار کامل + **همه ردیف‌های فعلی با مقدار `weight`**. اگر وزن‌ها صفر یا NULL هستند، دلیل ریشه‌ای «سقف اعتبار صفر» همین است — تأیید یا رد کن.
- **A8** — صفحه `/accounting/dynamic-capital`: چه کاری می‌کند؟ دکمهٔ «اجرای تخصیص» چه تابعی صدا می‌زند؟ آخرین اجرا کِی بوده؟ (`SELECT count(*), max(created_at) FROM customer_capital_allocations_dynamic;`)
- **A9** — کامپوننت `DynamicScoringSection` و هوک `useDynamicScoring`: مسیر فایل + منطق محاسبهٔ نمایشی. آیا محاسبهٔ سمت فرانت با محاسبهٔ سمت دیتابیس یکی است یا دو منطق موازی داریم؟

**خروجی لازم پکیج A**: یک نمودار متنی از زنجیرهٔ واقعی موجود (نه زنجیرهٔ ایده‌آل)، با علامت‌گذاری اینکه کدام حلقه وجود دارد و کدام نه.

---

### پکیج B — ورود دستی متریک روزانه و گیمیفیکیشن

نیازمندی کاربر (آیتم ۱۴۳ و ۱۵۶ تا ۱۶۲): فرمی که مدیر/کارشناس بتواند برای **تاریخ‌های مختلف** این پنج متریک را دستی وارد کند: میزان فروش، میزان سود، تعداد تماس ورودی، تعداد تماس خروجی، مدت مکالمه — با **پنجرهٔ ویرایش ۵ روزه**، اتصال به گیمیفیکیشن/لیدربرد به‌صورت **لحظه‌ای**، و **وزن‌دهی دستی** هر متریک.

بررسی کن و گزارش بده:

- **B1 (۱۵۶–۱۶۰)** — آیا جدولی برای متریک روزانهٔ هر کارمند وجود دارد؟ (اسم‌های محتمل: `daily_agent_metrics`, `employee_daily_metrics`, `agent_daily_stats`). با introspection کامل جداول بگرد، نه فقط حدس اسم. اگر نیست، صریح بنویس.
- **B2** — اگر جدولی هست: ستون‌هایش، constraint ها، آیا هر پنج متریک را دارد یا فقط بعضی؟
- **B3** — آیا فرم ورود دستی در UI وجود دارد؟ کدام route؟ (قانون ۰.۴.۱ را اعمال کن: اگر کامپوننت هست ولی mount نشده، `⚠️`)
- **B4** — پنجرهٔ ویرایش ۵ روزه: آیا در RLS، constraint، یا کد فرانت اعمال شده؟ کجا؟
- **B5 (۱۶۲)** — وزن‌دهی: جدول `kpi_rules` (یا معادلش) را کامل بخوان. ستون وزن دارد؟ از پنل ادمین قابل ویرایش است؟ route پنل کدام است؟ **ردیف‌های واقعی فعلی را نشان بده** با کلیدهای رویداد (`inbound_call`, `outbound_call`, `sale_closed`, ...).
- **B6 (۱۶۱)** — لیدربرد: از کدام جدول/view می‌خواند؟ مکانیزم به‌روزرسانی چیست — Supabase Realtime، polling با interval، یا فقط refetch دستی؟ **کد مربوطه را نشان بده.** «لحظه‌ای» بودن را با شواهد تأیید یا رد کن.
- **B7** — آیا مسیری وجود دارد که متریک دستی وارد‌شده به امتیاز گیمیفیکیشن تبدیل شود؟ اگر جدول متریک وجود دارد ولی هیچ تابعی آن را نمی‌خواند، این را صریح بنویس.
- **B8 (۱۴۳ — بخش آموزش)** — آیا در پروژه الگوی «راهنمای درون‌صفحه‌ای» وجود دارد (کامپوننت help/tooltip/onboarding/accordion راهنما)؟ اگر بله، نمونه‌اش را با مسیر فایل نشان بده تا بعداً از همان الگو استفاده شود.

---

### پکیج C — جستجوی سریع فروش

- **C1 (۱۴۲)** — مفهوم «مسئول محصول» در سیستم چیست؟ آیا ستونی مثل `responsible_user_id` روی `products` هست، یا از طریق برچسب/تگ تعیین می‌شود، یا جدول واسط دارد؟ کامل بگرد.
- **C2 (۱۴۲)** — تابع `get_sales_search_products` را کامل بخوان (`pg_get_functiondef`). آیا مسئول محصول را برمی‌گرداند؟ اگر نه، آیا داده‌اش در دسترس است که بشود اضافه کرد؟
- **C3** — همین تابع: `ORDER BY` انتهایی‌اش چیست؟ (سؤال باز از قبل: آیا محصولات موجود قبل از ناموجود می‌آیند یا فقط الفبایی؟) عیناً نقل کن.
- **C4** — همین تابع: منطق مخفی‌کردن محصولات ناموجود برای نقش‌های غیرممتاز — دقیقاً چه شرطی و برای چه نقش‌هایی؟
- **C5 (۱۴۶)** — «کپی متن فروش» فعلی: کدام فایل، کدام تابع، خروجی متنی‌اش دقیقاً چطور ساخته می‌شود؟ کد تولید متن را نشان بده.
- **C6 (۱۴۶)** — حالت‌های قیمت (نقدی، تسویه ۱ تا ۵ روزه، سایت، چکی، همکاری...): از کجا می‌آیند؟ جدول یا enum؟ **فهرست کامل حالت‌های موجود را از دیتابیس بگیر و نشان بده** (نه از روی نمونه متن).
- **C7 (۱۴۶)** — آیا در صفحهٔ جستجوی سریع فروش، هیچ زیرساخت انتخاب چندتایی (checkbox/selection state) وجود دارد؟ آیا جای دیگری در پروژه الگوی bulk-selection پیاده شده که بشود از آن تقلید کرد؟
- **C8** — وضعیت `SalesReminderPopup` روی هر دو برنچ: آیا در `_app.sales.search.tsx` برنچ سروری mount شده یا نه؟ (پیگیری مسئلهٔ باز از قبل)

---

### پکیج D — فروش، پیش‌فاکتور، ویزیتور

این بزرگ‌ترین پکیج است. آن را به سه زیربخش تقسیم کن.

#### D-الف: ساختار صفحه فروش (۱۴۷)

- **D1** — فایل route صفحه `/sales` را باز کن. **فهرست کامل کارت‌ها/دکمه‌ها را به ترتیب ظاهرشدن با شماره بنویس** (عنوان فارسی + مسیر مقصد + شرط نمایش/دسترسی). کاربر با CSS selector به `a:nth-child(2)` و `a:nth-child(5)` اشاره کرده؛ باید مشخص کنی این دو دقیقاً کدام‌اند.
- **D2** — برای کارت شماره ۵: مقصدش چه route ای است؟ آن route چه قوانین/اعتبارسنجی/دسترسی‌هایی دارد که کارت شماره ۲ ندارد؟ **تفاوت دو مسیر را دقیق فهرست کن** — این ورودی مستقیم پرامپت اجرایی ادغام است.
- **D3** — آیا این دو مسیر کد مشترک دارند یا دو پیاده‌سازی موازی‌اند؟ اگر موازی، حجم duplication چقدر است؟

#### D-ب: باگ ثبت پیش‌فاکتور و فیش (۱۴۸، ۱۵۲)

- **D4 (۱۴۸)** — صفحه `/sales/invoices/create`: مسیر فایل، فرم، منطق submit. **همهٔ شرط‌هایی که می‌توانند مانع ثبت شوند را فهرست کن** — اعتبارسنجی فرانت، constraint دیتابیس، RLS، trigger، قوانین اعتبار مشتری. برای هرکدام خط کد یا نام constraint را بده.
- **D5 (۱۴۸)** — سناریوی خاص کاربر: مشتری پیش‌واریز کرده و در پیش‌فاکتور «دریافت» زده شده. کدام‌یک از شرط‌های D4 در این سناریو fail می‌شود؟ اگر می‌توانی، پیام خطای واقعی را از لاگ یا از خواندن کد استخراج کن.
- **D6 (۱۴۸)** — صفحه `/accounting/receipts/create` و کامپوننت `PaymentReceiptForm.tsx`: دو نوع فیش (`payment` و `prepayment`) را کامل توضیح بده. اعتبارسنجی «حداقل یک پیش‌فاکتور برای اتصال انتخاب کنید» کجاست (فایل + خط)؟ آیا سمت سرور هم constraint دارد یا فقط فرانت؟
- **D7 (۱۴۸)** — آیا مسیری برای «ثبت دریافت بدون اتصال به پیش‌فاکتور» وجود دارد؟ اگر نه، این محدودیت طراحی است یا باگ؟
- **D8 (۱۵۲)** — وقتی ثبت پیش‌فاکتور به‌خاطر قوانین رد می‌شود: الان چه اتفاقی می‌افتد؟ toast؟ لاگ؟ آیا رکوردی از تلاش ناموفق ذخیره می‌شود؟ آیا جدولی برای «درخواست‌های رد‌شده» وجود دارد؟ آیا سیستم اعلان درون‌برنامه‌ای (notification) دارد که بشود به کاربر ارجاع داد؟

#### D-ج: انبار در فرآیند فروش و ویزیتور (۱۷۴، ۱۷۵، ۱۷۸، ۱۷۹، ۱۸۴، ۱۸۵–۱۹۰)

- **D9 (۱۷۴، ۱۸۴)** — «قطعی کردن پیش‌فاکتور» در کد کجاست؟ چه اتفاقی می‌افتد؟ **آیا هیچ کاهش موجودی انبار رخ می‌دهد؟** trigger یا تابع مربوطه را نشان بده، یا صریح بنویس وجود ندارد.
- **D10 (۱۷۵)** — آیا هیچ چک موجودی قبل از قطعی‌شدن وجود دارد؟
- **D11 (۱۷۸، ۱۷۹)** — آیا در فرم پیش‌فاکتور فیلد انتخاب انبار هست؟ آیا در مرحله قطعی‌شدن قابل تغییر است؟
- **D12 (۱۸۵، ۱۸۹)** — مفهوم «ویزیتور» در سیستم: **آیا اصلاً وجود دارد؟** با `rg` روی `visitor|ویزیتور` و introspection جداول بگرد. اگر نیست، صریح بنویس. اگر هست: جدول، رابطه با `sales_quotes`، اجباری بودن.
- **D13 (۱۸۶، ۱۸۷، ۱۹۰)** — گزارش‌های ویزیتورمحور: وجود دارند؟ کدام route؟ فیلتر دارند؟
- **D14 (۱۸۸)** — منطق «تغییر خودکار ویزیتور بعد از عدم وصول در مهلت تسویه»: آیا هیچ job/cron/trigger ای برای این وجود دارد؟ آیا اصلاً مفهوم «ویزیتور شرکت» تعریف شده؟

---

### پکیج E — مارکتینگ و پیشنهاد تبلیغات

- **E1 (۱۴۹)** — صفحه `/marketing/suggestions`: فایل route، منطق تولید پیشنهاد. الگوریتم فعلی را **مرحله‌به‌مرحله** توضیح بده (وزن برچسب، وزن کانال، موجودی، فروش اخیر — هرکدام با ضریب واقعی از کد/دیتابیس).
- **E2 (۱۴۹)** — ستون دهم جدول این صفحه («سهمیه امروز»): از کجا محاسبه می‌شود؟ آیا انتخاب رندوم در آن دخیل است یا رتبه‌بندی قطعی؟ کد را نشان بده.
- **E3 (۱۶۳)** — تعریف کانال: جدول کانال‌ها وجود دارد؟ از UI قابل افزودن است؟ **کانال‌های موجود فعلی را فهرست کن.**
- **E4 (۱۶۴، ۱۶۵)** — سقف روزانه هر کانال: ستون/جدولی برای آن هست؟ آیا الگوریتم پیشنهاد واقعاً به آن محدود می‌شود؟ (یعنی اگر سقف ۵ باشد، دقیقاً ۵ محصول برمی‌گردد؟)
- **E5 (۱۶۶)** — وزن محصولات در پیشنهاد: کجا تعریف می‌شود؟ آیا همان وزن برچسب است یا وزن مستقل محصول؟
- **E6 (۱۶۷، ۱۶۸)** — آیا انجام‌دادن یک پیشنهاد تبلیغاتی توسط مسئول مارکتینگ ثبت می‌شود؟ آیا به رویداد امتیاز گیمیفیکیشن وصل است؟ کلید رویدادش چیست؟ آیا در `kpi_rules` وجود دارد؟
- **E7 (۱۹۲)** — **«نامزدی تبلیغات» دقیقاً چیست؟** با `rg` روی `نامزد|candidacy|candidate` در کد و دیتابیس بگرد. اگر مفهومی در سیستم است، منطق کامل و شرایط ورود/خروج از حالت «نامزد» را توضیح بده. اگر پیدا نشد، بنویس چه چیزهایی جستجو کردی.

---

### پکیج F — فیش واریزی، OCR، GPT، خروجی اکسل

- **F1 (۱۴۰-الف)** — **باگ خروجی اکسل.** صفحه‌ای که دکمه خروجی اکسل دارد (لیست `payment_receipts`) را پیدا کن. کد export را کامل بخوان. سپس:
  - کوئری PostgREST داخل curl کاربر را عیناً از کد بازسازی کن و مقایسه کن
  - چک کن نام‌های FK داخل select معتبرند: `payment_receipts_destination_bank_account_id_fkey` و `payment_receipts_receiver_party_id_fkey` — با `\d payment_receipts` صحت نام constraint ها را تأیید کن
  - چک کن همهٔ ستون‌های داخل select واقعاً روی `payment_receipts` وجود دارند (یک ستون حذف‌شده کل کوئری را ۴۰۰ می‌کند)
  - چک کن RLS روی `payment_receipts`، `customers`، `bank_accounts`، `external_parties` اجازهٔ join می‌دهد یا نه
  - همان کوئری را با curl از سرور اجرا کن و **خطای واقعی PostgREST را عیناً گزارش کن** (این تنها راه قطعی‌شدن تشخیص است)
  - چک کن `limit=5000` باعث timeout یا محدودیت حافظه نمی‌شود
  - علت ریشه‌ای را با شاهد اعلام کن، نه با حدس
- **F2 (۱۴۴)** — آپلود عکس فیش و استخراج داده: در **اپلیکیشن** (نه سرویس جدا) آیا آپلود تصویر فیش وجود دارد؟ فیلد فایل روی `payment_receipts` هست؟ storage bucket تعریف شده؟
- **F3 (۱۴۴)** — سرویس `ocr-service/` را بررسی کن: وجود دارد؟ محتوایش چیست؟ آیا در `docker-compose` به‌عنوان سرویس تعریف شده؟ آیا اپ فرانت هیچ‌جا آن را صدا می‌زند؟ وضعیت `SUPABASE_SERVICE_KEY` در `.env` آن (فقط بگو پر است یا خالی — **مقدارش را چاپ نکن**). آیا `setup_db.py` اجرا شده (جداولش در دیتابیس هستند)؟
- **F4 (۱۹۳)** — یکپارچگی با مدل‌های زبانی: آیا در پروژه هیچ اتصال LLM وجود دارد (Ollama، OpenAI، edge function، جدول کلید API)؟ الگوی موجودش را نشان بده. آیا برای فیش قابل استفاده است؟ سیستم مدیریت کلید API (که قبلاً ساخته شده) کدام جدول است و آیا کلید نوع «GPT» را پشتیبانی می‌کند؟

---

### پکیج G — اشخاص، ایمپورت اکسل، خرید

- **G1 (۱۶۹)** — مدل «اشخاص»: جدول‌های `customers` و `external_parties` (که در curl آیتم ۱۴۰ دیده شد) چه تفاوتی دارند؟ آیا مفهوم واحدی به نام «شخص» وجود دارد یا چند مدل موازی؟ **این را دقیق روشن کن** چون آیتم ۱۷۲ مستقیماً به آن وابسته است.
- **G2 (۱۴۵، ۱۷۰)** — ایمپورت اکسل: آیا هیچ قابلیت ایمپورتی در سیستم هست (برای هر موجودیتی)؟ کتابخانه‌اش چیست (`xlsx`/SheetJS؟)؟ کدام route؟ اگر برای محصولات هست ولی برای اشخاص نیست (یا برعکس)، صریح بنویس.
- **G3 (۱۷۱)** — ثبت خرید از فروشنده: جدول `purchases`/`purchase_invoices` وجود دارد؟ route دارد؟ فرم دارد؟
- **G4 (۱۷۲)** — برای هر نوع «شخص» موجود، چک کن کدام عملیات ممکن است: خرید، فروش، دریافت، پرداخت، پیش‌فاکتور. **یک جدول ماتریسی بساز**: سطر = نوع شخص، ستون = عملیات، خانه = ✅/❌ با شاهد.

---

### پکیج H — انبار و موجودی

- **H1 (۱۷۶)** — آیا مفهوم «انبار» (چند انباره) وجود دارد؟ جدول `warehouses`؟ یا فقط یک عدد موجودی روی محصول؟ **این سؤال بنیادی است — اگر مدل چندانباره نداریم، آیتم‌های ۱۷۷، ۱۷۸، ۱۷۹، ۱۸۳ همگی خودکار `❌` می‌شوند.**
- **H2 (۱۷۳)** — موجودی چطور نگهداری می‌شود؟ ستون ساده روی `products`، یا جدول حرکات انبار (`stock_movements`)، یا فقط `stock_status` متنی (موجود/محدود/ناموجود)؟
- **H3 (۱۷۳)** — آیا خرید باعث افزایش موجودی می‌شود؟ trigger/تابع را نشان بده یا نبودش را تأیید کن.
- **H4 (۱۷۷)** — انتقال بین انبارها: وجود دارد؟
- **H5 (۱۸۳)** — گزارش ورود/خروج کالا به تفکیک انبار با تاریخ شمسی: وجود دارد؟ آیا اصلاً لاگ حرکات کالا نگهداری می‌شود؟
- **H6** — الگوی تاریخ شمسی در گزارش‌های موجود: کدام کتابخانه/تابع؟ (برای پرامپت اجرایی لازم می‌شود)

---

### پکیج I — خزانه و صندوق

- **I1 (۱۸۰)** — اسناد دریافت/پرداخت: انواع نقدی/چکی/بانکی در `payment_receipts.receipt_type` یا جدول جدا؟ **مقادیر واقعی موجود در ستون را با `SELECT DISTINCT` نشان بده.**
- **I2 (۱۸۰)** — آیا سند «پرداخت» (خروج پول) جدا از «دریافت» وجود دارد یا فقط دریافت داریم؟
- **I3 (۱۸۱)** — مفهوم «صندوق» و «مانده صندوق»: وجود دارد؟ جدول؟ route نمایش؟ اگر نیست، آیا از `bank_accounts` می‌شود استنتاج کرد؟
- **I4 (۱۸۲)** — گزارش ورود/خروج صندوق بر اساس بازه تاریخ: وجود دارد؟

---

### پکیج J — گزارش‌های سررسیدی

- **J1 (۱۵۰)** — گزارش مطالبات بر اساس سررسید (aging receivables): وجود دارد؟ آیا اصلاً فیلد «تاریخ سررسید» روی پیش‌فاکتور/فاکتور وجود دارد؟ **بدون فیلد سررسید، این گزارش غیرممکن است — این را اول چک کن.**
- **J2 (۱۵۱)** — گزارش بدهی بر اساس سررسید: همان بررسی از سمت بدهی.
- **J3** — فهرست کامل گزارش‌های موجود در سیستم (route ها + عنوان فارسی) — تا بدانیم پایه چه هست و گزارش جدید از کدام الگو پیروی کند.
- **J4** — الگوی محاسبهٔ مانده/بدهی مشتری در سیستم فعلی: کدام تابع/view؟ (پایهٔ هر دو گزارش جدید)

---

### پکیج K — واحد پول

- **K1 (۱۹۱)** — **این آیتم تحقیق دقیق و حوصله‌بر می‌خواهد.** برای هر ماژول اصلی مشخص کن: داده در دیتابیس به ریال ذخیره می‌شود یا تومان؟ در UI به کدام واحد نمایش داده می‌شود؟ آیا تبدیلی در میانه انجام می‌شود؟
- **K2** — آیا تابع/کامپوننت مشترکی برای فرمت پول وجود دارد؟ همه‌جا از آن استفاده می‌شود یا فرمت‌کردن محلی هم داریم؟
- **K3** — **یک جدول کامل بساز**: ستون‌ها = ماژول | جدول/ستون دیتابیس | واحد ذخیره | واحد نمایش | فایل نمایش‌دهنده | آیا ناسازگاری هست؟
- **K4** — هر جایی که ناسازگاری یا ریسک اشتباه ضریب ۱۰ دیدی، به‌عنوان یافتهٔ بحرانی جدا علامت بزن.

---

## بخش ۴ — قالب گزارش

### ساختار فایل‌ها

همه در `docs/research/` :

```
docs/research/
  00-SUMMARY.md            ← جدول خلاصه ۵۴ آیتم + یافته‌های بحرانی (آخر بنویس)
  00-ENVIRONMENT.md        ← نتیجه بخش ۱ (اول بنویس)
  A-capital-scoring.md
  B-manual-metrics.md
  C-sales-search.md
  D-sales-quotes.md
  E-marketing.md
  F-receipts-ocr.md
  G-persons-import.md
  H-inventory.md
  I-treasury.md
  J-reports.md
  K-currency.md
```

### قالب هر آیتم (دقیقاً همین ساختار)

```markdown
### آیتم ۱۵۴ — تعیین سهم کارشناس از سرمایه روز بر اساس امتیاز

**وضعیت:** ⚠️ ناقص

**پاسخ کوتاه:** (حداکثر دو جمله — این چیزی است که کاربر اول می‌خواند)

**شواهد:**
- L1 (UI): `src/routes/_app.accounting.dynamic-capital.tsx:1-140` — صفحه وجود دارد، فقط تخصیص مشتری
- L2 (فرانت): هوک `useDynamicScoring` در `src/hooks/...:22` → RPC `allocate_dynamic_capital`
- L3 (دیتابیس): تابع `allocate_dynamic_capital` (migration `...`) — ورودی سرمایه، خروجی سطر برای هر مشتری. هیچ مرحله میانی کارشناس ندارد.
- L4 (دسترسی): guard `requirePermission("accounting","manage")`; در `role_permissions`: admin, accountant

**شکاف نسبت به نیازمندی:** حلقهٔ «سرمایه روز → سهم کارشناس» اصلاً وجود ندارد. تخصیص مستقیم از کل سرمایه به مشتری می‌رود.

**برنچ:** روی هر دو برنچ یکسان است.

**وابستگی‌ها:** پیش‌نیاز ۱۴۱٫۳؛ به امتیاز کارشناس (آیتم ۱۵۳) وابسته است.

**برای رفع چه لازم است:** (فقط توصیف، بدون کد — دو تا چهار خط)

**ریسک/پیچیدگی تخمینی:** متوسط / بالا / پایین + دلیل یک‌خطی
```

### محتوای `00-SUMMARY.md`

1. **جدول اصلی**: شمارهٔ آیتم | عنوان کوتاه | وضعیت | پکیج | پیچیدگی رفع. هر ۵۴ ردیف، بدون استثنا.
2. **یافته‌های بحرانی**: چیزهایی که کاربر نمی‌دانست و باید فوراً بداند (ریسک داده، ریسک امنیتی، فرض‌های غلط).
3. **زنجیره‌های شکسته**: هر جا UI هست و بک‌اند نیست یا برعکس.
4. **ماژول‌های seed‌نشده در `role_permissions`** (ریسک دسترسی باز).
5. **تفاوت‌های دو برنچ که روی این آیتم‌ها اثر دارند.**
6. **گراف وابستگی**: کدام آیتم پیش‌نیاز کدام است. مثلاً اگر انبار چندگانه نداریم، ۱۷۷/۱۷۸/۱۷۹/۱۸۳ همه به آن گره خورده‌اند.
7. **پیشنهاد ترتیب اجرا**: گروه‌بندی آیتم‌ها به دسته‌های اجرایی منطقی با ذکر دلیل ترتیب. (فقط پیشنهاد ترتیب — نه پلن اجرا.)
8. **سؤالات باز برای کاربر**: جاهایی که تصمیم محصولی لازم است و تحقیق نمی‌تواند جوابش را بدهد.

---

## بخش ۵ — معیار پذیرش این تحقیق

تحقیق وقتی تمام است که همه این‌ها برقرار باشند:

- [ ] هر ۵۴ آیتم (۱۴۰ تا ۱۹۳) یک ورودی با وضعیت مشخص دارد — هیچ آیتمی جا نیفتاده
- [ ] هیچ ادعایی بدون شاهد (مسیر فایل + خط، یا نام تابع + migration، یا خروجی SQL) نیست
- [ ] برنچی که سرور از آن build شده مشخص شده و اثرش روی یافته‌ها لحاظ شده
- [ ] برای هر `❌`، روش جستجویی که به آن نتیجه رسیده ثبت شده
- [ ] هیچ فایل اپلیکیشنی تغییر نکرده — `git status --short` فقط فایل‌های `docs/research/` را نشان می‌دهد
- [ ] هیچ نوشتنی روی دیتابیس انجام نشده
- [ ] `00-SUMMARY.md` کامل است، شامل گراف وابستگی و سؤالات باز
- [ ] در پایان، خروجی `git status --short` و `git log --oneline -1` را گزارش کن تا تأیید شود چیزی خراب نشده

---

## بخش ۶ — نکات پایانی

- **جایی که مطمئن نیستی، `❓` بزن و دلیل بنویس.** یک `❓` صادقانه هزار برابر ارزشمندتر از یک `✅` اشتباه است. پرامپت اجرایی روی این گزارش ساخته می‌شود؛ هر خطای خوش‌بینانه در این مرحله، بعداً چند ساعت کار هدررفته می‌شود.
- **اگر وسط تحقیق چیزی پیدا کردی که در این سند نیست ولی مهم است** (ریسک داده، باگ امنیتی، ناسازگاری جدی)، در بخش «یافته‌های بحرانی» خلاصه بنویس — ولی برای رفعش دست به کد نبر.
- **هیچ کلید، توکن، یا رمزی را در گزارش چاپ نکن.** فقط بنویس «تنظیم شده» یا «خالی».
- گزارش برای انسان نوشته می‌شود: فارسی، مستقیم، بدون مقدمه‌چینی. جدول بهتر از پاراگراف است.