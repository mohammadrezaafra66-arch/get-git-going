# =============================================================================
# DEPRECATED — NOT AUTHORITATIVE
#
# Canonical Phase-0 OpenAPI per ADR-0007:
#   automation/openapi/automation-v1.yaml
#
# This stub retains the path for backward links only.
# Do not implement workers or control-plane routes against this file.
#
# See: docs/automation/OPENAPI_CANONICAL_RESOLUTION.md
#      openapi/README.md
# =============================================================================

openapi: 3.0.3
info:
  title: DEPRECATED — use automation/openapi/automation-v1.yaml
  version: 0.0.0-deprecated
  description: |
    **Not authoritative.**

    Canonical contract (ADR-0007):
    `automation/openapi/automation-v1.yaml`

    JSON Schemas:
    `automation/schemas/heartbeat.schema.json`
    `automation/schemas/job.schema.json`

    This file previously held a parallel draft using `/commands/claim` and
    `/runs/{run_id}` vocabulary. That draft informed DB table design but must
    not be implemented as a second API surface.

    Resolution doc:
    `docs/automation/OPENAPI_CANONICAL_RESOLUTION.md`
  x-canonical-path: automation/openapi/automation-v1.yaml
  x-deprecated: true
  x-deprecated-reason: Superseded by ADR-0007 canonical path under automation/openapi/
servers: []
paths: {}
components: {}
