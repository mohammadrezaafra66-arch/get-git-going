/**
 * پیکربندی متمرکز پاپ‌آپ‌ها و یادآوری‌ها (فقط UI).
 * تمام مقادیر زمانی/درصدی اینجا تعریف می‌شوند تا تغییر آینده ساده باشد.
 */

/** مدت زمان نگه‌داری پاپ‌آپ در مرکز اعلان‌ها — پیش‌فرض ۲۴ ساعت. */
export const POPUP_TTL_MS = 24 * 60 * 60 * 1000;

/** بیشتر از این تعداد روز در وضعیت ناموجود = نیاز به رسیدگی. */
export const STOCK_STALE_DAYS = 3;

/** قیمت خرید تومانی که بیش از این تعداد روز به‌روزرسانی نشده باشد. */
export const PURCHASE_PRICE_STALE_DAYS = 2;

/** آستانهٔ تغییر معادل دلاری قیمت خرید تومانی (درصد). */
export const USD_DRIFT_THRESHOLD_PCT = 3;

/** فاصلهٔ بررسی دوره‌ای یادآوری‌ها در پس‌زمینه (پیش‌فرض ۶۰ دقیقه). */
export const REMINDERS_REFRESH_INTERVAL_MS = 60 * 60 * 1000;
